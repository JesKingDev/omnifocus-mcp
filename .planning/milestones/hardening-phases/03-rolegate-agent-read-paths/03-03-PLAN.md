---
phase: 03-rolegate-agent-read-paths
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/tools/system/SystemTool.ts
  - tests/unit/tools/system/SystemTool-whoami.test.ts
autonomous: true
requirements:
  - GATE-01
  - GATE-02

must_haves:
  truths:
    - "SystemToolSchema Zod enum and hand-crafted inputSchema getter both include 'whoami' (dual-schema invariant)"
    - 'SystemTool description string mentions whoami'
    - "AGENT whoami response: { role: 'agent', roleSource: string } — identity field is structurally absent"
    - "OWNER whoami response: { role: 'owner', identity: { transport: 'stdio', roleSource: string, principal: null } }"
    - 'AGENT path test asserts identity is undefined, not just that role is present (D-15)'
    - 'SystemTool accepts optional ResolvedContext in constructor for whoami implementation'
  artifacts:
    - path: 'src/tools/system/SystemTool.ts'
      provides: 'whoami op: Zod enum + inputSchema getter + executeValidated case + getWhoami() method'
      contains: 'whoami'
    - path: 'tests/unit/tools/system/SystemTool-whoami.test.ts'
      provides: 'Activated whoami AGENT/OWNER redaction tests and dual-schema test'
  key_links:
    - from: 'src/tools/system/SystemTool.ts'
      to: 'src/contracts/roles.ts'
      via: 'import type { ResolvedContext, Role }'
      pattern: 'ResolvedContext'
    - from: 'src/tools/index.ts'
      to: 'src/tools/system/SystemTool.ts'
      via: 'new SystemTool(cache, context) — context passed from registerTools closure'
      pattern: 'new SystemTool'
---

<objective>
Build the system whoami operation in SystemTool with owner-only identity redaction (D-12 through D-15), satisfying the dual-schema invariant (Zod + inputSchema + description all updated), and activate the Wave 0 SystemTool-whoami.test.ts stubs.

Purpose: whoami is the first consumer of the owner-only-redaction-over-agent capability that advertisement introduces
(D-12). It gives tests a CallTool-based role assertion — replacing brittle startup-stderr log-scraping — and has zero
destructive risk (read-only op). The AGENT path structurally omits identity, making the redaction fail-safe by
construction: no sensitive value can exist on the agent path yet, and the test enforces the omission before Phase 4
populates principal.

Output: SystemTool with whoami op, role-scoped payload per D-13, dual-schema invariant satisfied, whoami unit tests
passing. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rolegate-agent-read-paths/03-CONTEXT.md
@.planning/phases/03-rolegate-agent-read-paths/03-RESEARCH.md
@.planning/phases/03-rolegate-agent-read-paths/03-PATTERNS.md
@.planning/phases/03-rolegate-agent-read-paths/03-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/contracts/roles.ts: export type Role = 'owner' | 'agent'; export type RoleSource = 'explicit-env' |
'fail-safe-default' | 'http-token'; // D-14: no 'launchd-label' export interface ResolvedIdentity { transport: 'stdio' |
'http'; roleSource: RoleSource; principal: string | null; } export interface ResolvedContext { identity:
ResolvedIdentity; role: Role; }

From src/tools/system/SystemTool.ts (current — read before editing): // Zod schema: z.object({ operation:
z.enum(['version', 'diagnostics', 'metrics', 'cache']), ... }).strict() // inputSchema getter: { operation: { enum:
['version', 'diagnostics', 'metrics', 'cache'] } } // executeValidated switch: cases for 'version', 'diagnostics',
'metrics', 'cache' // constructor(cache: CacheManager)

AGENT whoami response shape (D-13): { role: 'agent', roleSource: 'fail-safe-default' | 'explicit-env' | 'http-token' }
// identity field MUST be absent — not undefined-valued, not null — structurally not present

OWNER whoami response shape (D-13): { role: 'owner', identity: { transport: 'stdio', roleSource: string, principal: null
} } // principal is null until Phase 4; transport is 'stdio' for the stdio startup path

createSuccessResponseV2 pattern (from SystemTool.ts existing usage): return createSuccessResponseV2('system', payload,
undefined, { operation: 'whoami' }); </interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add whoami op to SystemTool (dual-schema: Zod + inputSchema + description + executeValidated)</name>
  <files>
    src/tools/system/SystemTool.ts
    tests/unit/tools/system/SystemTool-whoami.test.ts
  </files>

<read_first> 1. src/tools/system/SystemTool.ts — read the full file. Note: - Exact Zod schema declaration (find the
z.enum([...]) for operation) - Exact inputSchema getter return shape (find the properties.operation.enum array) -
Description string location - Constructor signature and class fields - executeValidated switch structure (cases and
default) - Existing createSuccessResponseV2 usage (lines 204-215 pattern) 2.
.planning/phases/03-rolegate-agent-read-paths/03-PATTERNS.md §SystemTool.ts whoami op — exact step-by-step changes for
Zod, inputSchema, description, constructor, executeValidated, and getWhoami() method. 3.
.planning/phases/03-rolegate-agent-read-paths/03-PATTERNS.md §SystemTool-whoami.test.ts — the exact assertion patterns;
copy the mock cache pattern from write-tool-policy-guard.test.ts. 4. tests/unit/tools/system/SystemTool-whoami.test.ts —
current Wave 0 stub content (the stubs to activate). 5. tests/unit/tools/write-tool-policy-guard.test.ts — mock cache
pattern to copy if not already in the stub file. </read_first>

  <behavior>
    SystemTool Zod schema:
    - operation z.enum() now includes 'whoami' alongside existing values

    SystemTool inputSchema getter:
    - properties.operation.enum now includes 'whoami' alongside existing values
    - Must be a new array literal, not a mutation of the existing enum array

    SystemTool description string:
    - Mentions 'whoami' — something like: "Use operation='whoami' to confirm the current role and identity context."

    SystemTool constructor:
    - Accepts optional second argument: context?: ResolvedContext
    - Stores as private readonly this.context

    SystemTool executeValidated:
    - case 'whoami': returns this.getWhoami()

    SystemTool.getWhoami() AGENT path:
    - role extracted from this.context?.role ?? 'agent'
    - roleSource extracted from this.context?.identity.roleSource ?? 'fail-safe-default'
    - Returns createSuccessResponseV2('system', { role, roleSource }, undefined, { operation: 'whoami' })
    - The response data object does NOT have an 'identity' key

    SystemTool.getWhoami() OWNER path:
    - role === 'owner'
    - Returns createSuccessResponseV2('system', { role, identity: { transport: this.context?.identity.transport ?? 'stdio', roleSource, principal: this.context?.identity.principal ?? null } }, undefined, { operation: 'whoami' })

    whoami AGENT unit test:
    - Construct SystemTool with mock cache and AGENT context (role: 'agent', identity: { transport: 'stdio', roleSource: 'fail-safe-default', principal: null })
    - Execute { operation: 'whoami' }
    - Parse response data
    - Assert data.role === 'agent'
    - Assert data.roleSource is defined and is one of the three valid RoleSource values
    - Assert data.identity === undefined — identity key is structurally absent (D-15)

    whoami OWNER unit test:
    - Construct SystemTool with owner context (role: 'owner', roleSource: 'explicit-env')
    - Execute { operation: 'whoami' }
    - Assert data.role === 'owner'
    - Assert data.identity is an object
    - Assert data.identity.transport === 'stdio'
    - Assert data.identity.roleSource === 'explicit-env'
    - Assert data.identity.principal === null (null until Phase 4)

    dual-schema unit test:
    - Construct SystemTool
    - Get inputSchema via tool.inputSchema
    - Assert inputSchema.properties.operation.enum includes 'whoami'
    - Import SystemToolSchema from the Zod schema (or parse the schema if exported) and assert 'whoami' is in the enum — or verify via trying to parse { operation: 'whoami' } through the schema successfully

  </behavior>

  <action>
    Read src/tools/system/SystemTool.ts in full before writing any changes. Then apply all five change areas as a single unit (dual-schema invariant requires Zod + inputSchema + description in one commit):

    1. Zod schema: add 'whoami' to the z.enum([...]) for the operation field. If the schema is defined as a standalone const (SystemToolSchema or similar), update it there. If inline, update inline.

    2. inputSchema getter: find the return value's properties.operation.enum array and add 'whoami'. Build a new array literal — do not push to an existing const (that would mutate across requests).

    3. Description string: append a sentence mentioning whoami. Search for the existing description = '...' and append: "Use operation='whoami' to confirm the current connection role and identity context."

    4. Constructor: add private readonly context?: ResolvedContext as a second constructor parameter. Add corresponding import: import type { ResolvedContext } from '../../contracts/roles.js' (check if already imported; add only if missing).

    5. executeValidated switch: add case 'whoami': return this.getWhoami(); before the default case.

    6. getWhoami() private method: implement per the D-13 role-scoped redaction. The AGENT path builds a data object with only role and roleSource keys (no identity key at all — do not set it to null or undefined, just do not include it). The OWNER path builds a data object with role and identity keys. Return createSuccessResponseV2('system', data, undefined, { operation: 'whoami' }). Match the pattern from lines 204-215 of the existing file.

    Then activate tests/unit/tools/system/SystemTool-whoami.test.ts:
    - Replace the vi.todo() stubs with real test implementations using the AGENT and OWNER context shapes above.
    - The AGENT test must assert 'expect(data.identity).toBeUndefined()' — not 'not to have property' — to satisfy the D-15 structural-omission requirement.
    - The dual-schema test asserts tool.inputSchema.properties.operation.enum includes 'whoami'.
    - The mock cache function is already in the stub file (added in Plan 01); use it as-is.

  </action>

  <verify>
    <automated>npm run build && npm run test:unit -- tests/unit/tools/system/SystemTool-whoami.test.ts</automated>
  </verify>

<acceptance_criteria> - grep -c "whoami" src/tools/system/SystemTool.ts returns >= 4 (Zod enum, inputSchema enum,
description, executeValidated case, getWhoami method — multiple occurrences) - grep -c "identity"
src/tools/system/SystemTool.ts returns >= 2 (OWNER path includes identity; AGENT path structurally omits it — no
identity key in AGENT return object) - grep -c "ResolvedContext" src/tools/system/SystemTool.ts returns >= 1
(constructor parameter typed) - npm run build exits 0 - npm run test:unit --
tests/unit/tools/system/SystemTool-whoami.test.ts exits 0 with all three real tests passing: (1) AGENT path:
data.identity is undefined (2) OWNER path: data.identity.principal === null (3) dual-schema: inputSchema enum includes
'whoami' - npm run test:unit exits 0 (no regression across full test suite) </acceptance_criteria>

  <done>
    SystemTool whoami op complete. Zod schema, inputSchema getter, and description all updated (dual-schema invariant). getWhoami() implements AGENT/OWNER redaction. AGENT path structurally omits identity. OWNER path returns identity with principal null. All three whoami unit tests pass.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                  |
| ---------------------------------- | -------------------------------------------------------------------------------------------- |
| SystemTool.getWhoami() AGENT path  | AGENT must not receive principal, transport, or identity key — structurally absent, not null |
| SystemTool constructor context arg | context is optional; defaults must be safe (role='agent', roleSource='fail-safe-default')    |

## STRIDE Threat Register

| Threat ID             | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                                                                                                |
| --------------------- | ---------------------- | -------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-3-leak              | Information Disclosure | whoami AGENT path leaks principal      | mitigate    | D-13: AGENT response data object built WITHOUT 'identity' key — structural absence, not null/undefined value. D-15: test asserts data.identity === undefined. principal remains in SENSITIVE_KEYS (logger.ts). |
| T-3-dual-schema       | Tampering              | Zod/inputSchema drift on whoami        | mitigate    | D-15: Both changed in one commit. Dual-schema test asserts inputSchema.properties.operation.enum includes 'whoami'.                                                                                            |
| T-3-rolsource-invalid | Tampering              | roleSource not one of the 3-value enum | mitigate    | D-14: Use the RoleSource type from contracts/roles.ts; context.identity.roleSource is typed; default to 'fail-safe-default'. No 'launchd-label' value exists.                                                  |

</threat_model>

<verification>
After this plan completes:
- npm run build exits 0
- npm run test:unit exits 0 (all test files)
- SystemTool-whoami.test.ts: AGENT response data has no 'identity' key
- SystemTool-whoami.test.ts: OWNER response data.identity.principal === null
- SystemTool-whoami.test.ts: inputSchema enum includes 'whoami'
- grep confirms SystemTool description mentions 'whoami'
</verification>

<success_criteria>

- SystemTool whoami op live with dual-schema invariant satisfied (Zod + inputSchema + description)
- AGENT path omits identity structurally; OWNER path includes identity with principal null
- All three whoami unit tests pass
- No regression in any other test file
- npm run build clean </success_criteria>

<output>
Create `.planning/phases/03-rolegate-agent-read-paths/03-03-SUMMARY.md` when done.
</output>
