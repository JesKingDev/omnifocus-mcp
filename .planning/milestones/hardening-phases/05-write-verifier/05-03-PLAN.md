---
phase: 05-write-verifier
plan: 03
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - src/tools/unified/schemas/read-schema.ts
  - src/tools/unified/OmniFocusReadTool.ts
  - src/contracts/ast/script-builder.ts
autonomous: true
requirements:
  - VERIFY-01

must_haves:
  truths:
    - 'read-schema.ts filterFields accepts ids: string[] (min 1, max 200)'
    - 'OmniFocusReadTool.ts inputSchema override advertises ids[] under filters (dual-schema invariant)'
    - 'A multi-id task lookup script can be built from buildTasksByIdSetScript'
    - 'The id-set filter routes through the query compiler correctly (not treated as unknown field)'
    - "FILTER_FIELD_NAMES picks up 'ids' automatically (no manual update needed)"
  artifacts:
    - path: 'src/tools/unified/schemas/read-schema.ts'
      provides: 'ids[] filter field in filterFields'
      contains: 'ids:'
    - path: 'src/tools/unified/OmniFocusReadTool.ts'
      provides: 'inputSchema override with ids[] advertised under filters'
      contains: 'ids'
    - path: 'src/contracts/ast/script-builder.ts'
      provides: 'buildTasksByIdSetScript function for multi-id OmniJS lookup'
      contains: 'buildTasksByIdSetScript'
  key_links:
    - from: 'src/tools/unified/verifier/WriteVerifier.ts'
      to: 'src/contracts/ast/script-builder.ts'
      via: 'import buildTasksByIdSetScript'
      pattern: 'buildTasksByIdSetScript'
    - from: 'src/tools/unified/schemas/read-schema.ts'
      to: 'src/tools/unified/OmniFocusReadTool.ts'
      via: 'dual-schema invariant — filterFields change requires inputSchema update'
      pattern: 'ids.*array'
---

<objective>
Build the read-back-by-id-set infrastructure required for D-13's single-batched-read-back design.

Purpose: The current read layer only accepts `filters.id: string` (single id). D-13 requires fetching multiple ids in
one osascript spawn for batch verification. This plan adds the Zod schema field (ids[]), the dual-schema inputSchema
update (CLAUDE.md invariant), and the multi-id OmniJS script builder. The WriteVerifier (Wave 2) will import
buildTasksByIdSetScript directly.

Output: read-schema.ts and OmniFocusReadTool.ts updated in the same commit (dual-schema invariant);
buildTasksByIdSetScript exported from script-builder.ts. </objective>

<execution_context> @/Users/jessicaking/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jessicaking/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/phases/05-write-verifier/05-CONTEXT.md
@.planning/phases/05-write-verifier/05-PATTERNS.md
@.planning/phases/05-write-verifier/05-RESEARCH.md
@.planning/phases/05-write-verifier/05-01-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Key contracts the executor needs. -->

From src/tools/unified/schemas/read-schema.ts (existing filterFields pattern):

```typescript
// Lines 43–44: existing single-id filter
const filterFields = {
  id: z.string().optional(), // Exact task ID lookup
  // ... (no ids[] key exists today — confirmed gap)
};
// Line 107: auto-derives from Object.keys — no manual update needed for FILTER_FIELD_NAMES
export const FILTER_FIELD_NAMES = Object.keys(filterFields) as readonly string[];
```

From src/tools/unified/OmniFocusReadTool.ts (inputSchema structure):

```typescript
// The get inputSchema() override at lines 228–292 hand-crafts the MCP advertisement.
// Filters section currently uses: filters: { type: 'object' }
// Must expand to advertise ids[] (D-13):
filters: {
  type: 'object',
  properties: {
    id:  { type: 'string', description: 'Exact single task ID lookup' },
    ids: { type: 'array', items: { type: 'string' },
           description: 'Fetch multiple tasks by id (batch write-verification). Max 200 per call.' },
  },
},
```

From OmniAutomation.ts (EMPIRICAL_LIMITS for chunking context):

- OmniJS bridge limit: 261,124 chars
- JXA limit: 523,266 chars
- Conservative safe threshold per RESEARCH.md Pitfall 7: 200 ids per chunk (Zod max)

From existing buildProjectByIdScript pattern (OmniJS inside evaluateJavascript):

```typescript
// OmniJS property access (never JXA method calls inside evaluateJavascript):
// task.name NOT task.name()
// task.tags NOT task.tags() (returns tag objects — map to .name for strings)
// task.containingProject NOT task.containingProject()
// task.flagged NOT task.flagged()
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add ids[] to read-schema.ts filterFields + dual-schema update in OmniFocusReadTool.ts</name>
  <files>
    src/tools/unified/schemas/read-schema.ts
    src/tools/unified/OmniFocusReadTool.ts
  </files>
  <read_first>
    - src/tools/unified/schemas/read-schema.ts — full filterFields object (lines 40–110 area); confirm no ids key; FILTER_FIELD_NAMES derivation
    - src/tools/unified/OmniFocusReadTool.ts — get inputSchema() override (lines 228–292); find the filters property definition; executeIdLookup (lines 472–529) as the cache-bypass reference
    - CLAUDE.md §"Dual-Schema Architecture" — the invariant: filterFields change requires same-commit inputSchema override update
    - 05-PATTERNS.md §"src/tools/unified/schemas/read-schema.ts" — the exact addition pattern
    - 05-PATTERNS.md §"src/tools/unified/OmniFocusReadTool.ts" — inputSchema filters expansion pattern
  </read_first>
  <action>
    TWO FILES — both in the same commit (dual-schema invariant from CLAUDE.md).

    CHANGE 1 — read-schema.ts:
    In the `filterFields` const object, add immediately after the `id` field:
    `ids: z.array(z.string()).min(1).max(200).optional(),`
    with the description comment "Batch read-back by id set (write verification). Max 200 per chunk (D-16)."
    Do NOT modify any other part of filterFields. `FILTER_FIELD_NAMES` auto-picks up the new key via
    `Object.keys(filterFields)` — confirm this by reading line 107 before editing.

    CHANGE 2 — OmniFocusReadTool.ts inputSchema override:
    Locate the `filters` property inside the `get inputSchema()` override. Currently it is likely:
    `filters: { type: 'object' }` (a minimal object type with no properties advertised).
    Expand it to:
    ```
    filters: {
      type: 'object',
      properties: {
        id: { type: 'string', description: 'Exact single task ID lookup' },
        ids: {
          type: 'array',
          items: { type: 'string' },
          description: 'Fetch multiple tasks by id (for write verification batch read-back). Max 200 per call.',
        },
      },
    },
    ```
    Do not alter any other property in the inputSchema override. Preserve exact surrounding structure.

    Also update the OmniFocusReadTool tool description string if it references the filter capabilities
    (check the `get description()` method). If the description mentions single-id lookup, add a clause
    noting batch id lookup is also supported. If no such language exists, leave description unchanged.

    After both changes, confirm `FILTER_FIELD_NAMES` includes 'ids' by mentally tracing `Object.keys`
    — no code change needed there.

  </action>
  <verify>
    <automated>cd /Users/jessicaking/projects/omnifocus-mcp && npm run build 2>&1 | grep -E "error TS" | head -10 && grep -c "ids:" src/tools/unified/schemas/read-schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `read-schema.ts` filterFields contains `ids: z.array(z.string()).min(1).max(200).optional()`
    - `OmniFocusReadTool.ts` inputSchema `filters` property advertises `ids` as an array of strings
    - `npm run build` exits 0 (no TypeScript errors)
    - `grep -c 'ids' src/tools/unified/OmniFocusReadTool.ts` returns ≥ 1 (in the inputSchema override)
    - Both files changed in one commit (invariant); no other files in this commit touch these two
    - `npm run test:unit` exits 0 (no regressions — existing read-schema tests still pass)
  </acceptance_criteria>
  <done>ids[] added to filterFields and inputSchema override in a single commit; build passes; dual-schema invariant satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Add buildTasksByIdSetScript to script-builder.ts (D-13 multi-id OmniJS lookup)</name>
  <files>
    src/contracts/ast/script-builder.ts
  </files>
  <read_first>
    - src/contracts/ast/script-builder.ts — existing exported functions; find buildProjectByIdScript or similar id-based lookup pattern to mirror; file structure
    - src/omnifocus/OmniAutomation.ts — EMPIRICAL_LIMITS (JXA 523266 / OmniJS 261124 chars); execJson signature
    - docs/dev/SETTER-PATTERNS.md — row 6 (tags OmniJS property syntax), row 7 (parent/move OmniJS-only)
    - 05-PATTERNS.md §"Shared Patterns" OmniJS property syntax block — CRITICAL: task.tags not task.tags(), task.flagged not task.flagged()
    - 05-RESEARCH.md Pattern 4: multi-id script builder example (OmniJS inside evaluateJavascript)
    - CLAUDE.md Quick Symptom Index — "X is not a function → You're in OmniJS - remove ()"
  </read_first>
  <action>
    Add `buildTasksByIdSetScript` as a new exported function in `script-builder.ts`.

    The function signature: `export function buildTasksByIdSetScript(ids: string[]): string`

    The function returns an OmniJS script (to run inside `evaluateJavascript`) that:
    1. Accepts the id array embedded as a JSON literal (`JSON.stringify(ids)` inlined as the script).
    2. Maps each id through `Task.byIdentifier(id)` — this is OmniJS, NOT JXA; use `Task.byIdentifier`
       (property access on the result), not method calls.
    3. For each found task, projects these fields (all OmniJS property access — NO trailing parens):
       - `id: task.id.primaryKey`
       - `name: task.name`
       - `note: task.note`
       - `flagged: task.flagged`
       - `sequential: task.sequential`
       - `estimatedMinutes: task.estimatedMinutes`
       - `dueDate: task.dueDate ? task.dueDate.toISOString() : null`
       - `deferDate: task.deferDate ? task.deferDate.toISOString() : null`
       - `plannedDate: task.plannedDate ? task.plannedDate.toISOString() : null`
       - `completionDate: task.completionDate ? task.completionDate.toISOString() : null`
       - `tags: task.tags.map(t => t.name)` — OmniJS: task.tags is a property (no parens), each tag's name is t.name (no parens)
       - `containingProject: task.containingProject ? task.containingProject.id.primaryKey : null`
    4. Filters out null results (tasks not found by id).
    5. Returns `JSON.stringify({ tasks: results })` as the final expression.

    The script must be wrapped in the standard OmniAutomation envelope if the project uses one
    (check the existing script builder patterns in the file for the exact wrapping convention —
    some scripts use `Application('OmniFocus').defaultDocument.windows[0].content.evaluateJavascript(...)`,
    some use the `evaluateJavascript` helper directly). Mirror the exact existing pattern.

    CRITICAL: This script runs inside `evaluateJavascript`. All field accesses are OmniJS (no
    trailing parentheses). The `task.tags` pitfall: if `()` is accidentally added to `task.tags`,
    the read-back will silently return `[Function]` instead of tag names — this is the JXA/OmniJS
    syntax bug documented in CLAUDE.md.

    The constant `VERIFY_READBACK_CHUNK_SIZE = 200` may also be added to this file or to
    WriteVerifier.ts — whichever location WriteVerifier.ts will import from. Choose based on which
    import path is cleaner (likely WriteVerifier.ts itself, since 200 is the Zod schema bound).

  </action>
  <verify>
    <automated>cd /Users/jessicaking/projects/omnifocus-mcp && npm run build 2>&1 | grep -E "error TS" | head -10 && grep -c "buildTasksByIdSetScript" src/contracts/ast/script-builder.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/contracts/ast/script-builder.ts` exports `buildTasksByIdSetScript`
    - The generated script body uses OmniJS property syntax throughout — grep confirms NO occurrences of `task.tags()`, `task.flagged()`, `task.name()` (with parens) inside the new function's return string
    - `npm run build` exits 0
    - `npm run test:unit` exits 0 (no regressions in existing script-builder tests)
    - Function handles empty array input gracefully (returns `{ tasks: [] }` script)
  </acceptance_criteria>
  <done>buildTasksByIdSetScript exported; OmniJS syntax verified; build and unit tests green</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                               |
| --------------------------------------- | ----------------------------------------------------------------------------------------- |
| WriteVerifier → buildTasksByIdSetScript | ids array comes from mutation result (server-internal); still Zod-bounded at schema layer |
| ids[] filter → OmniJS script            | Each id embedded as JSON; no user-controlled input reaches the script template            |

## STRIDE Threat Register

| Threat ID  | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                                          |
| ---------- | ---------------------- | --------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-03-01 | Tampering              | ids[] Zod filter field            | mitigate    | `z.array(z.string()).min(1).max(200).optional()` — Zod enforces max 200 per chunk (D-16); ids are server-internal from mutation result but still bounded |
| T-05-03-02 | Tampering              | id-set injection in OmniJS script | mitigate    | Ids serialized via `JSON.stringify(ids)` — no string interpolation of raw id values; JSON encoding prevents injection                                    |
| T-05-03-03 | Information Disclosure | stale read-back cache             | mitigate    | WriteVerifier calls execJson directly (injected dependency), bypassing CacheManager — confirmed by executeIdLookup pattern in PATTERNS.md                |

</threat_model>

<verification>
1. `npm run build` exits 0
2. `grep -c 'ids:' src/tools/unified/schemas/read-schema.ts` ≥ 1
3. `grep -c 'ids' src/tools/unified/OmniFocusReadTool.ts` ≥ 1 (inside inputSchema override)
4. `grep -c 'buildTasksByIdSetScript' src/contracts/ast/script-builder.ts` ≥ 1
5. `grep -v 'task\.tags()' src/contracts/ast/script-builder.ts | grep -c 'task\.tags'` ≥ 1 (property access, no parens)
6. `npm run test:unit` exits 0 (no regressions)
</verification>

<success_criteria>

- Dual-schema invariant satisfied: read-schema.ts ids[] and OmniFocusReadTool.ts inputSchema updated in same commit
- buildTasksByIdSetScript returns a valid OmniJS script using property access (not JXA method calls)
- Security: ids bounded to max 200 by Zod schema; no raw string interpolation in script builder
- No existing tests broken </success_criteria>

<output>
Create `.planning/phases/05-write-verifier/05-03-SUMMARY.md` when done
</output>
