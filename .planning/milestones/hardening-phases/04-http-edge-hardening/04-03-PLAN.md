---
phase: 04-http-edge-hardening
plan: 03
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - src/utils/cli.ts
autonomous: true
requirements:
  - HTTP-02
  - HTTP-04
  - HTTP-05

must_haves:
  truths:
    - "DEFAULT_CLI_CONFIG.host is '127.0.0.1', not '0.0.0.0'"
    - 'validateCLIConfig throws if host is not loopback in HTTP mode'
    - 'validateCLIConfig throws if agentToken is absent in HTTP mode'
    - 'validateCLIConfig throws if agentToken equals ownerToken'
    - 'validateCLIConfig throws if ownerToken is blank/whitespace'
    - 'stdio mode is unaffected by new assertions'
    - 'MCP_AGENT_TOKEN, MCP_OWNER_TOKEN, MCP_ALLOWED_HOSTS are read from env'
    - 'MCP_AUTH_TOKEN emits a deprecation warning and aliases to agentToken'
    - 'cli.test.ts is GREEN'
  artifacts:
    - path: 'src/utils/cli.ts'
      provides: 'CLIConfig with agentToken/ownerToken/allowedHosts fields + validateCLIConfig assertions'
      contains: "host: '127.0.0.1'"
  key_links:
    - from: 'src/index.ts'
      to: 'src/utils/cli.ts'
      via: 'import { CLIConfig, parseCLIArgs, validateCLIConfig }'
      pattern: 'validateCLIConfig'
    - from: 'src/utils/cli.ts'
      to: 'process.env'
      via: 'MCP_AGENT_TOKEN, MCP_OWNER_TOKEN, MCP_ALLOWED_HOSTS'
      pattern: 'MCP_AGENT_TOKEN'
---

<objective>
Wave 1, Plan B — harden the CLI config layer with loopback bind default, new token env vars,
startup assertions, and printHelp update.

Purpose: Runs in parallel with Plan 02 (no file overlap). Plan 04 depends on both. These are the fail-closed startup
guards that prevent misconfigured HTTP mode from starting.

Output: src/utils/cli.ts updated. cli.test.ts GREEN. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-http-edge-hardening/04-RESEARCH.md
@.planning/phases/04-http-edge-hardening/04-PATTERNS.md
@.planning/phases/04-http-edge-hardening/04-01-SUMMARY.md
</context>

<interfaces>
<!-- Current CLIConfig interface in src/utils/cli.ts (lines 8-13). Executor reads the actual file first. -->
<!-- Shown here so the executor understands the before/after without re-reading the file for context. -->

Before (current shape):

```typescript
export interface CLIConfig {
  httpMode: boolean;
  port: number;
  host: string;
  authToken?: string;
}
```

After (target shape per D-09, D-11, D-15):

```typescript
export interface CLIConfig {
  httpMode: boolean;
  port: number;
  host: string;
  authToken?: string; // retained for backward-compat (D-11 alias path)
  agentToken?: string; // MCP_AGENT_TOKEN → agent role
  ownerToken?: string; // MCP_OWNER_TOKEN → owner role
  allowedHosts?: string[]; // MCP_ALLOWED_HOSTS comma-separated
}
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLIConfig extension + parseCLIArgs env reading</name>

<files>src/utils/cli.ts</files>

<read_first> src/utils/cli.ts — read the full file. Critical sections: (1) CLIConfig interface (lines 8-13), (2)
DEFAULT_CLI_CONFIG (lines 19-25 approx), (3) parseCLIArgs env block (lines 74-77 approx), (4) the existing logger
redaction block in parseCLIArgs, (5) printHelp (lines 107-136 approx).

    .planning/phases/04-http-edge-hardening/04-PATTERNS.md §"src/utils/cli.ts (modify)" — exact diff
    for CLIConfig extension, exact parseCLIArgs additions including the MCP_AUTH_TOKEN alias logic and
    MCP_ALLOWED_HOSTS comma-split, and the logger redaction additions.

    .planning/phases/04-http-edge-hardening/04-RESEARCH.md §"Env Vars — Complete Registry After Phase 4"
    — the authoritative table of all env vars, their purpose, and whether they are required in HTTP mode.

</read_first>

  <behavior>
    - DEFAULT_CLI_CONFIG.host changes from '0.0.0.0' to '127.0.0.1' (D-13).
    - CLIConfig gains agentToken, ownerToken, allowedHosts fields as shown in the interfaces block above.
    - authToken field is retained (D-11 backward-compat; the old === path is removed elsewhere in Plans 02/04).
    - parseCLIArgs reads MCP_AGENT_TOKEN → config.agentToken.
    - parseCLIArgs reads MCP_OWNER_TOKEN → config.ownerToken.
    - parseCLIArgs handles MCP_AUTH_TOKEN: if MCP_AUTH_TOKEN is set AND MCP_AGENT_TOKEN is NOT set,
      emit logger.warn('MCP_AUTH_TOKEN is deprecated. Set MCP_AGENT_TOKEN instead.') and set
      config.agentToken = process.env.MCP_AUTH_TOKEN (D-11 alias).
    - parseCLIArgs reads MCP_ALLOWED_HOSTS: split on ',' then trim and filter empty strings →
      config.allowedHosts = string[].
    - Logger redaction block extended: agentToken and ownerToken are redacted ('[REDACTED]') when present,
      same pattern as the existing authToken redaction.
    - printHelp updated to document MCP_AGENT_TOKEN, MCP_OWNER_TOKEN, MCP_ALLOWED_HOSTS; MCP_AUTH_TOKEN
      marked as deprecated in the help text.
  </behavior>

  <action>
    Edit src/utils/cli.ts. Make the six changes described in the behavior section:
    (1) DEFAULT_CLI_CONFIG host default, (2) CLIConfig interface extension, (3-5) parseCLIArgs env block,
    (6) printHelp update.

    For the MCP_ALLOWED_HOSTS split: process.env.MCP_ALLOWED_HOSTS?.split(',').map(h => h.trim()).filter(Boolean).
    Assign to config.allowedHosts only when the result is non-empty.

    Do NOT add the validateCLIConfig assertions in this task — they are in Task 2. Keeping the two
    changes in separate tasks ensures the test for each is verifiable independently.

    Follow the existing code style: single-quoted strings, 2-space indentation (check actual file indentation
    and match it). Do not change any unrelated function or comment.

    Respect the sonarjs/todo-tag rule: no TODO comments. If a note is needed, use plain prose in a JSDoc
    comment.

  </action>

  <verify>
    <automated>cd /Users/jessicaking/projects/omnifocus-mcp && npm run build 2>&1 | grep -E "cli\\.ts|error" | head -10</automated>
  </verify>

<acceptance_criteria> - npm run build succeeds (no TypeScript errors in cli.ts). - grep -n "'0.0.0.0'" src/utils/cli.ts
shows 0 occurrences of '0.0.0.0' as the DEFAULT_CLI_CONFIG host value. (Other occurrences in comments or tests are
fine.) - grep -n "agentToken" src/utils/cli.ts shows the field in CLIConfig, the parseCLIArgs assignment, and the logger
redaction block. - grep -n "MCP_AUTH_TOKEN" src/utils/cli.ts shows the deprecation warning branch. - grep -n
"MCP_ALLOWED_HOSTS" src/utils/cli.ts shows the env read and comma-split. - printHelp output now mentions MCP_AGENT_TOKEN
(verify by grep -n "MCP_AGENT_TOKEN" src/utils/cli.ts finding ≥ 2 occurrences: parseCLIArgs and printHelp).
</acceptance_criteria>

<done>CLIConfig extended, parseCLIArgs reads new env vars, MCP_AUTH_TOKEN alias emits deprecation warning, printHelp
updated. Build passes.</done> </task>

<task type="auto" tdd="true">
  <name>Task 2: validateCLIConfig startup assertions (D-06, D-07, D-13)</name>

<files>src/utils/cli.ts</files>

<read_first> src/utils/cli.ts — read again (it was just modified by Task 1) to see the current state of
validateCLIConfig. The function exists and has a port-range check; the new assertions extend that existing if
(config.httpMode) block.

    .planning/phases/04-http-edge-hardening/04-PATTERNS.md §"validateCLIConfig assertions to add" — the
    exact Error message strings for each assertion. These must match the patterns expected by the Wave 0
    tests in cli.test.ts (/loopback/, /MCP_AGENT_TOKEN/, /different/, /empty/).

    .planning/phases/04-http-edge-hardening/04-RESEARCH.md §"Code Examples — Startup Assertions" — the
    full code pattern for all four new assertions, including the Pitfall 5 guard (assertions only fire
    inside if (config.httpMode)).

    tests/unit/utils/cli.test.ts (from Wave 0) — the expected throw message patterns that the assertions
    must satisfy. Read this file to confirm your Error strings match the regexes.

</read_first>

  <behavior>
    Four new assertions inside the existing if (config.httpMode) block in validateCLIConfig:
    1. D-13 loopback bind: config.host !== '127.0.0.1' && config.host !== 'localhost' → throw Error
       with message containing "loopback" and "127.0.0.1".
    2. D-07 mandatory agent token: !config.agentToken → throw Error with message containing "MCP_AGENT_TOKEN".
    3. D-06 distinct tokens: config.ownerToken && config.agentToken === config.ownerToken → throw Error
       with message containing "different".
    4. D-06 non-blank owner token: config.ownerToken !== undefined && config.ownerToken.trim() === '' →
       throw Error with message containing "empty".

    Assertion order matters: check host first, then mandatory token, then distinctness, then blank check.
    This gives the most actionable error message in the most common misconfiguration paths.

    stdio mode (config.httpMode === false): NONE of the new assertions run. Existing port-range check
    is also gated on httpMode — confirm and keep that behavior.

  </behavior>

  <action>
    Edit src/utils/cli.ts. Locate validateCLIConfig. Inside the existing if (config.httpMode) block,
    append the four new assertion blocks after the existing port check. Preserve exact Error message
    wording from the PATTERNS.md to ensure cli.test.ts regexes match.

    The loopback assertion message must include both "loopback" and "127.0.0.1" so both test patterns
    match. The MCP_AGENT_TOKEN assertion must include "MCP_AGENT_TOKEN" verbatim. The distinct-tokens
    assertion must include "different". The blank-owner assertion must include "empty".

    Do not change any other validation in the function. Do not remove the existing port-range check.

  </action>

  <verify>
    <automated>cd /Users/jessicaking/projects/omnifocus-mcp && npm run build 2>&1 | grep "error" | head -5; npm run test:unit -- tests/unit/utils/cli.test.ts 2>&1 | tail -20</automated>
  </verify>

<acceptance_criteria> - npm run test:unit -- tests/unit/utils/cli.test.ts passes GREEN: all 6 cases pass. - npm run
build succeeds. - The stdio-mode test case passes (validates no assertions fire with httpMode: false, host:
'0.0.0.0'). - grep -n "httpMode" src/utils/cli.ts confirms all new assertions are inside the httpMode guard. - grep -c
"throw new Error" src/utils/cli.ts shows ≥ 5 throw sites (1 existing port check + 4 new). </acceptance_criteria>

<done>validateCLIConfig has all four startup assertions. cli.test.ts is GREEN. Build passes.</done> </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                              |
| ----------------------- | ---------------------------------------------------------------------------------------- |
| process.env → CLIConfig | Env vars provide token material and host config; validated at startup before socket bind |
| validateCLIConfig gate  | The fail-closed assertions prevent a misconfigured HTTP mode from ever binding           |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                           | Disposition | Mitigation Plan                                                                                                      |
| ---------- | ---------------------- | --------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| T-04-03-01 | Authentication Bypass  | HTTP mode with no agentToken                        | mitigate    | D-07: validateCLIConfig throws if agentToken absent in HTTP mode (test: /MCP_AGENT_TOKEN/ throws)                    |
| T-04-03-02 | Elevation of Privilege | agentToken == ownerToken token collision            | mitigate    | D-06: validateCLIConfig throws if tokens equal (test: /different/ throws)                                            |
| T-04-03-03 | Authentication Bypass  | blank ownerToken accepted as valid                  | mitigate    | D-06: validateCLIConfig throws on whitespace-only ownerToken (test: /empty/ throws)                                  |
| T-04-03-04 | Elevation of Privilege | Open-interface bind exposing server beyond loopback | mitigate    | D-13: DEFAULT_CLI_CONFIG.host = '127.0.0.1'; validateCLIConfig throws on non-loopback host (test: /loopback/ throws) |
| T-04-03-05 | Information Disclosure | Token values leaked in startup logs                 | mitigate    | agentToken and ownerToken redacted as '[REDACTED]' in logger output, matching existing authToken pattern             |
| T-04-03-SC | Tampering              | npm/pip/cargo installs                              | accept      | No new packages; all changes are to existing source file                                                             |

</threat_model>

<verification>
- npm run test:unit -- tests/unit/utils/cli.test.ts passes GREEN (all 6 cases)
- npm run build succeeds
- grep -n "host: '127.0.0.1'" src/utils/cli.ts confirms default change
- grep -n "'0.0.0.0'" src/utils/cli.ts shows 0 occurrences as the default value
- Full test suite: npm run test:unit passes (no regressions)
</verification>

<success_criteria> src/utils/cli.ts: default host is 127.0.0.1, new token fields added, startup assertions fire
correctly, deprecation warning emitted for MCP_AUTH_TOKEN alias. cli.test.ts GREEN. </success_criteria>

<output>
Create .planning/phases/04-http-edge-hardening/04-03-SUMMARY.md when done
</output>
