---
phase: 04-http-edge-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/auth/token-registry.test.ts
  - tests/unit/auth/role-resolver.test.ts
  - tests/unit/utils/cli.test.ts
  - tests/unit/http-server.test.ts
autonomous: true
requirements:
  - HTTP-01
  - HTTP-02
  - HTTP-03
  - HTTP-04
  - HTTP-05

must_haves:
  truths:
    - 'Four test files exist covering all HTTP-01..HTTP-05 behaviors'
    - 'All new/updated tests fail (RED) because the source modules do not yet implement the behavior'
    - 'The role-resolver.test.ts stub test no longer calls the zero-argument resolveHttpIdentity() form'
    - 'npm run test:unit exits non-zero and the failures are traceable to missing/stub implementations'
  artifacts:
    - path: 'tests/unit/auth/token-registry.test.ts'
      provides: 'HTTP-01/HTTP-05 constant-time compare + registry construction test suite'
    - path: 'tests/unit/auth/role-resolver.test.ts'
      provides: 'Updated Phase 4 stub test replaced with filled-implementation assertions'
    - path: 'tests/unit/utils/cli.test.ts'
      provides: 'HTTP-02/HTTP-04 validateCLIConfig assertion tests'
    - path: 'tests/unit/http-server.test.ts'
      provides: 'HTTP-03 validateHostOrigin allowlist tests'
  key_links:
    - from: 'tests/unit/auth/token-registry.test.ts'
      to: 'src/auth/token-registry.ts'
      via: 'import'
      pattern: 'from.*token-registry'
    - from: 'tests/unit/auth/role-resolver.test.ts'
      to: 'src/auth/role-resolver.ts'
      via: 'import resolveHttpIdentity'
      pattern: 'resolveHttpIdentity'
    - from: 'tests/unit/utils/cli.test.ts'
      to: 'src/utils/cli.ts'
      via: 'import validateCLIConfig'
      pattern: 'validateCLIConfig'
    - from: 'tests/unit/http-server.test.ts'
      to: 'src/http-server.ts'
      via: 'import'
      pattern: 'http-server'
---

<objective>
Wave 0 — lay down the failing test contracts that all implementation waves must satisfy.

Purpose: Establish the Nyquist sampling gate. Every behavior required by HTTP-01..HTTP-05 gets a test that is RED before
any source file changes. Implementation waves make them GREEN without touching these test files.

Output: Four test files (three new, one updated). All new/updated tests fail. Pre-existing passing tests continue to
pass. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-http-edge-hardening/04-VALIDATION.md
@.planning/phases/04-http-edge-hardening/04-PATTERNS.md
@.planning/phases/04-http-edge-hardening/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: token-registry.test.ts + update role-resolver stub test</name>

<files>tests/unit/auth/token-registry.test.ts, tests/unit/auth/role-resolver.test.ts</files>

<read_first> tests/unit/auth/role-resolver.test.ts — read the full file; the "Phase 4 stub contract" describe block at
lines 106-115 is the exact section to replace. Understand the existing import pattern (lines 1-3) and it.each matrix
style (lines 13-43) to mirror in token-registry.test.ts.

    tests/unit/utils/sandbox-guard.test.ts — the established import/structure pattern for new unit test
    files in this repo (describe + it.each, typed test cases, expect.toThrow / toStrictEqual shapes).

    .planning/phases/04-http-edge-hardening/04-PATTERNS.md §"Test File Patterns" — exact import block, test
    matrix structure, and the full list of cases required for token-registry.test.ts.

    .planning/phases/04-http-edge-hardening/04-RESEARCH.md §"Phase Requirements → Test Map" — the canonical
    list of behaviors each test must cover per requirement ID.

</read_first>

  <behavior>
    token-registry.test.ts (new — all RED):
    - validateTokenSet with empty/null candidate → returns null (HTTP-01)
    - validateTokenSet with wrong token → returns null (HTTP-01)
    - validateTokenSet with matching agent token → returns { role: 'agent', principal: 'http-agent' } (HTTP-01)
    - validateTokenSet with matching owner token → returns { role: 'owner', principal: 'http-owner' } (HTTP-01, HTTP-05)
    - validateTokenSet with different-length tokens does not throw (SHA-256 hash normalization) (HTTP-01)
    - validateTokenSet with two-token registry checks ALL tokens even after first match — verify by using a
      spy or two valid tokens and confirming the return is the correct entry, not an early-exit artifact (HTTP-01 D-04)
    - buildTokenRegistry with MCP_AGENT_TOKEN set → single-entry map role=agent (HTTP-05)
    - buildTokenRegistry with MCP_OWNER_TOKEN set → map includes owner entry (HTTP-05)
    - buildTokenRegistry with MCP_AUTH_TOKEN alias (no MCP_AGENT_TOKEN) → maps to agent role (HTTP-05 D-11)
    - buildTokenRegistry with both agent + owner tokens → two-entry map (HTTP-05)

    role-resolver.test.ts (modify — update one describe block, all other tests continue to pass):
    - Remove the "Phase 4 stub contract" describe block (lines 106-115) that calls resolveHttpIdentity()
      with zero arguments and asserts roleSource='fail-safe-default', principal=null.
    - Replace with "Phase 4 implementation" describe block covering:
      - resolveHttpIdentity({ role: 'agent', principal: 'http-agent' }) → transport=http,
        roleSource=http-token, principal=http-agent (HTTP-05)
      - resolveHttpIdentity({ role: 'owner', principal: 'http-owner' }) → roleSource=http-token,
        principal=http-owner (HTTP-05)
    - Add TokenEntry import from src/auth/token-registry.js (will resolve once Wave 1 ships the module;
      the import causes the test file to fail at import-time on Wave 0, which is acceptable RED behavior).

  </behavior>

  <action>
    Create tests/unit/auth/token-registry.test.ts. Mirror the import/structure from role-resolver.test.ts
    (vitest describe + it.each pattern, typed test-case objects with a label field). Import buildTokenRegistry
    and validateTokenSet from '../../../src/auth/token-registry.js' and TokenEntry as a type import from the
    same path. The module does not exist yet — the import causes a compile/resolution failure, making tests RED.

    Cover all behavior items listed above. Use it.each for the validateTokenSet matrix; use individual it()
    calls for buildTokenRegistry cases. Never use Math.random() for tokens — use fixed hex strings like
    'a'.repeat(64) and 'b'.repeat(64) for clarity and length-variation tests.

    For the D-04 accumulate-then-branch test: create a two-token registry (agentToken, ownerToken).
    Call validateTokenSet with agentToken. Assert result.role === 'agent'. Call validateTokenSet with
    ownerToken. Assert result.role === 'owner'. This proves both entries are reachable, not just the first.
    A true constant-time audit would require instrumentation beyond unit tests; document the limitation as a
    comment in the test file.

    Modify tests/unit/auth/role-resolver.test.ts per the behavior section above. Add the TokenEntry import
    at the top (after existing imports). Replace only the "Phase 4 stub contract" describe block — do not
    touch any other describe block or test.

    Do NOT create fenced code blocks in this action text. The exact patterns are in the read_first files.

  </action>

  <verify>
    <automated>cd /Users/jessicaking/projects/omnifocus-mcp && npm run build 2>&1 | tail -5; npm run test:unit -- tests/unit/auth/token-registry.test.ts 2>&1 | tail -20</automated>
  </verify>

<acceptance_criteria> - tests/unit/auth/token-registry.test.ts exists and contains at minimum 8 test cases
(validateTokenSet matrix + buildTokenRegistry cases). - tests/unit/auth/role-resolver.test.ts no longer contains the
string "fail-safe-default" in any test expectation: grep -c "fail-safe-default" tests/unit/auth/role-resolver.test.ts
yields 0 (or only the import comment, not in expect calls). - Both test files fail when run: npm run test:unit exits
with a non-zero code referencing missing module src/auth/token-registry.ts (or a type error from resolveHttpIdentity now
expecting a TokenEntry argument). - Pre-existing role-resolver tests (parseRole matrix, resolveStdioIdentity) continue
to pass: all describes other than the one replaced are unmodified. - No .js files created — TypeScript only.
</acceptance_criteria>

<done>token-registry.test.ts and updated role-resolver.test.ts committed. npm run test:unit fails on the new test files
specifically (module not found or signature mismatch). All pre-existing passing tests still pass.</done> </task>

<task type="auto" tdd="true">
  <name>Task 2: cli.test.ts + http-server.test.ts (Wave 0 stubs)</name>

<files>tests/unit/utils/cli.test.ts, tests/unit/http-server.test.ts</files>

<read_first> tests/unit/utils/sandbox-guard.test.ts — the canonical pattern for config-validation throw tests in this
repo: plain describe + it blocks, import the function and error type directly, use .toThrow() with a regex or error
class.

    .planning/phases/04-http-edge-hardening/04-PATTERNS.md §"tests/unit/utils/cli.test.ts (NEW)"
    and §"tests/unit/http-server.test.ts (NEW)" — exact import blocks, base config object, and the precise
    list of test cases with the expected throw message fragments (e.g., /loopback/, /MCP_AGENT_TOKEN/,
    /different/, /empty/).

    src/utils/cli.ts — read to understand the current CLIConfig interface shape and validateCLIConfig
    function signature. The function exists but lacks the new assertions; tests will fail because the
    assertions are not yet present.

    src/http-server.ts — read to understand the current class shape. The validateHostOrigin method does not
    exist yet; tests will fail because the method is absent.

</read_first>

  <behavior>
    cli.test.ts (new — cases fail because assertions not yet in validateCLIConfig):
    - valid HTTP config (host: '127.0.0.1', agentToken present, ownerToken absent) → does NOT throw (HTTP-02)
    - non-loopback host in HTTP mode → throws with message matching /loopback/ or /127.0.0.1/ (HTTP-02 D-13)
    - agentToken absent in HTTP mode → throws with message matching /MCP_AGENT_TOKEN/ (HTTP-04 D-07)
    - agentToken === ownerToken → throws with message matching /different/ (HTTP-05 D-06)
    - ownerToken blank/whitespace → throws with message matching /empty/ (HTTP-05 D-06)
    - stdio mode (httpMode: false, host: '0.0.0.0') → does NOT throw (Pitfall 5 guard)

    http-server.test.ts (new — cases fail because validateHostOrigin method not yet added):
    - validateHostOrigin with unknown Host header (e.g. 'evil.attacker.com') → returns false / yields 400 (HTTP-03)
    - validateHostOrigin with Host: 'localhost' → returns true (HTTP-03 D-15)
    - validateHostOrigin with Host: '127.0.0.1' → returns true (HTTP-03 D-15)
    - validateHostOrigin with Host: '127.0.0.1:3000' → returns true (HTTP-03 D-15)
    - validateHostOrigin with MCP_ALLOWED_HOSTS entry present → returns true (HTTP-03 D-15)
    - validateHostOrigin with malformed Origin header → returns false (HTTP-03)

    Because validateHostOrigin will be a private method on HttpServerManager, test it via a lightweight
    approach: either extract it to a testable pure function, or test via a minimal HttpServerManager stub
    that exposes the method. Document the chosen approach in a comment in the test file.

  </behavior>

  <action>
    Create tests/unit/utils/cli.test.ts. Import validateCLIConfig and CLIConfig type from
    '../../../src/utils/cli.js'. The base config for HTTP tests is:
    { httpMode: true, port: 3000, host: '127.0.0.1', agentToken: 'a'.repeat(64) }.
    Use spread syntax for each variant. Mirror the sandbox-guard.test.ts style: each it() is
    independent, no shared mutable state.

    Create tests/unit/http-server.test.ts. The validateHostOrigin logic will be on HttpServerManager as a
    private method. To make it testable without spinning up a real server, plan for the implementation to
    extract the pure host-validation logic into a standalone exported function (e.g., buildAllowedHostSet and
    isHostAllowed exported from http-server.ts or a helper module). Write tests against that expected export.
    If the executor finds a different shape in the implementation, tests must be adjusted to match — the
    key requirement is that the behaviors listed above are unit-testable without a live HTTP server.

    Import from '../../../src/http-server.js'. If the expected function does not exist in the current
    source, the import will fail or the function will be undefined — tests go RED as required.

    Apply the established error-response shape from PATTERNS.md when asserting HTTP 400 behavior.
    Do NOT spin up an express/http server in Wave 0 tests — pure function extraction is the approach.

  </action>

  <verify>
    <automated>cd /Users/jessicaking/projects/omnifocus-mcp && npm run test:unit -- tests/unit/utils/cli.test.ts tests/unit/http-server.test.ts 2>&1 | tail -20</automated>
  </verify>

<acceptance_criteria> - tests/unit/utils/cli.test.ts exists with ≥ 6 test cases covering all validateCLIConfig assertion
paths. - tests/unit/http-server.test.ts exists with ≥ 6 test cases covering validateHostOrigin/allowlist logic. - Both
files fail (RED) when run against the current source: the assertions do not exist yet, so either the test throws "not a
function" or the validation passes when it should throw. - Pre-existing test suite unaffected: npm run test:unit
(excluding the new files) continues to pass. - TypeScript only; no .js files created. </acceptance_criteria>

<done>cli.test.ts and http-server.test.ts committed. npm run test:unit exits non-zero for both new files. All
pre-existing passing tests still pass.</done> </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                                     |
| -------------------- | ----------------------------------------------------------------------------------------------- |
| test → source module | Test imports resolve at compile time; a missing module is a RED signal, not a security boundary |

## STRIDE Threat Register

| Threat ID  | Category  | Component              | Disposition | Mitigation Plan                                                                        |
| ---------- | --------- | ---------------------- | ----------- | -------------------------------------------------------------------------------------- |
| T-04-W0-01 | Tampering | Wave 0 test files      | accept      | Tests are RED by design; no production behavior changes in this plan                   |
| T-04-W0-SC | Tampering | npm/pip/cargo installs | accept      | No new packages installed in this plan; node:crypto and vitest already in package.json |

Note: The substantive STRIDE register (timing attack, DNS-rebinding, token collision, auth bypass) is in Plans 02–04
where the mitigations are implemented. </threat_model>

<verification>
After both tasks complete:
- npm run test:unit exits non-zero
- The failing tests reference src/auth/token-registry.ts (missing module) and the absent
  validateCLIConfig assertions / validateHostOrigin function
- npm run test:unit (pre-existing tests only, excluding the four new/modified files) passes
- No new .js files in the test directory
</verification>

<success_criteria> Four test files in place, all Phase 4 behaviors covered, all new tests RED, pre-existing tests GREEN.
</success_criteria>

<output>
Create .planning/phases/04-http-edge-hardening/04-01-SUMMARY.md when done
</output>
