---
phase: 02-operation-policy-deny-deletes-gating
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/contracts/roles.ts
  - src/auth/operation-policy.ts
  - tests/unit/auth/operation-policy.test.ts
autonomous: true
requirements:
  - POLICY-01
  - POLICY-02
  - POLICY-03
  - POLICY-04
  - POLICY-05
  - POLICY-06
  - POLICY-07

must_haves:
  truths:
    - 'decide(role, operation, target) is a pure, synchronous, fail-closed function exported from
      src/auth/operation-policy.ts and is the single source of truth consulted by both enforcement layers (D-02)'
    - "Any (role, op, target) triple not explicitly in the D-08 table resolves to 'deny' — unknown ops never fail open;
      the AGENT role has no hard-delete path under any state (uniform deny, no carve-outs) (D-07)"
    - "PolicyOutcome = 'allow' | 'deny' | 'gate' is exported and consumed by both enforcement layers"
    - 'The exhaustive matrix test covers every D-08 cell plus the unknown-op fail-closed row — all rows pass'
  artifacts:
    - path: 'src/contracts/roles.ts'
      provides: 'PolicyOutcome type exported alongside Role'
      contains: 'export type PolicyOutcome'
    - path: 'src/auth/operation-policy.ts'
      provides: 'decide() function + policy table'
      exports: ['decide', 'PolicyOutcome']
    - path: 'tests/unit/auth/operation-policy.test.ts'
      provides: 'Exhaustive it.each policy matrix'
      contains: 'unknown op → deny'
  key_links:
    - from: 'src/auth/operation-policy.ts'
      to: 'src/contracts/roles.ts'
      via: 'import type { Role }'
      pattern: 'import.*Role.*contracts/roles'
    - from: 'tests/unit/auth/operation-policy.test.ts'
      to: 'src/auth/operation-policy.ts'
      via: 'import { decide }'
      pattern: 'import.*decide.*operation-policy'
---

<objective>
Define the policy decision function decide() and its exhaustive unit test. This is the single source of truth for all Phase 2 enforcement — both the funnel guard (Plan 02) and the script-builder re-assertion (Plan 03) import and call this same function.

Purpose: Establish the fail-closed policy contract before any enforcement point is wired. Tests written first (TDD)
ensure the decision table is exhaustively verified before a single guard goes live.

Output: src/contracts/roles.ts (PolicyOutcome added), src/auth/operation-policy.ts (decide() + table),
tests/unit/auth/operation-policy.test.ts (exhaustive matrix). </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-operation-policy-deny-deletes-gating/02-CONTEXT.md
@.planning/phases/02-operation-policy-deny-deletes-gating/02-PATTERNS.md

@src/contracts/roles.ts @src/auth/role-resolver.ts @tests/unit/auth/role-resolver.test.ts

<interfaces>
<!-- Phase 1 contracts reused directly. Extracted from codebase. -->

From src/contracts/roles.ts: export type Role = 'owner' | 'agent'; export type RoleSource = 'explicit-env' |
'fail-safe-default' | 'http-token'; export interface ResolvedIdentity { transport, roleSource, principal } export
interface ResolvedContext { identity: ResolvedIdentity; role: Role }

From src/auth/role-resolver.ts (module structure to mirror): export function parseRole(env: Record<string, string |
undefined> = process.env): Role // Block-comment style: lists anti-patterns deliberately absent // Pure, synchronous,
single return expression, no helper branches // Unknown input → fail-safe (for decide(): 'deny', not 'allow')

From tests/unit/auth/role-resolver.test.ts (test pattern to mirror): it.each<{ label: string; env: ...; expected: ...
}>([ { label: 'exact match: owner', ... }, ... ])('$label → $expected', ({ env, expected }) => {
expect(parseRole(env)).toBe(expected); }); </interfaces> </context>

<feature>
  <name>Operation policy decide() function with exhaustive matrix test</name>
  <files>src/contracts/roles.ts, src/auth/operation-policy.ts, tests/unit/auth/operation-policy.test.ts</files>
  <behavior>
    D-08 canonical table — AGENT role outcomes:
      deny:  operation='delete' / target='task'     → 'deny'
      deny:  operation='delete' / target='project'  → 'deny'
      deny:  operation='delete' / target='folder'   → 'deny'
      deny:  operation='bulk_delete' (any target)   → 'deny'
      gate:  operation='tag_manage' / target='delete' → 'gate'
      gate:  operation='tag_manage' / target='merge'  → 'gate'
      gate:  operation='tag_manage' / target='perspective_delete' → 'gate'  (forward-declared/inert per D-08)
      allow: operation='complete'  (any target)    → 'allow'
      allow: operation='drop'      (any target)    → 'allow'
      allow: operation='create'    (any target)    → 'allow'
      allow: operation='update'    (any target)    → 'allow'
      allow: operation='tag_manage' / target='create'   → 'allow'
      allow: operation='tag_manage' / target='rename'   → 'allow'
      allow: operation='tag_manage' / target='nest'     → 'allow'
      allow: operation='tag_manage' / target='unnest'   → 'allow'
      allow: operation='tag_manage' / target='reparent' → 'allow'
      allow: anything not listed above for 'owner' role → 'allow'
    Fail-closed default:
      agent + unknown/unlisted operation → 'deny'
      agent + unknown target on a gated op group → 'deny'
    OWNER role: ALL operations → 'allow' (no gating, no deny per POLICY-06)
  </behavior>
  <implementation>
    RED phase:
    - Create tests/unit/auth/operation-policy.test.ts with it.each matrix (see PATTERNS.md § it.each policy matrix test pattern). One row per D-08 cell. Add mandatory "unknown op → deny" and "owner/delete/task → allow" rows. Test imports decide() from src/auth/operation-policy.ts (file does not exist yet) — run npm run test:unit, confirm red.

    GREEN phase:
    1. Add `export type PolicyOutcome = 'allow' | 'deny' | 'gate';` to src/contracts/roles.ts immediately after the Role type (line ~27). The file already notes PolicyEngine (Phase 2) as consumer; no structural change needed.

    2. Create src/auth/operation-policy.ts following the role-resolver.ts module structure:
       - Block-comment header listing anti-patterns absent (no state checks, no async, no || 'allow' fallback)
       - Import `import type { Role } from '../contracts/roles.js';` and `import type { PolicyOutcome } from '../contracts/roles.js';`
       - Export the policy table as a `const` lookup structure (nested by role → operation → target) — exact internal shape is discretionary per 02-CONTEXT.md Claude's Discretion, but must be a single source of truth
       - Export `decide(role: Role, operation: string, target?: string): PolicyOutcome` — pure, synchronous, no side effects. Fail-closed default: any (role, op, target) not explicitly listed → 'deny'. OWNER role always returns 'allow' (full pass-through per POLICY-06 / D-08)
       - Per D-07: no state-dependent carve-outs; no || 'allow' fallback anywhere in the function body

    Run npm run test:unit — confirm green.

    REFACTOR phase:
    - Tighten JSDoc on decide() noting the fail-closed contract and the two-call-site consumers (funnel guard Plan 02, script-builder re-assertion Plan 03)
    - Run npm run build and npm run test:unit — both must pass

  </implementation>
</feature>

<read_first>

- src/contracts/roles.ts — insertion point for PolicyOutcome (after Role type ~line 27); confirm existing exports
- src/auth/role-resolver.ts — module structure, block-comment style, fail-closed idiom to mirror exactly
- tests/unit/auth/role-resolver.test.ts — it.each matrix test pattern to replicate
- .planning/phases/02-operation-policy-deny-deletes-gating/02-CONTEXT.md — D-02, D-07, D-08 (canonical taxonomy table,
  fail-closed rule, OWNER pass-through)
- .planning/phases/02-operation-policy-deny-deletes-gating/02-PATTERNS.md — § it.each policy matrix test pattern, §
  fail-closed function pattern, § string-literal union contract type </read_first>

<acceptance_criteria>

- src/contracts/roles.ts contains the line: `export type PolicyOutcome = 'allow' | 'deny' | 'gate';`
- src/auth/operation-policy.ts contains: `export function decide(`
- src/auth/operation-policy.ts contains no `|| 'allow'` expression (grep for `\|\| 'allow'` returns empty)
- decide('agent', 'delete', 'task') === 'deny'
- decide('agent', 'bulk_delete', 'task') === 'deny'
- decide('agent', 'tag_manage', 'delete') === 'gate'
- decide('agent', 'tag_manage', 'merge') === 'gate'
- decide('agent', 'tag_manage', 'create') === 'allow'
- decide('agent', 'complete', 'task') === 'allow'
- decide('agent', 'unknown_op_xyz', 'task') === 'deny' (fail-closed)
- decide('owner', 'delete', 'task') === 'allow' (OWNER full pass-through)
- decide('owner', 'tag_manage', 'delete') === 'allow' (OWNER no gating)
- npm run test:unit exits 0 with all operation-policy.test.ts rows green
- npm run build exits 0 (no TypeScript errors) </acceptance_criteria>

<verify>
  <automated>npm run test:unit 2>&1 | grep -E "operation-policy|FAIL|PASS|error"</automated>
  <automated>npm run build 2>&1 | grep -E "error|warning" | grep -v "node_modules"</automated>
</verify>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                                                                                                                                  |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| caller → decide() | The role string must come from parseRole() (Phase 1), not from user input. The operation/target strings come from the compiled mutation — validated by Zod before reaching the policy layer. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                | Disposition | Mitigation Plan                                                                                                                                                                                                                         |
| --------- | ---------------------- | ---------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | ----------------------------------------------------------------------------------------------------------------- |
| T-2-01    | Elevation of Privilege | decide() fail-open on unknown ops        | mitigate    | Explicit fail-closed default: any (role, op, target) not in the table → 'deny'. Enforced by the "unknown op → deny" mandatory test row. No `                                                                                            |     | 'allow'` fallback is permitted — banned by the anti-patterns comment and asserted by grep in acceptance criteria. |
| T-2-02    | Tampering              | PolicyOutcome type drift (two consumers) | mitigate    | PolicyOutcome is defined once in src/contracts/roles.ts. Both Plan 02 and Plan 03 import the same type — no local redefinition. TypeScript compile gate catches divergence.                                                             |
| T-2-03    | Spoofing               | Role string fabricated by caller         | accept      | decide() receives Role from parseRole() (Phase 1), which has its own fail-safe. decide() is a pure function — it trusts the Role it receives. The call-site invariant (only pass the parsed role) is enforced at the funnel in Plan 02. |
| T-2-SC    | Tampering              | npm install slopcheck                    | mitigate    | No new npm packages introduced in this plan. All imports are project-internal.                                                                                                                                                          |

</threat_model>

<verification>
1. `npm run test:unit` — all operation-policy.test.ts rows green; role-resolver.test.ts still green (no regression)
2. `npm run build` — zero TypeScript errors
3. `grep -c "export type PolicyOutcome" src/contracts/roles.ts` returns 1
4. `grep -c "export function decide" src/auth/operation-policy.ts` returns 1
5. `grep -v "^[[:space:]]*//" src/auth/operation-policy.ts | grep -c "|| 'allow'"` returns 0 (fail-open expression absent)
</verification>

<success_criteria>

- PolicyOutcome type exported from src/contracts/roles.ts
- decide() function exported from src/auth/operation-policy.ts: pure, synchronous, fail-closed
- All D-08 table cells covered by it.each test rows including mandatory "unknown op → deny" and "owner pass-through"
  rows
- npm run test:unit and npm run build both exit 0 </success_criteria>

<output>
Create `.planning/phases/02-operation-policy-deny-deletes-gating/02-01-SUMMARY.md` when done.
</output>
