---
phase: 02-operation-policy-deny-deletes-gating
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/contracts/ast/mutation-script-builder.ts
  - src/contracts/ast/tag-mutation-script-builder.ts
  - tests/unit/auth/operation-policy.test.ts
autonomous: true
requirements:
  - POLICY-04

must_haves:
  truths:
    - 'buildDeleteScript() throws POLICY error before emitting any JXA when called for an agent role'
    - 'buildDeleteTagScript() and buildMergeTagsScript() throw POLICY error before emitting any JXA when called for an
      agent role'
    - 'Both script builders call the same decide() — defense-in-depth re-assertion, no duplicated policy logic (D-03)'
    - 'An OWNER role caller reaches script generation in both builders without error'
  artifacts:
    - path: 'src/contracts/ast/mutation-script-builder.ts'
      provides: 'assertPolicyAllow() re-assertion in buildDeleteScript()'
      contains: 'assertPolicyAllow'
    - path: 'src/contracts/ast/tag-mutation-script-builder.ts'
      provides: 'assertPolicyAllow() re-assertion in buildDeleteTagScript() and buildMergeTagsScript()'
      contains: 'assertPolicyAllow'
  key_links:
    - from: 'src/contracts/ast/mutation-script-builder.ts'
      to: 'src/auth/operation-policy.ts'
      via: 'import { decide }'
      pattern: 'import.*decide.*operation-policy'
    - from: 'src/contracts/ast/tag-mutation-script-builder.ts'
      to: 'src/auth/operation-policy.ts'
      via: 'import { decide }'
      pattern: 'import.*decide.*operation-policy'
---

<objective>
Add the defense-in-depth re-assertion (D-03) to both script builders. Each destructive/gated builder calls the same decide() function — the identical one used by the funnel guard in Plan 02. If a code path somehow bypasses the funnel, the re-assertion is the last line of defense before JXA is emitted.

Purpose: POLICY-04 requires enforcement at the single mutation funnel AND a re-assertion in the script builders. The two
layers call one function; there is no duplicated policy logic. This is the belt-and-suspenders guarantee against funnel
bypass.

Output: mutation-script-builder.ts (assertPolicyAllow in buildDeleteScript), tag-mutation-script-builder.ts
(assertPolicyAllow in buildDeleteTagScript + buildMergeTagsScript), plus a re-assertion integration test row appended to
operation-policy.test.ts. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-operation-policy-deny-deletes-gating/02-CONTEXT.md
@.planning/phases/02-operation-policy-deny-deletes-gating/02-PATTERNS.md
@.planning/phases/02-operation-policy-deny-deletes-gating/02-01-SUMMARY.md
@.planning/phases/02-operation-policy-deny-deletes-gating/02-02-SUMMARY.md

@src/contracts/ast/mutation-script-builder.ts @src/contracts/ast/tag-mutation-script-builder.ts
@src/auth/operation-policy.ts @src/contracts/roles.ts

<interfaces>
<!-- Extracted from codebase. Executor reads these before touching any file. -->

From src/auth/operation-policy.ts (Plan 01 output): export function decide(role: Role, operation: string, target?:
string): PolicyOutcome // 'allow' | 'deny' | 'gate'. Synchronous, pure, fail-closed.

From src/contracts/ast/mutation-script-builder.ts: export async function buildDeleteScript(target: MutationTarget, id:
string): Promise<GeneratedMutationScript> // Existing sandbox guard at line ~2001: validateTaskInSandbox(id, 'delete') /
validateProjectInSandbox(...) // assertPolicyAllow() inserts BEFORE the sandbox guard (policy check before any I/O) //
Function signature must gain a `role: Role` parameter

From src/contracts/ast/tag-mutation-script-builder.ts: export function buildDeleteTagScript(data: { tagName: string }):
GeneratedMutationScript // First line: validateTagMutation(data.tagName) — assertPolicyAllow inserts BEFORE this //
Function signature must gain a `role: Role` parameter

export function buildMergeTagsScript(data: { tagName: string; targetTag: string }): GeneratedMutationScript // Same
pattern: assertPolicyAllow inserts before validateTagMutation // Function signature must gain a `role: Role` parameter

assertPolicyAllow pattern (from PATTERNS.md): function assertPolicyAllow(role: Role, operation: string, target: string):
void { const outcome = decide(role, operation, target); if (outcome !== 'allow') { throw new
Error(`POLICY: ${outcome.toUpperCase()} ${operation}/${target} for role '${role}'`); } } // Throws, not returns. Script
builders are not request handlers; errors propagate to the funnel catch block.

Tag operation targets for decide(): buildDeleteTagScript → decide(role, 'tag_manage', 'delete') buildMergeTagsScript →
decide(role, 'tag_manage', 'merge') buildDeleteScript → decide(role, 'delete', target) where target is the
MutationTarget ('task'|'project'|'folder') </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Add assertPolicyAllow re-assertion to both script builders</name>
  <files>
    src/contracts/ast/mutation-script-builder.ts,
    src/contracts/ast/tag-mutation-script-builder.ts
  </files>

<read_first>

- src/contracts/ast/mutation-script-builder.ts — read buildDeleteScript (line ~2000) for exact insertion point; read
  validateTaskInSandbox (lines 330–340) for the existing guard structure that assertPolicyAllow mirrors; check existing
  imports at top of file
- src/contracts/ast/tag-mutation-script-builder.ts — read buildDeleteTagScript (line 344) and buildMergeTagsScript
  (line 390) for insertion points; confirm validateTagMutation (lines 22–27) shape and where each builder calls it;
  check imports at top of file
- src/auth/operation-policy.ts — confirm decide() signature post Plan 01
- src/contracts/roles.ts — confirm Role type import path
- .planning/phases/02-operation-policy-deny-deletes-gating/02-PATTERNS.md — § assertPolicyAllow pattern, § re-assertion
  call sites for both files
- .planning/phases/02-operation-policy-deny-deletes-gating/02-CONTEXT.md — D-03 (same decide(), throw on non-allow, no
  duplicated logic) </read_first>

  <action>
  In BOTH script builder files:

1. Add to imports: `import { decide } from '../../auth/operation-policy.js';` and
   `import type { Role } from '../../contracts/roles.js';`

2. Define a shared synchronous guard function in each file (or a single shared module if the executor judges that
   cleaner — discretion per 02-CONTEXT.md). The guard body is: assertPolicyAllow(role: Role, operation: string, target:
   string): void — calls decide(role, operation, target); throws
   `new Error('POLICY: ' + outcome.toUpperCase() + ' ' + operation + '/' + target + ' for role \'' + role + '\'')` if
   outcome !== 'allow'. Synchronous, no return value, propagates to the funnel's catch block.

In mutation-script-builder.ts — buildDeleteScript():

- Add `role: Role` as a NEW first parameter (before `target` and `id`). This is a public export; update its callers. The
  primary caller is handleTaskDelete / handleProjectOperation in OmniFocusWriteTool.ts — check those call sites and add
  `parseRole()` as the role argument, consistent with how the funnel guard obtains the role. Use
  `import { parseRole } from '../auth/role-resolver.js'` if not already present.
- Insert assertPolicyAllow(role, 'delete', target) as the FIRST statement of buildDeleteScript(), before the sandbox
  guard.

In tag-mutation-script-builder.ts — buildDeleteTagScript():

- Add `role: Role` as a NEW first parameter before `data`.
- Insert assertPolicyAllow(role, 'tag_manage', 'delete') as the FIRST statement, before
  validateTagMutation(data.tagName).
- Update callers of buildDeleteTagScript in OmniFocusWriteTool.ts to pass the role (use parseRole() at the call site).

In tag-mutation-script-builder.ts — buildMergeTagsScript():

- Add `role: Role` as a NEW first parameter before `data`.
- Insert assertPolicyAllow(role, 'tag_manage', 'merge') as the FIRST statement.
- Update callers of buildMergeTagsScript in OmniFocusWriteTool.ts to pass the role.

Caller update notes:

- In OmniFocusWriteTool.ts, buildDeleteScript is called inside handleTaskDelete and handleProjectOperation.
  buildDeleteTagScript and buildMergeTagsScript are called inside handleTagManage. In each case, add parseRole() as the
  role argument. The funnel guard (Plan 02) has already imported parseRole, so the import is available.
- No other public consumers of these builders exist in this codebase (confirm with grep before editing).

sonarjs/todo-tag rule note (from STATE.md Decisions): Do NOT add TODO comments. Use plain prose in JSDoc if annotation
is needed. </action>

  <verify>
    <automated>npm run build 2>&1 | grep -E "error" | grep -v "node_modules"</automated>
    <automated>grep -c "assertPolicyAllow" src/contracts/ast/mutation-script-builder.ts</automated>
    <automated>grep -c "assertPolicyAllow" src/contracts/ast/tag-mutation-script-builder.ts</automated>
    <automated>grep -c "import.*decide.*operation-policy" src/contracts/ast/mutation-script-builder.ts</automated>
    <automated>grep -c "import.*decide.*operation-policy" src/contracts/ast/tag-mutation-script-builder.ts</automated>
  </verify>

  <done>
  - assertPolicyAllow present in both script builder files
  - Both files import decide() from operation-policy.js (not a local copy)
  - buildDeleteScript, buildDeleteTagScript, buildMergeTagsScript each gain a `role: Role` first parameter
  - All callers in OmniFocusWriteTool.ts updated to pass role via parseRole()
  - npm run build exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-assertion integration test + full test suite verification</name>
  <files>tests/unit/auth/operation-policy.test.ts</files>

<read_first>

- tests/unit/auth/operation-policy.test.ts — existing matrix test from Plan 01; append new describe block
- tests/unit/auth/role-resolver.test.ts — test style and import pattern
- src/contracts/ast/mutation-script-builder.ts — buildDeleteScript signature after Task 1 (confirm role parameter added)
- src/contracts/ast/tag-mutation-script-builder.ts — buildDeleteTagScript / buildMergeTagsScript signatures after Task 1
  </read_first>

  <behavior>
  Re-assertion unit tests (appended to operation-policy.test.ts as a new describe block):
    - buildDeleteScript('agent', 'task', 'fake-id') throws with message containing 'POLICY: DENY'
    - buildDeleteTagScript('agent', { tagName: 'test-tag' }) throws with message containing 'POLICY: GATE'
    - buildMergeTagsScript('agent', { tagName: 'src-tag', targetTag: 'dest-tag' }) throws with message containing 'POLICY: GATE'
    - buildDeleteScript('owner', 'task', 'fake-id') does NOT throw (owner role passes through re-assertion)
    - buildDeleteTagScript('owner', { tagName: 'test-tag' }) does NOT throw
    Note: buildDeleteScript is async; use await expect(...).rejects.toThrow(...) for async assertions.
    Note: These calls will attempt to run JXA if the re-assertion passes for owner — wrap owner tests in a try/catch or use a mock; or assert that the thrown error is NOT a POLICY error (i.e., if it throws, the error.message does not contain 'POLICY:').

Full suite: - npm run test:unit exits 0 (all tests from Plans 01 + 02 + 03 green) </behavior>

  <action>
  Append a new describe block to tests/unit/auth/operation-policy.test.ts:

describe('script-builder re-assertion — defense-in-depth (D-03)', () => { ... })

Import buildDeleteScript from src/contracts/ast/mutation-script-builder.js and buildDeleteTagScript,
buildMergeTagsScript from src/contracts/ast/tag-mutation-script-builder.js.

For deny cases (AGENT role): assert that the function throws an error whose message includes 'POLICY: DENY'. For gate
cases (AGENT role): assert that the function throws an error whose message includes 'POLICY: GATE'. For owner
pass-through: buildDeleteScript is async and will attempt JXA after the guard passes. Since the test environment has no
OmniFocus running, it will fail — but the error message must NOT contain 'POLICY:'. Use: await
expect(buildDeleteScript('owner', 'task', 'fake-id')).rejects.not.toThrow(/POLICY:/). Same pattern for synchronous
builders: expect(() => buildDeleteTagScript('owner', { tagName: 'test-tag' })).not.toThrow(/POLICY:/). (The synchronous
builders may throw a TEST GUARD error from validateTagMutation if OMNIFOCUS_MCP_TEST_MODE is active — that is
acceptable; it is not a POLICY error.)

After all new tests are green, run the complete suite: npm run test:unit. </action>

  <verify>
    <automated>npm run test:unit 2>&1 | grep -E "FAIL|PASS|error TS"</automated>
    <automated>grep -c "defense-in-depth" tests/unit/auth/operation-policy.test.ts</automated>
  </verify>

  <done>
  - describe('script-builder re-assertion — defense-in-depth (D-03)') block present in operation-policy.test.ts
  - All three deny/gate assertions verify POLICY throw for agent role
  - Owner pass-through assertions confirm no POLICY error
  - npm run test:unit exits 0 across all test files
  - npm run build exits 0
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                                                                                                                                                                      |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Funnel guard → script builder          | A code path reaching the script builder without going through the funnel (e.g., a test helper or a new routing branch added in a future patch) would skip the funnel guard. The re-assertion is the catch-all for this boundary. |
| Script builder internal → JXA emission | The re-assertion fires before any string is built. Even partial JXA generation is prevented.                                                                                                                                     |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                    | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                        |
| --------- | ---------------------- | ---------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-2-09    | Elevation of Privilege | Funnel bypass — code path reaches buildDeleteScript without the funnel guard | mitigate    | assertPolicyAllow is the FIRST statement in each destructive/gated builder (before sandbox guard and before any script string construction). Throws synchronously; the error propagates to the funnel's catch block and surfaces as a tool error. Re-assertion integration tests (Task 2) confirm this for agent role. |
| T-2-10    | Tampering              | Duplicated policy logic — re-assertion drifts from funnel table              | mitigate    | Both layers call the same decide() from src/auth/operation-policy.ts. There is no separate policy table in the script builders. The import path grep assertions confirm a single source.                                                                                                                               |
| T-2-11    | Elevation of Privilege | Role parameter fabrication — caller passes 'owner' to a builder directly     | accept      | Script builders are internal to the server process. Direct external callers cannot reach them — all external surface goes through the funnel. The parameter is typed as Role (string literal union); no runtime coercion.                                                                                              |
| T-2-SC    | Tampering              | npm install slopcheck                                                        | mitigate    | No new npm packages introduced. All imports are project-internal.                                                                                                                                                                                                                                                      |

</threat_model>

<verification>
1. `npm run test:unit` — all tests green: operation-policy.test.ts (matrix + re-assertion), write-tool-policy-guard.test.ts (batch-parity + guard), role-resolver.test.ts (Phase 1 unchanged)
2. `npm run build` — zero TypeScript errors
3. `grep -c "assertPolicyAllow" src/contracts/ast/mutation-script-builder.ts` returns at least 1
4. `grep -c "assertPolicyAllow" src/contracts/ast/tag-mutation-script-builder.ts` returns at least 2 (delete + merge)
5. `grep -c "import.*decide.*operation-policy" src/contracts/ast/mutation-script-builder.ts` returns 1
6. `grep -c "import.*decide.*operation-policy" src/contracts/ast/tag-mutation-script-builder.ts` returns 1
7. `grep -rn "POLICY_DENY_DELETE\|POLICY_GATE_REQUIRES_OWNER" src/` confirms codes only in OmniFocusWriteTool.ts (the funnel), not duplicated in script builders (builders throw, they don't use response codes)
</verification>

<success_criteria>

- assertPolicyAllow() present in both script builders, calling the shared decide()
- All destructive/gated builders (buildDeleteScript, buildDeleteTagScript, buildMergeTagsScript) have role parameter and
  re-assertion as first statement
- All callers in OmniFocusWriteTool.ts updated to pass role via parseRole()
- Re-assertion integration test (describe 'defense-in-depth') confirms throws for agent, no POLICY throw for owner
- npm run test:unit and npm run build both exit 0
- Zero duplicated policy logic: policy table lives in operation-policy.ts only </success_criteria>

<output>
Create `.planning/phases/02-operation-policy-deny-deletes-gating/02-03-SUMMARY.md` when done.
</output>
