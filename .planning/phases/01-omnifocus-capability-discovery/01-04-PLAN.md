---
phase: 01-omnifocus-capability-discovery
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - docs/reference/omnifocus-capabilities.md
  - .planning/phases/01-omnifocus-capability-discovery/01-VALIDATION.md
autonomous: false
requirements:
  - DISC-01
  - DISC-02

must_haves:
  truths:
    - 'The report covers all seven area codes with at least one finding each (DISC-01)'
    - 'Every area section records a 3-way verdict (native/extend/build) with a rubric reason and evidence tag (DISC-02)'
    - 'Every finding tagged evidence: verified traces to a probe run with output pasted in the report'
    - 'Every evidence: unverified finding has an explicit follow-up note — no silent gaps'
    - 'Each downstream phase (2–6) can cite at least one DISC-<AREA>-NN finding for its build-vs-reuse decision'
    - '01-VALIDATION.md nyquist_compliant is set to true after the sign-off checklist passes'
    - 'The report is committed to the repo as a durable reference artifact'
  artifacts:
    - path: 'docs/reference/omnifocus-capabilities.md'
      provides: 'Finalized capability-discovery report with all seven areas, all verdicts, all evidence tags'
      min_lines: 300
      contains:
        'DISC-TAG-01, DISC-FILTER-01, DISC-FIELD-01, DISC-MODEL-01, DISC-PERSP-01, DISC-CAPTURE-01, DISC-AUTO-01'
    - path: '.planning/phases/01-omnifocus-capability-discovery/01-VALIDATION.md'
      provides: 'Validation sign-off with nyquist_compliant: true'
      contains: 'nyquist_compliant: true'
  key_links:
    - from: 'Phase 2 (CAP-01, LINE-01)'
      to: 'docs/reference/omnifocus-capabilities.md'
      via: 'DISC-CAPTURE-01 (inbox creation native), DISC-FIELD-01 (note field), DISC-TAG-01 (tag OmniJS path)'
      pattern: 'DISC-CAPTURE-01|DISC-FIELD-01|DISC-TAG-01'
    - from: 'Phase 3 (ROUTE-01..04)'
      to: 'docs/reference/omnifocus-capabilities.md'
      via: 'DISC-MODEL-05 (moveTasks), DISC-FILTER-01 (targeted collections), DISC-TAG-02 (tag lookup)'
      pattern: 'DISC-MODEL-05|DISC-FILTER-01|DISC-TAG-02'
    - from: 'Phase 6 (READAS-01, PROV-01)'
      to: 'docs/reference/omnifocus-capabilities.md'
      via: 'DISC-PERSP-01 (archivedFilterRules), DISC-PERSP-03 (no tasks API), DISC-PERSP-04 (no create)'
      pattern: 'DISC-PERSP-01|DISC-PERSP-03|DISC-PERSP-04'
---

<objective>
Run the final consistency audit of docs/reference/omnifocus-capabilities.md, resolve or explicitly flag any remaining unverified claims, verify that each downstream phase (2–6) can cite at least one finding, and sign off the phase by updating 01-VALIDATION.md.

Purpose: This is the gate pass. No phase-2 planning should begin until the report has been verified as complete,
consistent, and usable as a citation source for downstream phases. The human checkpoint confirms the report is fit for
purpose before the phase is marked complete.

Output: A finalized docs/reference/omnifocus-capabilities.md, updated 01-VALIDATION.md with nyquist_compliant: true, and
a human confirmation that the report reads as a usable downstream citation source. </objective>

<execution_context> @/Users/jessicaking/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jessicaking/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-CONTEXT.md
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-VALIDATION.md
@/Users/jessicaking/projects/omnifocus-mcp/.planning/ROADMAP.md
@/Users/jessicaking/projects/omnifocus-mcp/.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consistency audit — verify all findings, verdicts, and downstream-citation coverage</name>
  <files>docs/reference/omnifocus-capabilities.md</files>

<read_first> - docs/reference/omnifocus-capabilities.md — the complete, current report (read in full) -
.planning/phases/01-omnifocus-capability-discovery/01-VALIDATION.md — sign-off checklist and DISC-01/DISC-02
verification rows - .planning/ROADMAP.md — Phase 1 success criteria (three criteria); Phases 2–6 requirement IDs to
verify citation coverage - .planning/REQUIREMENTS.md — DISC-01 (full area list: tagging, filtering, custom fields,
perspectives, data model, native capture, automation surfaces), DISC-02 (verdict requirement) </read_first>

  <action>
    Run a structured audit of docs/reference/omnifocus-capabilities.md against the DISC-01/DISC-02 acceptance criteria and the VALIDATION.md sign-off checklist. Fix any gaps found. Do NOT modify correctly-completed findings — only fill holes or strengthen flagged items.

    Audit Step 1 — Seven area sections present:
    Confirm the report has sections for: TAG, FILTER, FIELD, PERSP, MODEL, CAPTURE, AUTO.
    Cross-check against DISC-01 wording (tagging / filtering / custom fields / perspectives / project-task data model + sequencing + dependencies / native capture inbox+templates / automation surfaces OmniAutomation+URL+plug-ins).
    If any area is missing or incomplete (still has "— to be populated —"), surface it immediately — do not proceed to step 2.

    Audit Step 2 — Verdicts and evidence tags:
    For each finding across all seven areas, verify:
    (a) A verdict line is present (native / extend / build) — not blank.
    (b) A rubric reason is present (one-line "don't build what's already solved" rationale).
    (c) An evidence tag is present (verified / doc / unverified).
    (d) A Downstream cite is present (at least "—" is acceptable for findings with no downstream use; but build-decision findings MUST cite a phase).
    For any `evidence: unverified` finding: confirm an explicit follow-up note is present explaining what would need to be probed and when. If the note is missing, add it.

    Audit Step 3 — Verified findings trace to probes:
    List all findings with `evidence: verified`. For each one, confirm that either:
    (a) A probe output block is pasted in the report immediately below or near the finding, OR
    (b) The source cites an existing codebase file (e.g., SETTER-PATTERNS.md row 6) with a specific reference.
    Any `evidence: verified` finding with neither → downgrade to `evidence: doc` or add the missing probe output.

    Audit Step 4 — Downstream-phase citation coverage:
    For each downstream phase, confirm at least one finding can be cited for its primary build-vs-reuse call:
    - Phase 2 (CAP-01, LINE-01, PERM-01/02): cite DISC-CAPTURE-01 (inbox creation), DISC-FIELD-01 (note field), DISC-TAG-01/02 (tag path)
    - Phase 3 (ROUTE-01..04): cite DISC-MODEL-05 (moveTasks), DISC-FILTER-01 (targeted collections), DISC-TAG-02 (tag lookup)
    - Phase 4 (REVIEW-01/02): cite DISC-TAG-01/03 (agent-okay/review tags are conventional)
    - Phase 5 (ARCH-01..03): cite DISC-TAG-03 (archaeology tag is conventional), DISC-FIELD-01 (note field for session ID)
    - Phase 6 (READAS-01, PROV-01): cite DISC-PERSP-01/02/03/04 (archivedFilterRules, no tasks API, no create-from-scratch)
    If any downstream phase has zero citable findings, add a "Downstream cite" annotation to the most relevant existing finding.

    Audit Step 5 — Fix Mermaid diagram accuracy:
    Re-read the TL;DR Mermaid diagram created in Plan 01 (scaffold). After all findings are populated, update the diagram to reflect the actual verdict for each area: use color coding matching native/extend/build (muted greens for native, muted blues for extend, muted amber for build). Ensure it still has ≤12 nodes and remains legible.

    After the audit:
    - Apply all fixes to docs/reference/omnifocus-capabilities.md in a single write pass
    - List every fix made in a brief audit-log section at the bottom of the report (an HTML comment block: &lt;!-- AUDIT-LOG ... --&gt;) so the human reviewer can see what changed

  </action>

  <verify>
    <automated>grep -c "evidence: unverified" /Users/jessicaking/projects/omnifocus-mcp/docs/reference/omnifocus-capabilities.md</automated>
  </verify>

<acceptance_criteria> - All seven area sections present with no "— to be populated —" stubs - Every finding has:
Verdict, Rubric, Evidence tag, Source, Downstream cite (or "—" for non-build findings) - Every `evidence: verified`
finding has probe output or a specific codebase source reference - Every `evidence: unverified` finding has an explicit
follow-up note — zero silent unverified findings - Phases 2–6 each have at least one citable DISC-&lt;AREA&gt;-NN
finding documented in the Downstream cite fields - Mermaid TL;DR diagram is updated to reflect actual verdicts -
AUDIT-LOG comment block at the bottom records every fix made - grep for "evidence: unverified" returns only findings
that have an explicit follow-up note in their Source or Rubric field (can be non-zero, but each must have a note)
</acceptance_criteria>

<done>Consistency audit complete. Report is structurally complete with all verdicts, evidence tags, and downstream-phase
citation coverage. Audit log committed.</done> </task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    docs/reference/omnifocus-capabilities.md is now fully populated with findings for all seven capability areas (TAG, FILTER, FIELD, PERSP, MODEL, CAPTURE, AUTO). Each finding has a 3-way verdict (native/extend/build), a rubric reason, an evidence tag (verified/doc/unverified), and a downstream-phase citation. Gate-claim probes ran against OmniFocus 4.8.11 and their outputs are pasted into matching findings. Unverified findings are explicitly flagged. The D-08 automation-surface fit matrix is in the AUTO section.
  </what-built>

  <how-to-verify>
    1. Open docs/reference/omnifocus-capabilities.md in your editor or markdown viewer.
    2. Confirm the report covers all seven areas: TAG, FILTER, FIELD, PERSP, MODEL, CAPTURE, AUTO — each with a populated "### Area Verdict" subsection.
    3. Spot-check: For DISC-PERSP-01 (archivedFilterRules), confirm the evidence tag is `verified` for in-session write and `unverified` for restart persistence, with an explicit follow-up note.
    4. Spot-check: For DISC-CAPTURE-01 (OmniJS new Task inbox), confirm evidence is `verified` with probe output.
    5. Spot-check: For at least one finding per downstream phase, confirm the "Downstream cite" field names the phase and requirement (e.g., "Phase 2 — CAP-01").
    6. Check the AUTO section for the D-08 fit matrix — confirm it has columns for Agent Capture, Routing Writes, Perspective Provisioning, and MCP Server Basis.
    7. Scan for any finding where `evidence: unverified` appears without a follow-up note. If you find one, describe it in your response.
    8. Confirm the report reads as a usable citation source: "Would a future Claude building Phase 2 be able to look at DISC-CAPTURE-01 and make a definitive build-vs-reuse call?" If the answer is no for any finding, describe the gap.
  </how-to-verify>

<resume-signal>Type "approved" to proceed to VALIDATION.md sign-off, or describe any findings that need correction
(e.g., "DISC-PERSP-01 is missing a follow-up note" or "DISC-AUTO section has no fit matrix").</resume-signal> </task>

<task type="auto">
  <name>Task 3: Update VALIDATION.md sign-off and finalize phase</name>
  <files>.planning/phases/01-omnifocus-capability-discovery/01-VALIDATION.md</files>

<read_first> - .planning/phases/01-omnifocus-capability-discovery/01-VALIDATION.md — current status (pending), sign-off
checklist, per-task verification map - docs/reference/omnifocus-capabilities.md — final state after audit (confirm all
audit steps passed before updating VALIDATION.md) </read_first>

  <action>
    After the human checkpoint approves the report (or after any corrections are applied per human feedback):

    Step 1 — Update 01-VALIDATION.md:
    - Change frontmatter `nyquist_compliant: false` to `nyquist_compliant: true`
    - Change frontmatter `wave_0_complete: false` to `wave_0_complete: true`
    - Update the `status: draft` to `status: complete`
    - In the Per-Task Verification Map table, update each row's Status from "⬜ pending" to "✅ green" for rows that are now satisfied. If any row cannot be marked green (e.g., DISC-01 restart-persistence row is unverified), mark it "⚠️ flaky" with a note.
    - In the Validation Sign-Off checklist, mark each item as checked [x] where satisfied.
    - Add a sign-off line at the bottom: "Approved: [date] — Phase 1 report complete, gates Phases 2–6."

    Step 2 — Confirm the phase is ready for /gsd-verify-work:
    Output a summary of:
    - Total findings: N across M area codes
    - Verified findings: X (evidence: verified)
    - Doc-cited findings: Y (evidence: doc)
    - Unverified (flagged): Z (evidence: unverified — each has a follow-up note)
    - Downstream phases covered: Phase 2 (N findings cited), Phase 3 (N), Phase 4 (N), Phase 5 (N), Phase 6 (N)

    Do not advance the ROADMAP's phase-1 checkbox — that is the /gsd-verify-work step.

  </action>

  <verify>
    <automated>grep -c "nyquist_compliant: true" /Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-VALIDATION.md</automated>
  </verify>

<acceptance_criteria> - 01-VALIDATION.md frontmatter: nyquist_compliant: true, wave_0_complete: true, status: complete -
Per-Task Verification Map rows updated (✅ green for satisfied rows, ⚠️ flaky with notes for any unresolved unverified
items) - Validation Sign-Off checklist items are checked [x] - Sign-off line with date is present - Summary of finding
counts (verified/doc/unverified) is output by the executor for the phase SUMMARY </acceptance_criteria>

<done>01-VALIDATION.md updated to nyquist_compliant: true. Phase 1 is ready for /gsd-verify-work. The
capability-discovery report is finalized and committed as a durable reference artifact in the repo.</done> </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                                  |
| -------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| report → downstream phases | The finalized report is the citation contract for Phases 2–6; incorrect verdicts here propagate to incorrect build decisions |
| human reviewer → report    | Human confirms the report is a usable citation source; this gate is the only external validation in this phase               |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                      | Disposition | Mitigation Plan                                                                                                                                                                                                         |
| --------- | ---------------------- | ------------------------------------------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-01   | Repudiation            | Audit step silently accepts an unverified finding without a follow-up note     | mitigate    | Audit Step 2 explicitly checks every `evidence: unverified` finding for a follow-up note; AUDIT-LOG records what was found and fixed. Human checkpoint step 7 asks the reviewer to scan for silent unverified findings. |
| T-04-02   | Repudiation            | A downstream phase has no citable finding and plans against a wrong assumption | mitigate    | Audit Step 4 verifies citation coverage for each of Phases 2–6; fixes any gap before the human checkpoint.                                                                                                              |
| T-04-03   | Information Disclosure | Report contains real user task or perspective content in example findings      | mitigate    | Audit includes a scan for real task names in finding examples; replace with generic placeholders if found.                                                                                                              |
| T-04-04   | Tampering              | Human checkpoint skipped / bypassed without genuine review                     | accept      | The checkpoint gate type is "blocking" — the executor must not auto-advance. The gate is proportionate: one focused review, specific verification steps, clear resume signal.                                           |
| T-04-SC   | Tampering              | npm/pip/cargo installs                                                         | accept      | No new packages in this plan                                                                                                                                                                                            |

</threat_model>

<verification>
After all tasks complete:

1. `test -f docs/reference/omnifocus-capabilities.md` — file exists
2. `wc -l docs/reference/omnifocus-capabilities.md` — at least 300 lines (full report)
3. For each area code, confirm at least one finding exists:
   `grep -c "DISC-TAG-\|DISC-FILTER-\|DISC-FIELD-\|DISC-PERSP-\|DISC-MODEL-\|DISC-CAPTURE-\|DISC-AUTO-" docs/reference/omnifocus-capabilities.md`
   — at least 20
4. `grep -c "nyquist_compliant: true" .planning/phases/01-omnifocus-capability-discovery/01-VALIDATION.md` — returns 1
5. `grep "to be populated" docs/reference/omnifocus-capabilities.md` — returns empty (no stubs)
6. `grep -c "Area Verdict" docs/reference/omnifocus-capabilities.md` — returns 7
7. Human has typed "approved" at the checkpoint </verification>

<success_criteria>

- docs/reference/omnifocus-capabilities.md is the finalized Phase 1 report: all seven area sections complete, all
  verdicts present, all unverified findings explicitly flagged with follow-up notes
- Every downstream phase (2–6) has at least one citable DISC-&lt;AREA&gt;-NN finding
- 01-VALIDATION.md updated to nyquist_compliant: true
- Human has reviewed the report and typed "approved" at the checkpoint
- Phase 1 is ready for /gsd-verify-work </success_criteria>

<output>
Create .planning/phases/01-omnifocus-capability-discovery/01-04-SUMMARY.md when done
</output>
