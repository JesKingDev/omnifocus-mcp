---
phase: 01-omnifocus-capability-discovery
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - docs/reference/omnifocus-capabilities.md
  - probes/disc-persp-01-filter-rules-persist.js
  - probes/disc-persp-02-custom-all-enumerate.js
autonomous: false
requirements:
  - DISC-01
  - DISC-02

must_haves:
  truths:
    - 'PERSP area section documents archivedFilterRules read/write, Perspective.Custom.all enumeration, and the
      no-perspective.tasks gap'
    - 'CAPTURE area section documents all five native capture surfaces with OmniJS new Task(name,inbox) as the MCP path'
    - 'AUTO area section contains the automation-surface fit matrix required by D-08 (agent capture / routing writes /
      PROV-01 / MCP reliance)'
    - 'PERSP gate-claim probe for archivedFilterRules uses a disposable probe-created test perspective (not the real
      JessOS perspective) wherever possible; any write to a real perspective is gated behind a human checkpoint'
    - 'PERSP gate-claim probe confirms whether Perspective.Custom.all enumerates the JessOS perspective'
    - 'All seven area sections now have a populated Area Verdict subsection with a single-value verdict
      (native/extend/build — no slash-combined values)'
    - 'Probe output in the report is sanitized: counts, booleans, identifiers — no perspective names from the live
      database'
  artifacts:
    - path: 'docs/reference/omnifocus-capabilities.md'
      provides: 'PERSP, CAPTURE, AUTO sections fully populated; automation fit matrix; all seven areas complete'
      contains: 'DISC-PERSP-01, DISC-CAPTURE-01, DISC-AUTO-01'
    - path: 'probes/disc-persp-01-filter-rules-persist.js'
      provides:
        'Gate-claim probe: archivedFilterRules write on a disposable test perspective; real-perspective write path gated
        by human checkpoint'
    - path: 'probes/disc-persp-02-custom-all-enumerate.js'
      provides: 'Gate-claim probe: Perspective.Custom.all enumerates perspectives; JessOS presence check'
  key_links:
    - from: 'docs/reference/omnifocus-capabilities.md#disc-persp-01'
      to: 'probes/disc-persp-01-filter-rules-persist.js'
      via: 'Source field in finding table'
      pattern: 'DISC-PERSP-01'
    - from: 'docs/reference/omnifocus-capabilities.md#disc-auto-01'
      to: 'Phase 2–6 plans'
      via: 'Downstream cite field — automation fit matrix guides PROV-01/CAP-01/ROUTE-01'
      pattern: 'DISC-AUTO-01'
    - from: 'docs/reference/omnifocus-capabilities.md#disc-capture-01'
      to: 'probes/disc-capture-01-inbox-note-roundtrip.js (Plan 02)'
      via: 'Evidence cross-reference from Plan 02'
      pattern: 'DISC-CAPTURE-01'
---

<objective>
Populate the PERSP, CAPTURE, and AUTO area sections of docs/reference/omnifocus-capabilities.md, run the two remaining gate-claim probes for PERSP, and write the automation-surface fit assessment required by D-08.

Purpose: PERSP and AUTO are the areas with the most phase-6 and phase-3 implications respectively. The
archivedFilterRules persistence probe is a direct gate for PROV-01 planning. The automation fit matrix (D-08) is a
cross-cutting finding that gives every downstream phase a clear answer on which surface to use for each operation.

Output: PERSP/CAPTURE/AUTO sections fully written; two probe scripts in probes/; automation fit matrix in the AUTO
section; all seven report areas complete. </objective>

<execution_context> @/Users/jessicaking/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jessicaking/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-CONTEXT.md
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-VALIDATION.md
@/Users/jessicaking/projects/omnifocus-mcp/CLAUDE.md
@/Users/jessicaking/projects/omnifocus-mcp/docs/dev/OMNIJS-FIRST-PATTERN.md
@/Users/jessicaking/projects/omnifocus-mcp/docs/dev/SETTER-PATTERNS.md
@/Users/jessicaking/projects/omnifocus-mcp/src/omnifocus/scripts/perspectives.ts
@/Users/jessicaking/projects/omnifocus-mcp/tests/manual/perspectives/test-perspective-rules.js
@/Users/jessicaking/projects/omnifocus-mcp/tests/manual/perspectives/test-perspective-comprehensive.ts

<interfaces>
<!-- Key patterns for PERSP, CAPTURE, AUTO areas. Extracted from codebase. -->

From src/omnifocus/scripts/perspectives.ts (LIST_PERSPECTIVES_SCRIPT pattern): Enumerates Perspective.BuiltIn.all and
Perspective.Custom.all Reads name, identifier, archivedFilterRules, archivedTopLevelFilterAggregation per custom
perspective

From tests/manual/perspectives/test-perspective-rules.js: Reads and writes perspective.archivedFilterRules Demonstrates
the write path that disc-persp-01 will use

From RESEARCH.md § CAPTURE: Five native capture surfaces: Quick Entry (not scriptable), Clippings, Mail Drop, Share
sheet, URL scheme omnifocus:///add MCP path: OmniJS new Task(name, inbox) — already in mutation-script-builder.ts

From RESEARCH.md § AUTO: Fit matrix rows: OmniJS (primary), URL schemes (capture alternate), Plug-ins (not suitable),
Shortcuts (not suitable) D-08 requires fit notes per surface for: agent capture, routing writes, perspective
provisioning, MCP reliance </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Write disc-persp-02 enumeration probe and run it (read-only)</name>
  <files>
    probes/disc-persp-02-custom-all-enumerate.js
  </files>

<read_first>

- .planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md — § PERSP — Perspectives (full section, including
  the "no perspective.tasks" gap and PROV-01 limitation)
- .planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md — § Gate-Claim Probe Targets (Perspective.Custom.all
  enumerates JessOS row)
- src/omnifocus/scripts/perspectives.ts — existing perspective enumeration pattern </read_first>

    <action>
      Write an OmniJS-first read-only probe that:
      - Calls Perspective.Custom.all and returns a sanitized count + presence check
      - Checks whether a perspective named "JessOS" (case-insensitive substring match) is present
      - Returns JSON with ONLY: { customPerspectiveCount: number, jessosFound: boolean, jessosIdentifier: string | null,
          ofVersion: string }
      - Does NOT return the full names array or any other user-visible perspective content

      This is a pure read probe — no write, no cleanup needed. It does not modify any perspective.

      Run the probe: `osascript -l JavaScript probes/disc-persp-02-custom-all-enumerate.js`
      Record the sanitized evidence appendix: command | timestamp | sanitized result | OF version.

    </action>

    <verify>
      <automated>test -f /Users/jessicaking/projects/omnifocus-mcp/probes/disc-persp-02-custom-all-enumerate.js</automated>
    </verify>

<acceptance_criteria>

- probes/disc-persp-02-custom-all-enumerate.js exists and is read-only (no writes)
- The probe returns only { customPerspectiveCount, jessosFound, jessosIdentifier, ofVersion } — not a full names array
- The probe runs without a thrown exception against OmniFocus 4.8.11
- Sanitized evidence appendix recorded (count + boolean, no perspective names from the live database)
  </acceptance_criteria>

<done>disc-persp-02 probe created and run. Enumeration result (count + JessOS presence) recorded as sanitized evidence.
No user perspective names embedded.</done> </task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The disc-persp-02 read-only probe ran and confirmed the count of custom perspectives and JessOS presence (or
    absence). The next step is the disc-persp-01 probe, which tests archivedFilterRules write behavior. This probe
    prefers to write to a disposable test perspective. However, if no disposable test perspective exists in your
    OmniFocus, the probe will need to write to a real custom perspective (then restore it). Before that write runs
    autonomously against your live configuration, we need your confirmation.
  </what-built>

  <how-to-verify>
    1. In OmniFocus, open the Perspectives window (Perspectives menu → Show Perspectives).
    2. Decide if you want to create a disposable test perspective now (recommended). If yes:
       - Create a new custom perspective named "disc-probe-test-perspective" with any simple settings.
       - This perspective will be used as the write target for disc-persp-01 and can be deleted after the probe runs.
    3. If you prefer not to create a test perspective, the probe will instead write to the JessOS perspective
       (or the first custom perspective found), reading the current archivedFilterRules, writing the same value
       back, reading back immediately, and restoring. This is safe but constitutes writing to your real configuration.
    4. Confirm which path to take:
       - Option A: "I created disc-probe-test-perspective — use that"
       - Option B: "Use JessOS / first available perspective — I accept that the probe will write and restore"
  </how-to-verify>

  <resume-signal>
    Type "Option A" or "Option B" (or describe a different preference). This unblocks Task 3 (the write probe).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write and run disc-persp-01 filter-rules persistence probe + populate PERSP section</name>
  <files>
    docs/reference/omnifocus-capabilities.md,
    probes/disc-persp-01-filter-rules-persist.js
  </files>

<read_first>

- .planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md — § Gate-Claim Probe Targets (archivedFilterRules
  persistence row), Open Questions #3 (archivedFilterRules restart question)
- tests/manual/perspectives/test-perspective-rules.js — existing perspective filter-rule probe pattern to reuse
- docs/dev/SETTER-PATTERNS.md — write-then-read-back requirement
- docs/reference/omnifocus-capabilities.md — current PERSP stub + disc-persp-02 evidence already recorded </read_first>

    <action>
      ## Probe safety requirements

      Same discipline as Plan 02 probes: UUID-suffixed unique name for any created objects, abort-on-collision,
      try/finally wrapping, ID tracking, cleanedUp in return JSON.

      ## Step 1 — Write probe: probes/disc-persp-01-filter-rules-persist.js

      The target perspective depends on the human's Option A / Option B choice from the checkpoint:

      Option A (disposable test perspective exists): Target the perspective named "disc-probe-test-perspective".
      Confirm it exists before writing; if it doesn't exist, abort with { aborted: true, reason: "test-perspective-not-found" }.

      Option B (writing to JessOS or first available): Target JessOS (case-insensitive) or the first custom perspective
      if JessOS is absent. Log the perspectiveName in the output so the user can verify.

      In either case, the probe must:
      - Read and save the current archivedFilterRules value as a backup string (capture this BEFORE any write)
      - Write the same value back (a no-op write) to confirm the write path accepts the call
      - Read back immediately in the same script; record whether the read-back matches the written value
      - Restore the original value before returning
      - Wrap the entire write → read-back → restore sequence in try/finally so restore runs even on exception
      - Return JSON: { perspectiveName: string, perspectiveIdentifier: string, writeAccepted: boolean,
          immediateReadBackMatch: boolean, originalRestored: boolean,
          restartPersistenceStatus: "unverified — in-session write confirmed; cross-restart requires manual OF restart",
          ofVersion: string }

      IMPORTANT: Writing the same value back does not prove meaningful mutation — the probe only confirms the write
      API accepts the call without throwing. The in-session read-back confirms the value round-trips. Cross-restart
      persistence cannot be tested without manually quitting and reopening OmniFocus; this is flagged explicitly as
      unverified and will be tested at the Plan 04 human checkpoint.

      ## Step 2 — Run the probe

      Execute: `osascript -l JavaScript probes/disc-persp-01-filter-rules-persist.js`
      Verify it returns originalRestored: true. If originalRestored: false, STOP and surface the perspective name and
      identifier immediately — the user must manually restore archivedFilterRules via OmniFocus undo before proceeding.

      ## Step 3 — Populate PERSP section

      Replace the PERSP stub with findings. Write at minimum:
      - DISC-PERSP-01: archivedFilterRules (read/write) — JSON archive of perspective rules, available since OF 4.2.
        In-session write confirmed by probe disc-persp-01. Cross-restart persistence: unverified (requires manual
        OF restart — see follow-up note). Verdict: extend. Evidence: verified for in-session write + read-back;
        evidence: unverified for cross-restart persistence. Follow-up note: "Cross-restart persistence requires a
        manual write→quit→reopen→read cycle. This is a Phase 6 PROV-01 gate. Plan 04 human checkpoint includes this
        manual step." Downstream cite: Phase 6 (PROV-01 — perspective repair).
      - DISC-PERSP-02: Perspective.Custom.all — enumerates user-created custom perspectives; byName(), byIdentifier().
        Evidence: verified (probe disc-persp-02). Records whether JessOS was found (boolean). Verdict: native.
        Downstream cite: Phase 6 (READAS-01, PROV-01).
      - DISC-PERSP-03: No perspective.tasks or perspective.matchingTasks property in OmniJS API. Verdict: build
        (perspective task resolution requires replicating filter logic in code — this is genuine custom logic OF does
        not provide). Evidence: doc (omni-automation.com/omnifocus/perspective.html — property absent). Rubric:
        "No direct API; READAS-01 requires building a filter-rule interpreter using archivedFilterRules data."
        Downstream cite: Phase 6 (READAS-01).
        NOTE: The underlying data (archivedFilterRules) is native, but the task-resolution operation itself is not
        surfaced natively — DISC-PERSP-03 verdict is build because the operation doesn't exist; the data it
        would operate on does.
      - DISC-PERSP-04: Perspective.Custom cannot be created programmatically — no constructor. Only existing custom
        perspectives can have filter rules written. Verdict: native (repair is native; create-from-scratch is not
        supported and would require build if ever needed). Evidence: doc (omni-automation.com). Downstream cite:
        Phase 6 (PROV-01 "provision or repair" — repair is the implementable path).
        NOTE: This is a single-value verdict. "Native" here means "the supported repair operation is native"; the
        absence of a create-from-scratch API is documented as a constraint in the Downstream cite note.
      Area-level verdict: extend. Rubric: "Perspective list/read already implemented; archivedFilterRules write
      available for repair; task-resolution (DISC-PERSP-03) is build but scoped to Phase 6 only."

      Record sanitized evidence appendices for disc-persp-01 and disc-persp-02 (counts, booleans, identifiers, OF
      version — no perspective names from the live database except for the probe-target perspective already identified
      in the checkpoint).

    </action>

    <verify>
      <automated>grep -c "DISC-PERSP-0[1234]" /Users/jessicaking/projects/omnifocus-mcp/docs/reference/omnifocus-capabilities.md</automated>
    </verify>

<acceptance_criteria>

- probes/disc-persp-01-filter-rules-persist.js exists: try/finally wrapping the write sequence, restores original
  archivedFilterRules before returning, returns originalRestored: true and a restartPersistenceStatus field
- disc-persp-01 returns originalRestored: true — user's perspective is left in its original state
- Both probes exit without a thrown exception against live OmniFocus 4.8.11
- docs/reference/omnifocus-capabilities.md PERSP section contains DISC-PERSP-01 through 04 — each with all fields and a
  single-value verdict from {native, extend, build}
- DISC-PERSP-01 carries evidence: verified for in-session write + evidence: unverified for cross-restart, with an
  explicit follow-up note naming Plan 04 human checkpoint as the resolution step
- DISC-PERSP-03 verdict is build (not extend, not "extend-not-build")
- DISC-PERSP-04 verdict is native (single value — constraints on create-from-scratch documented in the Downstream cite
  note, not in the verdict field)
- PERSP area-level verdict is exactly one of: native / extend / build
- Sanitized evidence appendices recorded (counts/booleans/identifiers, no private perspective names)
  </acceptance_criteria>

<done>PERSP section fully populated. Two perspective probes committed to probes/. archivedFilterRules in-session write
confirmed; restart persistence explicitly flagged as unverified with Plan 04 checkpoint as the resolution step.
DISC-PERSP-03 is build; DISC-PERSP-04 is native.</done> </task>

<task type="auto">
  <name>Task 4: Write CAPTURE and AUTO area findings + automation fit matrix (D-08)</name>
  <files>
    docs/reference/omnifocus-capabilities.md
  </files>

<read_first>

- .planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md — § CAPTURE — Native Capture (full section), § AUTO
  — Automation Surfaces (full section), § AUTO fit matrix table
- .planning/phases/01-omnifocus-capability-discovery/01-CONTEXT.md — D-08 (automation-surface fit assessment
  requirement)
- .planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md — § Assumptions Log (A2 plug-in invocability, A5
  template system)
- docs/dev/JXA-VS-OMNIJS-PATTERNS.md — JXA sunset status, when JXA outer is still needed
- docs/reference/omnifocus-capabilities.md — current CAPTURE and AUTO stubs, and the disc-capture-01 probe output
  already recorded in Plan 02 </read_first>

    <action>
      ## Evidence tag discipline

      For all AUTO findings: the codebase (JXA-VS-OMNIJS-PATTERNS.md, CLAUDE.md) is `evidence: doc` — not `evidence: verified`.
      `evidence: verified` is reserved for live-probed claims. Using the existing codebase as proof that OmniJS works
      is `doc` evidence: the codebase documents the behavior; it is not a live probe run in this phase.

      Area-level verdicts must be single values from {native, extend, build}.

      ## Step 1 — Populate CAPTURE section

      Replace the CAPTURE stub with findings. Write at minimum:
      - DISC-CAPTURE-01: OmniJS new Task(name, inbox) is the MCP server's primary capture path. Supports all OmniJS
        properties in the same script. Evidence: verified (probe disc-capture-01 from Plan 02 — cross-reference the
        sanitized evidence appendix already in the report; do not re-run). Verdict: extend — inbox creation is native;
        the permission gate (PERM-01/02) and lineage stamping (LINE-01) are thin agent-layer additions. Downstream cite:
        Phase 2 (CAP-01, LINE-01).
      - DISC-CAPTURE-02: URL scheme omnifocus:///add — one-way write, supports name/note/project/tags/defer/due/flag/
        repeat-rule. Cannot read data back. Evidence: doc (inside.omnifocus.com/url-schemes). Verdict: extend (alternate
        capture path for external triggers; not the MCP's primary path). Downstream cite: Phase 3 if external webhooks
        are needed for routing triggers.
      - DISC-CAPTURE-03: Quick Entry, Clippings, Mail Drop, Share sheet — native user-facing capture surfaces NOT
        accessible from MCP/script context. Evidence: doc (support.omnigroup.com/documentation/omnifocus). Verdict:
        native (OF handles these; MCP does not need to replicate). Rubric: "User-facing capture is solved natively;
        agent capture is a separate concern." Downstream cite: none (out of agent scope).
      - DISC-CAPTURE-04: OmniFocus has no built-in template system accessible via OmniJS. URL scheme
        omnifocus:///paste (TaskPaper format) exists as a workaround. Evidence: unverified — no template API found in
        official docs or Context7; absence not definitively confirmed. Follow-up note: "Low priority for Phase 1 scope;
        mark for re-examination if Phase 2 needs structured task templates." Verdict: build (if templates needed, must
        be custom; TaskPaper paste is the nearest native path). Downstream cite: Phase 2 if capture needs template-driven
        task creation.
      Area-level verdict: extend. Rubric: "Inbox creation is native via OmniJS; permission gating and lineage are agent
      additions; no template system exists."

      ## Step 2 — Populate AUTO section

      Replace the AUTO stub with findings. This section must include the D-08 automation-surface fit assessment.

      Write findings:
      - DISC-AUTO-01: OmniJS (via osascript evaluateJavascript) — primary automation surface. Full read/write access to
        entire OF data model. Already the exclusive surface for all MCP mutations and complex reads. Evidence: doc
        (JXA-VS-OMNIJS-PATTERNS.md + CLAUDE.md — codebase documentation of the established pattern). Verdict: native.
        Rubric: "OmniJS is the only surface with full read/write access; MCP already relies on it exclusively."
        Downstream cite: All phases.
      - DISC-AUTO-02: JXA / AppleScript — outer executor only (passes scripts to OmniJS); legacy/sunset per Omni Group.
        NOT used for direct property access. Evidence: doc (JXA-VS-OMNIJS-PATTERNS.md — codebase documentation).
        Verdict: extend (minimal wrapper — call evaluateJavascript, nothing more). Downstream cite: All phases (JXA
        outer remains the osascript entry point, but contains no logic).
      - DISC-AUTO-03: URL schemes (omnifocus:///add, /paste, /perspective/[name], x-callback-url) — one-way write path;
        cannot read data. Fit for external capture triggers (webhooks, Shortcuts). Evidence: doc (inside.omnifocus.com/
        url-schemes). Verdict: native (for capture alternate path). Downstream cite: Phase 3 (TRIG-01) if manual
        trigger uses URL scheme; Phase 6 navigation.
      - DISC-AUTO-04: Omni Automation Plug-ins — invocation requires OF foreground + selection; MCP background context
        cannot invoke plug-ins reliably. Evidence: unverified (A2 from Assumptions Log — based on plug-in invocation
        model; probe not run). Follow-up note: "Flag for Phase 6 if PROV-01 needs plug-in path; current plan uses
        archivedFilterRules write which does not require a plug-in." Verdict: build (not applicable for agent-layer
        operations; use OmniJS directly). Downstream cite: Phase 6 (PROV-01) only if archivedFilterRules path proves
        insufficient.
      - DISC-AUTO-05: Apple Shortcuts — OF 4.5 added Get Action / Get Project / Get Perspective / Get Tag actions. Not
        relevant to the MCP server's execution path (MCP uses osascript, not Shortcuts). Evidence: doc (mjtsai.com/blog/
        2024/12/11/omnifocus-4-5/ cited in RESEARCH.md). Verdict: native (for user-facing automation; not the MCP
        path). Downstream cite: none for MCP; potentially Phase 3 if manual triggers via Shortcuts are requested.

      Write the D-08 Automation-Surface Fit Assessment as a dedicated subsection "### Automation Surfaces: Fit
      Assessment (D-08)" using the fit matrix from RESEARCH.md. The matrix columns must be: Surface / Agent Capture
      (CAP-01) / Routing Writes (ROUTE-01..04) / Perspective Provisioning (PROV-01) / MCP Server Basis. Each cell
      records "Primary path", "Possible alternate", "Required", "Not suitable (reason)", or the actual finding.

      Area-level verdict: native (the OmniJS surface is native — it is the OF-provided automation API). Rubric:
      "OmniJS is the right surface for all agent operations; JXA is a necessary but minimal outer wrapper; URL schemes
      cover external trigger use cases."

      ## Step 3 — Confirm all seven area sections are fully populated

      Do a final read of docs/reference/omnifocus-capabilities.md and confirm:
      - Each of the seven area sections has a populated "### Area Verdict" subsection with a single-value verdict
      - No area section still says "— to be populated —"
      - The D-08 fit matrix is present in the AUTO section
      - Every area-level verdict is exactly one of: native / extend / build (check for slash-combined values and fix
        any found)

    </action>

    <verify>
      <automated>grep -c "DISC-CAPTURE-0[1234]\|DISC-AUTO-0[12345]" /Users/jessicaking/projects/omnifocus-mcp/docs/reference/omnifocus-capabilities.md</automated>
    </verify>

<acceptance_criteria>

- docs/reference/omnifocus-capabilities.md CAPTURE section contains DISC-CAPTURE-01 through 04 — each with all fields
- DISC-CAPTURE-01 cross-references the disc-capture-01 sanitized evidence appendix already in the report (does not
  re-run the probe)
- DISC-CAPTURE-04 carries evidence: unverified with an explicit follow-up note
- CAPTURE area-level verdict: extend (single value)
- docs/reference/omnifocus-capabilities.md AUTO section contains DISC-AUTO-01 through 05 — each with all fields
- DISC-AUTO-01 and DISC-AUTO-02 carry evidence: doc (not evidence: verified — they cite codebase docs, not live probes)
- DISC-AUTO-04 carries evidence: unverified for plug-in invocability with an explicit follow-up note
- AUTO section contains the D-08 fit matrix subsection with all four milestone-needs columns
- AUTO area-level verdict: native (single value)
- All seven area sections are fully populated (no "— to be populated —" stubs remaining)
- Every area has a populated "### Area Verdict" subsection with a single-value verdict (grep confirms zero
  slash-combined values) </acceptance_criteria>

<done>CAPTURE and AUTO sections fully written. D-08 automation-surface fit assessment is in the AUTO section. All seven
area sections of docs/reference/omnifocus-capabilities.md are now populated. Evidence tags are correct (doc for codebase
citations, verified for live probes). Two explicitly-flagged unverified findings (DISC-CAPTURE-04, DISC-AUTO-04) each
have follow-up notes.</done> </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| disc-persp-01 → perspective store       | Write probe modifies archivedFilterRules on a perspective; must restore before returning                                    |
| human checkpoint → write decision       | Human decides whether disc-persp-01 targets a disposable test perspective or the real JessOS config                         |
| report AUTO section → downstream phases | The fit matrix is a high-stakes claim — incorrect fit calls would cause downstream phases to plan against the wrong surface |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                         | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                            |
| --------- | ---------------------- | --------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-03-01   | Tampering              | disc-persp-01 writes archivedFilterRules to user's real perspective               | mitigate    | Human checkpoint gates the write decision before Task 3 runs. Probe uses try/finally so restore runs even on exception. Returns originalRestored: true/false. If false, executor surfaces the perspective name and identifier immediately so user can manually restore via OmniFocus undo. |
| T-03-02   | Tampering              | disc-persp-01 accidentally targets wrong perspective                              | mitigate    | Probe explicitly identifies the target perspective by identifier before writing; logs perspectiveName and perspectiveIdentifier in output; executor reviews JSON before accepting the finding.                                                                                             |
| T-03-03   | Tampering              | disc-persp-01 "same value write-back" does not prove meaningful mutation          | accept      | This limitation is documented explicitly in the probe output and in DISC-PERSP-01. The probe confirms the write API accepts the call; it does not prove rule changes would persist. Cross-restart is flagged unverified with Plan 04 checkpoint as the resolution step.                    |
| T-03-04   | Information Disclosure | Perspective names from Perspective.Custom.all committed to report                 | mitigate    | disc-persp-02 returns only { customPerspectiveCount, jessosFound, jessosIdentifier, ofVersion }. No names array in the probe output. Sanitized evidence appendix in the report contains counts and booleans only.                                                                          |
| T-03-05   | Repudiation            | DISC-AUTO-04 (plug-in invocability) recorded as unverified without a probe        | accept      | The unverified status is explicit in the finding with a follow-up note. Phase 6 planning acknowledges this gap. Acceptable as long as archivedFilterRules path is the primary PROV-01 path.                                                                                                |
| T-03-06   | Repudiation            | DISC-CAPTURE-04 (no template system) recorded without definitive proof of absence | accept      | Absence of evidence in official docs is the best available signal. Explicitly flagged unverified with follow-up note. Low-risk: if templates exist, Phase 2 gets a simpler path, not a worse one.                                                                                          |
| T-03-SC   | Tampering              | npm/pip/cargo installs                                                            | accept      | No new packages in this plan                                                                                                                                                                                                                                                               |

</threat_model>

<verification>
After all tasks complete:

1. `grep -c "DISC-PERSP-0[1234]" docs/reference/omnifocus-capabilities.md` — returns 4
2. `grep -c "DISC-CAPTURE-0[1234]" docs/reference/omnifocus-capabilities.md` — returns 4
3. `grep -c "DISC-AUTO-0[12345]" docs/reference/omnifocus-capabilities.md` — returns 5
4. `grep -c "Fit Assessment\|D-08" docs/reference/omnifocus-capabilities.md` — at least 1 (D-08 fit matrix present)
5. `grep -c "to be populated" docs/reference/omnifocus-capabilities.md` — returns 0 (no stubs remain)
6. `grep -c "Area Verdict" docs/reference/omnifocus-capabilities.md` — returns 7 (one per area section)
7. `ls probes/disc-persp-01-filter-rules-persist.js probes/disc-persp-02-custom-all-enumerate.js` — both exist
8. `grep -c "native/\|extend/\|build/" docs/reference/omnifocus-capabilities.md` — returns 0 (no slash-combined
   verdicts) </verification>

<success_criteria>

- PERSP, CAPTURE, and AUTO sections of docs/reference/omnifocus-capabilities.md are fully populated
- All seven area sections have a populated Area Verdict subsection with a single-value verdict (per D-04)
- Two probe scripts committed to probes/ (disc-persp-01, disc-persp-02)
- disc-persp-01 runs cleanly and restores the target perspective to its original state (originalRestored: true)
- The D-08 automation-surface fit matrix is in the AUTO section covering all four milestone-needs columns
- DISC-AUTO-01/02 carry evidence: doc (codebase citations, not live probes) per D-05
- Unverified findings (DISC-PERSP-01 restart persistence, DISC-CAPTURE-04, DISC-AUTO-04) are explicitly flagged with
  follow-up notes — not silent gaps
- Probe evidence in the report is sanitized (counts/booleans/identifiers, no private perspective names)
  </success_criteria>

<output>
Create .planning/phases/01-omnifocus-capability-discovery/01-03-SUMMARY.md when done
</output>
