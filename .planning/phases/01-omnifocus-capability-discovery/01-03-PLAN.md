---
phase: 01-omnifocus-capability-discovery
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - docs/reference/omnifocus-capabilities.md
  - probes/disc-persp-01-filter-rules-persist.js
  - probes/disc-persp-02-custom-all-enumerate.js
autonomous: true
requirements:
  - DISC-01
  - DISC-02

must_haves:
  truths:
    - 'PERSP area section documents archivedFilterRules read/write, Perspective.Custom.all enumeration, and the
      no-perspective.tasks gap'
    - 'CAPTURE area section documents all five native capture surfaces with OmniJS new Task(name,inbox) as the MCP path'
    - 'AUTO area section contains the automation-surface fit matrix required by D-08 (agent capture / routing writes /
      PROV-01 / MCP reliance)'
    - 'PERSP gate-claim probe confirms archivedFilterRules persistence behavior with evidence: verified or flags it
      unverified with an explicit note'
    - 'PERSP gate-claim probe confirms whether Perspective.Custom.all enumerates the JessOS perspective'
    - 'All seven area sections now have a populated Area Verdict subsection'
  artifacts:
    - path: 'docs/reference/omnifocus-capabilities.md'
      provides: 'PERSP, CAPTURE, AUTO sections fully populated; automation fit matrix; all seven areas complete'
      contains: 'DISC-PERSP-01, DISC-CAPTURE-01, DISC-AUTO-01'
    - path: 'probes/disc-persp-01-filter-rules-persist.js'
      provides: 'Gate-claim probe: archivedFilterRules write persistence across OmniFocus restart simulation'
    - path: 'probes/disc-persp-02-custom-all-enumerate.js'
      provides: 'Gate-claim probe: Perspective.Custom.all enumerates JessOS perspective'
  key_links:
    - from: 'docs/reference/omnifocus-capabilities.md#disc-persp-01'
      to: 'probes/disc-persp-01-filter-rules-persist.js'
      via: 'Source field in finding table'
      pattern: 'DISC-PERSP-01'
    - from: 'docs/reference/omnifocus-capabilities.md#disc-auto-01'
      to: 'Phase 2–6 plans'
      via: 'Downstream cite field — automation fit matrix guides PROV-01/CAP-01/ROUTE-01'
      pattern: 'DISC-AUTO-01'
    - from: 'docs/reference/omnifocus-capabilities.md#disc-capture-01'
      to: 'probes/disc-capture-01-inbox-note-roundtrip.js (Plan 02)'
      via: 'Evidence cross-reference from Plan 02'
      pattern: 'DISC-CAPTURE-01'
---

<objective>
Populate the PERSP, CAPTURE, and AUTO area sections of docs/reference/omnifocus-capabilities.md, run the two remaining gate-claim probes for PERSP, and write the automation-surface fit assessment required by D-08.

Purpose: PERSP and AUTO are the areas with the most phase-6 and phase-3 implications respectively. The
archivedFilterRules persistence probe is a direct gate for PROV-01 planning. The automation fit matrix (D-08) is a
cross-cutting finding that gives every downstream phase a clear answer on which surface to use for each operation.

Output: PERSP/CAPTURE/AUTO sections fully written; two probe scripts in probes/; automation fit matrix in the AUTO
section; all seven report areas complete. </objective>

<execution_context> @/Users/jessicaking/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jessicaking/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-CONTEXT.md
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-VALIDATION.md
@/Users/jessicaking/projects/omnifocus-mcp/CLAUDE.md
@/Users/jessicaking/projects/omnifocus-mcp/docs/dev/OMNIJS-FIRST-PATTERN.md
@/Users/jessicaking/projects/omnifocus-mcp/docs/dev/SETTER-PATTERNS.md
@/Users/jessicaking/projects/omnifocus-mcp/src/omnifocus/scripts/perspectives.ts
@/Users/jessicaking/projects/omnifocus-mcp/tests/manual/perspectives/test-perspective-rules.js
@/Users/jessicaking/projects/omnifocus-mcp/tests/manual/perspectives/test-perspective-comprehensive.ts

<interfaces>
<!-- Key patterns for PERSP, CAPTURE, AUTO areas. Extracted from codebase. -->

From src/omnifocus/scripts/perspectives.ts (LIST_PERSPECTIVES_SCRIPT pattern): Enumerates Perspective.BuiltIn.all and
Perspective.Custom.all Reads name, identifier, archivedFilterRules, archivedTopLevelFilterAggregation per custom
perspective

From tests/manual/perspectives/test-perspective-rules.js: Reads and writes perspective.archivedFilterRules Demonstrates
the write path that disc-persp-01 will use

From RESEARCH.md § CAPTURE: Five native capture surfaces: Quick Entry (not scriptable), Clippings, Mail Drop, Share
sheet, URL scheme omnifocus:///add MCP path: OmniJS new Task(name, inbox) — already in mutation-script-builder.ts

From RESEARCH.md § AUTO: Fit matrix rows: OmniJS (primary), URL schemes (capture alternate), Plug-ins (not suitable),
Shortcuts (not suitable) D-08 requires fit notes per surface for: agent capture, routing writes, perspective
provisioning, MCP reliance </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Write PERSP area findings + run archivedFilterRules persistence probes</name>
  <files>
    docs/reference/omnifocus-capabilities.md,
    probes/disc-persp-01-filter-rules-persist.js,
    probes/disc-persp-02-custom-all-enumerate.js
  </files>

<read_first> - .planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md — § PERSP — Perspectives (full section
including the "no perspective.tasks" gap and PROV-01 limitation) -
.planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md — § Gate-Claim Probe Targets (archivedFilterRules
persistence row, Perspective.Custom.all enumerates JessOS row) -
.planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md — Open Questions #3 (archivedFilterRules restart
question) - tests/manual/perspectives/test-perspective-rules.js — existing perspective filter-rule probe pattern to
reuse - src/omnifocus/scripts/perspectives.ts — existing perspective enumeration pattern -
docs/reference/omnifocus-capabilities.md — current PERSP stub </read_first>

  <action>
    Step 1 — Write probe: probes/disc-persp-02-custom-all-enumerate.js
    Write an OmniJS-first read-only probe that:
    - Calls Perspective.Custom.all and returns an array of { name, identifier } for each custom perspective
    - Checks whether a perspective named "JessOS" (case-insensitive substring match) is present
    - Returns JSON: { customPerspectiveCount: number, names: string[], jessosFound: boolean, jessosIdentifier: string | null }
    This is a pure read probe — no write, no cleanup needed.

    Step 2 — Write probe: probes/disc-persp-01-filter-rules-persist.js
    Write an OmniJS-first probe that tests archivedFilterRules write behavior. This is a write probe — it must use write-then-read-back, avoid touching any real user perspective, and clean up. Strategy:
    - Find the JessOS perspective (or the first custom perspective if JessOS is absent) — do NOT modify it permanently
    - Read and save the current archivedFilterRules value as a backup string
    - Write a minimal no-op modification to archivedFilterRules (e.g., write the same value back, or append a trailing space that won't affect behavior)
    - Read back immediately in the same script; record whether the read-back matches the written value
    - Restore the original value before returning
    - For restart persistence: since we cannot quit/relaunch OF in a probe, record that the in-session read-back confirms the write was accepted; flag the cross-restart question as still-assumed (document as `evidence: unverified — in-session write confirmed, cross-restart not tested without manual OF restart`)
    - Returns JSON: { perspectiveName: string, writeAccepted: boolean, immediateReadBackMatch: boolean, originalRestored: boolean, restartPersistenceStatus: "unverified — in-session confirmed" }
    CRITICAL: Restore original archivedFilterRules before returning. The user's JessOS perspective is real data.

    Step 3 — Run both probes:
    Execute: `osascript -l JavaScript probes/disc-persp-02-custom-all-enumerate.js`
    Execute: `osascript -l JavaScript probes/disc-persp-01-filter-rules-persist.js`
    Record the JSON output for both.

    Step 4 — Populate PERSP section in docs/reference/omnifocus-capabilities.md:
    Replace the PERSP stub with findings. Write at minimum:
    - DISC-PERSP-01: archivedFilterRules (read/write) — JSON archive of perspective rules, available since OF 4.2. In-session write confirmed by probe. Cross-restart persistence: status based on probe output (unverified if not tested with restart). Verdict: extend (for read/list — already implemented; for write/repair — available but cross-restart needs manual confirmation). Evidence: verified for in-session; unverified for restart (with follow-up note: "requires manual OF restart to confirm; gate for Phase 6 PROV-01"). Downstream cite: Phase 6 (PROV-01 — perspective repair).
    - DISC-PERSP-02: Perspective.Custom.all — enumerates user-created custom perspectives; byName(), byIdentifier(). Evidence: verified (probe disc-persp-02 output). Records whether JessOS was found. Downstream cite: Phase 6 (READAS-01, PROV-01).
    - DISC-PERSP-03: No perspective.tasks or perspective.matchingTasks property in OmniJS API — confirmed absent. Workaround: replicate perspective's archivedFilterRules as OmniJS predicates. Verdict: build (perspective task resolution requires replicating filter logic in code). Evidence: doc (omni-automation.com/omnifocus/perspective.html — no such property). Rubric: "No direct API; READAS-01 requires building a filter-rule interpreter — this is an `extend` not `build` as the underlying data (archivedFilterRules) is native." Downstream cite: Phase 6 (READAS-01).
    - DISC-PERSP-04: Perspective.Custom cannot be created programmatically — no constructor. Only existing custom perspectives can have filter rules written. Verdict: native/limited (repair is possible; create-from-scratch is not). Evidence: doc (omni-automation.com). Downstream cite: Phase 6 (PROV-01 "provision or repair" — repair is the implementable path).
    Area verdict summary: extend (list/read already implemented) + build (perspective task resolution). Rubric: "Perspective list/repair is native OF Pro API; task-resolution requires replicating filter logic; create-from-scratch is not supported."

    Paste both probe outputs below the relevant finding tables with probe name and run date labels.

  </action>

  <verify>
    <automated>grep -c "DISC-PERSP-0[1234]" /Users/jessicaking/projects/omnifocus-mcp/docs/reference/omnifocus-capabilities.md</automated>
  </verify>

<acceptance_criteria> - probes/disc-persp-02-custom-all-enumerate.js exists (read-only, no cleanup needed) -
probes/disc-persp-01-filter-rules-persist.js exists, writes then restores archivedFilterRules, confirms cleanup -
disc-persp-01 returns originalRestored: true — user's perspective is left in its original state - Both probes exit
without a thrown exception against live OmniFocus 4.8.11 - docs/reference/omnifocus-capabilities.md PERSP section
contains DISC-PERSP-01 through 04 — each with all fields - DISC-PERSP-01 carries `evidence: unverified` for
cross-restart persistence with an explicit follow-up note (per D-05) - DISC-PERSP-02 carries `evidence: verified` with
probe output pasted - DISC-PERSP-03 records the no-perspective.tasks gap and its extend-not-build resolution - PERSP
area verdict summary is present - Probe outputs pasted in the report </acceptance_criteria>

<done>PERSP section fully populated. Two perspective probes committed to probes/. archivedFilterRules in-session write
confirmed; restart persistence flagged as unverified with a clear follow-up note for Phase 6 planning.</done> </task>

<task type="auto">
  <name>Task 2: Write CAPTURE and AUTO area findings + automation fit matrix (D-08)</name>
  <files>
    docs/reference/omnifocus-capabilities.md
  </files>

<read_first> - .planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md — § CAPTURE — Native Capture (full
section), § AUTO — Automation Surfaces (full section), § AUTO fit matrix table -
.planning/phases/01-omnifocus-capability-discovery/01-CONTEXT.md — D-08 (automation-surface fit assessment
requirement) - .planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md — § Assumptions Log (A2 plug-in
invocability, A5 template system) - docs/dev/JXA-VS-OMNIJS-PATTERNS.md — JXA sunset status, when JXA outer is still
needed - docs/reference/omnifocus-capabilities.md — current CAPTURE and AUTO stubs, and the disc-capture-01 probe output
already pasted in Plan 02 </read_first>

  <action>
    Step 1 — Populate CAPTURE section in docs/reference/omnifocus-capabilities.md:
    Replace the CAPTURE stub with findings. Write at minimum:
    - DISC-CAPTURE-01: OmniJS new Task(name, inbox) is the MCP server's primary capture path. Supports all OmniJS properties (name, note, tags, dates, flagged) in the same script. Evidence: verified (probe disc-capture-01 from Plan 02 — cross-reference the probe output already in the report; do not re-run). Verdict: extend — inbox creation is native; the permission gate (PERM-01/02) and lineage stamping (LINE-01) are thin agent-layer additions. Downstream cite: Phase 2 (CAP-01, LINE-01).
    - DISC-CAPTURE-02: URL scheme omnifocus:///add — one-way write, supports name/note/project/tags/defer/due/flag/repeat-rule. Cannot read data back. Evidence: doc (inside.omnifocus.com/url-schemes). Verdict: extend (alternate capture path for external triggers; not the MCP's primary path). Downstream cite: Phase 3 if external webhooks are needed for routing triggers.
    - DISC-CAPTURE-03: Quick Entry, Clippings, Mail Drop, Share sheet — native user-facing capture surfaces NOT accessible from MCP/script context. Evidence: doc (support.omnigroup.com/documentation/omnifocus/universal/4.3.3/en/capture-methods/). Verdict: native (OF handles these; MCP does not need to replicate). Rubric: "User-facing capture is solved natively; agent capture is a separate concern." Downstream cite: none (out of agent scope).
    - DISC-CAPTURE-04: OmniFocus has no built-in template system accessible via OmniJS. URL scheme omnifocus:///paste (TaskPaper format) exists as a workaround. Evidence: unverified — no template API found in official docs or Context7; absence not definitively confirmed. Follow-up note: "Low priority for Phase 1 scope; mark for re-examination if Phase 2 needs structured task templates." Verdict: build (if templates needed, must be custom; TaskPaper paste is the nearest native path). Downstream cite: Phase 2 if capture needs template-driven task creation.
    Area verdict summary: extend. Rubric: "Inbox creation is native via OmniJS; permission gating and lineage are agent additions; no template system exists."

    Step 2 — Populate AUTO section in docs/reference/omnifocus-capabilities.md:
    Replace the AUTO stub with findings. This section must include the D-08 automation-surface fit assessment — not just inventory.

    Write findings:
    - DISC-AUTO-01: OmniJS (via osascript evaluateJavascript) — primary automation surface. Full read/write access to entire OF data model. Already the exclusive surface for all MCP mutations and complex reads. Evidence: verified (codebase). Verdict: native. Rubric: "OmniJS is the only surface with full read/write access; MCP already relies on it exclusively." Downstream cite: All phases.
    - DISC-AUTO-02: JXA / AppleScript — outer executor only (passes scripts to OmniJS); legacy/sunset per Omni Group. NOT used for direct property access. Evidence: verified (codebase JXA-VS-OMNIJS-PATTERNS.md). Verdict: extend (minimal wrapper — call evaluateJavascript, nothing more). Downstream cite: All phases (JXA outer remains the osascript entry point, but contains no logic).
    - DISC-AUTO-03: URL schemes (omnifocus:///add, /paste, /perspective/[name], x-callback-url) — one-way write path; cannot read data. Fit for external capture triggers (webhooks, Shortcuts). Evidence: doc (inside.omnifocus.com/url-schemes). Verdict: native (for capture alternate path). Downstream cite: Phase 3 (TRIG-01) if manual trigger uses URL scheme; Phase 6 navigation.
    - DISC-AUTO-04: Omni Automation Plug-ins — invocation requires OF foreground + selection; MCP background context cannot invoke plug-ins reliably. Evidence: unverified (A2 from Assumptions Log — based on plug-in invocation model; probe not run). Follow-up note: "Flag for Phase 6 if PROV-01 needs plug-in path; current plan uses archivedFilterRules write which does not require a plug-in." Verdict: build (not applicable for agent-layer operations; use OmniJS directly). Downstream cite: Phase 6 (PROV-01) only if archivedFilterRules path proves insufficient.
    - DISC-AUTO-05: Apple Shortcuts — OF 4.5 added Get Action / Get Project / Get Perspective / Get Tag actions. Not relevant to the MCP server's execution path (MCP uses osascript, not Shortcuts). Evidence: doc (mjtsai.com/blog/2024/12/11/omnifocus-4-5/ cited in RESEARCH.md). Verdict: native (for user-facing automation; not the MCP path). Downstream cite: none for MCP; potentially Phase 3 if manual triggers via Shortcuts are requested.

    Write the D-08 Automation-Surface Fit Assessment as a dedicated subsection "### Automation Surfaces: Fit Assessment (D-08)" using the fit matrix from RESEARCH.md. The matrix columns must be: Surface / Agent Capture (CAP-01) / Routing Writes (ROUTE-01..04) / Perspective Provisioning (PROV-01) / MCP Server Basis. This is the direct deliverable for D-08. Each cell records "Primary path", "Possible alternate", "Required", "Not suitable (reason)", or the actual finding.

    Area verdict summary: native (OmniJS surface). Rubric: "OmniJS is the right surface for all agent operations; JXA is a necessary but minimal outer wrapper; URL schemes cover external trigger use cases."

    Step 3 — Confirm all seven area sections are now fully populated:
    Do a final read of docs/reference/omnifocus-capabilities.md and confirm:
    - Each of the seven area sections has a populated "### Area Verdict" subsection (native/extend/build + rubric + evidence)
    - No area section still says "— to be populated —"
    - The D-08 fit matrix is present in the AUTO section

  </action>

  <verify>
    <automated>grep -c "DISC-CAPTURE-0[1234]\|DISC-AUTO-0[12345]" /Users/jessicaking/projects/omnifocus-mcp/docs/reference/omnifocus-capabilities.md</automated>
  </verify>

<acceptance_criteria> - docs/reference/omnifocus-capabilities.md CAPTURE section contains DISC-CAPTURE-01 through 04 —
each with all fields - DISC-CAPTURE-01 cross-references the disc-capture-01 probe output already in the report (does not
re-run the probe) - DISC-CAPTURE-04 carries `evidence: unverified` with an explicit follow-up note - CAPTURE area
verdict summary: extend - docs/reference/omnifocus-capabilities.md AUTO section contains DISC-AUTO-01 through 05 — each
with all fields - DISC-AUTO-04 carries `evidence: unverified` for plug-in invocability with an explicit follow-up note -
AUTO section contains the D-08 fit matrix subsection with all four milestone-needs columns (Agent Capture, Routing
Writes, Perspective Provisioning, MCP Server Basis) - AUTO area verdict summary: native - All seven area sections are
fully populated (no "— to be populated —" stubs remaining) - Every area has a populated "### Area Verdict" subsection
</acceptance_criteria>

<done>CAPTURE and AUTO sections fully written. D-08 automation-surface fit assessment is in the AUTO section. All seven
area sections of docs/reference/omnifocus-capabilities.md are now populated. Two unverified findings are explicitly
flagged with follow-up notes (DISC-CAPTURE-04, DISC-AUTO-04).</done> </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| disc-persp-01 → JessOS perspective      | Write probe modifies archivedFilterRules on a real user perspective; must restore before returning                          |
| report AUTO section → downstream phases | The fit matrix is a high-stakes claim — incorrect fit calls would cause downstream phases to plan against the wrong surface |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                         | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                  |
| --------- | ---------------------- | --------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-01   | Tampering              | disc-persp-01 writes archivedFilterRules to user's real JessOS perspective        | mitigate    | Probe reads original value, writes a same-or-minimal-diff value, reads back to confirm, then restores original. Returns originalRestored: true. If restore fails, probe surfaces the perspective name so user can manually restore from OF's built-in undo.                      |
| T-03-02   | Tampering              | disc-persp-01 accidentally targets wrong perspective                              | mitigate    | Probe explicitly identifies the target perspective by name before writing; logs perspectiveName in output; executor reviews JSON before accepting the finding.                                                                                                                   |
| T-03-03   | Information Disclosure | Perspective names from Perspective.Custom.all embedded in committed report        | mitigate    | disc-persp-02 output may contain sensitive perspective names. Executor must review probe output before pasting into the report; redact any names beyond the count and JessOS confirmation. Generic summary acceptable: "Found N custom perspectives; JessOS: [found/not found]." |
| T-03-04   | Repudiation            | DISC-AUTO-04 (plug-in invocability) recorded as unverified without a probe        | accept      | The unverified status is explicit in the finding with a follow-up note. Phase 6 planning acknowledges this gap. Acceptable as long as archivedFilterRules path is the primary PROV-01 path.                                                                                      |
| T-03-05   | Repudiation            | DISC-CAPTURE-04 (no template system) recorded without definitive proof of absence | accept      | Absence of evidence in official docs is the best available signal. Explicitly flagged unverified with follow-up note. Low-risk: if templates exist, Phase 2 gets a simpler path, not a worse one.                                                                                |
| T-03-SC   | Tampering              | npm/pip/cargo installs                                                            | accept      | No new packages in this plan                                                                                                                                                                                                                                                     |

</threat_model>

<verification>
After both tasks complete:

1. `grep -c "DISC-PERSP-0[1234]" docs/reference/omnifocus-capabilities.md` — returns 4
2. `grep -c "DISC-CAPTURE-0[1234]" docs/reference/omnifocus-capabilities.md` — returns 4
3. `grep -c "DISC-AUTO-0[12345]" docs/reference/omnifocus-capabilities.md` — returns 5
4. `grep -c "Fit Assessment\|D-08" docs/reference/omnifocus-capabilities.md` — at least 1 (D-08 fit matrix present)
5. `grep -c "to be populated" docs/reference/omnifocus-capabilities.md` — returns 0 (no stubs remain)
6. `grep -c "Area Verdict" docs/reference/omnifocus-capabilities.md` — returns 7 (one per area section)
7. `ls probes/disc-persp-01-filter-rules-persist.js probes/disc-persp-02-custom-all-enumerate.js` — both exist
   </verification>

<success_criteria>

- PERSP, CAPTURE, and AUTO sections of docs/reference/omnifocus-capabilities.md are fully populated
- All seven area sections have a populated Area Verdict subsection
- Two probe scripts committed to probes/ (disc-persp-01, disc-persp-02)
- disc-persp-01 runs cleanly and restores the user's perspective to its original state
- The D-08 automation-surface fit matrix is in the AUTO section covering all four milestone-needs columns
- Unverified findings (DISC-PERSP-01 restart persistence, DISC-CAPTURE-04, DISC-AUTO-04) are explicitly flagged with
  follow-up notes — not silent gaps </success_criteria>

<output>
Create .planning/phases/01-omnifocus-capability-discovery/01-03-SUMMARY.md when done
</output>
