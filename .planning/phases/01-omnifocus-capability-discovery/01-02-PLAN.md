---
phase: 01-omnifocus-capability-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - docs/reference/omnifocus-capabilities.md
  - probes/disc-tag-01-auto-create.js
  - probes/disc-tag-02-addtag-omni.js
  - probes/disc-model-01-sequential-write.js
  - probes/disc-capture-01-inbox-note-roundtrip.js
autonomous: true
requirements:
  - DISC-01
  - DISC-02

must_haves:
  truths:
    - 'TAG area section contains findings covering hierarchical tags, addTag OmniJS path, and the auto-create gate-claim
      probe result'
    - 'FILTER area section documents the OmniJS iteration model, inbox/tag.remainingTasks/project.flattenedTasks
      patterns, and the no-.whose() constraint'
    - 'FIELD area section documents task.note as the custom-data extension point with a native verdict'
    - 'MODEL area section covers project.sequential, task.taskStatus enum, repetitionRule setter, and the
      no-native-cross-task-dependency finding'
    - 'Four gate-claim probe scripts exist under probes/ and their output is pasted into the matching findings'
    - 'Every finding in TAG/FILTER/FIELD/MODEL has a verdict (native/extend/build), rubric reason, and evidence tag'
  artifacts:
    - path: 'docs/reference/omnifocus-capabilities.md'
      provides: 'TAG, FILTER, FIELD, MODEL sections fully populated'
      contains: 'DISC-TAG-01, DISC-FILTER-01, DISC-FIELD-01, DISC-MODEL-01'
    - path: 'probes/disc-tag-01-auto-create.js'
      provides: 'Gate-claim probe: does tag.addTag(tagName) auto-create an absent Tag?'
    - path: 'probes/disc-model-01-sequential-write.js'
      provides: 'Gate-claim probe: project.sequential write-back persistence'
    - path: 'probes/disc-capture-01-inbox-note-roundtrip.js'
      provides: 'Gate-claim probe: new Task(name,inbox) + task.note round-trip'
  key_links:
    - from: 'docs/reference/omnifocus-capabilities.md#disc-tag-01'
      to: 'probes/disc-tag-01-auto-create.js'
      via: 'Source field in finding table'
      pattern: 'DISC-TAG-01'
    - from: 'docs/reference/omnifocus-capabilities.md#disc-model-01'
      to: 'probes/disc-model-01-sequential-write.js'
      via: 'Source field in finding table'
      pattern: 'DISC-MODEL-01'
    - from: 'docs/reference/omnifocus-capabilities.md#disc-capture-01'
      to: 'probes/disc-capture-01-inbox-note-roundtrip.js'
      via: 'Source field in finding table'
      pattern: 'DISC-CAPTURE-01'
---

<objective>
Populate the TAG, FILTER, FIELD, and MODEL area sections of docs/reference/omnifocus-capabilities.md with fully-formed findings, and run the four gate-claim probes for those areas.

Purpose: These four areas have the clearest verdicts from research (mostly native/extend) and together cover the core
data model the agent will rely on for Phases 2–3. Getting their findings locked with `evidence: verified` or explicit
evidence tags removes uncertainty before the heavier PERSP/CAPTURE/AUTO areas in Plan 03.

Output: TAG/FILTER/FIELD/MODEL sections fully written, four probe scripts in probes/, probe output pasted into matching
findings. </objective>

<execution_context> @/Users/jessicaking/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jessicaking/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-CONTEXT.md
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-VALIDATION.md
@/Users/jessicaking/projects/omnifocus-mcp/CLAUDE.md
@/Users/jessicaking/projects/omnifocus-mcp/docs/dev/OMNIJS-FIRST-PATTERN.md
@/Users/jessicaking/projects/omnifocus-mcp/docs/dev/JXA-VS-OMNIJS-PATTERNS.md
@/Users/jessicaking/projects/omnifocus-mcp/docs/dev/SETTER-PATTERNS.md
@/Users/jessicaking/projects/omnifocus-mcp/docs/OMNIFOCUS_QUERY_ALTERNATIVES.md
@/Users/jessicaking/projects/omnifocus-mcp/src/contracts/ast/mutation-script-builder.ts
@/Users/jessicaking/projects/omnifocus-mcp/src/contracts/ast/tag-mutation-script-builder.ts

<interfaces>
<!-- Key patterns extracted from codebase for this plan's scope. Use these directly — no exploration needed. -->

From docs/dev/SETTER-PATTERNS.md (key rows): Row 1: reviewInterval — P4 (read-modify-reassign) Row 2: repetitionRule —
P2 (construct new RepetitionRule object) Row 3: project.status — P1 (enum constant assignment) Row 6: task.tags (JXA) —
SILENT NO-OP; use OmniJS addTag() instead

From docs/OMNIFOCUS_QUERY_ALTERNATIVES.md (key patterns): inbox — direct OmniJS global, no scan needed
tag.remainingTasks — tag-scoped collection project.flattenedTasks — project-scoped .whose()/.where() — FORBIDDEN (slow,
unreliable)

From mutation-script-builder.ts (tag assignment pattern): All tag assignment goes through OmniJS addTag(); never JXA
task.tags

From OMNIJS-FIRST-PATTERN.md (probe template): Outer: Application('OmniFocus') + evaluateJavascript() Inner: all logic
uses property access (no parens) — task.name not task.name() </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Write TAG and FILTER area findings + run tag gate-claim probes</name>
  <files>
    docs/reference/omnifocus-capabilities.md,
    probes/disc-tag-01-auto-create.js,
    probes/disc-tag-02-addtag-omni.js
  </files>

<read_first> - .planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md — § TAG — Tagging (full section), §
FILTER — Filtering (full section), § Probe Design Patterns, § Gate-Claim Probe Targets (tag auto-creation row) -
docs/dev/SETTER-PATTERNS.md — row 6 (JXA tag silent no-op), row context for OmniJS addTag() -
src/contracts/ast/tag-mutation-script-builder.ts — existing tag assignment implementation -
docs/OMNIFOCUS_QUERY_ALTERNATIVES.md — filtering patterns - docs/reference/omnifocus-capabilities.md — current scaffold
(read the TAG and FILTER stub sections before writing) </read_first>

  <action>
    Step 1 — Write probe: probes/disc-tag-01-auto-create.js
    Create the probes/ directory if absent. Write a throwaway OmniJS-first probe (outer JXA + inner evaluateJavascript) that:
    - Uses a test-only tag name: "disc-probe-tag-autocreate-test" (unlikely to collide with real data)
    - Attempts task.addTag() passing a string name directly (no Tag object) and records whether it throws or silently fails
    - Attempts the canonical OmniJS path: fetch flattenedTags.byName("disc-probe-tag-autocreate-test") — if null, create via new Tag("disc-probe-tag-autocreate-test", document.tagTree); then addTag()
    - Reads back the task's tags after assignment to confirm
    - Cleans up: removes the test tag and deletes the test task
    - Returns JSON: { tagAutoCreateFromString: boolean, addTagViaObjectWorks: boolean, readBackConfirmed: boolean, cleanedUp: boolean }
    Do not embed fenced code blocks in this action — write the probe file using the Write tool directly.
    Use the standard OmniJS-first probe pattern from OMNIJS-FIRST-PATTERN.md (outer JXA + inner evaluateJavascript).
    Write-then-read-back is mandatory per SETTER-PATTERNS.md and RESEARCH.md probe design patterns.

    Step 2 — Write probe: probes/disc-tag-02-addtag-omni.js
    Write a simpler probe confirming that task.addTag() in OmniJS (object-based, not string) assigns the tag and it persists on read-back. This is doc evidence from SETTER-PATTERNS.md row 6 that becomes verified evidence. Same cleanup discipline: remove test tag + task.

    Step 3 — Run both probes:
    Execute: `osascript -l JavaScript probes/disc-tag-01-auto-create.js`
    Execute: `osascript -l JavaScript probes/disc-tag-02-addtag-omni.js`
    Record the JSON output for each.

    Step 4 — Populate TAG section in docs/reference/omnifocus-capabilities.md:
    Replace the TAG stub section with fully-formed findings. Write at minimum:
    - DISC-TAG-01: Tag assignment requires OmniJS bridge (JXA silently no-ops). Verdict: extend. Source: SETTER-PATTERNS.md row 6 + probe disc-tag-02. Evidence: verified (after probe runs). Downstream cite: Phase 2 (CAP-01, PERM-01), Phase 4 (REVIEW-01).
    - DISC-TAG-02: Tag auto-creation behavior when addTag() is called with a string vs. Tag object. Verdict and evidence: set based on probe disc-tag-01 output. Downstream cite: Phase 2 tag management (PERM-01).
    - DISC-TAG-03: Tags are hierarchical first-class objects (Tag class with name/id/parent/children/tasks). Verdict: native. Evidence: doc (omni-automation.com/omnifocus/tag.html). Downstream cite: Phase 4 (REVIEW-01/02) — agent-workflow tags (agent-okay, review-output, review-capture, archaeology) are conventional, no OF extension required.
    Include the area-level verdict summary in the "### Area Verdict" subsection: extend overall, rubric: "OF has a complete tag API; thin OmniJS wrapper already exists in mutation-script-builder; agent adds semantic conventions only."

    Step 5 — Populate FILTER section in docs/reference/omnifocus-capabilities.md:
    Replace the FILTER stub section with findings. Write at minimum:
    - DISC-FILTER-01: Targeted collection API (inbox, tag.remainingTasks, project.flattenedTasks) — no full scan for scoped queries. Verdict: native. Evidence: verified (codebase OMNIFOCUS_QUERY_ALTERNATIVES.md). Downstream cite: Phase 3 routing (ROUTE-01).
    - DISC-FILTER-02: .whose()/.where() is forbidden — too slow and unreliable. Verdict: native (OF has it but it's unusable). Evidence: verified (CLAUDE.md + OMNIFOCUS_QUERY_ALTERNATIVES.md). Rubric: existing repo policy; enforced at CLAUDE.md level. Downstream cite: All phases using task queries.
    - DISC-FILTER-03: Date/flag combined filtering requires flattenedTasks linear scan (no server-side date-range API). Verdict: extend. Evidence: verified (codebase). Downstream cite: Phase 4 review/today view filtering.
    Area verdict summary: extend. Rubric: "OF filtering is imperative OmniJS iteration; AST query compiler wraps it; no new build needed."
    Evidence on all FILTER findings can be `evidence: verified` from codebase confirmation.

    Paste the probe output from Step 3 as a fenced code block (non-JavaScript, use text or json fence) immediately below the finding row table for DISC-TAG-01 and DISC-TAG-02. Label each block "Probe output (disc-tag-01-auto-create.js, run YYYY-MM-DD)".

  </action>

  <verify>
    <automated>grep -c "DISC-TAG-0[123]\|DISC-FILTER-0[123]" /Users/jessicaking/projects/omnifocus-mcp/docs/reference/omnifocus-capabilities.md</automated>
  </verify>

<acceptance_criteria> - probes/disc-tag-01-auto-create.js exists and follows OmniJS-first pattern with
write-then-read-back and cleanup - probes/disc-tag-02-addtag-omni.js exists - Both probes exit without a thrown
exception when run against live OmniFocus 4.8.11 - Both probes clean up their test tag and test task -
docs/reference/omnifocus-capabilities.md TAG section contains DISC-TAG-01, DISC-TAG-02, DISC-TAG-03 — each with Verdict,
Rubric, Evidence, Source, Downstream cite fields - TAG area verdict summary is present -
docs/reference/omnifocus-capabilities.md FILTER section contains DISC-FILTER-01, DISC-FILTER-02, DISC-FILTER-03 — each
with all fields - FILTER area verdict summary is present - Probe output for DISC-TAG-01 and DISC-TAG-02 is pasted in the
report - No `evidence: unverified` in TAG or FILTER sections without an explicit follow-up note </acceptance_criteria>

<done>TAG and FILTER sections fully populated in docs/reference/omnifocus-capabilities.md. Two probe scripts committed
to probes/ with output recorded in the report.</done> </task>

<task type="auto">
  <name>Task 2: Write FIELD and MODEL area findings + run gate-claim probes</name>
  <files>
    docs/reference/omnifocus-capabilities.md,
    probes/disc-model-01-sequential-write.js,
    probes/disc-capture-01-inbox-note-roundtrip.js
  </files>

<read_first> - .planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md — § FIELD — Custom Fields (full
section), § MODEL — Project/Task Data Model (full section), § Gate-Claim Probe Targets (sequential write-back row, new
Task + note round-trip row) - docs/dev/SETTER-PATTERNS.md — row 2 (repetitionRule P2 pattern), row 3 (project.status P1
enum), general P4 read-modify-reassign pattern - src/contracts/ast/mutation-script-builder.ts — existing task note write
and project.sequential patterns - docs/reference/omnifocus-capabilities.md — current state of FIELD and MODEL stubs
</read_first>

  <action>
    Step 1 — Write probe: probes/disc-model-01-sequential-write.js
    Write an OmniJS-first probe that:
    - Creates a test project named "disc-probe-sequential-test" with sequential: true
    - Reads back project.sequential immediately after write
    - Sets sequential: false and reads back again
    - Records: { initialWrite: true, readBack1: boolean, writeBack: false, readBack2: boolean, persisted: boolean }
    - Cleans up: drops the test project (sets project.status to Project.Status.Dropped or deletes it)
    Write-then-read-back required. No .whose()/.where(). Use flattenedProjects to find the created project for cleanup.

    Step 2 — Write probe: probes/disc-capture-01-inbox-note-roundtrip.js
    Write an OmniJS-first probe that:
    - Creates a test task in the inbox: new Task("disc-probe-note-roundtrip-test", inbox)
    - Sets task.note to a test session-ID string: "SESSION:disc-probe-test-001"
    - Reads back task.note immediately
    - Confirms the inbox collection reflects the task immediately (read inbox.tasks, find task by name)
    - Records: { taskCreated: boolean, noteWritten: string, noteReadBack: string, notePersisted: boolean, inboxReflectsImmediately: boolean }
    - Cleans up: deletes/drops the test task
    This probe covers two gate-claims: the note round-trip (LINE-01 gate) AND inbox immediacy (CAP-01 gate).

    Step 3 — Run both probes:
    Execute: `osascript -l JavaScript probes/disc-model-01-sequential-write.js`
    Execute: `osascript -l JavaScript probes/disc-capture-01-inbox-note-roundtrip.js`
    Record the JSON output. Note: the disc-capture-01 probe also provides the "inbox collection immediacy" gate-claim evidence — this feeds the CAPTURE section in Plan 03. Paste this output in the MODEL section now (for the note round-trip aspect) and note it will also be cited in the CAPTURE section.

    Step 4 — Populate FIELD section in docs/reference/omnifocus-capabilities.md:
    Replace the FIELD stub with findings. Write at minimum:
    - DISC-FIELD-01: task.note is the custom-data extension point — free-form text or structured content (e.g., JSON prefix). Verdict: native. Evidence: doc (omni-automation.com/omnifocus/task.html). Rubric: "OF has no first-class custom fields; task.note is the intended extension surface." Downstream cite: Phase 2 (LINE-01 session lineage), Phase 5 (ARCH-03 archaeology tag).
    - DISC-FIELD-02: Structured native fields — estimatedMinutes, defer/due/completion dates, flagged, plannedDate (v4.7+), taskStatus enum. Verdict: native. Evidence: doc (omni-automation.com/task.html) + note that plannedDate is v4.7+; confirmed available on 4.8.11 per version. Downstream cite: Phase 3 (task routing metadata).
    - DISC-FIELD-03: No first-class custom fields in OmniFocus — confirmed by official discourse + docs search. Verdict: native (use note field). Evidence: doc (discourse.omnigroup.com/t/custom-fields). Rubric: "Custom fields would require build; OF community confirms absence; note field is the accepted pattern."
    Area verdict summary: native. Rubric: "task.note handles all agent custom-data needs; no build required."

    Step 5 — Populate MODEL section in docs/reference/omnifocus-capabilities.md:
    Replace the MODEL stub with findings. Write at minimum:
    - DISC-MODEL-01: project.sequential (Boolean) controls available tasks. Write-back pattern: direct assignment in OmniJS. Evidence: verified (probe disc-model-01; paste output). Verdict: native. Downstream cite: Phase 3 (ROUTE-03 project creation with sequential flag).
    - DISC-MODEL-02: task.taskStatus enum (Available, Blocked, Completed, Dropped, DueSoon, Next, Overdue). Verdict: native. Evidence: doc (omni-automation.com/omnifocus/task.html). Downstream cite: Phase 3 routing (available-task detection).
    - DISC-MODEL-03: OmniFocus has NO native cross-project task dependency system. Sequential project ordering is the only built-in dependency mechanism. Verdict: native (for sequential), build (if cross-task dependencies ever needed — not in this milestone). Evidence: doc (community plug-in ksalzke/dependency-omnifocus-plugin exists because native dependency is absent). Rubric: "Don't build cross-task dependencies — sequential projects cover the agent-workflow need; if needed later, use the community plug-in." Downstream cite: Phase 3 (ROUTE-01 — project matching, not dependency tracking).
    - DISC-MODEL-04: repetitionRule setter requires P2 pattern (new Task.RepetitionRule(...)); project.reviewInterval requires P4 (read-modify-reassign). Verdict: extend (thin wrapper needed for correct setter behavior). Evidence: verified (SETTER-PATTERNS.md rows 1–2). Downstream cite: Phase 2 if recurrence is set on captured tasks.
    - DISC-MODEL-05: moveTasks([task], destination) — moves tasks between projects in OmniJS. Verdict: native. Evidence: doc (omni-automation.com). Downstream cite: Phase 3 (ROUTE-01 task filing).
    Area verdict summary: native. Rubric: "OF data model is fully native; extend only for setter-pattern wrappers that already exist in mutation-script-builder."

    Paste probe outputs from Step 3 below the relevant finding tables. Use fenced text/json blocks labeled with the probe name and run date.

  </action>

  <verify>
    <automated>grep -c "DISC-FIELD-0[123]\|DISC-MODEL-0[12345]" /Users/jessicaking/projects/omnifocus-mcp/docs/reference/omnifocus-capabilities.md</automated>
  </verify>

<acceptance_criteria> - probes/disc-model-01-sequential-write.js exists, follows OmniJS-first with write-then-read-back
and cleanup - probes/disc-capture-01-inbox-note-roundtrip.js exists, confirms task.note round-trip and inbox immediacy -
Both probes exit without a thrown exception against live OmniFocus 4.8.11 - Both probes clean up their test data
(project dropped, task deleted) - docs/reference/omnifocus-capabilities.md FIELD section contains DISC-FIELD-01, 02, 03
— each with all required fields - FIELD area verdict summary: native - docs/reference/omnifocus-capabilities.md MODEL
section contains DISC-MODEL-01 through 05 — each with all required fields - MODEL area verdict summary: native (with
extend qualifier for setter wrappers) - Probe outputs from disc-model-01 and disc-capture-01 are pasted in the report -
disc-capture-01 probe output is noted as also providing CAPTURE-section evidence for Plan 03 - No `evidence: unverified`
in FIELD or MODEL sections without an explicit follow-up note </acceptance_criteria>

<done>FIELD and MODEL sections fully populated in docs/reference/omnifocus-capabilities.md. Two probe scripts committed
to probes/ with output recorded. The disc-capture-01 probe output is available for Plan 03's CAPTURE section.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                            |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------ |
| probe scripts → OmniFocus live store | Write probes (disc-model-01, disc-capture-01) mutate the user's real OF database; cleanup is mandatory |
| probe output → report                | Probe output embedded in committed report; must not contain sensitive user task content                |

## STRIDE Threat Register

| Threat ID | Category               | Component                                            | Disposition | Mitigation Plan                                                                                                                                                                                                                                         |
| --------- | ---------------------- | ---------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Tampering              | Write probes mutate live OF store without cleanup    | mitigate    | Every write probe (disc-model-01, disc-capture-01) must include explicit cleanup: drop the test project, delete the test task. Probe returns cleanedUp: boolean. If cleanup fails, surface the orphaned object name so the user can remove it manually. |
| T-02-02   | Tampering              | disc-tag-01 creates a real tag that isn't cleaned up | mitigate    | Probe must delete the test tag ("disc-probe-tag-autocreate-test") and confirm it is absent on read-back before returning.                                                                                                                               |
| T-02-03   | Information Disclosure | Probe output contains real task names from inbox     | mitigate    | Probes create their own test objects with known names; they do not read or embed existing inbox content. Probe JSON output contains only test-object data.                                                                                              |
| T-02-04   | Repudiation            | FIELD finding cites discourse post as `doc` evidence | accept      | The discourse citation is for "no custom fields" — a negative claim; the risk of being wrong is low (OF would have to have added custom fields without documentation). Acceptable as doc evidence.                                                      |
| T-02-05   | Tampering              | disc-capture-01 leaves a task in the inbox           | mitigate    | Probe must clean up the test task before returning. Probe name "disc-probe-note-roundtrip-test" is distinctive enough to identify if cleanup fails.                                                                                                     |
| T-02-SC   | Tampering              | npm/pip/cargo installs                               | accept      | No new packages; build uses locked package-lock.json                                                                                                                                                                                                    |

</threat_model>

<verification>
After both tasks complete:

1. `ls probes/disc-tag-01-auto-create.js probes/disc-tag-02-addtag-omni.js probes/disc-model-01-sequential-write.js probes/disc-capture-01-inbox-note-roundtrip.js`
   — all four exist
2. `grep -c "DISC-TAG-0[123]" docs/reference/omnifocus-capabilities.md` — returns 3
3. `grep -c "DISC-FILTER-0[123]" docs/reference/omnifocus-capabilities.md` — returns 3
4. `grep -c "DISC-FIELD-0[123]" docs/reference/omnifocus-capabilities.md` — returns 3
5. `grep -c "DISC-MODEL-0[12345]" docs/reference/omnifocus-capabilities.md` — returns 5
6. `grep -v "^#" docs/reference/omnifocus-capabilities.md | grep -c "evidence: unverified"` — returns 0 for
   TAG/FILTER/FIELD/MODEL sections (all are verified or doc-cited)
7. `grep -c "Area Verdict" docs/reference/omnifocus-capabilities.md` — at least 4 area verdicts present </verification>

<success_criteria>

- TAG, FILTER, FIELD, and MODEL area sections of docs/reference/omnifocus-capabilities.md are fully populated with
  DISC-&lt;AREA&gt;-NN findings
- Every finding has: Verdict (native/extend/build), Rubric (one-line reason), Evidence tag, Source, Downstream cite
- Four probe scripts exist in probes/ — all run cleanly against OmniFocus 4.8.11 and clean up after themselves
- Gate-claim probe outputs for disc-tag-01/02 and disc-model-01/disc-capture-01 are pasted into the matching report
  findings with evidence: verified
- No silent unverified findings in these four areas </success_criteria>

<output>
Create .planning/phases/01-omnifocus-capability-discovery/01-02-SUMMARY.md when done
</output>
