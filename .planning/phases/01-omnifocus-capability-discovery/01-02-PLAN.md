---
phase: 01-omnifocus-capability-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - docs/reference/omnifocus-capabilities.md
  - probes/disc-tag-01-auto-create.js
  - probes/disc-tag-02-addtag-omni.js
  - probes/disc-model-01-sequential-write.js
  - probes/disc-capture-01-inbox-note-roundtrip.js
autonomous: true
requirements:
  - DISC-01
  - DISC-02

must_haves:
  truths:
    - 'TAG area section contains findings covering hierarchical tags, addTag OmniJS path, and the auto-create gate-claim
      probe result'
    - 'FILTER area section documents the OmniJS iteration model, inbox/tag.remainingTasks/project.flattenedTasks
      patterns, and the no-.whose() constraint'
    - 'FIELD area section documents task.note as the custom-data extension point with a native verdict'
    - 'MODEL area section covers project.sequential, task.taskStatus enum, repetitionRule setter, and the
      no-native-cross-task-dependency finding'
    - 'Four gate-claim probe scripts exist under probes/ — all write probes use try/finally, UUID-suffixed names, and
      abort-on-collision'
    - 'Every finding in TAG/FILTER/FIELD/MODEL has a single-value verdict (native/extend/build), rubric reason, and
      correct evidence tag (verified only for live-probe results, doc for codebase citations)'
    - 'Probe output recorded in the report is sanitized: counts, booleans, target IDs, app version — no user-visible
      task or tag names'
  artifacts:
    - path: 'docs/reference/omnifocus-capabilities.md'
      provides: 'TAG, FILTER, FIELD, MODEL sections fully populated'
      contains: 'DISC-TAG-01, DISC-FILTER-01, DISC-FIELD-01, DISC-MODEL-01'
    - path: 'probes/disc-tag-01-auto-create.js'
      provides: 'Gate-claim probe: does tag.addTag(tagName) auto-create an absent Tag?'
    - path: 'probes/disc-model-01-sequential-write.js'
      provides: 'Gate-claim probe: project.sequential write-back persistence'
    - path: 'probes/disc-capture-01-inbox-note-roundtrip.js'
      provides: 'Gate-claim probe: new Task(name,inbox) + task.note round-trip'
  key_links:
    - from: 'docs/reference/omnifocus-capabilities.md#disc-tag-01'
      to: 'probes/disc-tag-01-auto-create.js'
      via: 'Source field in finding table'
      pattern: 'DISC-TAG-01'
    - from: 'docs/reference/omnifocus-capabilities.md#disc-model-01'
      to: 'probes/disc-model-01-sequential-write.js'
      via: 'Source field in finding table'
      pattern: 'DISC-MODEL-01'
    - from: 'docs/reference/omnifocus-capabilities.md#disc-capture-01'
      to: 'probes/disc-capture-01-inbox-note-roundtrip.js'
      via: 'Source field in finding table'
      pattern: 'DISC-CAPTURE-01'
---

<objective>
Populate the TAG, FILTER, FIELD, and MODEL area sections of docs/reference/omnifocus-capabilities.md with fully-formed findings, and run the four gate-claim probes for those areas.

Purpose: These four areas have the clearest verdicts from research (mostly native/extend) and together cover the core
data model the agent will rely on for Phases 2–3. Getting their findings locked with `evidence: verified` (from live
probes) or `evidence: doc` (from codebase documentation) removes uncertainty before the heavier PERSP/CAPTURE/AUTO areas
in Plan 03.

Output: TAG/FILTER/FIELD/MODEL sections fully written, four probe scripts in probes/, sanitized probe evidence recorded
in matching findings. </objective>

<execution_context> @/Users/jessicaking/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jessicaking/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-CONTEXT.md
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md
@/Users/jessicaking/projects/omnifocus-mcp/.planning/phases/01-omnifocus-capability-discovery/01-VALIDATION.md
@/Users/jessicaking/projects/omnifocus-mcp/CLAUDE.md
@/Users/jessicaking/projects/omnifocus-mcp/docs/dev/OMNIJS-FIRST-PATTERN.md
@/Users/jessicaking/projects/omnifocus-mcp/docs/dev/JXA-VS-OMNIJS-PATTERNS.md
@/Users/jessicaking/projects/omnifocus-mcp/docs/dev/SETTER-PATTERNS.md
@/Users/jessicaking/projects/omnifocus-mcp/docs/OMNIFOCUS_QUERY_ALTERNATIVES.md
@/Users/jessicaking/projects/omnifocus-mcp/src/contracts/ast/mutation-script-builder.ts
@/Users/jessicaking/projects/omnifocus-mcp/src/contracts/ast/tag-mutation-script-builder.ts

<interfaces>
<!-- Key patterns extracted from codebase for this plan's scope. Use these directly — no exploration needed. -->

From docs/dev/SETTER-PATTERNS.md (key rows): Row 1: reviewInterval — P4 (read-modify-reassign) Row 2: repetitionRule —
P2 (construct new RepetitionRule object) Row 3: project.status — P1 (enum constant assignment) Row 6: task.tags (JXA) —
SILENT NO-OP; use OmniJS addTag() instead

From docs/OMNIFOCUS_QUERY_ALTERNATIVES.md (key patterns): inbox — direct OmniJS global, no scan needed
tag.remainingTasks — tag-scoped collection project.flattenedTasks — project-scoped .whose()/.where() — FORBIDDEN (slow,
unreliable)

From mutation-script-builder.ts (tag assignment pattern): All tag assignment goes through OmniJS addTag(); never JXA
task.tags

From OMNIJS-FIRST-PATTERN.md (probe template): Outer: Application('OmniFocus') + evaluateJavascript() Inner: all logic
uses property access (no parens) — task.name not task.name() </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Write TAG and FILTER area findings + run tag gate-claim probes</name>
  <files>
    docs/reference/omnifocus-capabilities.md,
    probes/disc-tag-01-auto-create.js,
    probes/disc-tag-02-addtag-omni.js
  </files>

<read_first>

- .planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md — § TAG — Tagging (full section), § FILTER —
  Filtering (full section), § Probe Design Patterns, § Gate-Claim Probe Targets (tag auto-creation row)
- docs/dev/SETTER-PATTERNS.md — row 6 (JXA tag silent no-op), row context for OmniJS addTag()
- src/contracts/ast/tag-mutation-script-builder.ts — existing tag assignment implementation
- docs/OMNIFOCUS_QUERY_ALTERNATIVES.md — filtering patterns
- docs/reference/omnifocus-capabilities.md — current scaffold (read the TAG and FILTER stub sections before writing)
  </read_first>

    <action>
      ## Evidence tag discipline (mandatory before writing any finding)

      Apply the following rules for every finding written in this task (per D-05 as defined in Plan 01 scaffold):
      - `evidence: verified` — only for findings where a live probe on 4.8.11 is run in this plan and probe output is
        recorded. NEVER for CLAUDE.md references, SETTER-PATTERNS.md rows, or OMNIFOCUS_QUERY_ALTERNATIVES.md — those
        are `evidence: doc`.
      - `evidence: doc` — for findings cited from codebase docs (SETTER-PATTERNS.md, CLAUDE.md,
        OMNIFOCUS_QUERY_ALTERNATIVES.md), or official OF4 documentation.
      - Area-level verdicts must be exactly one value from {native, extend, build}. No slash-combined values
        (e.g., `native/limited` is forbidden).

      ## Probe safety requirements (mandatory for all write probes in this task)

      Every write probe must:
      (a) Generate a unique object name using a timestamp or random suffix (e.g., `disc-probe-tag-autocreate-<timestamp>`).
          Before creating any object, check whether an object with that name already exists and ABORT the probe if one
          is found (do not modify objects the probe did not create).
      (b) Track the ID of every object created during the run.
      (c) Wrap the create → read-back → cleanup sequence in try/finally so cleanup runs even if an exception is thrown.
      (d) Remove only objects whose IDs were captured in the tracking set (step b).
      (e) Return cleanedUp: boolean in the JSON result.

      ## Probe output recording

      After running each probe, record a compact sanitized evidence appendix (not raw output) in the report:
      - Format: command | timestamp | sanitized result | cleanup result | OF version
      - Sanitized result means: counts, booleans, IDs/identifiers — never user-visible tag names, task names, or content
        that exists in the user's live OmniFocus database.
      - The probe-created test objects (e.g., "disc-probe-tag-autocreate-1749600000") are acceptable to name because
        they were created by the probe and were cleaned up.

      ## Step 1 — Write probe: probes/disc-tag-01-auto-create.js

      Create the probes/ directory if absent. Write a throwaway OmniJS-first probe (outer JXA + inner evaluateJavascript)
      that follows the probe safety requirements above. The probe:
      - Generates a unique name: "disc-probe-tag-autocreate-" + Date.now()
      - Aborts (returns { aborted: true, reason: "collision" }) if a tag by that name already exists
      - Attempts task.addTag() passing a string name directly (no Tag object) and records whether it throws or silently
        fails
      - Attempts the canonical OmniJS path: fetch flattenedTags.byName(probeName) — if null, create via
        new Tag(probeName, document.tagTree); then addTag()
      - Reads back the task's tags after assignment to confirm (write-then-read-back per SETTER-PATTERNS.md)
      - In try/finally: cleanup removes the test tag and test task using their captured IDs
      - Returns JSON: { tagAutoCreateFromString: boolean, addTagViaObjectWorks: boolean, readBackConfirmed: boolean,
          cleanedUp: boolean, probeTagName: string, ofVersion: string }

      ## Step 2 — Write probe: probes/disc-tag-02-addtag-omni.js

      Write a simpler probe (with the same try/finally + UUID-name + abort-on-collision discipline) confirming that
      task.addTag() in OmniJS (object-based, not string) assigns the tag and it persists on read-back. Returns JSON
      with the same fields. Cleanup: remove test tag + task.

      ## Step 3 — Run both probes

      Execute: `osascript -l JavaScript probes/disc-tag-01-auto-create.js`
      Execute: `osascript -l JavaScript probes/disc-tag-02-addtag-omni.js`
      Verify both return JSON with cleanedUp: true. If either returns cleanedUp: false, surface the probe name so the
      user can manually remove the orphaned object before proceeding.

      ## Step 4 — Populate TAG section

      Replace the TAG stub section with fully-formed findings. Write at minimum:
      - DISC-TAG-01: Tag assignment requires OmniJS bridge (JXA silently no-ops). Verdict: extend. Source:
        SETTER-PATTERNS.md row 6 + probe disc-tag-02. Evidence: doc for the SETTER-PATTERNS.md claim; upgrade to
        verified after probe disc-tag-02 confirms it on 4.8.11. Downstream cite: Phase 2 (CAP-01, PERM-01), Phase 4
        (REVIEW-01).
      - DISC-TAG-02: Tag auto-creation behavior when addTag() is called with a string vs. Tag object. Verdict and
        evidence: set based on probe disc-tag-01 output (evidence: verified after probe runs). Downstream cite: Phase 2
        tag management (PERM-01).
      - DISC-TAG-03: Tags are hierarchical first-class objects (Tag class with name/id/parent/children/tasks). Verdict:
        native. Evidence: doc (omni-automation.com/omnifocus/tag.html). Downstream cite: Phase 4 (REVIEW-01/02) —
        agent-workflow tags (agent-okay, review-output, review-capture, archaeology) are conventional, no OF extension
        required.
      Area-level verdict: extend. Rubric: "OF has a complete tag API; thin OmniJS wrapper already exists in
      mutation-script-builder; agent adds semantic conventions only."

      Record the sanitized evidence appendix from Step 3 immediately below the DISC-TAG-01 and DISC-TAG-02 finding
      tables (format: command | timestamp | sanitized result | cleanup result | OF version).

      ## Step 5 — Populate FILTER section

      Replace the FILTER stub section with findings. Write at minimum:
      - DISC-FILTER-01: Targeted collection API (inbox, tag.remainingTasks, project.flattenedTasks) — no full scan for
        scoped queries. Verdict: native. Evidence: doc (OMNIFOCUS_QUERY_ALTERNATIVES.md — this is a codebase doc
        citation, not a live probe). Downstream cite: Phase 3 routing (ROUTE-01).
      - DISC-FILTER-02: .whose()/.where() is forbidden — too slow and unreliable. Verdict: native (OF has it but it is
        unusable in this context). Evidence: doc (CLAUDE.md + OMNIFOCUS_QUERY_ALTERNATIVES.md). Rubric: existing repo
        policy; enforced at CLAUDE.md level. Downstream cite: All phases using task queries.
      - DISC-FILTER-03: Date/flag combined filtering requires flattenedTasks linear scan (no server-side date-range API).
        Verdict: extend. Evidence: doc (OMNIFOCUS_QUERY_ALTERNATIVES.md + existing AST query compiler in codebase).
        Downstream cite: Phase 4 review/today view filtering.
      Area-level verdict: extend. Rubric: "OF filtering is imperative OmniJS iteration; AST query compiler wraps it; no
      new build needed."

    </action>

    <verify>
      <automated>grep -c "DISC-TAG-0[123]\|DISC-FILTER-0[123]" /Users/jessicaking/projects/omnifocus-mcp/docs/reference/omnifocus-capabilities.md</automated>
    </verify>

<acceptance_criteria>

- probes/disc-tag-01-auto-create.js exists and follows OmniJS-first pattern with UUID-suffixed name, abort-on-collision,
  try/finally cleanup, write-then-read-back, and cleanedUp in return JSON
- probes/disc-tag-02-addtag-omni.js exists with the same probe safety discipline
- Both probes exit without a thrown exception when run against live OmniFocus 4.8.11
- Both probes return cleanedUp: true
- docs/reference/omnifocus-capabilities.md TAG section contains DISC-TAG-01, DISC-TAG-02, DISC-TAG-03 — each with
  Verdict (single value), Rubric, Evidence (verified only for live-probe results, doc for SETTER-PATTERNS.md), Source,
  Downstream cite
- TAG area verdict is exactly one of: native / extend / build (no slash-combined value)
- FILTER section contains DISC-FILTER-01, DISC-FILTER-02, DISC-FILTER-03 — all with correct evidence: doc (not verified,
  as these are codebase-doc citations, not live probes)
- FILTER area verdict is exactly one of: native / extend / build
- Sanitized evidence appendix for DISC-TAG-01 and DISC-TAG-02 is recorded (counts/booleans/IDs, no user-visible tag
  names from the live database)
- No `evidence: unverified` in TAG or FILTER sections without an explicit follow-up note </acceptance_criteria>

<done>TAG and FILTER sections fully populated in docs/reference/omnifocus-capabilities.md. Two probe scripts committed
to probes/ with sanitized evidence recorded. Evidence tags conform to D-05: doc for codebase citations, verified for
live-probe results.</done> </task>

<task type="auto">
  <name>Task 2: Write FIELD and MODEL area findings + run gate-claim probes</name>
  <files>
    docs/reference/omnifocus-capabilities.md,
    probes/disc-model-01-sequential-write.js,
    probes/disc-capture-01-inbox-note-roundtrip.js
  </files>

<read_first>

- .planning/phases/01-omnifocus-capability-discovery/01-RESEARCH.md — § FIELD — Custom Fields (full section), § MODEL —
  Project/Task Data Model (full section), § Gate-Claim Probe Targets (sequential write-back row, new Task + note
  round-trip row)
- docs/dev/SETTER-PATTERNS.md — row 2 (repetitionRule P2 pattern), row 3 (project.status P1 enum), general P4
  read-modify-reassign pattern
- src/contracts/ast/mutation-script-builder.ts — existing task note write and project.sequential patterns
- docs/reference/omnifocus-capabilities.md — current state of FIELD and MODEL stubs </read_first>

    <action>
      ## Evidence tag discipline and area-verdict rules

      Same as Task 1: `evidence: verified` only for live probe results recorded here; `evidence: doc` for
      SETTER-PATTERNS.md citations. Area-level verdicts must be exactly one value from {native, extend, build}.

      For MODEL: the area-level verdict is native. DISC-MODEL-04 (setter-pattern wrappers) gets its own finding-level
      verdict of extend — this nuance belongs in the per-finding row, not in the area-level summary.

      For DISC-MODEL-03: the "no-native-cross-task-dependency" finding has a native verdict for the sequential ordering
      mechanism and a build verdict for cross-task dependencies. These are two distinct findings, not one combined
      verdict. Write them as DISC-MODEL-03a and DISC-MODEL-03b (or as two separate DISC-MODEL rows) so each carries a
      clean single verdict.

      ## Probe safety requirements

      Every write probe must follow the same discipline as Task 1: UUID-suffixed unique name, abort-on-collision,
      try/finally wrapping the create → read-back → cleanup sequence, ID tracking, cleanedUp in return JSON.

      For project cleanup in disc-model-01: use project.status = Project.Status.Dropped (per SETTER-PATTERNS.md P1
      pattern for project.status). Do NOT use a destructive bulk delete or a path that bypasses the hardened mutation
      policy. The test project was created by the probe; setting it to Dropped is the non-destructive supported teardown.

      ## Step 1 — Write probe: probes/disc-model-01-sequential-write.js

      Write an OmniJS-first probe that:
      - Generates a unique project name: "disc-probe-sequential-" + Date.now()
      - Aborts with { aborted: true, reason: "collision" } if a project by that name already exists
      - Creates a test project with sequential: true
      - Reads back project.sequential immediately after write (write-then-read-back required)
      - Sets sequential: false and reads back again
      - In try/finally: sets project.status = Project.Status.Dropped for the probe's project ID
      - Returns JSON: { probeProjectName: string, initialWrite: true, readBack1: boolean, writeBack: false,
          readBack2: boolean, persisted: boolean, cleanedUp: boolean, ofVersion: string }
      No .whose()/.where(). Use flattenedProjects to find the created project for cleanup, matching by the captured
      project ID (not by name).

      ## Step 2 — Write probe: probes/disc-capture-01-inbox-note-roundtrip.js

      Write an OmniJS-first probe that:
      - Generates a unique task name: "disc-probe-note-" + Date.now()
      - Aborts with { aborted: true, reason: "collision" } if a task by that generated name already exists in inbox
      - Creates a test task in the inbox: new Task(probeName, inbox)
      - Sets task.note to a test session-ID string: "SESSION:disc-probe-" + Date.now()
      - Reads back task.note immediately
      - Confirms the inbox collection reflects the task immediately (read inbox.tasks, find task by captured ID)
      - In try/finally: removes the test task using its captured ID
      - Returns JSON: { probeTaskName: string, taskCreated: boolean, noteWritten: boolean, noteReadBack: string,
          notePersisted: boolean, inboxReflectsImmediately: boolean, cleanedUp: boolean, ofVersion: string }
      The noteReadBack field should contain the session-ID string (which the probe created — not user data). This probe
      covers two gate-claims: the note round-trip (LINE-01 gate) AND inbox immediacy (CAP-01 gate).

      ## Step 3 — Run both probes

      Execute: `osascript -l JavaScript probes/disc-model-01-sequential-write.js`
      Execute: `osascript -l JavaScript probes/disc-capture-01-inbox-note-roundtrip.js`
      Verify both return JSON with cleanedUp: true. If either returns cleanedUp: false, surface the probe name so the
      user can manually remove the orphaned object. Note that disc-capture-01 also provides "inbox collection immediacy"
      gate-claim evidence — this feeds the CAPTURE section in Plan 03.

      ## Step 4 — Populate FIELD section

      Replace the FIELD stub with findings. Write at minimum:
      - DISC-FIELD-01: task.note is the custom-data extension point — free-form text or structured content (e.g., JSON
        prefix). Verdict: native. Evidence: doc (omni-automation.com/omnifocus/task.html). Rubric: "OF has no
        first-class custom fields; task.note is the intended extension surface." Downstream cite: Phase 2 (LINE-01
        session lineage), Phase 5 (ARCH-03 archaeology tag).
      - DISC-FIELD-02: Structured native fields — estimatedMinutes, defer/due/completion dates, flagged, plannedDate
        (v4.7+), taskStatus enum. Verdict: native. Evidence: doc (omni-automation.com/task.html) + note that plannedDate
        is v4.7+; confirmed available on 4.8.11 per version. Downstream cite: Phase 3 (task routing metadata).
      - DISC-FIELD-03: No first-class custom fields in OmniFocus. Verdict: native (use note field — no build required).
        Evidence: doc (discourse.omnigroup.com/t/custom-fields). Rubric: "Custom fields would require build; OF
        community confirms absence; note field is the accepted pattern."
      Area-level verdict: native. Rubric: "task.note handles all agent custom-data needs; no build required."

      ## Step 5 — Populate MODEL section

      Replace the MODEL stub with findings. Write at minimum:
      - DISC-MODEL-01: project.sequential (Boolean) controls available tasks. Write-back pattern: direct assignment in
        OmniJS. Evidence: verified (probe disc-model-01 run in this plan). Verdict: native. Downstream cite: Phase 3
        (ROUTE-03 project creation with sequential flag).
      - DISC-MODEL-02: task.taskStatus enum (Available, Blocked, Completed, Dropped, DueSoon, Next, Overdue). Verdict:
        native. Evidence: doc (omni-automation.com/omnifocus/task.html). Downstream cite: Phase 3 routing
        (available-task detection).
      - DISC-MODEL-03: Sequential project ordering is the only built-in dependency mechanism. Verdict: native. Evidence:
        doc (omni-automation.com/omnifocus/project.html). Rubric: "Sequential projects cover the agent-workflow need."
        Downstream cite: Phase 3 (ROUTE-01).
      - DISC-MODEL-04: OmniFocus has NO native cross-project task dependency system. Verdict: build (if cross-task
        dependencies ever needed — not in this milestone). Evidence: doc (community plug-in
        ksalzke/dependency-omnifocus-plugin exists because native dependency is absent). Rubric: "Don't build cross-task
        dependencies for this milestone; if needed later, use the community plug-in." Downstream cite: Phase 3 only if
        cross-task dependency surfaces.
      - DISC-MODEL-05: repetitionRule setter requires P2 pattern (new Task.RepetitionRule(...)); project.reviewInterval
        requires P4 (read-modify-reassign). Verdict: extend (thin wrapper needed for correct setter behavior). Evidence:
        doc (SETTER-PATTERNS.md rows 1–2). Downstream cite: Phase 2 if recurrence is set on captured tasks.
      - DISC-MODEL-06: moveTasks([task], destination) — moves tasks between projects in OmniJS. Verdict: native.
        Evidence: doc (omni-automation.com). Downstream cite: Phase 3 (ROUTE-01 task filing).
      Area-level verdict: native. Rubric: "OF data model is fully native; setter-pattern wrappers (DISC-MODEL-05) are
      thin and already exist in mutation-script-builder."

      Record sanitized evidence appendices for disc-model-01 and disc-capture-01 below the relevant finding tables
      (format: command | timestamp | sanitized result | cleanup result | OF version). No user-visible project or task
      names from the live database.

    </action>

    <verify>
      <automated>grep -c "DISC-FIELD-0[123]\|DISC-MODEL-0[123456]" /Users/jessicaking/projects/omnifocus-mcp/docs/reference/omnifocus-capabilities.md</automated>
    </verify>

<acceptance_criteria>

- probes/disc-model-01-sequential-write.js exists: OmniJS-first, UUID-suffixed name, abort-on-collision, try/finally,
  project cleanup via Project.Status.Dropped (not destructive delete), cleanedUp in return JSON
- probes/disc-capture-01-inbox-note-roundtrip.js exists: same probe safety discipline, confirms task.note round-trip and
  inbox immediacy
- Both probes exit without a thrown exception against live OmniFocus 4.8.11
- Both probes return cleanedUp: true
- docs/reference/omnifocus-capabilities.md FIELD section contains DISC-FIELD-01, 02, 03 — each with all required fields;
  area-level verdict is exactly one of native/extend/build
- FIELD area-level verdict: native
- docs/reference/omnifocus-capabilities.md MODEL section contains DISC-MODEL-01 through 06 — each with all required
  fields and a single verdict value
- DISC-MODEL-05 verdict is extend (setter wrappers) as a per-finding verdict — the area-level verdict remains native
- MODEL area-level verdict: native (not "native with extend qualifier" — that nuance is in DISC-MODEL-05's row)
- Sanitized evidence appendices from disc-model-01 and disc-capture-01 are recorded in the report (counts/booleans/
  probe-created names only — no real user project or task names)
- disc-capture-01 probe output is noted as also providing CAPTURE-section evidence for Plan 03
- No `evidence: unverified` in FIELD or MODEL sections without an explicit follow-up note </acceptance_criteria>

<done>FIELD and MODEL sections fully populated in docs/reference/omnifocus-capabilities.md. Two probe scripts committed
to probes/ with sanitized evidence recorded. Evidence tags conform to D-05. Area-level verdicts are single values per
D-04. The disc-capture-01 probe output is available for Plan 03's CAPTURE section.</done> </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                            |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------ |
| probe scripts → OmniFocus live store | Write probes (disc-model-01, disc-capture-01) mutate the user's real OF database; cleanup is mandatory |
| probe output → report                | Probe output embedded in committed report; must not contain sensitive user task content                |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                           | Disposition | Mitigation Plan                                                                                                                                                                                                                                                        |
| --------- | ---------------------- | ------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Tampering              | Write probes mutate live OF store without cleanup                   | mitigate    | Every write probe uses try/finally so cleanup runs even on exception. Probe tracks created object IDs and removes only those IDs. Returns cleanedUp: boolean. If cleanedUp: false, executor surfaces the probe-created object name so the user can remove it manually. |
| T-02-02   | Tampering              | Probe collides with a pre-existing object of the same name          | mitigate    | All probes generate UUID-suffixed unique names (e.g., disc-probe-tag-autocreate-1749600000). Probe checks for pre-existing object before creating and ABORTS (returns aborted: true) if a collision is found — it never modifies objects it did not create.            |
| T-02-03   | Tampering              | disc-tag-01/02 create real tags that aren't cleaned up              | mitigate    | Probes capture the tag ID at creation and delete that specific tag in the try/finally block. cleanedUp: false triggers a manual-cleanup surface.                                                                                                                       |
| T-02-04   | Information Disclosure | Probe output contains real task or tag names from the live database | mitigate    | Probes create objects with probe-generated names only; they do not read or embed existing database content. Sanitized evidence appendix records counts, booleans, probe-created IDs, and OF version — not names of any real user objects.                              |
| T-02-05   | Tampering              | disc-capture-01 leaves a task in the inbox on exception             | mitigate    | try/finally cleanup runs even on exception; removes task by its captured ID. If cleanedUp: false, the probe-generated task name (disc-probe-note-<timestamp>) is surfaced for manual removal.                                                                          |
| T-02-06   | Tampering              | disc-model-01 drops real user project via overly broad cleanup      | mitigate    | Cleanup targets only the project ID captured at creation. Uses Project.Status.Dropped (not a destructive hard delete). No .whose()/.where() scan that could match an unintended project.                                                                               |
| T-02-07   | Repudiation            | FIELD finding cites discourse post as `doc` evidence                | accept      | The discourse citation is for "no custom fields" — a negative claim; the risk of being wrong is low (OF would have to have added custom fields without documentation). Acceptable as doc evidence.                                                                     |
| T-02-SC   | Tampering              | npm/pip/cargo installs                                              | accept      | No new packages; build uses locked package-lock.json                                                                                                                                                                                                                   |

</threat_model>

<verification>
After both tasks complete:

1. `ls probes/disc-tag-01-auto-create.js probes/disc-tag-02-addtag-omni.js probes/disc-model-01-sequential-write.js probes/disc-capture-01-inbox-note-roundtrip.js`
   — all four exist
2. `grep -c "DISC-TAG-0[123]" docs/reference/omnifocus-capabilities.md` — returns 3
3. `grep -c "DISC-FILTER-0[123]" docs/reference/omnifocus-capabilities.md` — returns 3
4. `grep -c "DISC-FIELD-0[123]" docs/reference/omnifocus-capabilities.md` — returns 3
5. `grep -c "DISC-MODEL-0[123456]" docs/reference/omnifocus-capabilities.md` — returns 6
6. `grep -v "^#" docs/reference/omnifocus-capabilities.md | grep -c "evidence: unverified"` — returns 0 for
   TAG/FILTER/FIELD/MODEL sections (all are verified or doc-cited)
7. `grep -c "Area Verdict" docs/reference/omnifocus-capabilities.md` — at least 4 area verdicts present
8. `grep -c "native/\|extend/\|build/" docs/reference/omnifocus-capabilities.md` — returns 0 (no slash-combined
   verdicts) </verification>

<success_criteria>

- TAG, FILTER, FIELD, and MODEL area sections of docs/reference/omnifocus-capabilities.md are fully populated with
  DISC-&lt;AREA&gt;-NN findings
- Every finding has: Verdict (single value — native/extend/build per D-04), Rubric (one-line reason), Evidence tag (per
  D-05: verified for live probes, doc for codebase citations), Source, Downstream cite
- Four probe scripts exist in probes/ — all use try/finally, UUID-suffixed names, abort-on-collision, and return
  cleanedUp: true when run against OmniFocus 4.8.11
- Sanitized evidence appendices for tag and model probes recorded in matching report findings (no private user data)
- No silent unverified findings in these four areas </success_criteria>

<output>
Create .planning/phases/01-omnifocus-capability-discovery/01-02-SUMMARY.md when done
</output>
