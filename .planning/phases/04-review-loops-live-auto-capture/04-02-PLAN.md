---
phase: 04-review-loops-live-auto-capture
plan: 02
type: execute
wave: 2
depends_on: ['04-01']
files_modified:
  - .claude/skills/capture-live-blocker/SKILL.md
  - tests/integration/tools/unified/end-to-end.test.ts
autonomous: true
requirements: [LIVE-01]

must_haves:
  truths:
    - 'A standalone capture-live-blocker skill drives omnifocus_write to capture a single concrete blocker into the
      OmniFocus inbox in real time'
    - 'Live capture lands in the inbox (no project), stamped with the of-mcp:lineage note, the capture-live marker tag,
      and the auto-stamped agent-okay tag'
    - 'Live capture carries NO archaeology tag and NO review-* tag — it stays distinct from Phase 5 and routes later via
      the existing Phase 3 loop'
    - 'Live capture reuses PERM-02 verbatim — the funnel owns the gate verdict (POLICY_GATE_CAPTURE_CONFIRM in
      interactive mode), the skill renders the prompt, an owner allow-all-session grant suppresses re-prompts; no second
      permission mechanism is built'
    - 'Live capture invents no date (no dueDate/deferDate) — a captured blocker is undated (D-05)'
  artifacts:
    - path: '.claude/skills/capture-live-blocker/SKILL.md'
      provides:
        'standalone live-capture skill (conservative named-signal trigger, PERM-02 rendering, inbox+lineage+capture-live
        create)'
      contains: 'capture-live'
      min_lines: 40
    - path: 'tests/integration/tools/unified/end-to-end.test.ts'
      provides: 'live-capture integration case extending the D-08b agent-create-with-lineage harness'
      contains: 'capture-live'
  key_links:
    - from: '.claude/skills/capture-live-blocker/SKILL.md'
      to: 'omnifocus_write create (inbox)'
      via: 'mutation.data with tags:[capture-live] + lineage.sessionId'
      pattern: 'capture-live'
    - from: 'tests/integration/tools/unified/end-to-end.test.ts'
      to: 'agent-role server + agent-okay tag filter read-back'
      via: 'create-with-lineage round-trip'
      pattern: 'OMNIFOCUS_MCP_ROLE'
---

<objective>
Author the standalone live-capture skill (LIVE-01) and prove the live-capture write end-to-end against the agent-role capture path.

This is the SKILLS slice. It creates `.claude/skills/capture-live-blocker/SKILL.md` — a conservative, in-the-moment,
single-item skill that turns a concrete blocker into an OmniFocus inbox task with permission — and extends the shipped
D-08b agent-create-with-lineage integration harness with a `capture-live` case asserting the marker tag applies,
agent-okay auto-stamps, the lineage note persists, and archaeology is absent.

No new server code. The skill drives the existing `omnifocus_write` create path; the funnel auto-stamps agent-okay + the
lineage note, applies PERM-02's gate, and the write-verifier confirms persistence. Depends on 04-01 because the
`capture-live` tag must be in FUNCTIONAL_TAG_ALLOWLIST before the integration spec can create it under the test-mode
guard.

Purpose: LIVE-01 — during a live Claude Code session the agent captures a concrete blocker or open question as an
OmniFocus task in real time (with permission), without the archaeology tag. Output: the capture-live-blocker SKILL.md +
a live-capture integration case in end-to-end.test.ts. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-review-loops-live-auto-capture/04-CONTEXT.md
@.planning/phases/04-review-loops-live-auto-capture/04-RESEARCH.md
@.planning/phases/04-review-loops-live-auto-capture/04-PATTERNS.md
@.planning/phases/04-review-loops-live-auto-capture/04-VALIDATION.md
@.claude/skills/route-inbox-to-projects/SKILL.md

<interfaces>
<!-- Source-of-truth identifiers the executor needs. Use these directly. -->

From .claude/skills/route-inbox-to-projects/SKILL.md (the skill template to mirror — same section order):

- Frontmatter: `name:` (kebab-case, matches dir) + `description:` (starts "Use when Jess…", enumerates trigger phrases).
- Section order to copy: Overview -> Idempotency note -> Permission rendering (PERM-02) -> Tool call reference table ->
  Out of scope -> Common mistakes table.

From src/tools/unified/OmniFocusWriteTool.ts (the funnel behavior the skill relies on — DO NOT modify):

- On a create with `data.lineage`, the funnel appends the of-mcp:lineage stamp to the note AND, when role=agent,
  auto-appends 'agent-okay' to data.tags. The skill need NOT pass agent-okay itself.
- Gate handling: if outcome === 'gate' and isAllowedAllThisSession() -> continue (owner allow-all-session bypass); else
  if create + parseMode()==='interactive' -> returns POLICY_GATE_CAPTURE_CONFIRM, which the skill renders as a yes/no
  prompt. The funnel owns the verdict; the skill only renders.
- The write-verifier fires automatically for every agent write — the skill must NOT call it explicitly.

Live-capture omnifocus_write call shape (verified against the D-08b test + funnel): mutation.operation='create',
mutation.target='task', data: { name: <concise blocker>, note: <context>, tags: ['capture-live'], lineage: { sessionId:
<cc-session-uuid> } }

- No `project` key -> inbox (DISC-CAPTURE-01). No dueDate/deferDate (D-05). Pass capture-live explicitly; let the funnel
  auto-stamp agent-okay (research Open Question #1). NEVER add archaeology or review-\* tags.

From tests/integration/tools/unified/end-to-end.test.ts (the live-capture harness to extend — stable anchors):

- describe block: `describe('Phase 2 D-08b — agent create with lineage stamps agent-okay tag', ...)`.
- Agent-role spawn:
  `spawn('node', [serverPath], { stdio: [...], env: { ...process.env, OMNIFOCUS_MCP_ROLE: 'agent' } })` (exercises
  lineage-attestation bypass + agent-okay auto-stamp).
- Scoped name: `runScopedName('phase2-lineage-tag')` -> use `runScopedName('phase4-live-capture')` for the new case.
- Read-back through the agent-okay tag filter:
  `filters: { tags: { all: ['agent-okay'] } }, fields: ['name','tags','note'], limit: 200`, then
  `find((t) => t.name === lineageTaskName)`.
- Existing assertions: `expect(task.tags).toContain('agent-okay')`, `expect(task.note).toContain('of-mcp:lineage')`.
  Self-clean: the analog deletes the created task in a finally block via omnifocus_write delete — copy that.
  </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Author the capture-live-blocker SKILL.md (LIVE-01)</name>
  <read_first>
    - .claude/skills/route-inbox-to-projects/SKILL.md (source-of-truth skill template — copy the frontmatter shape and the section order verbatim, adapt content for live capture)
    - .planning/phases/04-review-loops-live-auto-capture/04-PATTERNS.md (the exact frontmatter, the omnifocus_write invocation, the PERM-02 prompt-rendering convention text, the common-mistakes rows)
    - .planning/phases/04-review-loops-live-auto-capture/04-RESEARCH.md (Skill-Authoring Conventions table; D-08 conservative trigger; D-09 PERM-02 reuse; D-10 placement; D-11 capture reuse; Discretion #3 standalone-skill rationale)
  </read_first>
  <action>
    Create .claude/skills/capture-live-blocker/SKILL.md mirroring route-inbox-to-projects/SKILL.md. Frontmatter: name `capture-live-blocker` (matches dir), description starting "Use when" keyed on the agent NOTICING a concrete blocker / unresolvable open question / "TODO later" mid live session (the noticing surface — distinct from a user utterance; this is why it is standalone per Discretion #3). Sections in this order: Overview (cite D-08/D-09/D-10/D-11, the "OmniFocus is canonical" stance, and state it adds no server code and drives omnifocus_write); a conservative judgment rule enumerating what counts as a concrete blocker (explicit blocker / follow-up / "TODO later" / unresolvable open question — bias to NOT capture, mirroring routing's "bias to leave", rare/trusted/single-item per D-08); Idempotency note (re-noticing the same blocker must not double-create — agent judgment, not server dedup); Permission rendering (PERM-02, D-09 — on a POLICY_GATE_CAPTURE_CONFIRM response show the proposed inbox task and ask "Capture this? (yes / no)"; if the owner already granted allow-all-this-session, skip the prompt and proceed; never build a second permission mechanism); a Tool call reference table giving the omnifocus_write create shape with data name/note, tags set to the capture-live marker only, lineage.sessionId, no project (inbox), no dueDate/deferDate, and the note that lineage auto-triggers the of-mcp:lineage stamp + agent-okay auto-stamp so the skill passes only capture-live; an Out of scope section (no archaeology tag — Phase 5; no routing — the Phase 3 loop routes it later; no review-* surfacing — a captured blocker is not yet review work); a latency note (OmniFocus calls can take 10+ seconds — do not retry on slowness); a Common mistakes table with rows: JXA task.addTags() no-op then use omnifocus_write; adding archaeology (never on a live capture); inventing a date (use no date, not a due date); skipping the permission prompt; treating a 10+ second response as an error. The skill is agent-facing prose plus the tool-call JSON shape; do NOT include server implementation code.
  </action>
  <verify>
    <automated>test -f .claude/skills/capture-live-blocker/SKILL.md && grep -q "capture-live-blocker" .claude/skills/capture-live-blocker/SKILL.md && grep -q "capture-live" .claude/skills/capture-live-blocker/SKILL.md && grep -q "POLICY_GATE_CAPTURE_CONFIRM" .claude/skills/capture-live-blocker/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: SKILL.md exists at .claude/skills/capture-live-blocker/SKILL.md with frontmatter name `capture-live-blocker` matching the directory.
    - Source assertion: SKILL.md references the `capture-live` marker tag, `lineage`, and `POLICY_GATE_CAPTURE_CONFIRM` (PERM-02 rendering).
    - Behavior assertion: the documented omnifocus_write shape passes NO project key (inbox), NO dueDate/deferDate, tags = [capture-live] only, and lineage.sessionId — and the Out-of-scope section explicitly forbids archaeology, routing, and review-* tags.
    - Source assertion: `grep -c "archaeology" .claude/skills/capture-live-blocker/SKILL.md` >= 1 AND it appears only in a do-NOT / out-of-scope context (the skill never instructs adding archaeology).
    - The skill mirrors the route-inbox-to-projects section order (Overview, Idempotency, Permission rendering, Tool call reference, Out of scope, Common mistakes).
  </acceptance_criteria>
  <done>capture-live-blocker/SKILL.md exists, standalone, mirrors the route skill structure, documents the inbox+lineage+capture-live create and the PERM-02 prompt, and forbids archaeology/routing/review-* and invented dates.</done>
</task>

<task type="auto">
  <name>Task 2: Integration case — live capture stamps capture-live + agent-okay + lineage, no archaeology, in inbox (LIVE-01)</name>
  <read_first>
    - tests/integration/tools/unified/end-to-end.test.ts (the file being modified — read the `describe('Phase 2 D-08b — agent create with lineage stamps agent-okay tag')` block; this is the agent-role live-capture harness to extend, not rebuild)
    - tests/integration/helpers/run-id.ts (runScopedName for the collision-free task name)
    - .planning/phases/04-review-loops-live-auto-capture/04-PATTERNS.md (the agent-role spawn, the create-with-lineage call with tags:[capture-live], the read-back assertions, the finally-block self-clean)
    - .planning/phases/04-review-loops-live-auto-capture/04-RESEARCH.md (D-10/D-11 placement + capture reuse; Open Question #1 — pass capture-live explicitly, let the funnel auto-stamp agent-okay)
  </read_first>
  <action>
    Extend tests/integration/tools/unified/end-to-end.test.ts with a Phase 4 live-capture case inside (or as a sibling describe to) the D-08b agent-create-with-lineage block, reusing its agent-role server spawn (OMNIFOCUS_MCP_ROLE='agent') and its read-back-through-agent-okay-tag-filter pattern. Use runScopedName('phase4-live-capture') for the task name. Issue an omnifocus_write create with data { name, tags: ['capture-live'], lineage: { sessionId: 'integration-test-session' } } and NO project key. Read back via filters { tags: { all: ['agent-okay'] } }, fields ['name','tags','note','project'] (add 'project' to the D-08b analog's field list so inbox placement is directly assertable), and find the task by name. Assert: tags contains 'agent-okay' (funnel auto-stamp fired), tags contains 'capture-live' (marker applied), note contains 'of-mcp:lineage' (lineage persisted), and tags does NOT contain 'archaeology' (D-08/D-10 — live capture stays distinct from Phase 5). Assert inbox placement directly: the read-back task's project field is null/absent (the create passed no project key — DISC-CAPTURE-01 inbox path). If the read projection does not return a usable project field even when requested, fall back to asserting the create response carries no project and document that in the test comment. Self-clean: delete the created task in a finally block via omnifocus_write delete, copying the analog's cleanup. Do NOT add review-* tags or any date.
  </action>
  <verify>
    <automated>npm run build && npm run test:integration -- end-to-end</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: the live-capture create round-trips with tags ⊇ [agent-okay, capture-live], note containing of-mcp:lineage, and tags NOT containing archaeology.
    - Source assertion: `grep -c "capture-live" tests/integration/tools/unified/end-to-end.test.ts` >= 1 and the new case uses OMNIFOCUS_MCP_ROLE agent (reuses the D-08b harness, no new fixtures).
    - Source assertion: the new case asserts `not.toContain('archaeology')` and adds no review-* tag and no date field.
    - Behavior assertion: the read-back task's `project` field is null/absent (inbox placement), with `'project'` added to the read-back `fields` list; if the projection cannot return it, the create response carries no project key and a test comment records the fallback.
    - Test command `npm run test:integration -- end-to-end` green; the created task is deleted in a finally block (no orphan in the sandbox).
  </acceptance_criteria>
  <done>end-to-end.test.ts proves a live capture lands in the inbox with capture-live + auto-stamped agent-okay + lineage note and no archaeology tag, reusing the agent-role D-08b harness and self-cleaning.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                            | Description                                                                                                       |
| --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| agent (Claude Code skill) -> omnifocus_write funnel | the live-capture create crosses from untrusted agent judgment into the OmniFocus store via the single funnel      |
| live/interactive session -> PERM-02 gate            | an agent create in interactive mode must pass the prompt-before-create gate (or an owner allow-all-session grant) |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                                              |
| --------- | ---------------------- | ----------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-04-03   | Spoofing               | agent forges a live capture to bypass the gate                                | mitigate    | The lineage-attestation bypass is scoped to recoverable inbox create only (OmniFocusWriteTool gate block); destructive ops are untouched. The skill passes lineage.sessionId; the funnel — not the skill — owns the verdict. Reused verbatim, no new bypass. |
| T-04-04   | Elevation of Privilege | agent self-grants allow-all-session to skip the prompt                        | mitigate    | setAllowAllThisSession throws if role != owner (session-state.ts). The skill renders the prompt but cannot grant the bypass; only the owner can. Reused unchanged.                                                                                           |
| T-04-05   | Tampering              | capture-live widens the test-mode write surface                               | mitigate    | capture-live is added to FUNCTIONAL_TAG_ALLOWLIST in plan 04-01 explicitly; task name still obeys the \_\_test- sandbox guard; the integration case self-cleans via a finally-block delete.                                                                  |
| T-04-06   | Information Disclosure | wrong-placement — a live capture filed into the current project leaks context | mitigate    | D-10: placement is inbox-only (no project key); routing happens later via the Phase 3 loop. The integration case asserts no project on read-back.                                                                                                            |
| T-04-SC   | Tampering              | npm/pip/cargo installs                                                        | accept      | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). No supply-chain surface added.                                                                                                                                        |

</threat_model>

<verification>
- `npm run build` clean before integration (the capture-live allowlist entry from 04-01 must be compiled).
- `npm run test:integration -- end-to-end` green (the new live-capture case + the existing D-08b case).
- Manual (UAT, per 04-VALIDATION Manual-Only): in a real interactive Claude Code session, confirm the skill renders the PERM-02 prompt before create and that an owner allow-all-session grant suppresses subsequent prompts.
- Use npm, NOT bun — a bare `npx vitest run` trips the sandbox guard.
</verification>

<success_criteria>

- A standalone capture-live-blocker skill exists, mirrors the route-skill structure, and documents the
  inbox+lineage+capture-live create + the PERM-02 prompt (LIVE-01).
- The integration case proves the live capture stamps capture-live + agent-okay + the lineage note, carries no
  archaeology tag, and lands in the inbox.
- No new server code, no second permission mechanism, no invented dates, no review-\* or archaeology tags on the live
  capture. </success_criteria>

<output>
Create `.planning/phases/04-review-loops-live-auto-capture/04-02-SUMMARY.md` when done.
</output>
