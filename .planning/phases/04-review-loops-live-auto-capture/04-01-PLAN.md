---
phase: 04-review-loops-live-auto-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contracts/ast/mutation-script-builder.ts
  - tests/unit/contracts/ast/mutation-script-builder.test.ts
  - tests/integration/tools/unified/review-tag.test.ts
autonomous: true
requirements: [REVIEW-01, REVIEW-02]

must_haves:
  truths:
    - 'review-output and review-capture are accepted functional tags in test mode (arbitrary tags still rejected)'
    - 'An active task accepts flagged=true + plannedDate=today + review-capture in one update and all three round-trip
      via an independent read-back'
    - 'A completed task accepts the review-output tag and the tag reads back (no future plannedDate written on completed
      work)'
    - 'Phase 4 writes only native task properties (flag, planned date, tag membership) — no perspective machinery, no
      archivedFilterRules, no Perspective.Custom enumeration'
    - 'No custom flag-clearing sweeper exists — review-flag lifecycle relies on native completion + manual unflag only'
  artifacts:
    - path: 'src/contracts/ast/mutation-script-builder.ts'
      provides: 'FUNCTIONAL_TAG_ALLOWLIST extended with review-output, review-capture, capture-live'
      contains: 'review-output'
    - path: 'tests/unit/contracts/ast/mutation-script-builder.test.ts'
      provides: 'allowlist unit assertions for the 3 new functional tags'
      contains: 'review-capture'
    - path: 'tests/integration/tools/unified/review-tag.test.ts'
      provides: 'review-capture active round-trip + review-output completed round-trip'
      contains: 'review-output'
  key_links:
    - from: 'tests/integration/tools/unified/review-tag.test.ts'
      to: 'omnifocus_write update + omnifocus_read'
      via: 'assertFieldPersisted round-trip'
      pattern: 'assertFieldPersisted'
    - from: 'isTestTagAllowed'
      to: 'FUNCTIONAL_TAG_ALLOWLIST'
      via: 'array membership'
      pattern: 'FUNCTIONAL_TAG_ALLOWLIST.includes'
---

<objective>
Register the Phase 4 review vocabulary in the server's functional-tag allowlist and prove the review-loop surfacing writes (REVIEW-01/02) round-trip against live OmniFocus.

This is the SERVER slice: it adds three tag-name strings (`review-output`, `review-capture`, `capture-live`) to
`FUNCTIONAL_TAG_ALLOWLIST` so the existing write funnel will accept them in test mode, extends the allowlist unit test,
and lands two integration specs that exercise the existing flagged/plannedDate/addTag setters with the new review tags.
Zero new server capability — every setter, the funnel, and the verifier already shipped.

The `capture-live` tag is added here too (alongside the review tags) so the live-capture integration spec in plan 04-02
can use it; this plan is therefore a hard Wave-1 dependency of 04-02.

Purpose: REVIEW-01 (agent work carries a review tag and surfaces in today's view via flag + plannedDate) and REVIEW-02
(review-output vs review-capture classification) need the tags allowlisted and the round-trip proven before any skill
drives them. Output: extended allowlist constant + unit test, one new integration spec covering the active
(review-capture) and completed (review-output) review-tag paths. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-review-loops-live-auto-capture/04-CONTEXT.md
@.planning/phases/04-review-loops-live-auto-capture/04-RESEARCH.md
@.planning/phases/04-review-loops-live-auto-capture/04-PATTERNS.md
@.planning/phases/04-review-loops-live-auto-capture/04-VALIDATION.md

<interfaces>
<!-- Source-of-truth identifiers the executor needs. Use these directly — no codebase exploration required. -->

From src/contracts/ast/mutation-script-builder.ts (stable anchors — do NOT cite line numbers):

- `export const FUNCTIONAL_TAG_ALLOWLIST: readonly string[] = ['agent-okay', 'routing-unplaced'];`
- `export function isTestTagAllowed(tag: string): boolean` returns
  `tag.startsWith(TEST_TAG_PREFIX) || FUNCTIONAL_TAG_ALLOWLIST.includes(tag)`
- The JSDoc block above the const begins "Functional/system tags the product legitimately applies". Each existing tag
  has a one-sentence rationale.
- `isTestTagAllowed` is the single control point — called at three filter sites
  (`data.tags.filter((t) => !isTestTagAllowed(t))`, `allTags.filter(...)`). Adding a name to the array is sufficient; no
  call-site edits.
- update-task OmniJS body already sets `task.flagged = changes.flagged`,
  `task.plannedDate = changes.plannedDate ? new Date(...) : null`, and iterates `changes.addTags` through
  `resolveTag(name, true)` → `task.addTag(tag)` (find-or-create). No new builder code.

From tests/unit/contracts/ast/mutation-script-builder.test.ts:

- Existing describe block (stable anchor):
  `describe('FUNCTIONAL_TAG_ALLOWLIST / isTestTagAllowed (Phase 3 routing-unplaced — D-12)', ...)`. Imports
  `FUNCTIONAL_TAG_ALLOWLIST` and `isTestTagAllowed` already present at top of file.

From tests/integration/tools/unified/field-roundtrip.test.ts (the round-trip harness to copy):

- Harness imports: `expectOk` (helpers/expect-ok.js), `assertFieldPersisted` (helpers/assert-field-persisted.js),
  `SANDBOX_FOLDER_NAME` / `ensureSandboxFolder` / `fullCleanup` (helpers/sandbox-manager.js), `runScopedName` /
  `runScopedTag` (helpers/run-id.js).
- Driver shape:
  `await assertFieldPersisted(client, { readTool: 'omnifocus_read', readParams: taskQuery(id, readFields), extract: (r) => extract(findTask(r, id)), expected, context })`.
- plannedDate read-back uses epoch comparison with the verifier's ±60s date tolerance (field-comparator DATE_FIELDS).
- `afterAll` calls `fullCleanup` and asserts `expect(report.errors, ...).toHaveLength(0)`. </interfaces> </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend FUNCTIONAL_TAG_ALLOWLIST with the Phase 4 tags + unit test</name>
  <read_first>
    - src/contracts/ast/mutation-script-builder.ts (the file being modified — read the FUNCTIONAL_TAG_ALLOWLIST const + its JSDoc block + isTestTagAllowed; these are the source-of-truth allowlist)
    - tests/unit/contracts/ast/mutation-script-builder.test.ts (the source-of-truth allowlist test — read the `describe('FUNCTIONAL_TAG_ALLOWLIST / isTestTagAllowed (Phase 3 routing-unplaced — D-12)')` block, the exact analog to copy)
    - .planning/phases/04-review-loops-live-auto-capture/04-PATTERNS.md (the verbatim allowlist change + the per-tag test cases)
  </read_first>
  <behavior>
    - isTestTagAllowed('review-output') === true; FUNCTIONAL_TAG_ALLOWLIST contains 'review-output'
    - isTestTagAllowed('review-capture') === true; FUNCTIONAL_TAG_ALLOWLIST contains 'review-capture'
    - isTestTagAllowed('capture-live') === true; FUNCTIONAL_TAG_ALLOWLIST contains 'capture-live'
    - Regression: isTestTagAllowed('agent-okay') and isTestTagAllowed('routing-unplaced') still true
    - isTestTagAllowed('some-random-tag') === false (arbitrary tags still rejected)
  </behavior>
  <action>
    Resolve the Claude's-Discretion live-capture marker name to `capture-live` (per D-10 / research Discretion #4 — collision-free with agent-okay/routing-unplaced/review-output/review-capture/future archaeology; signals provenance "captured live"). Add `review-output` (D-01/D-02), `review-capture` (D-01/D-02), and `capture-live` (D-10) to the `FUNCTIONAL_TAG_ALLOWLIST` array literal in mutation-script-builder.ts. Do NOT touch `isTestTagAllowed` or the three filter call sites — the guard already reads the array. Extend the JSDoc block above the const ("Functional/system tags the product legitimately applies") with one rationale sentence per new tag, mirroring the existing agent-okay/routing-unplaced sentences and citing the Phase 4 decision (D-01/D-02 for the review tags, D-10 for capture-live). In the unit test, add three `it(...)` cases inside the existing `FUNCTIONAL_TAG_ALLOWLIST / isTestTagAllowed` describe, copying the `routing-unplaced` row verbatim with each new name, plus keep the arbitrary-tag rejection assertion. No new imports — `FUNCTIONAL_TAG_ALLOWLIST` and `isTestTagAllowed` are already imported. Confirm no Zod/inputSchema change is needed (flagged/plannedDate/addTags/tags/note/lineage already exist in the write schema — adding allowlist strings is not a schema change, per Pitfall 3).
  </action>
  <verify>
    <automated>npm run build && npm run test:unit -- mutation-script-builder</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -v '^[[:space:]]*//' src/contracts/ast/mutation-script-builder.ts | grep -c "review-output\|review-capture\|capture-live"` returns 3 (the three array entries, comments stripped).
    - Test command `npm run test:unit -- mutation-script-builder` passes with the three new allowlist cases green and the arbitrary-tag-rejection case still green.
    - Behavior: isTestTagAllowed returns true for all three new tags and for agent-okay/routing-unplaced, false for 'some-random-tag'.
    - No change to isTestTagAllowed, TEST_TAG_PREFIX, or any *.filter(isTestTagAllowed) call site.
  </acceptance_criteria>
  <done>FUNCTIONAL_TAG_ALLOWLIST contains the 3 new tags with JSDoc rationale; unit test asserts all 3 allowed + arbitrary rejected; build + targeted unit run green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration spec — review-capture active round-trip + review-output completed round-trip (REVIEW-01/02)</name>
  <read_first>
    - tests/integration/tools/unified/review-tag.test.ts (the file being created)
    - tests/integration/tools/unified/field-roundtrip.test.ts (source-of-truth round-trip harness — copy its imports, sandbox setup, assertFieldPersisted driver, and afterAll fullCleanup exactly; no new fixtures)
    - tests/integration/helpers/assert-field-persisted.ts (the round-trip assertion helper signature)
    - tests/integration/helpers/run-id.ts (runScopedName / runScopedTag for collision-free names)
    - .planning/phases/04-review-loops-live-auto-capture/04-PATTERNS.md (the flagged/plannedDate/tags row shapes + the completed-branch instructions)
    - .planning/phases/04-review-loops-live-auto-capture/04-RESEARCH.md (Discretion #2 — active work gets full flag+plannedDate+review-capture; completed work gets review-output tag ONLY, no future plannedDate)
  </read_first>
  <behavior>
    - Active task: one omnifocus_write update sets flagged=true + plannedDate=today + addTags:[review-capture]; an independent omnifocus_read read-back returns flagged=true, plannedDate≈today (±60s), tags includes review-capture.
    - Completed task: create a task, complete it, then omnifocus_write update addTags:[review-output]; read-back returns tags includes review-output. Do NOT write a future plannedDate on the completed task (Discretion #2 — completed work gets the tag only).
    - Cleanup: every created task is removed; fullCleanup report has zero errors.
  </behavior>
  <action>
    Create tests/integration/tools/unified/review-tag.test.ts modeling field-roundtrip.test.ts. Reuse the existing helpers verbatim — expectOk, assertFieldPersisted, sandbox-manager (SANDBOX_FOLDER_NAME / ensureSandboxFolder / fullCleanup), run-id (runScopedName for the task name, runScopedTag if a scoped tag is needed) — no new conftest/fixtures. Spawn the server the same way field-roundtrip does (owner-role round-trip path is sufficient for REVIEW writes — these are owner-applied review tags, not the agent live-capture path which 04-02 covers). Describe block: "Phase 4 REVIEW-01/02 — review-tag round-trip". Case 1 (REVIEW-01 active / review-capture, D-04): create an active sandbox task, issue ONE update with `changes: { flagged: true, plannedDate: <today date-only string>, addTags: ['review-capture'] }`, then drive three assertFieldPersisted reads (flagged → true; plannedDate → today epoch with the existing ±60s extract pattern; tags → contains 'review-capture'). Case 2 (REVIEW-01 completed / review-output, Discretion #2): create a task, complete it via omnifocus_write (operation update, changes complete/completed per the existing complete path), then update with `changes: { addTags: ['review-output'] }`, and assert one read-back returns tags containing 'review-output' — assert NO future plannedDate was set. Do NOT exercise clearPlannedDate or any clear* path (OMN-55 flake, Pitfall 2; Phase 4 relies on native completion for lifecycle, not programmatic clear). afterAll: fullCleanup with the zero-errors assertion copied from the analog.
  </action>
  <verify>
    <automated>npm run build && npm run test:integration -- review-tag</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion (active): the review-capture case round-trips flagged=true AND plannedDate≈today AND tags⊇[review-capture] through an independent read-back via assertFieldPersisted.
    - Behavior assertion (completed): the review-output case completes the task, applies review-output, and reads the tag back; no plannedDate is written on the completed task.
    - Source assertion: `grep -c "assertFieldPersisted" tests/integration/tools/unified/review-tag.test.ts` ≥ 1 (reuses the shared harness, not a bespoke read loop).
    - Source assertion: the spec contains NO reference to clearPlannedDate / clearEstimatedMinutes / any clear* operation (`grep -c "clear" tests/integration/tools/unified/review-tag.test.ts` returns 0).
    - Test command `npm run test:integration -- review-tag` green; fullCleanup reports zero errors.
  </acceptance_criteria>
  <done>review-tag.test.ts proves the active review-capture (flag+plannedDate+tag) and completed review-output (tag-only) round-trips against live OmniFocus, reusing existing helpers, self-cleaning, with no clear* dependency.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                            |
| ------------------------------------ | -------------------------------------------------------------------------------------- |
| agent/owner → omnifocus_write funnel | tag + flag + date writes cross into the OmniFocus store via the single mutation funnel |
| test-mode tag guard → live store     | new functional tag names must pass isTestTagAllowed before a sandbox write is allowed  |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                                               |
| --------- | ---------------------- | ------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-01   | Tampering              | FUNCTIONAL_TAG_ALLOWLIST widening the test-mode write surface | mitigate    | Only the three named tags (review-output, review-capture, capture-live) are added explicitly; isTestTagAllowed is unchanged so task name/folder still obey the \_\_test- sandbox guard; integration specs self-clean via fullCleanup (zero-errors assertion). |
| T-04-02   | Elevation of Privilege | review writes via the funnel                                  | accept      | No new write field or policy rule — review tags ride the existing operation-policy verdicts and the write-verifier round-trip unchanged. Owner-applied review tags are within owner authority; no new privilege is granted.                                   |
| T-04-SC   | Tampering              | npm/pip/cargo installs                                        | accept      | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). No supply-chain surface added.                                                                                                                                         |

</threat_model>

<verification>
- `npm run build` clean (FUNCTIONAL_TAG_ALLOWLIST is a source constant — rebuild before integration, per Runtime State Inventory).
- `npm run test:unit -- mutation-script-builder` green (allowlist cases).
- `npm run test:integration -- review-tag` green (round-trip cases); tolerate the OMN-55 clear* re-run flake elsewhere in the suite, but this spec adds no clear* dependency.
- Use npm, NOT bun — a bare `npx vitest run` trips the sandbox guard.
</verification>

<success_criteria>

- review-output, review-capture, capture-live are in FUNCTIONAL_TAG_ALLOWLIST with JSDoc rationale (REVIEW-02 vocabulary
  registered; capture-live pre-positioned for 04-02).
- The active review-capture update (flag + plannedDate=today + tag) round-trips (REVIEW-01).
- The completed review-output tag-only update round-trips with no future plannedDate (REVIEW-01 completed branch,
  Discretion #2).
- No new Zod/inputSchema field, no perspective machinery, no clear\* sweeper. </success_criteria>

<output>
Create `.planning/phases/04-review-loops-live-auto-capture/04-01-SUMMARY.md` when done.
</output>
