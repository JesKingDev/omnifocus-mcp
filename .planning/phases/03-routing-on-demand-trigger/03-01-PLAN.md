---
phase: 03-routing-on-demand-trigger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contracts/ast/mutation-script-builder.ts
  - tests/unit/contracts/ast/mutation-script-builder.test.ts
  - tests/integration/tools/unified/end-to-end.test.ts
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-03
  - ROUTE-04

must_haves:
  truths:
    - 'Filing a task to a project dispatches moveTasks() through the write funnel with an allow verdict'
    - 'Applying the routing-unplaced marker tag goes through the OmniJS addTag bridge, not JXA'
    - 'Creating a project for the infer branch dispatches through the funnel with an allow verdict'
    - 'The routing-unplaced tag name passes the FUNCTIONAL_TAG_ALLOWLIST guard in test mode'
    - 'Re-applying routing-unplaced to an already-tagged item is handled idempotently by the skill (via check before
      apply)'
    - 'Decisions honored by this plan: D-10 (allow + funnel + verify for routing writes), D-11 (native moveTasks filing,
      no custom mover), D-12 (durable routing-unplaced marker tag on left items)'
  artifacts:
    - path: 'src/contracts/ast/mutation-script-builder.ts'
      provides: 'FUNCTIONAL_TAG_ALLOWLIST with routing-unplaced added at line 71'
      contains: 'routing-unplaced'
    - path: 'tests/unit/contracts/ast/mutation-script-builder.test.ts'
      provides: 'Unit test: routing-unplaced passes isTestTagAllowed; agent-ok still passes'
      exports: []
    - path: 'tests/integration/tools/unified/end-to-end.test.ts'
      provides:
        'Integration tests: file-task routing write (ROUTE-01), marker-tag apply (ROUTE-04), project-create (ROUTE-03)'
  key_links:
    - from: 'FUNCTIONAL_TAG_ALLOWLIST'
      to: 'isTestTagAllowed()'
      via: 'FUNCTIONAL_TAG_ALLOWLIST.includes(tag)'
      pattern: 'routing-unplaced.*FUNCTIONAL_TAG_ALLOWLIST'
    - from: 'omnifocus_write update + project field'
      to: 'moveTasks([task], project.beginning)'
      via: 'buildUpdateTaskScript → changes.project OmniJS bridge block'
      pattern: "moveTasks.*project\\.beginning"
    - from: 'omnifocus_write update + addTags:[routing-unplaced]'
      to: 'task.addTag(tag)'
      via: 'OmniJS bridge addTags block in buildUpdateTaskScript'
      pattern: 'addTag.*routing-unplaced'

user_setup: []
---

<objective>
Extend the server-side mutation infrastructure to support Phase 3 routing operations and prove them with tests.

Purpose: The routing skill (Plan 02) calls three write operations — file a task to a project (moveTasks via
update+project), apply the routing-unplaced marker tag (addTags via OmniJS bridge), and create a project for the infer
branch. All three already dispatch through the funnel with allow verdicts. This plan adds routing-unplaced to
FUNCTIONAL_TAG_ALLOWLIST (so integration tests can apply the marker in test mode) and adds unit + integration tests that
prove each write path for Phase 3.

Output: Updated mutation-script-builder.ts (allowlist), new unit tests asserting routing-unplaced is a valid functional
tag, and integration tests proving the three routing write operations via the existing funnel. </objective>

<execution_context> @/Users/jessicaking/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jessicaking/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-routing-on-demand-trigger/03-CONTEXT.md
@.planning/phases/03-routing-on-demand-trigger/03-PATTERNS.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/contracts/ast/mutation-script-builder.ts (lines 60–75):

```typescript
export const TEST_TAG_PREFIX = '__test-';

// Phase 3: extend this to ['agent-ok', 'routing-unplaced']
export const FUNCTIONAL_TAG_ALLOWLIST: readonly string[] = ['agent-ok'];

/** A tag is allowed in test mode if it is sandbox-prefixed OR a known functional tag. */
export function isTestTagAllowed(tag: string): boolean {
  return tag.startsWith(TEST_TAG_PREFIX) || FUNCTIONAL_TAG_ALLOWLIST.includes(tag);
}
```

From src/contracts/filters.ts (lines 508–524):

```typescript
/**
 * Returns a normalized filter matching only tasks tagged 'agent-ok'.
 * `inInbox` is intentionally omitted so the predicate applies equally to
 * inbox tasks and project-filed tasks that were captured by the agent.
 */
export function agentOkayPredicate(): NormalizedTaskFilter {
  return normalizeFilter({ tags: ['agent-ok'], tagsOperator: 'AND' as TagOperator });
}
```

From src/auth/operation-policy.ts (lines ~59–65) — policy for routing write ops:

```typescript
// AGENT_POLICY (relevant entries):
update: 'allow',           // → moveTasks() path (filing) + addTags (marker tag)
create_project: 'allow',   // → project create (infer branch)
```

moveTasks OmniJS block in buildUpdateTaskScript (lines ~1452–1473):

- Try Project.byIdentifier(changes.project), then flattenedProjects.find(p => p.name === changes.project)
- On match: moveTasks([task], project.beginning)
- On no match: returns { error: true, message: 'Project not found: ...' }

addTags OmniJS block in buildUpdateTaskScript (lines ~1538–1541):

- For each tagName in changes.addTags: find or create tag, call task.addTag(tag)
- JXA task.addTags() silently no-ops — this bridge path is the only working route </interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add routing-unplaced to FUNCTIONAL_TAG_ALLOWLIST + unit test</name>
  <files>src/contracts/ast/mutation-script-builder.ts, tests/unit/contracts/ast/mutation-script-builder.test.ts</files>

<read_first> - src/contracts/ast/mutation-script-builder.ts — read lines 60–76 (FUNCTIONAL_TAG_ALLOWLIST,
isTestTagAllowed, TEST_TAG_PREFIX). Understand the full allowlist guard before modifying. -
tests/unit/contracts/ast/mutation-script-builder.test.ts — read the full file to understand existing test structure and
find the right describe block for the new assertions. - .planning/phases/03-routing-on-demand-trigger/03-PATTERNS.md —
"Test sandbox guard for functional tags" section confirms the exact change needed. </read_first>

  <behavior>
    - Test: isTestTagAllowed('routing-unplaced') returns true after the allowlist is extended
    - Test: isTestTagAllowed('agent-ok') still returns true (regression guard)
    - Test: isTestTagAllowed('some-random-tag') still returns false (non-allowlist tag is rejected)
    - Test: isTestTagAllowed('__test-foo') still returns true (prefix path unchanged)
  </behavior>

  <action>
    Write the failing tests first (RED). Tests assert the four behaviors above — import FUNCTIONAL_TAG_ALLOWLIST and isTestTagAllowed from 'src/contracts/ast/mutation-script-builder.js' (the compiled path), group under a "FUNCTIONAL_TAG_ALLOWLIST / isTestTagAllowed" describe block in the existing test file.

    Then make them pass (GREEN): in mutation-script-builder.ts, change the FUNCTIONAL_TAG_ALLOWLIST array from ['agent-ok'] to ['agent-ok', 'routing-unplaced'] (per D-12 tag name, confirmed no collision with Phase 4 REVIEW-* namespace per 03-PATTERNS.md resolution). The comment block above the export explains the allowlist purpose — extend the comment to mention routing-unplaced and cite D-12.

    No other code changes. The allowlist is a readonly string array — it is the only production change in this task.

  </action>

  <verify>
    <automated>npm run build && npm run test:unit -- --filter="FUNCTIONAL_TAG_ALLOWLIST"</automated>
  </verify>

<acceptance_criteria> - `grep -c "routing-unplaced" src/contracts/ast/mutation-script-builder.ts` returns a non-zero
count - `grep "FUNCTIONAL_TAG_ALLOWLIST" src/contracts/ast/mutation-script-builder.ts` shows both 'agent-ok' and
'routing-unplaced' in the array - All four behavior tests pass (GREEN) - npm run test:unit exits 0 (no regressions)
</acceptance_criteria>

<done>FUNCTIONAL_TAG_ALLOWLIST contains 'routing-unplaced'; unit tests assert all four behaviors; no regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for the three Phase 3 routing write operations</name>
  <files>tests/integration/tools/unified/end-to-end.test.ts</files>

<read_first> - tests/integration/tools/unified/end-to-end.test.ts — read the Phase 2 D-08b describe block (lines
~829–990) in full. This is the structural analog: agent role, sandbox fixture, create → tag stamp → read-back. Task 2's
tests follow this shape for the three routing write operations. -
.planning/phases/03-routing-on-demand-trigger/03-PATTERNS.md — "Policy funnel — all write operations" section and the
moveTasks + addTags patterns sections. Understand what each operation returns and how to assert on it. -
src/contracts/ast/mutation-script-builder.ts — lines 1441–1473 (moveTasks dispatch) and lines 1538–1541 (addTags bridge)
to know the error shape vs success shape for assertions. - .planning/phases/03-routing-on-demand-trigger/03-CONTEXT.md —
D-10: write-verifier fires automatically; D-11: file via update+project field; D-12: marker tag via update+addTags.
</read_first>

  <behavior>
    Write three describe blocks under a "Phase 3 Routing — write operations" parent describe:

    Describe A: "ROUTE-01 — file task to existing project via update+project"
    - Test: agent update with {id: taskId, project: "sandbox-project-name"} returns a non-error result
    - Test: read the task back; its project field matches the target project name
    - Setup: create a task in the sandbox inbox (agent role + lineage, following D-08b pattern); find or use an existing sandbox project as the target
    - The write-verifier fires automatically; assert the returned verification status is not 'failed'

    Describe B: "ROUTE-04 — apply routing-unplaced marker tag via update+addTags"
    - Test: agent update with {id: taskId, addTags: ['routing-unplaced']} returns a non-error result
    - Test: read the task back with filters.tags.any:['routing-unplaced']; the task appears in results
    - Setup: create a sandbox inbox task (agent + lineage)
    - Verify the tag persisted (not silently no-op'd via JXA) by doing a tag-filtered read-back

    Describe C: "ROUTE-03 — create project for infer branch via create/project"
    - Test: agent create/project with {name: "__test-routing-project-<uuid>", folder: sandbox-folder-name} returns an id in the result
    - Test: omnifocus_read type:"projects" filters.name.contains:"__test-routing-project" returns the project
    - Cleanup: complete or drop the created project at the end of the test (do not leave stray projects in OmniFocus)

    All three describe blocks use the agent role (not owner). Sandbox fixture and cleanup follow the D-08b pattern exactly.

  </behavior>

  <action>
    Write the failing tests first (RED): add the three describe blocks at the bottom of the "Phase 3 Routing — write operations" parent describe in end-to-end.test.ts. Tests should fail because the allowlist gate would have blocked routing-unplaced before Task 1 (Task 1 now allows it), and the test stubs are not yet implemented.

    Then implement (GREEN): fill in the actual tool call payloads and assertions as specified in the behavior section. Use the same helper utilities (expectOk, sandbox task creation, etc.) that the D-08b block uses.

    Key assertions for each describe:
    - A: result.verificationStatus !== 'failed'; task read-back shows project field matching target
    - B: result does not contain an error; read-back with tags filter returns the task with routing-unplaced in its tags array
    - C: create result contains an id; project read-back finds the project by name; cleanup drops/completes the project

    Do not invoke WriteVerifier.verify() explicitly — the funnel does it. Assert on the mutation result's verificationStatus field if present, or the absence of an error key.

  </action>

  <verify>
    <automated>npm run build && npm run test:integration 2>&1 | grep -E "ROUTE-0[134]|routing write|PASS|FAIL" | head -30</automated>
  </verify>

<acceptance_criteria> - All three describe blocks (A, B, C) pass (GREEN) against live OmniFocus - Describe B confirms
routing-unplaced tag persisted via a tag-filtered read-back (not just mutation result) - Describe C cleans up the
created project — no stray "\_\_test-routing-project-\*" projects remain after the run - npm run test:integration exits
0 (full suite; or the Phase 3 suite passes with no regressions in existing tests) - npm run test:unit still exits 0 (no
regressions from the allowlist change in Task 1) </acceptance_criteria>

<done>Three integration test suites pass: file-to-project (ROUTE-01 path), marker-tag (ROUTE-04 path), project-create
(ROUTE-03 path). All run against live OmniFocus with sandbox cleanup.</done> </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                                                              |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| skill → OmniFocusWriteTool | The routing skill calls omnifocus_write with task IDs and project names sourced from agent reads. Malformed or unexpected values cross here.             |
| FUNCTIONAL_TAG_ALLOWLIST   | Controls which tag names may pass through the test sandbox guard. Adding routing-unplaced must not widen the blast radius for production tag operations. |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                                                                                                                                                 |
| --------- | ---------------------- | -------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-01   | Tampering              | FUNCTIONAL_TAG_ALLOWLIST         | mitigate    | The list is `readonly string[]` — no runtime mutation. A test asserting the array contents (Task 1) catches accidental removals of 'agent-ok'. The allowlist addition is the minimum change: one tag name, no new branch logic. |
| T-03-02   | Elevation of Privilege | isTestTagAllowed() in test mode  | accept      | The guard applies only in test mode (isTestMode() check at call sites). Production writes bypass isTestTagAllowed entirely — adding routing-unplaced to the allowlist has zero surface area in production.                      |
| T-03-03   | Tampering              | Integration test sandbox cleanup | mitigate    | Describe C creates "\_\_test-routing-project-<uuid>" inside the sandbox folder and drops/completes it in afterEach/afterAll. Failure to clean up leaves a visible stray project — the test name prefix makes it identifiable.   |
| T-03-SC   | Tampering              | npm/pip installs                 | accept      | No new packages introduced. All changes are to existing TypeScript source files and test files. Package legitimacy gate not triggered.                                                                                          |

</threat_model>

<verification>
Overall phase checks for Plan 01:
- FUNCTIONAL_TAG_ALLOWLIST contains exactly ['agent-ok', 'routing-unplaced'] (no over-widening)
- isTestTagAllowed('routing-unplaced') === true; isTestTagAllowed('some-other-tag') === false
- Integration tests prove moveTasks() dispatch, addTags() bridge, and project-create all route through the funnel
- No new production code paths added — only allowlist + tests
- npm run test:unit && npm run test:integration both exit 0
</verification>

<success_criteria>

- routing-unplaced is in FUNCTIONAL_TAG_ALLOWLIST and has a unit test proving it passes isTestTagAllowed
- Three routing write operations (file, tag, project-create) have passing integration tests against live OmniFocus
- No stray test artifacts left in OmniFocus after the integration suite
- Zero regressions in unit or integration test suites </success_criteria>

<output>
Create `.planning/phases/03-routing-on-demand-trigger/03-01-SUMMARY.md` when done.
</output>
