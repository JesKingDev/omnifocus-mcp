---
phase: 02-capture-permission-gating
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - src/contracts/filters.ts
  - tests/unit/auth/agent-okay-predicate.test.ts
autonomous: false
requirements:
  - PERM-01
  - CAP-01
  - LINE-01
must_haves:
  truths:
    - "agentOkayPredicate() returns a NormalizedTaskFilter with tags=['agent-okay'] and tagsOperator='AND' (D-06, D-08a)"
    - 'A task created via omnifocus_write with lineage param is stamped with agent-okay tag and has the of-mcp:lineage
      block in its note (D-08b, D-06)'
    - 'Agent capture path (CAP-01): inbox create with no project produces a task in inbox with the lineage stamp and
      agent-okay tag'
    - 'The predicate is exportable from filters.ts and usable by Phase 3 routing without modification (D-07)'
    - 'Single tag decision: agent-okay serves as both origin marker and PERM-01 gate tag (planner decision per D-08
      discretion)'
  artifacts:
    - path: 'src/contracts/filters.ts'
      provides: 'agentOkayPredicate() exported function'
      exports: ['agentOkayPredicate']
    - path: 'tests/unit/auth/agent-okay-predicate.test.ts'
      provides: 'PERM-01 predicate compilation tests (all GREEN)'
  key_links:
    - from: 'src/contracts/filters.ts'
      to: 'src/contracts/ast/builder.ts'
      via: 'agentOkayPredicate() returns NormalizedTaskFilter, consumed by buildAST()'
      pattern: 'agentOkayPredicate'
    - from: 'CAP-01 integration test'
      to: 'src/tools/unified/OmniFocusWriteTool.ts'
      via: 'create task with lineage → agent-okay tag stamped on task'
      pattern: 'agent-okay'
---

<objective>
Add agentOkayPredicate() to src/contracts/filters.ts and confirm the full Phase 2 end-to-end: that an agent create call with a lineage param produces a task in the inbox with the agent-okay tag and the of-mcp:lineage note block. End with a human verification checkpoint confirming the task appears in OmniFocus.

Purpose: PERM-01 proof (D-08a predicate unit test turns GREEN; D-08b capture stamp confirmed). CAP-01 proof (inbox task
created without project). LINE-01 proof (lineage note block present and parseable). This is the Phase 2 completion gate.

Output: All Wave 0 unit tests GREEN; capture round-trip verified manually in OmniFocus. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capture-permission-gating/02-CONTEXT.md
@.planning/phases/02-capture-permission-gating/02-RESEARCH.md
@.planning/phases/02-capture-permission-gating/02-PATTERNS.md
@.planning/phases/02-capture-permission-gating/02-VALIDATION.md

<interfaces>
<!-- Contracts to consume — no new design work here -->

From src/contracts/filters.ts (lines 61–113, 440–487): export interface TaskFilter { tags?: string[]; tagsOperator?:
TagOperator; // 'AND' | 'OR' | 'NOT_IN' inInbox?: boolean; // ... many other fields } export type NormalizedTaskFilter =
TaskFilter & { [NORMALIZED_KEY]: true } export function normalizeFilter(filter: TaskFilter): NormalizedTaskFilter

From src/contracts/ast/builder.ts (line 14): export function buildAST(filter: NormalizedTaskFilter): ASTNode

From src/contracts/ast/emitters/omnijs.ts: export function emitOmniJS(ast: ASTNode): string

Phase 2 single-tag decision (planner decision per D-08 Claude's Discretion): One tag 'agent-okay' serves as both origin
marker (stamped at create time) and PERM-01 gate tag (predicate checks for this tag). Phase 3 can distinguish origin
from user-tagged tasks via the presence of the of-mcp:lineage note block. This reduces OmniJS script complexity and
matches DISC-TAG-03 agent-tag conventions. inInbox is omitted from the predicate so Phase 3 routing can apply it to
non-inbox tasks.

From Wave 2/3 outputs: src/contracts/ast/lineage.ts → export composeLineageStamp()
src/tools/unified/OmniFocusWriteTool.ts → lineage param accepted; agent-okay tag stamped on create
src/auth/session-state.ts → isAllowedAllThisSession()

Note: the agent-okay tag stamping at create time is wired in Wave 3's OmniFocusWriteTool changes. This plan only adds
the READ-SIDE predicate to filters.ts. The write-side stamp (addTags: ['agent-okay']) must already be present in the
create dispatch from Wave 3. If not yet wired, add it there first. </interfaces> </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add agentOkayPredicate() to src/contracts/filters.ts</name>
  <files>src/contracts/filters.ts</files>
  <read_first>
    - src/contracts/filters.ts (lines 61–113, 350–487) — TaskFilter interface fields tags/tagsOperator/inInbox; normalizeFilter() at lines 440–487; NormalizedTaskFilter branded type
    - .planning/phases/02-capture-permission-gating/02-PATTERNS.md § "src/contracts/filters.ts — agentOkayPredicate() helper" — exact function shape, why inInbox is omitted, import pattern
    - .planning/phases/02-capture-permission-gating/02-CONTEXT.md § D-06 — predicate is a thin composition over tags + inInbox filters; Phase 3 consumes it
    - .planning/phases/02-capture-permission-gating/02-CONTEXT.md § D-07 — Phase 2 owns the predicate; Phase 3 owns consuming it
    - tests/unit/auth/agent-okay-predicate.test.ts (Wave 0) — four tests that must turn GREEN after this task
    - .planning/phases/02-capture-permission-gating/02-RESEARCH.md § "Claim 4" — filter pipeline verified; normalizeFilter is the mandatory downstream consumer
  </read_first>
  <behavior>
    - agentOkayPredicate().tags contains 'agent-okay'
    - agentOkayPredicate().tagsOperator === 'AND'
    - agentOkayPredicate() has no inInbox field (omitted for Phase 3 reuse)
    - buildAST(agentOkayPredicate()) does not throw
    - emitOmniJS(buildAST(agentOkayPredicate())) contains 'agent-okay'
    - The predicate does NOT match tasks without the 'agent-okay' tag (structural negative: predicate requires the tag)
  </behavior>
  <action>
    In src/contracts/filters.ts, add an exported function agentOkayPredicate() after normalizeFilter(). The function takes no arguments and returns NormalizedTaskFilter. Body: call normalizeFilter({ tags: ['agent-okay'], tagsOperator: 'AND' as TagOperator }) and return the result.

    Add JSDoc explaining:
    - Returns a normalized filter matching only tasks tagged 'agent-okay'
    - Phase 2 scope: read-side predicate for PERM-01 (D-06). Phase 3 routing consumes this without modification.
    - inInbox is omitted so the predicate applies to non-inbox tasks as well (Phase 3 routing needs this).
    - Single-tag decision: 'agent-okay' is both the origin marker (stamped at create time) and the PERM-01 gate tag. Phase 5 archaeology distinguishes agent-created from user-tagged tasks via the of-mcp:lineage block in the note.

    Do NOT add inInbox: true. Do NOT create a new file — add to the existing filters.ts. Do NOT modify any existing exported symbol.

    Also verify that the agent-okay tag stamp is wired in the create path from Wave 3 (OmniFocusWriteTool.ts). The create dispatch should include addTags: ['agent-okay'] or equivalent when the agent role creates a task. If that wiring is missing from Wave 3's output, add it to OmniFocusWriteTool.executeValidated() in the create-task dispatch path (after the gate bypass, before buildCreateTaskScript): set data.tags = [...(data.tags ?? []), 'agent-okay'] or use the addTags field in CreateDataSchema. Check Wave 3 SUMMARY to confirm if this was included.

    Build: npm run build. Run agent-okay-predicate tests.

  </action>
  <verify>
    <automated>cd /Users/jessicaking/projects/omnifocus-mcp && npm run build 2>&1 | tail -5 && npm run test:unit -- --reporter=verbose --testPathPattern="agent-okay-predicate" 2>&1 | grep -E "PASS|FAIL|agent-okay" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - agentOkayPredicate() exported from src/contracts/filters.ts (grep: `grep "agentOkayPredicate" src/contracts/filters.ts`)
    - All 4 agent-okay-predicate.test.ts tests pass GREEN
    - No inInbox field in the predicate (grep: `grep "inInbox" src/contracts/filters.ts | grep "agentOkayPredicate"` returns 0 results)
    - npm run build exits 0
    - npm run test:unit passes all previously-passing tests (no regression)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Wave 1–4 implementation complete. All Phase 2 automated tests should be GREEN:
    - lineage-stamp.test.ts: 4 tests (composeLineageStamp compose/idempotent/no-note/overrides)
    - agent-okay-predicate.test.ts: 4 tests (structure/AST compile/OmniJS emit/negative)
    - role-resolver.test.ts parseMode block: 8+ tests (literal-only default-deny matrix)
    - operation-policy.test.ts create→gate row: 1 test

    Full unit suite: `npm run test:unit` must be green.

    CAP-01 + PERM-01 + LINE-01 end-to-end: create an inbox task with a lineage param (interactive mode, owner role — set OMNIFOCUS_MCP_ROLE=owner to bypass gate) and verify it appears in OmniFocus with the agent-okay tag and lineage note block.

  </what-built>
  <how-to-verify>
    Step 1 — Unit suite green:
      cd /Users/jessicaking/projects/omnifocus-mcp && npm run test:unit
      Expected: all tests pass, exit 0.

    Step 2 — Full suite (requires OmniFocus running):
      npm run test:unit && npm run test:integration
      Expected: all tests pass.

    Step 3 — Manual capture round-trip (CAP-01/LINE-01/PERM-01):
      a. Open OmniFocus. Note the current inbox count.
      b. Run the MCP server in owner mode (OMNIFOCUS_MCP_ROLE=owner in the environment).
      c. Call omnifocus_write with:
         { mutation: { operation: 'create', target: 'task', data: { name: 'Phase 2 test task', lineage: { sessionId: 'test-session-12345' } } } }
      d. Confirm: task appears in OmniFocus inbox (inbox count +1).
      e. Open the task. Check: the task has the 'agent-okay' tag.
      f. Open the task notes. Confirm: the note contains the lineage block in canonical form:
         <!-- of-mcp:lineage
         {"v":1,"agent":"claude-code","session":"test-session-12345","created_at":"..."}
         -->
      g. Run the MCP server in agent/background mode (OMNIFOCUS_MCP_ROLE=agent, OMNIFOCUS_MCP_INTERACTIVE unset).
      h. Call the same create mutation WITHOUT the agent-okay tag already on the task.
         Expected: the call returns a POLICY_GATE_CAPTURE_CONFIRM error (interactive) or POLICY_DENY (background).
      i. Run the same call in interactive mode (OMNIFOCUS_MCP_INTERACTIVE=true).
         Expected: returns POLICY_GATE_CAPTURE_CONFIRM with structured preview.

    Step 4 — Phase 5 parse contract check:
      Parse the lineage block from Step 3f using: /<!-- of-mcp:lineage\n(.*?)\n-->/s
      JSON.parse the capture group. Confirm: v===1, agent==='claude-code', session==='test-session-12345', created_at is a valid ISO-8601 string.

  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass. Describe any failures or discrepancies for diagnosis.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                          |
| ------------------------------------ | ------------------------------------------------------------------------------------ |
| agent read predicate → OmniJS script | agentOkayPredicate() compiles through the existing filter AST — no new trust surface |
| manual test → live OmniFocus         | Human verification; no automation trust concern                                      |

## STRIDE Threat Register

| Threat ID | Category               | Component                      | Disposition      | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                               |
| --------- | ---------------------- | ------------------------------ | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-08   | Spoofing               | agent-okay tag self-tagging    | accept (Phase 3) | Agent can create agent-okay tag (tag_manage.create: 'allow'). The PERM-01 gate protects write-gating on create, not tag assignment. An agent could retroactively tag an arbitrary task agent-okay to bring it into Phase 3 routing scope — this is documented as a Phase 3 routing concern, not Phase 2. Phase 2 only builds the predicate + stamp. Document in Phase 3 plan. |
| T-02-09   | Information Disclosure | lineage sessionId in task note | accept           | sessionId is provenance (CC session), not a credential. Any user with OmniFocus access can read task notes — acceptable for provenance purposes. No PII beyond session reference.                                                                                                                                                                                             |
| T-02-SC   | Tampering              | npm installs                   | accept           | No new packages installed this plan                                                                                                                                                                                                                                                                                                                                           |

</threat_model>

<verification>
`npm run test:unit` exits 0 — all Wave 0 tests plus pre-existing tests GREEN.
`npm run test:unit && npm run test:integration` exits 0 (requires OmniFocus running).
Manual capture round-trip confirms: inbox task with agent-okay tag + of-mcp:lineage note block, correct JSON payload parseable by Phase 5 regex.
POLICY_GATE_CAPTURE_CONFIRM returned for interactive agent create with no grant.
</verification>

<success_criteria>

- agentOkayPredicate() in src/contracts/filters.ts, exported, returns tags=['agent-okay']+tagsOperator='AND', no inInbox
- All 4 agent-okay-predicate.test.ts tests GREEN
- All Phase 2 Wave 0 tests GREEN (lineage-stamp x4, agent-okay-predicate x4, parseMode matrix x8+, policy-gate row x1)
- Manual verification: inbox task with agent-okay tag and of-mcp:lineage note block confirmed in OmniFocus
- Phase 5 parse contract holds: lineage block in canonical HTML-comment-JSON form, parseable by
  /<!-- of-mcp:lineage\n(.*?)\n-->/s + JSON.parse
- POLICY_GATE_CAPTURE_CONFIRM returned for interactive agent creates without grant </success_criteria>

<output>
Create .planning/phases/02-capture-permission-gating/02-04-SUMMARY.md when done
</output>
