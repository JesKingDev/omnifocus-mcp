---
phase: 02-capture-permission-gating
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/contracts/roles.ts
  - src/auth/role-resolver.ts
  - src/auth/operation-policy.ts
  - src/session-manager.ts
  - src/auth/session-state.ts
autonomous: true
requirements:
  - PERM-01
  - PERM-02
must_haves:
  truths:
    - "parseMode() returns 'interactive' only for exact env literal 'true'; all other values → 'background' (D-05)"
    - 'ResolvedContext carries a mode field (D-04); callers of ResolvedContext compile without changes'
    - "decide('agent','create','task') returns 'gate' (D-01 policy table flip)"
    - 'allowAllThisSession grant is stored in SessionConfig (HTTP) and in stdio singleton (src/auth/session-state.ts) —
      owner-only settable (D-02)'
    - 'Agent capture is never left broken mid-wave: policy flip + grant bypass ship atomically in this plan'
  artifacts:
    - path: 'src/contracts/roles.ts'
      provides: 'Mode type + mode field on ResolvedContext'
      contains: "mode: 'interactive' | 'background'"
    - path: 'src/auth/role-resolver.ts'
      provides: 'parseMode() function'
      exports: ['parseMode']
    - path: 'src/auth/operation-policy.ts'
      provides: "create: 'gate' in AGENT_POLICY"
      contains: "create: 'gate'"
    - path: 'src/session-manager.ts'
      provides: 'allowAllThisSession field on SessionConfig'
      contains: 'allowAllThisSession'
    - path: 'src/auth/session-state.ts'
      provides: 'stdio session grant singleton'
      exports: ['isAllowedAllThisSession', 'setAllowAllThisSession', 'resetSessionGrant']
  key_links:
    - from: 'src/auth/operation-policy.ts'
      to: 'src/auth/session-state.ts'
      via: 'isAllowedAllThisSession() check in grant bypass'
      pattern: 'isAllowedAllThisSession'
    - from: 'src/auth/role-resolver.ts'
      to: 'src/contracts/roles.ts'
      via: 'import Mode type'
      pattern: 'parseMode'
---

<objective>
Implement the core connection-bound identity contracts: the mode field on ResolvedContext, the parseMode() literal-only default-deny parser, the policy table flip (create → gate), and the allowAllThisSession grant storage for both HTTP and stdio paths. These five changes must land atomically — the policy flip blocks agent creates until the grant bypass is present.

Purpose: Satisfy PERM-01 (background mode blocks untagged creates) and PERM-02 (interactive mode returns a gate verdict
that the agent renders as a confirmation prompt). The grant bypass ensures existing owner creates and session-granted
agent creates are never broken after the policy flip.

Output: parseMode() passes the Wave 0 test matrix; decide('agent','create','task') returns 'gate'; grant state
accessible from both HTTP and stdio paths. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capture-permission-gating/02-CONTEXT.md
@.planning/phases/02-capture-permission-gating/02-RESEARCH.md
@.planning/phases/02-capture-permission-gating/02-PATTERNS.md

<interfaces>
<!-- Production contracts to extend — read these before editing -->

From src/contracts/roles.ts (lines 22–84): export type Role = 'owner' | 'agent'; export type PolicyOutcome = 'allow' |
'deny' | 'gate'; export type RoleSource = 'explicit-env' | 'fail-safe-default' | 'http-token'; export interface
ResolvedIdentity { transport: ...; roleSource: RoleSource; principal: string | null; } export interface ResolvedContext
{ identity: ResolvedIdentity; role: Role; } // Phase 2 adds: mode: 'interactive' | 'background'

From src/auth/role-resolver.ts (lines 43–63): export function parseRole(env?): Role { return env.OMNIFOCUS_MCP_ROLE ===
'owner' ? 'owner' : 'agent'; } export function resolveStdioIdentity(env?): ResolvedIdentity { ... } // Phase 2 adds:
export function parseMode(env?): 'interactive' | 'background' // Anti-patterns absent: no toLowerCase, no trim, no
truthy check, no || fallback

From src/auth/operation-policy.ts (lines 32–67): const AGENT_POLICY: Record<string, PolicyOutcome | Record<string,
PolicyOutcome>> = { delete: 'deny', bulk_delete: 'deny', complete: 'allow', drop: 'allow', create: 'allow', // ← MUST
CHANGE TO 'gate' (D-01) update: 'allow', batch: 'allow', create_folder: 'allow', tag_manage: { delete: 'gate', merge:
'gate', ... } }; export function decide(role: Role, operation: string, target?: string): PolicyOutcome

From src/session-manager.ts (lines 17–31): export interface SessionConfig { sessionId: string; transport:
StreamableHTTPServerTransport; server: Server; createdAt: Date; lastActivity: Date; principal: string | null; // Phase 2
adds: allowAllThisSession?: boolean; } export class SessionManager { getSession(sessionId: string): SessionConfig |
undefined }

New file: src/auth/session-state.ts (no prior analog — mirrors SessionConfig pattern): // stdio mode: single-session
process, module-level grant export function isAllowedAllThisSession(): boolean export function
setAllowAllThisSession(role: Role): void // throws if role !== 'owner' export function resetSessionGrant(): void // for
tests only </interfaces> </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Mode type to roles.ts and parseMode() to role-resolver.ts (per D-04/D-05)</name>
  <files>src/contracts/roles.ts, src/auth/role-resolver.ts</files>
  <read_first>
    - src/contracts/roles.ts (lines 22–84) — where Role type is defined; add Mode type near Role; add mode field to ResolvedContext
    - src/auth/role-resolver.ts (lines 30–63) — existing imports and parseRole/resolveStdioIdentity; add parseMode() following parseRole exactly
    - .planning/phases/02-capture-permission-gating/02-PATTERNS.md § "src/auth/role-resolver.ts — parseMode() addition" — exact function shape and JSDoc anti-pattern list
    - .planning/phases/02-capture-permission-gating/02-CONTEXT.md § D-04, D-05 — env var name OMNIFOCUS_MCP_INTERACTIVE; exact literal 'true' only; default-deny to 'background'
    - tests/unit/auth/role-resolver.test.ts — parseMode test matrix already written (Wave 0); these tests must turn GREEN after this task
  </read_first>
  <behavior>
    - parseMode({ OMNIFOCUS_MCP_INTERACTIVE: 'true' }) → 'interactive'
    - parseMode({}) → 'background'
    - parseMode({ OMNIFOCUS_MCP_INTERACTIVE: '' }) → 'background'
    - parseMode({ OMNIFOCUS_MCP_INTERACTIVE: 'True' }) → 'background'
    - parseMode({ OMNIFOCUS_MCP_INTERACTIVE: 'TRUE' }) → 'background'
    - parseMode({ OMNIFOCUS_MCP_INTERACTIVE: ' true ' }) → 'background' (no trim)
    - parseMode({ OMNIFOCUS_MCP_INTERACTIVE: '1' }) → 'background'
    - parseMode({ OMNIFOCUS_MCP_INTERACTIVE: 'yes' }) → 'background'
  </behavior>
  <action>
    In src/contracts/roles.ts: add `export type Mode = 'interactive' | 'background';` near the Role type definition (lines 27–28 area). Add `mode: Mode;` field to `ResolvedContext` (lines 81–84 area). Update the "extend here FIRST" comment if present. Do NOT rename, remove, or restructure any existing field.

    In src/auth/role-resolver.ts: add `import type { ..., Mode } from '../contracts/roles.js';` to the existing import (extend it). Add `export function parseMode(env: Record<string, string | undefined> = process.env): Mode` immediately after parseRole (lines 43–55 area). The function body is a single ternary: return 'interactive' if and only if env.OMNIFOCUS_MCP_INTERACTIVE === 'true', else 'background'. Add JSDoc listing the same anti-patterns as parseRole's header (no toLowerCase, no trim, no truthy check, no fallback). Env var name: OMNIFOCUS_MCP_INTERACTIVE.

    If resolveStdioIdentity's return type is ResolvedIdentity and ResolvedIdentity does NOT have a mode field, do NOT add mode to ResolvedIdentity — mode lives on ResolvedContext, not ResolvedIdentity. Thread mode through only if ResolvedContext construction sites need it; otherwise leave callsite threading to Wave 3 when OmniFocusWriteTool.executeValidated() reads parseMode() directly.

    Build: npm run build. Verify parseMode test matrix turns GREEN.

  </action>
  <verify>
    <automated>cd /Users/jessicaking/projects/omnifocus-mcp && npm run build 2>&1 | tail -5 && npm run test:unit -- --reporter=verbose --testPathPattern="role-resolver" 2>&1 | grep -E "parseMode|PASS|FAIL" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `Mode = 'interactive' | 'background'` exported from src/contracts/roles.ts
    - `mode: Mode` field present on ResolvedContext interface
    - parseMode() exported from src/auth/role-resolver.ts
    - parseMode() body is a single ternary with exact literal check '=== true' (no coercion)
    - All 8+ parseMode it.each rows in role-resolver.test.ts pass GREEN
    - npm run build exits 0 — no TypeScript errors from adding mode to ResolvedContext
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip create→gate in AGENT_POLICY; add allowAllThisSession to SessionConfig and session-state.ts singleton (per D-01/D-02)</name>
  <files>
    src/auth/operation-policy.ts,
    src/session-manager.ts,
    src/auth/session-state.ts
  </files>
  <read_first>
    - src/auth/operation-policy.ts (lines 32–73) — AGENT_POLICY table; create: 'allow' at line 49 to change; tag_manage.delete: 'gate' at lines 62–63 as the existing gate pattern to follow
    - src/session-manager.ts (lines 17–31) — SessionConfig interface; principal field (lines 24–30) as the ownership-constrained analog for allowAllThisSession
    - .planning/phases/02-capture-permission-gating/02-PATTERNS.md § "src/auth/session-state.ts" — full shape of the stdio singleton: isAllowedAllThisSession / setAllowAllThisSession / resetSessionGrant
    - .planning/phases/02-capture-permission-gating/02-RESEARCH.md § "Pitfall 2" — stdio path has no SessionManager; module-level singleton required
    - .planning/phases/02-capture-permission-gating/02-CONTEXT.md § D-01, D-02 — grant is forge-resistant, owner-only
    - tests/unit/auth/operation-policy.test.ts — 'agent/create/task → gate' row (Wave 0 changed this to expect 'gate') must turn GREEN here
  </read_first>
  <behavior>
    - decide('agent', 'create', 'task') → 'gate'  (previously 'allow' — this is the behavior change)
    - decide('owner', 'create', 'task') → 'allow' (unchanged)
    - decide('agent', 'create', 'project') → 'gate' (all creates now gated for agent)
    - setAllowAllThisSession('owner') → sets grant; isAllowedAllThisSession() returns true
    - setAllowAllThisSession('agent') → throws Error with message containing 'owner'
    - resetSessionGrant() → isAllowedAllThisSession() returns false again
  </behavior>
  <action>
    In src/auth/operation-policy.ts: change `create: 'allow'` to `create: 'gate'` at line 49. Add a JSDoc comment above the create row: "agent create operations are gated — interactive mode prompts the user, background mode requires agent-okay tag (D-01, PERM-01/PERM-02)". Do not change any other row.

    In src/session-manager.ts: add `allowAllThisSession?: boolean;` to the SessionConfig interface with JSDoc: "Set only by an owner-authenticated call (D-02). Never settable by agent-supplied call arguments." Add a method to SessionManager: `setAllowAllThisSession(sessionId: string, role: Role): void` — throws if role !== 'owner', otherwise sets `session.allowAllThisSession = true` on the found SessionConfig. If session not found, throws a clear error. Import Role from contracts/roles.ts if not already imported.

    Create src/auth/session-state.ts as a new file. This is the stdio-mode analog for the HTTP SessionConfig grant. Implement three exported functions: isAllowedAllThisSession(), setAllowAllThisSession(role: Role) (throws if role !== 'owner', sets module-level _allowAllThisSession = true), resetSessionGrant() (sets _allowAllThisSession = false — for test cleanup). Import Role from '../contracts/roles.js'. Add module docstring: "stdio mode: single-session process — module-level grant state (Pitfall 2, RESEARCH.md)."

    Do NOT use the session-state.ts singleton in OmniFocusWriteTool.ts yet — that wiring happens in Wave 3 (plan 02-03). This task only creates the module and tests its contract.

    Build: npm run build. Verify operation-policy test matrix row turns GREEN.

  </action>
  <verify>
    <automated>cd /Users/jessicaking/projects/omnifocus-mcp && npm run build 2>&1 | tail -5 && npm run test:unit -- --reporter=verbose --testPathPattern="operation-policy" 2>&1 | grep -E "create.*task|PASS|FAIL" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/auth/operation-policy.ts line with `create: 'gate'` is present (grep confirms: `grep -v '^' src/auth/operation-policy.ts | grep "create: 'gate'"`)
    - The 'agent/create/task → gate' test row in operation-policy.test.ts passes GREEN
    - No other previously-passing decide() rows changed
    - SessionConfig.allowAllThisSession?: boolean is present in src/session-manager.ts
    - src/auth/session-state.ts exports isAllowedAllThisSession, setAllowAllThisSession, resetSessionGrant
    - setAllowAllThisSession('agent') throws with message referencing 'owner'
    - npm run build exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                           |
| ------------------ | ------------------------------------------------------------------------------------- |
| env → parseMode    | Untrusted env input resolves mode; literal-only parse is the defense                  |
| agent call → grant | Agent-supplied args must never set allowAllThisSession; only owner-auth code path may |

## STRIDE Threat Register

| Threat ID | Category               | Component                | Disposition | Mitigation Plan                                                                                                                                                                          |
| --------- | ---------------------- | ------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Elevation of Privilege | parseMode()              | mitigate    | Exact literal check `=== 'true'` only; no coercion, no trim, no case folding — mirrors parseRole() airtight pattern (D-05)                                                               |
| T-02-02   | Elevation of Privilege | setAllowAllThisSession() | mitigate    | Role parameter checked before mutation; throws on non-owner; call only from owner-authenticated code paths (D-02)                                                                        |
| T-02-03   | Elevation of Privilege | AGENT_POLICY.create flip | mitigate    | create → gate is a stricter policy; agent creates now hit the grant check before proceeding; policy flip and bypass ship atomically in this plan so no broken state exists between waves |
| T-02-SC   | Tampering              | npm installs             | accept      | No new packages installed this plan                                                                                                                                                      |

</threat_model>

<verification>
`npm run test:unit` passes the parseMode matrix (all 8+ rows GREEN) and the `agent/create/task → gate` policy row (GREEN).
`npm run build` exits 0 — no TypeScript compilation errors from ResolvedContext mode field or SessionConfig extension.
</verification>

<success_criteria>

- Mode type exported from src/contracts/roles.ts; mode field on ResolvedContext
- parseMode() in src/auth/role-resolver.ts: single ternary, no coercion
- AGENT_POLICY.create = 'gate' in src/auth/operation-policy.ts
- allowAllThisSession?: boolean on SessionConfig in src/session-manager.ts
- src/auth/session-state.ts exists with three exports; setAllowAllThisSession throws on non-owner
- npm run build exits 0; npm run test:unit shows parseMode + policy gate rows GREEN </success_criteria>

<output>
Create .planning/phases/02-capture-permission-gating/02-02-SUMMARY.md when done
</output>
