---
phase: 02-capture-permission-gating
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/contracts/ast/lineage.ts
  - src/tools/unified/schemas/write-schema.ts
  - src/tools/unified/OmniFocusWriteTool.ts
autonomous: true
requirements:
  - LINE-01
  - PERM-02
  - CAP-01
must_haves:
  truths:
    - 'composeLineageStamp() appends the canonical <!-- of-mcp:lineage ... --> block with blank-line separator (D-09)'
    - 'composeLineageStamp() strips any existing lineage block before re-appending (idempotent on update) (D-10)'
    - 'The lineage param is accepted by omnifocus_write — Zod schema AND inputSchema override both updated (D-11,
      dual-schema rule)'
    - "lineage is NOT added to TaskCreateData — the exhaustiveness guard in mutation-script-builder.ts does NOT include
      'lineage'; note composition happens upstream (RESEARCH Pitfall 3)"
    - 'When role=agent and no grant and mode=interactive, the funnel returns POLICY_GATE_CAPTURE_CONFIRM (PERM-02)'
    - 'When role=agent and no grant and mode=background, the funnel returns POLICY_GATE_BACKGROUND_ONLY (distinct from
      POLICY_GATE_REQUIRES_OWNER used for tag_manage gate ops — agent skill needs distinct codes to render correct UX)'
    - 'When role=agent and allowAllThisSession=true, the funnel bypasses the gate and proceeds (D-02)'
    - 'composeLineageStamp() is called BEFORE extractIntent() snapshots the intent — data.note contains the full
      composed string when handed to the verifier (RESEARCH Pitfall 4)'
    - 'agent-ok tag is unconditionally appended to data.tags when role=agent creates a task with lineage (D-06
      write-side stamp, D-08b) — not conditional on Wave 4'
  artifacts:
    - path: 'src/contracts/ast/lineage.ts'
      provides: 'composeLineageStamp() export + LINEAGE_RE constant'
      exports: ['composeLineageStamp', 'LINEAGE_RE']
    - path: 'src/tools/unified/schemas/write-schema.ts'
      provides: 'LineageSchema Zod type + lineage field on CreateDataSchema'
      contains: 'LineageSchema'
    - path: 'src/tools/unified/OmniFocusWriteTool.ts'
      provides:
        'lineage in inputSchema override + gate verdict dispatch (POLICY_GATE_CAPTURE_CONFIRM /
        POLICY_GATE_BACKGROUND_ONLY) + agent-ok tag stamp + grant bypass'
      contains: 'POLICY_GATE_CAPTURE_CONFIRM'
  key_links:
    - from: 'src/tools/unified/OmniFocusWriteTool.ts'
      to: 'src/contracts/ast/lineage.ts'
      via: 'import composeLineageStamp; called before buildCreateTaskScript'
      pattern: 'composeLineageStamp'
    - from: 'src/tools/unified/OmniFocusWriteTool.ts'
      to: 'src/auth/session-state.ts'
      via: 'import isAllowedAllThisSession; checked in gate branch'
      pattern: 'isAllowedAllThisSession'
    - from: 'src/tools/unified/schemas/write-schema.ts'
      to: 'src/tools/unified/OmniFocusWriteTool.ts'
      via: 'LineageInput type consumed in executeValidated() to compose stamp'
      pattern: 'LineageInput'
---

<objective>
Build the write-path changes: the composeLineageStamp() helper, the LineageSchema Zod + inputSchema additions (dual-schema rule), and the gate verdict dispatch + stamp composition + agent-ok tag wiring in OmniFocusWriteTool.executeValidated(). All three must land together because the dual-schema rule mandates schema + inputSchema in one change, and the stamp composition must be wired before the verifier path is exercised.

Purpose: LINE-01 (every agent-created task carries its session lineage in notes), PERM-02 (interactive gate verdict
returned; background gate uses a distinct verdict code), and D-06 write-side stamp (agent-ok tag on every agent create)
are all write-path concerns sharing the same executeValidated() function. Combining them avoids a second pass through
the most complex file in the codebase.

Output: The lineage-stamp.test.ts Wave 0 tests turn GREEN; the funnel returns POLICY_GATE_CAPTURE_CONFIRM for
interactive agent creates with no grant and POLICY_GATE_BACKGROUND_ONLY for background mode; agent-ok tag is stamped
unconditionally on every agent create with lineage. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capture-permission-gating/02-CONTEXT.md
@.planning/phases/02-capture-permission-gating/02-RESEARCH.md
@.planning/phases/02-capture-permission-gating/02-PATTERNS.md

<interfaces>
<!-- Production contracts — read before editing -->

From src/contracts/ast/mutation-script-builder.ts: export async function buildCreateTaskScript(data: TaskCreateData):
Promise<GeneratedMutationScript> // data.note: string | undefined — the final composed note string to embed //
buildTaskDataObject exhaustiveness guard at line 2353: Record<keyof TaskCreateData, true> // keys: name, note, project,
parentTaskId, tags, dueDate, deferDate, plannedDate, // flagged, estimatedMinutes, repetitionRule // — lineage MUST NOT
be added here (Pitfall 3)

From src/tools/unified/verifier/intent-extractor.ts (lines 84, 104–115): const TASK_CREATE_FIELDS = ['name', 'note',
'flagged', 'estimatedMinutes', 'tags', 'sequential']; export function extractIntent(compiledOp: unknown): Record<string,
unknown> // extractIntent reads op['data']['note'] — so data.note must contain the full // composed string (including
lineage block) BEFORE extractIntent is called

From src/tools/unified/OmniFocusWriteTool.ts: // executeValidated() structure (lines 395–470): // parseRole() at line
404 // normalizeArgsToPolicy() at line 410 // for (const item of policyItems) { decide() → gate branch at line 438 } //
gate branch returns createErrorResponseV2('POLICY_GATE_REQUIRES_OWNER', ...) // inputSchema override at lines 204–268:
// const createDataProperties: Record<string, unknown> at line 210 // createDataProperties includes
name/note/project/tags/dueDate/... at lines 211–225 // lineage: object with sessionId/agent/createdAt must be added here
(D-11)

From src/tools/unified/schemas/write-schema.ts (lines 130, 143–165): const strictObj = <T extends z.ZodRawShape>(shape:
T) => z.object(shape, ...).strict() export const CreateDataSchema = z.union([...]) — last arm has strictObj({ name,
note, ... repetitionRule }) // Add lineage: LineageSchema.optional() as last field in that strictObj //
RepetitionRuleSchema pattern (lines 18–53): the model for LineageSchema shape

From src/auth/session-state.ts (Wave 2 output): export function isAllowedAllThisSession(): boolean export function
setAllowAllThisSession(role: Role): void

From src/auth/role-resolver.ts (Wave 2 output): export function parseMode(env?): 'interactive' | 'background'

New file: src/contracts/ast/lineage.ts export const LINEAGE_RE: RegExp // /\n\n<!-- of-mcp:lineage\n.*?\n-->/s export
function composeLineageStamp(userNote: string | undefined, lineage: { sessionId: string; agent?: string; createdAt?:
string }): string </interfaces> </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/contracts/ast/lineage.ts with composeLineageStamp() (D-09/D-10)</name>
  <files>src/contracts/ast/lineage.ts</files>
  <read_first>
    - .planning/phases/02-capture-permission-gating/02-CONTEXT.md § D-09 — canonical lineage block format: blank-line separator, HTML comment sentinels, JSON payload with v/agent/session/created_at keys
    - .planning/phases/02-capture-permission-gating/02-CONTEXT.md § D-10 — strip-and-reappend on update (idempotency)
    - .planning/phases/02-capture-permission-gating/02-PATTERNS.md § "composeLineageStamp() helper" — LINEAGE_RE regex, full function shape, idempotency requirement
    - .planning/phases/02-capture-permission-gating/02-RESEARCH.md § "Pattern 3: Lineage Stamp Composition" — the exact regex and payload key names
    - tests/unit/contracts/ast/lineage-stamp.test.ts (Wave 0) — four tests that must turn GREEN after this task
  </read_first>
  <behavior>
    - composeLineageStamp('My note', { sessionId: 'sess-123' }): result starts with 'My note', contains '\n\n<!-- of-mcp:lineage\n', JSON has v:1, agent:'claude-code', session:'sess-123'
    - composeLineageStamp(existingStampedNote, { sessionId: 'new-sess' }): result has exactly one lineage block, new sessionId in payload (idempotent)
    - composeLineageStamp(undefined, { sessionId: 'sess-abc' }): result starts with '\n\n<!-- of-mcp:lineage\n'
    - composeLineageStamp('note', { sessionId: 's', agent: 'test-agent', createdAt: '2026-01-01T00:00:00.000Z' }): JSON has agent:'test-agent', created_at:'2026-01-01T00:00:00.000Z'
  </behavior>
  <action>
    Create src/contracts/ast/lineage.ts as a new module. Export two things:

    LINEAGE_RE: a RegExp constant matching the exact pattern `/\n\n<!-- of-mcp:lineage\n.*?\n-->/s` — use the 's' flag (dotAll) so the `.*?` matches newlines inside the JSON payload.

    composeLineageStamp(userNote: string | undefined, lineage: { sessionId: string; agent?: string; createdAt?: string }): string — the function strips any existing lineage block from userNote using LINEAGE_RE, trims the end of the base string, then appends a blank-line-separated block. The JSON payload uses JSON.stringify with keys: v (number 1), agent (string, default 'claude-code'), session (string, from lineage.sessionId), created_at (string, from lineage.createdAt ?? new Date().toISOString()). The stamp is `\n\n<!-- of-mcp:lineage\n${payload}\n-->`. Return base + stamp.

    Add module JSDoc explaining the sentinel format, the Phase 5 parse regex, and the strip-before-reappend invariant.

    Do NOT import from mutation-script-builder.ts or any tool file. This module has no dependencies except built-ins. It is pure string transformation.

    Build: npm run build. Run lineage-stamp tests.

  </action>
  <verify>
    <automated>cd /Users/jessicaking/projects/omnifocus-mcp && npm run build 2>&1 | tail -5 && npm run test:unit -- --reporter=verbose --testPathPattern="lineage-stamp" 2>&1 | grep -E "PASS|FAIL|stamp|idempotent" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/contracts/ast/lineage.ts exists and exports LINEAGE_RE and composeLineageStamp
    - All 4 lineage-stamp.test.ts tests pass GREEN
    - LINEAGE_RE has the 's' (dotAll) flag — verify: `grep "LINEAGE_RE" src/contracts/ast/lineage.ts | grep "/s"`
    - JSON payload keys are exactly: v, agent, session, created_at (match D-09 canonical form)
    - No import of mutation-script-builder.ts or any tool-layer file
    - npm run build exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add LineageSchema to write-schema.ts + lineage to inputSchema override + gate verdict dispatch + agent-ok tag stamp in OmniFocusWriteTool (D-11, PERM-02, D-06)</name>
  <files>
    src/tools/unified/schemas/write-schema.ts,
    src/tools/unified/OmniFocusWriteTool.ts
  </files>
  <read_first>
    - src/tools/unified/schemas/write-schema.ts (lines 1–165) — RepetitionRuleSchema pattern at lines 18–53 + line 159; strictObj at line 130; CreateDataSchema structure; SameKeys guard pattern at lines 12–13
    - src/tools/unified/OmniFocusWriteTool.ts (lines 60–70, 204–270, 395–470) — existing imports; createDataProperties object; executeValidated() gate branch at line 438
    - .planning/phases/02-capture-permission-gating/02-PATTERNS.md § "src/tools/unified/schemas/write-schema.ts" — LineageSchema shape, strictObj pattern, CreateDataSchema attachment
    - .planning/phases/02-capture-permission-gating/02-PATTERNS.md § "src/tools/unified/OmniFocusWriteTool.ts" — gate response fork, createDataProperties lineage addition, import pattern
    - .planning/phases/02-capture-permission-gating/02-RESEARCH.md § "Pattern 2" — POLICY_GATE_CAPTURE_CONFIRM for interactive mode; POLICY_GATE_BACKGROUND_ONLY (distinct code) for background; both differ from POLICY_GATE_REQUIRES_OWNER (tag_manage gate ops)
    - .planning/phases/02-capture-permission-gating/02-RESEARCH.md § "Pitfall 3" — do NOT add lineage to TaskCreateData; "Pitfall 4" — compose stamp before extractIntent sees data.note
    - .planning/phases/02-capture-permission-gating/02-CONTEXT.md § D-11 (dual-schema rule), D-01 (POLICY_GATE_CAPTURE_CONFIRM), D-02 (grant bypass), D-06 (agent-ok tag stamp at create time)
    - src/contracts/ast/lineage.ts (Task 1 output) — composeLineageStamp import path
    - src/auth/session-state.ts (Wave 2 output) — isAllowedAllThisSession import path
    - src/auth/role-resolver.ts (Wave 2 output) — parseMode import path
    - CLAUDE.md § "Dual-Schema Architecture" — both Zod schema AND inputSchema override MUST be updated in the same change
    - docs/dev/SETTER-PATTERNS.md — note setter behavior; no silent failure with composed note string
  </read_first>
  <behavior>
    - CreateDataSchema accepts { name: 'X', lineage: { sessionId: 'sess-1' } } without Zod error
    - CreateDataSchema rejects { name: 'X', lineage: { sessionId: 'sess-1', unknownKey: 'bad' } } (strict schema)
    - OmniFocusWriteTool.inputSchema includes lineage object with required sessionId
    - executeValidated() with role=agent, mode=interactive, no grant → returns error with code POLICY_GATE_CAPTURE_CONFIRM
    - executeValidated() with role=agent, mode=background, no grant → returns error with code POLICY_GATE_BACKGROUND_ONLY (distinct from POLICY_GATE_CAPTURE_CONFIRM and POLICY_GATE_REQUIRES_OWNER)
    - executeValidated() with role=agent, allowAllThisSession=true → proceeds past gate to create
    - When lineage param is present, data.note is set to composeLineageStamp(original note, lineage) BEFORE buildCreateTaskScript is called
    - When role=agent and lineage param is present, 'agent-ok' is appended to data.tags unconditionally BEFORE buildCreateTaskScript is called (D-06 write-side stamp, per D-08b)
    - lineage param does NOT appear in TaskCreateData passed to buildCreateTaskScript (consumed upstream)
  </behavior>
  <action>
    In src/tools/unified/schemas/write-schema.ts: Add LineageSchema before CreateDataSchema using strictObj(). Shape: { sessionId: z.string(), agent: z.string().optional(), createdAt: z.string().optional() }. Export type LineageInput = z.infer<typeof LineageSchema>. Add `lineage: LineageSchema.optional()` as the last field inside the CreateDataSchema's create-operation strictObj arm (after the existing repetitionRule field). Do NOT add lineage to UpdateChangesSchema or any other schema arm.

    In src/tools/unified/OmniFocusWriteTool.ts, make these changes:

    Imports: add imports for parseMode from '../../auth/role-resolver.js', isAllowedAllThisSession from '../../auth/session-state.js', composeLineageStamp from '../../contracts/ast/lineage.js', and LineageInput type from './schemas/write-schema.js'.

    inputSchema override (createDataProperties object, lines 210–225): add lineage property after reviewInterval. Shape: `{ type: 'object', description: 'Agent-origin provenance stamp (LINE-01). Server composes the note block.', properties: { sessionId: { type: 'string', description: 'Originating Claude Code session ID' }, agent: { type: 'string', description: 'Agent identifier; defaults to "claude-code"' }, createdAt: { type: 'string', description: 'ISO-8601 timestamp; defaults to server time' } }, required: ['sessionId'] }`.

    executeValidated() gate branch (lines 438–460): Replace the existing gate block with a three-way mode-aware fork. The new logic (in order):
    (1) If isAllowedAllThisSession() is true, fall through to execution — no gate response.
    (2) Else if parseMode() === 'interactive' AND item.operation === 'create', return createErrorResponseV2 with code 'POLICY_GATE_CAPTURE_CONFIRM', message 'Creating a task requires confirmation in interactive mode.', suggestion 'Confirm with the user and call omnifocus_write again, or call the owner grant endpoint first.', data: { dryRun: true, preview: { wouldAffect: { operation: item.operation, target: item.target } }, ownerCommand: { mutation: args.mutation } }.
    (3) Else if parseMode() === 'background' AND item.operation === 'create', return createErrorResponseV2 with code 'POLICY_GATE_BACKGROUND_ONLY', message 'Background-mode agent create requires agent-ok tag on the target task.', suggestion 'Tag the task with agent-ok to allow agent operations, or run in interactive mode for a confirmation prompt.', data: { dryRun: true, preview: { wouldAffect: { operation: item.operation, target: item.target } } }. This code is DISTINCT from POLICY_GATE_REQUIRES_OWNER (used only for tag_manage/delete and tag_manage/merge structural operations). The agent-side skill uses these distinct codes to branch its UX: POLICY_GATE_CAPTURE_CONFIRM → present a "yes/no" conversation; POLICY_GATE_BACKGROUND_ONLY → tell the user the background session cannot prompt; POLICY_GATE_REQUIRES_OWNER → tell the user to get owner approval for destructive tag ops.
    (4) Else use the existing POLICY_GATE_REQUIRES_OWNER response (for tag_manage/delete, tag_manage/merge, and any other gate ops that are not create).

    agent-ok tag stamp: In executeValidated(), in the create task dispatch path (after the grant bypass check, before calling composeLineageStamp or buildCreateTaskScript), add this wiring: if role === 'agent' (from parseRole()) and lineage param is present, append 'agent-ok' to data.tags: `data.tags = [...(data.tags ?? []), 'agent-ok']`. This is the D-06 write-side stamp (per D-08b). Do NOT gate this on mode — it applies in both interactive and background paths when the agent creates with lineage. This ensures Plan 04's integration test can assert agent-ok tag without conditional hedging.

    Stamp composition: In executeValidated(), in the create task dispatch path (look for where buildCreateTaskScript is called or where compiled op data is assembled), extract the lineage field from the validated args before passing to the compiler. If args contains a lineage object (per the Zod schema), call composeLineageStamp(args.data?.note, lineage) and set the note field on the data object to the resulting string BEFORE the compiled op is passed to the verifier. Strip lineage from the data object before passing to buildCreateTaskScript (do NOT pass lineage into TaskCreateData).

    The exact insertion point for stamp composition: find where `compiled` is assembled from args in executeValidated(). The MutationCompiler.compile() call produces the compiled op; `data.note` must be the composed string in the compiled op when it reaches the verifier. If the compiler copies data by reference, mutate data.note before calling compile(); if it copies by value, mutate the compiled op's data.note after compile() but before calling verifier.verify().

    Build: npm run build. Check all existing test:unit tests still pass.

  </action>
  <verify>
    <automated>cd /Users/jessicaking/projects/omnifocus-mcp && npm run build 2>&1 | tail -5 && npm run test:unit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - LineageSchema exported from write-schema.ts; lineage field on CreateDataSchema (grep: `grep "LineageSchema" src/tools/unified/schemas/write-schema.ts`)
    - lineage property present in OmniFocusWriteTool.inputSchema createDataProperties (grep: `grep "lineage" src/tools/unified/OmniFocusWriteTool.ts | grep "sessionId"`)
    - POLICY_GATE_CAPTURE_CONFIRM string present in OmniFocusWriteTool.ts (grep: `grep "POLICY_GATE_CAPTURE_CONFIRM" src/tools/unified/OmniFocusWriteTool.ts`)
    - POLICY_GATE_BACKGROUND_ONLY string present in OmniFocusWriteTool.ts (grep: `grep "POLICY_GATE_BACKGROUND_ONLY" src/tools/unified/OmniFocusWriteTool.ts`) — this code is distinct from POLICY_GATE_REQUIRES_OWNER
    - agent-ok tag appended to data.tags when role=agent and lineage present (grep: `grep "agent-ok" src/tools/unified/OmniFocusWriteTool.ts`)
    - isAllowedAllThisSession and composeLineageStamp are imported in OmniFocusWriteTool.ts
    - lineage does NOT appear in the TaskCreateData passed to buildCreateTaskScript (grep mutation-script-builder.ts for 'lineage' → 0 results)
    - npm run build exits 0
    - npm run test:unit passes all previously-passing tests (no regression in existing create/update/batch suites)
    - lineage-stamp.test.ts tests remain GREEN (composeLineageStamp was already passing from Task 1)
    - The PERM-02 Wave 0 tests in OmniFocusWriteTool.test.ts turn GREEN: POLICY_GATE_CAPTURE_CONFIRM returned for interactive agent + no grant; grant bypass test passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                                                           |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| agent call args → lineage param | Agent-supplied sessionId treated as provenance (not authorization); Zod strict() rejects unknown keys before stamp composition        |
| gate branch → session grant     | isAllowedAllThisSession() reads module-level state set only by owner-auth path; agent cannot self-elevate by calling the set function |

## STRIDE Threat Register

| Threat ID | Category               | Component                                 | Disposition | Mitigation Plan                                                                                                                                                                                                     |
| --------- | ---------------------- | ----------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-04   | Tampering              | lineage sessionId                         | mitigate    | LineageSchema.strict() rejects unknown keys; JSON.stringify() escapes all content before embedding in the note — no injection path into the OmniJS note string (D-11)                                               |
| T-02-05   | Elevation of Privilege | gate bypass via allowAllThisSession       | mitigate    | isAllowedAllThisSession() reads state that setAllowAllThisSession() protects behind role === 'owner' check; agent call args cannot set this value (D-02)                                                            |
| T-02-06   | Elevation of Privilege | per-call mode self-elevation              | mitigate    | parseMode() reads process.env at call time, not args; no mode param exists in the write schema; gate decision ignores any mode-like field in args (D-05)                                                            |
| T-02-07   | Tampering              | lineage stamp in note verifier round-trip | mitigate    | composeLineageStamp() called before extractIntent() captures data.note; verifier intent snapshot contains the full composed string; field-comparator.ts trim-compare handles trailing whitespace (Pitfall 4 closed) |
| T-02-08   | Spoofing               | background-create verdict code reuse      | mitigate    | POLICY_GATE_BACKGROUND_ONLY is distinct from POLICY_GATE_REQUIRES_OWNER; agent skill branches UX on code; code reuse would cause "get owner approval" UX for a scenario that requires "tag the task" UX             |
| T-02-SC   | Tampering              | npm installs                              | accept      | No new packages installed this plan                                                                                                                                                                                 |

</threat_model>

<verification>
`npm run build` exits 0.
`npm run test:unit` passes all existing tests AND the new lineage-stamp.test.ts tests.
`grep "POLICY_GATE_CAPTURE_CONFIRM" src/tools/unified/OmniFocusWriteTool.ts` returns a result.
`grep "POLICY_GATE_BACKGROUND_ONLY" src/tools/unified/OmniFocusWriteTool.ts` returns a result.
`grep "lineage" src/contracts/ast/mutation-script-builder.ts` returns 0 results (lineage not added to script builder).
`grep "lineage" src/tools/unified/schemas/write-schema.ts | grep "LineageSchema"` returns a result.
`grep "agent-ok" src/tools/unified/OmniFocusWriteTool.ts` returns a result (write-side stamp per D-06).
</verification>

<success_criteria>

- src/contracts/ast/lineage.ts exports LINEAGE_RE and composeLineageStamp
- write-schema.ts has LineageSchema (strict) and lineage on CreateDataSchema
- OmniFocusWriteTool.ts inputSchema includes lineage object with required sessionId
- Gate branch in executeValidated() returns POLICY_GATE_CAPTURE_CONFIRM for interactive + no grant
- Gate branch in executeValidated() returns POLICY_GATE_BACKGROUND_ONLY for background + no grant (distinct verdict)
- Grant bypass (isAllowedAllThisSession) short-circuits the gate
- agent-ok tag unconditionally appended when role=agent + lineage present (D-06 write-side stamp)
- Stamp composition happens before extractIntent() sees data.note
- lineage NOT in TaskCreateData / mutation-script-builder.ts
- npm run test:unit green (no regression)
- PERM-02 Wave 0 tests in OmniFocusWriteTool.test.ts turn GREEN </success_criteria>

<output>
Create .planning/phases/02-capture-permission-gating/02-03-SUMMARY.md when done
</output>
