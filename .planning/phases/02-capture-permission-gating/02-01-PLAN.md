---
phase: 02-capture-permission-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/contracts/ast/lineage-stamp.test.ts
  - tests/unit/auth/agent-okay-predicate.test.ts
  - tests/unit/auth/role-resolver.test.ts
  - tests/unit/auth/operation-policy.test.ts
  - tests/unit/tools/unified/OmniFocusWriteTool.test.ts
  - tests/unit/tools/unified/verifier/WriteVerifier.test.ts
autonomous: true
requirements:
  - CAP-01
  - LINE-01
  - PERM-01
  - PERM-02
must_haves:
  truths:
    - 'Running `npm run test:unit` after this plan exits with failures on every new test (RED state — files exist,
      assertions defined, implementations absent)'
    - 'Every Wave 0 test file listed in 02-VALIDATION.md exists and is parseable by vitest'
    - '`parseMode()` test cases are present in role-resolver.test.ts (no import yet — SKIP_IMPLEMENT placeholder OK)'
    - "`decide('agent','create','task') → 'gate'` row is present in operation-policy.test.ts (currently passes 'allow' —
      will be RED after policy change in Wave 1)"
    - 'OmniFocusWriteTool.test.ts has a describe block covering PERM-02 gate verdict + session-grant bypass (RED until
      Wave 3 wiring lands)'
    - 'WriteVerifier.test.ts has a test covering stamped note round-trip (RED until Wave 3 stamp composition lands)'
  artifacts:
    - path: 'tests/unit/contracts/ast/lineage-stamp.test.ts'
      provides: 'LINE-01 stamp composition + idempotency test cases'
    - path: 'tests/unit/auth/agent-okay-predicate.test.ts'
      provides: 'PERM-01 predicate compilation test cases (D-08a)'
    - path: 'tests/unit/auth/role-resolver.test.ts'
      provides: 'parseMode() default-deny parse matrix (extended)'
    - path: 'tests/unit/auth/operation-policy.test.ts'
      provides: 'create → gate row in policy matrix (extended)'
    - path: 'tests/unit/tools/unified/OmniFocusWriteTool.test.ts'
      provides: 'PERM-02 funnel gate verdict + session-grant bypass tests (extended)'
    - path: 'tests/unit/tools/unified/verifier/WriteVerifier.test.ts'
      provides: 'LINE-01 stamped note round-trip test (extended)'
  key_links:
    - from: 'tests/unit/contracts/ast/lineage-stamp.test.ts'
      to: 'src/contracts/ast/lineage.ts'
      via: 'import composeLineageStamp'
      pattern: 'composeLineageStamp'
    - from: 'tests/unit/auth/agent-okay-predicate.test.ts'
      to: 'src/contracts/filters.ts'
      via: 'import agentOkayPredicate'
      pattern: 'agentOkayPredicate'
---

<objective>
Create the Wave 0 test scaffolds that define the acceptance contract for every new behavior in Phase 2. These tests run RED until the implementation waves land. No production code is touched here.

Purpose: The Nyquist contract from 02-VALIDATION.md requires test files to exist before or alongside the tasks they
verify. Writing them first locks the acceptance criteria and makes every later implementation step self-checking.

Output: Six test locations (two new files, four extended files), all in RED state under `npm run test:unit`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capture-permission-gating/02-CONTEXT.md
@.planning/phases/02-capture-permission-gating/02-RESEARCH.md
@.planning/phases/02-capture-permission-gating/02-PATTERNS.md
@.planning/phases/02-capture-permission-gating/02-VALIDATION.md

<interfaces>
<!-- Existing test infrastructure — mirror exactly -->

From tests/unit/contracts/ast/mutation-script-builder.test.ts (lines 1–16): import { describe, it, expect, beforeEach,
afterEach } from 'vitest'; import { buildCreateTaskScript } from
'../../../../src/contracts/ast/mutation-script-builder.js';

From tests/unit/contracts/ast/filter-coverage.test.ts (lines 12–17): import { describe, it, expect } from 'vitest';
import { buildAST } from '../../../../src/contracts/ast/builder.js'; import { emitOmniJS } from
'../../../../src/contracts/ast/emitters/omnijs.js'; import type { TaskFilter } from
'../../../../src/contracts/filters.js';

From tests/unit/auth/role-resolver.test.ts (lines 1–3, 14–42): import { describe, it, expect } from 'vitest'; import {
parseRole, resolveStdioIdentity, resolveHttpIdentity } from '../../../src/auth/role-resolver.js'; it.each<{ label:
string; env: Record<string, string | undefined>; expected: ... }>([...])('$label → $expected', ...)

From tests/unit/auth/operation-policy.test.ts (lines 1–2): import { describe, it, expect } from 'vitest'; import {
decide, allowedOperations, normalizeArgsToPolicy } from '../../../src/auth/operation-policy.js';

From tests/unit/tools/unified/OmniFocusWriteTool.test.ts (lines 1–53): import { describe, it, expect, vi, beforeEach,
afterEach } from 'vitest'; import { OmniFocusWriteTool } from '../../../../src/tools/unified/OmniFocusWriteTool.js'; —
tests run as OWNER via process.env['OMNIFOCUS_MCP_ROLE'] = 'owner'. New PERM-02 describe block runs as AGENT via delete
process.env['OMNIFOCUS_MCP_ROLE'].

From tests/unit/tools/unified/verifier/WriteVerifier.test.ts (lines 1–50): import { describe, it, expect, ... } from
'vitest'; import { WriteVerifier } from '../../../../../src/tools/unified/verifier/WriteVerifier.js'; — helpers
makeCompiledOp(), makeIntent(), makeReadBackMatch() already present.

Contracts not yet implemented (Wave 1+ creates these — use import-only stubs or vi.mock): src/contracts/ast/lineage.ts →
export composeLineageStamp(userNote, lineage): string src/contracts/filters.ts → export agentOkayPredicate():
NormalizedTaskFilter (add to existing file) src/auth/role-resolver.ts → export parseMode(env?): 'interactive' |
'background' </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Create lineage-stamp.test.ts and agent-okay-predicate.test.ts (new Wave 0 files)</name>
  <files>
    tests/unit/contracts/ast/lineage-stamp.test.ts,
    tests/unit/auth/agent-okay-predicate.test.ts
  </files>
  <read_first>
    - tests/unit/contracts/ast/mutation-script-builder.test.ts (lines 1–36) — import style + describe/it shape to mirror
    - tests/unit/contracts/ast/filter-coverage.test.ts (lines 12–55) — filter compilation test shape to mirror
    - .planning/phases/02-capture-permission-gating/02-PATTERNS.md § "tests/unit/contracts/ast/lineage-stamp.test.ts" and § "tests/unit/auth/agent-okay-predicate.test.ts" — exact test cases required
    - .planning/phases/02-capture-permission-gating/02-CONTEXT.md § D-09 — canonical lineage block format (sentinel, JSON payload keys: v/agent/session/created_at)
    - .planning/phases/02-capture-permission-gating/02-CONTEXT.md § D-08 — PERM-01 proof: predicate compiles to filter, negative test required
  </read_first>
  <action>
    Create tests/unit/contracts/ast/lineage-stamp.test.ts. Import composeLineageStamp from '../../../../src/contracts/ast/lineage.js' (this file does not exist yet — vitest will report an import error, which is the correct RED state). Write four it() blocks:
    (1) 'appends lineage block after existing note with blank-line separator' — call composeLineageStamp('My note', { sessionId: 'sess-123' }), assert result contains '\n\n<!-- of-mcp:lineage\n', assert result starts with 'My note', assert JSON in block has keys v=1, agent='claude-code', session='sess-123', created_at is an ISO-8601 string.
    (2) 'strips existing lineage block before re-appending (idempotent)' — call composeLineageStamp twice with same inputs, assert the second output === the first (no duplicate blocks).
    (3) 'works with no user note (undefined)' — call with undefined as first arg, assert result starts with '\n\n<!-- of-mcp:lineage\n'.
    (4) 'respects caller-supplied agent and createdAt overrides' — pass agent='test-agent' and createdAt='2026-01-01T00:00:00.000Z', assert JSON block has those values.

    Create tests/unit/auth/agent-okay-predicate.test.ts. Import agentOkayPredicate from '../../../src/contracts/filters.js' (function does not exist yet — RED state). Import normalizeFilter and buildAST and emitOmniJS. Write four it() blocks:
    (1) 'returns a normalized filter with tags=[agent-okay] and tagsOperator=AND' — call agentOkayPredicate(), assert result.tags contains 'agent-okay' and result.tagsOperator === 'AND'.
    (2) 'predicate compiles to a valid AST via buildAST (no throw)' — call buildAST(agentOkayPredicate()), assert no throw.
    (3) 'emitted OmniJS script contains agent-okay tag name' — call emitOmniJS(buildAST(agentOkayPredicate())), assert output contains 'agent-okay'.
    (4) 'a task without agent-okay tag does NOT satisfy the filter (negative test)' — call agentOkayPredicate(), assert result does NOT have tags containing 'untagged' (structural negative: the predicate requires agent-okay, not untagged).

    Both files: use the describe/it/expect import from vitest matching the pattern of role-resolver.test.ts (no beforeEach needed). Do NOT include fenced code blocks in this action — the test code goes into the files via Write tool.

    lineage is NOT a field on TaskCreateData — do not reference TaskCreateData anywhere in these tests. Tests work directly with composeLineageStamp() and agentOkayPredicate() only.

  </action>
  <verify>
    <automated>cd /Users/jessicaking/projects/omnifocus-mcp && npm run test:unit -- --reporter=verbose 2>&1 | grep -E "lineage-stamp|agent-okay-predicate|FAIL|Cannot find module" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/contracts/ast/lineage-stamp.test.ts exists with 4 it() blocks covering append, idempotency, undefined-note, and override cases
    - tests/unit/auth/agent-okay-predicate.test.ts exists with 4 it() blocks covering structure, AST compile, OmniJS emit, and negative case
    - Both files produce import errors or test failures (RED state) — NOT green (implementation not yet written)
    - Neither file imports from or modifies any production source file
    - No fenced code block comment styles in the test files themselves (clean TS)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend role-resolver.test.ts and operation-policy.test.ts with Phase 2 cases; extend OmniFocusWriteTool.test.ts with PERM-02 gate verdict tests</name>
  <files>
    tests/unit/auth/role-resolver.test.ts,
    tests/unit/auth/operation-policy.test.ts,
    tests/unit/tools/unified/OmniFocusWriteTool.test.ts
  </files>
  <read_first>
    - tests/unit/auth/role-resolver.test.ts (lines 1–45) — existing it.each matrix shape; parseRole import already present
    - tests/unit/auth/operation-policy.test.ts (lines 1–100) — existing decide() matrix; line 89 has 'agent/create/task → allow' row
    - tests/unit/tools/unified/OmniFocusWriteTool.test.ts (lines 1–53) — import shape, createMockCache helper, beforeEach env setup pattern (runs as OWNER — new describe block must run as AGENT)
    - tests/unit/tools/write-tool-policy-guard.test.ts (lines 1–60) — the makeTool() + callExecute() helpers; how to set agent env for policy tests
    - .planning/phases/02-capture-permission-gating/02-PATTERNS.md § "describe/it.each test matrix for parse functions" — exact it.each shape for parseMode
    - .planning/phases/02-capture-permission-gating/02-CONTEXT.md § D-05 — parseMode test cases: exact literal 'true' → interactive; all others → background
    - .planning/phases/02-capture-permission-gating/02-CONTEXT.md § D-01 — policy table change: create → gate for agent
    - .planning/phases/02-capture-permission-gating/02-RESEARCH.md § "Pattern 2" — POLICY_GATE_CAPTURE_CONFIRM for interactive; POLICY_GATE_BACKGROUND_ONLY (distinct code) for background
  </read_first>
  <action>
    Extend tests/unit/auth/role-resolver.test.ts. Add a second import for parseMode alongside the existing parseRole import (parseMode is not yet exported — this causes a RED compile failure on the parseMode tests, which is correct). Add a new describe block 'parseMode — default-deny parse matrix' with an it.each table of at least 8 rows mirroring the existing parseRole matrix pattern:
    - exact match: 'true' → 'interactive'
    - undefined → 'background'
    - empty string '' → 'background'
    - whitespace only → 'background'
    - wrong case 'True' → 'background'
    - wrong case 'TRUE' → 'background'
    - typo 'truee' → 'background'
    - garbage '1' → 'background'
    Each row: { label, env: { OMNIFOCUS_MCP_INTERACTIVE: ... }, expected: 'interactive' | 'background' }. Use '$label → $expected' label format matching the existing describe.

    Extend tests/unit/auth/operation-policy.test.ts. Find the existing row at line ~89: 'agent/create/task → allow'. Change its expected value from 'allow' to 'gate'. This single change makes the existing test RED until the Wave 1 policy table flip lands.

    Extend tests/unit/tools/unified/OmniFocusWriteTool.test.ts. Add a new describe block 'PERM-02 gate verdict dispatch' after the existing describe blocks (do NOT modify any existing beforeEach/afterEach or test). The new block must:
    (a) Set up its own beforeEach that deletes process.env['OMNIFOCUS_MCP_ROLE'] (agent/fail-safe default) and sets process.env['OMNIFOCUS_MCP_INTERACTIVE'] = 'true', restoring both in afterEach.
    (b) Use the same createMockCache() and OmniFocusWriteTool pattern, with execJson mocked to resolve undefined (policy guard fires before JXA dispatch — no script execution needed).
    (c) Write two it() tests:
      (c1) 'returns POLICY_GATE_CAPTURE_CONFIRM for agent + interactive mode + no grant' — call tool.execute({ mutation: { operation: 'create', target: 'task', data: { name: 'Gate test' } } }), assert result.success === false, assert result.error.code === 'POLICY_GATE_CAPTURE_CONFIRM'. This test is RED until Wave 3 wiring (Plan 02-03 Task 2) returns the correct code.
      (c2) 'allows create when isAllowedAllThisSession returns true (session grant bypass)' — mock isAllowedAllThisSession from 'src/auth/session-state.js' to return true (via vi.mock or vi.spyOn), call the same create mutation, assert result.success === true OR that execJson was called (i.e. the gate was bypassed). This test is RED until Wave 3 wiring (grant bypass) lands.

    For the vi.mock approach: use vi.mock('../../../../src/auth/session-state.js', () => ({ isAllowedAllThisSession: () => true, setAllowAllThisSession: vi.fn(), resetSessionGrant: vi.fn() })) scoped to the new describe block using vi.doMock or a nested describe with vi.mocked. Match the existing mock pattern in write-tool-policy-guard.test.ts.

    Do NOT add any new imports beyond parseMode to role-resolver.test.ts. Do NOT change decide() or any other passing test row in operation-policy.test.ts.

  </action>
  <verify>
    <automated>cd /Users/jessicaking/projects/omnifocus-mcp && npm run test:unit -- --reporter=verbose 2>&1 | grep -E "parseMode|create.*task|PERM-02|FAIL|SyntaxError" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - role-resolver.test.ts has a new 'parseMode' describe block with 8+ it.each rows
    - operation-policy.test.ts has the 'agent/create/task → gate' row (changed from 'allow') — this test is RED until Wave 1 policy flip
    - OmniFocusWriteTool.test.ts has a 'PERM-02 gate verdict dispatch' describe block with 2 it() tests: POLICY_GATE_CAPTURE_CONFIRM and session-grant bypass
    - No other passing tests were changed or removed
    - `npm run test:unit` exits non-zero (at minimum the changed policy row fails + parseMode import fails + new PERM-02 tests fail)
    - All three files remain valid TypeScript (no syntax errors visible from grep)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extend WriteVerifier.test.ts with LINE-01 stamped note round-trip test</name>
  <files>
    tests/unit/tools/unified/verifier/WriteVerifier.test.ts
  </files>
  <read_first>
    - tests/unit/tools/unified/verifier/WriteVerifier.test.ts (lines 1–100) — existing describe shape, makeCompiledOp(), makeIntent(), makeReadBackMatch() helpers; how the verifier's verify() is called
    - .planning/phases/02-capture-permission-gating/02-CONTEXT.md § D-09 — canonical lineage block format (sentinel, JSON payload)
    - .planning/phases/02-capture-permission-gating/02-RESEARCH.md § "Pitfall 4" — verifier compares intentObj.note (composed string) against readBack.note; false WRITE_UNVERIFIED_MISMATCH if stamp is applied after extractIntent captures the intent
    - .planning/phases/02-capture-permission-gating/02-RESEARCH.md § "Pattern 3" — LINEAGE_RE regex pattern, composeLineageStamp shape
    - src/tools/unified/verifier/WriteVerifier.ts — verify() signature (read what it expects for compiledOp and readBack)
  </read_first>
  <action>
    Extend tests/unit/tools/unified/verifier/WriteVerifier.test.ts. Add a new describe block 'LINE-01 lineage stamp round-trip' after the existing describe blocks. Do NOT modify any existing test. The new block must:

    (1) Import composeLineageStamp from '../../../../../src/contracts/ast/lineage.js'. This import will cause a RED module-not-found error until lineage.ts is created in Wave 3 — that is the correct RED state for this Wave 0 scaffold.

    (2) Write one it() test: 'stamped note in compiled op does NOT produce WRITE_UNVERIFIED_MISMATCH when read-back contains the same composed note'. The test:
    - Calls composeLineageStamp('User note', { sessionId: 'sess-abc' }) to produce composedNote.
    - Constructs a compiledOp using makeCompiledOp({ data: { name: 'T', note: composedNote } }).
    - Constructs a readBack object where the task.note field also equals composedNote (simulating that OmniFocus persisted exactly what we wrote).
    - Calls the verifier's verify() (or the relevant method that compares intents) with these arguments.
    - Asserts that the result does NOT contain a WRITE_UNVERIFIED_MISMATCH error code — i.e., the note comparison passes.

    (3) Write one it() test: 'extractIntent on a compiled op with composed note includes the lineage block in the note field'. The test:
    - Calls composeLineageStamp('Original note', { sessionId: 'sess-xyz' }) to produce composedNote.
    - Confirms composedNote includes the string 'of-mcp:lineage' (the sentinel).
    - This test is a guard for Pitfall 4: if stamp composition happened after extractIntent captured the intent, the intent would have 'Original note' not the composed string. This test verifies the composition is present in what the intent snapshot will see.

    This block's purpose is to be the automated catch for the Pitfall 4 false-mismatch scenario. State that explicitly in a comment above the describe block: "Guard for Pitfall 4 (RESEARCH.md): composeLineageStamp must be called BEFORE extractIntent sees data.note. If these tests pass green but the integration test shows WRITE_UNVERIFIED_MISMATCH, the composition site in OmniFocusWriteTool.ts is after the extractIntent call — move it earlier."

    These tests are RED (FAIL or import error) until Wave 3 creates src/contracts/ast/lineage.ts.

  </action>
  <verify>
    <automated>cd /Users/jessicaking/projects/omnifocus-mcp && npm run test:unit -- --reporter=verbose 2>&1 | grep -E "WriteVerifier|lineage.*round-trip|LINE-01|FAIL|Cannot find module" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - WriteVerifier.test.ts has a 'LINE-01 lineage stamp round-trip' describe block with 2 it() tests
    - The describe block imports composeLineageStamp from the lineage.ts path (does not exist yet — RED state)
    - Test 1 asserts no WRITE_UNVERIFIED_MISMATCH when intent and read-back share the same composed note
    - Test 2 asserts composed note contains the 'of-mcp:lineage' sentinel (guards Pitfall 4 ordering)
    - A comment above the describe block names the Pitfall 4 scenario as the reason for these tests
    - No existing WriteVerifier tests are modified or removed
    - `npm run test:unit` continues to exit non-zero (new import fails RED)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                              |
| ---------- | -------------------------------------------------------- |
| Test → src | Tests import from src; no trust concern (test-only code) |

## STRIDE Threat Register

| Threat ID | Category  | Component              | Disposition | Mitigation Plan                                               |
| --------- | --------- | ---------------------- | ----------- | ------------------------------------------------------------- |
| T-02-SC   | Tampering | npm/pip/cargo installs | accept      | No new packages installed this plan; no install tasks present |

Wave 0 plans introduce no new trust surfaces — tests only. </threat_model>

<verification>
`npm run test:unit` exits non-zero (RED state expected).
All six new/extended test locations exist and contain the specified test cases.
No production source files were modified.
</verification>

<success_criteria>

- Six test locations exist: lineage-stamp.test.ts (new), agent-okay-predicate.test.ts (new), role-resolver.test.ts +
  parseMode block (extended), operation-policy.test.ts + create→gate row (extended), OmniFocusWriteTool.test.ts +
  PERM-02 gate verdict block (extended), WriteVerifier.test.ts + LINE-01 round-trip block (extended)
- Every test in these files either fails or errors — implementation does not exist yet
- `npm run test:unit` count of failing tests increases (not decreases) from baseline
- No new npm packages installed </success_criteria>

<output>
Create .planning/phases/02-capture-permission-gating/02-01-SUMMARY.md when done
</output>
