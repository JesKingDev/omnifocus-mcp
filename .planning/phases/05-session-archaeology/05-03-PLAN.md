---
phase: 05-session-archaeology
plan: 03
type: execute
wave: 2
depends_on: ['05-01', '05-02']
files_modified:
  - .claude/skills/session-archaeology/SKILL.md
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, LINE-01]
nyquist_compliant: true

must_haves:
  truths:
    - 'Invoking the skill scans the last 7 days of active (non-archived) Claude Code transcripts for unresolved open
      loops — main encoded-cwd dir + worktree-agent siblings, isSidechain excluded, windowed by content timestamp
      (ARCH-01, D-01, D-02, D-03)'
    - 'A read-only per-session summary table (Session | What it was about | Open loops? | Count) fires and waits for
      plain-text yes/edit/abort before any write — never bulk auto-creates (ARCH-02, D-04, D-04a)'
    - 'ONE merged gate shows loop + pre-computed routing placement together; a single yes covers extraction + placement
      (D-06)'
    - 'Approved loops become OmniFocus tasks in the matched/inferred project (inbox fallback) tagged archaeology +
      agent-okay with a lineage stamp (ARCH-03, D-05, LINE-01)'
    - 'Before surfacing, the skill reads archaeology-tagged tasks (active + completed) with details:true, parses session
      IDs via LINEAGE_RE, and skips already-extracted sessions (D-07, LINE-01)'
  artifacts:
    - path: '.claude/skills/session-archaeology/SKILL.md'
      provides:
        'the session-archaeology agent skill — detection rubric, merged gate, inline routing ladder, dedup, create path'
      min_lines: 100
      contains: 'yes / edit / abort'
  key_links:
    - from: '.claude/skills/session-archaeology/SKILL.md'
      to: 'probes/archaeology-prefilter.js'
      via: 'inline CLI invocation in the scan step'
      pattern: 'archaeology-prefilter'
    - from: '.claude/skills/session-archaeology/SKILL.md'
      to: 'omnifocus_read archaeology tag + details:true'
      via: 'dedup read step'
      pattern: 'archaeology'
    - from: '.claude/skills/session-archaeology/SKILL.md'
      to: 'omnifocus_write create + archaeology + agent-okay + lineage'
      via: 'Pass-2 create-and-file step'
      pattern: 'lineage'
---

<objective>
Author the one user-invoked skill this phase ships: `.claude/skills/session-archaeology/SKILL.md`. It drives the Wave-1 pre-filter probe and the existing `omnifocus_read`/`omnifocus_write` surfaces to scan 7 days of active transcripts (ARCH-01), detect open loops by the D-03 rubric, dedup against already-extracted sessions (D-07), pre-compute a routing placement per loop, present ONE merged summarize-then-approve gate (ARCH-02, D-04/D-06), and on `yes` create well-placed `archaeology`-tagged tasks (ARCH-03). All intelligence lives in the prompt; the skill adds no server code (the allowlist entry landed in Plan 01).

Purpose: this is the deliverable that recovers buried loops before they die at context-window boundaries — the whole
phase goal — expressed as an agent procedure mirroring the shipped `route-inbox-to-projects` shape. Output: one SKILL.md
following the established skill pattern. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-session-archaeology/05-RESEARCH.md
@.planning/phases/05-session-archaeology/05-PATTERNS.md
@.claude/skills/route-inbox-to-projects/SKILL.md
@.claude/skills/capture-live-blocker/SKILL.md

<interfaces>
Pre-filter (Plan 02 — invoke its CLI inline; do not reimplement the rule):
`probes/archaeology-prefilter.js` — emits `{ session_id, timestamp, role, text }` records grouped by session, already noise-stripped, isSidechain-excluded, 7-day-windowed.

Dedup read (verified vs OmniFocusReadTool.ts — `details:true` mandatory):
`{ query: { type:"tasks", filters:{ tags:{ all:["archaeology"] } }, details:true } }` — parse each note via `LINEAGE_RE`
→ JSON.parse → `.session`. Union an active read and a completed read (`filters.completed:true` / `status:"completed"`)
into one extracted-session Set (Plan 01 resolved Open Q1 = union-completed).

Create + file (per capture-live-blocker §"Tool call reference" + route-skill §"Tool call reference"): `omnifocus_write`
create/task with `data:{ name, note, tags:["archaeology"], project?:<name>, lineage:{ sessionId } }`. The funnel
auto-stamps `agent-okay` when role=agent + lineage present — pass only `archaeology`. No `project` key → inbox fallback
(ARCH-03).

Routing ladder (follow INLINE — route-skill §"Routing Decision Rules" + §"Vault Signal Read"): MATCH (existing project
names it) → INFER (vault `omnifocus-project` frontmatter names it; create if missing) → LEAVE (→ inbox). Bias to leave.

Summary "What it was about" source (RESEARCH Open Q3 resolution): prefer the transcript's `ai-title` line when present;
fall back to agent one-line summary of the session's prose. </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Author session-archaeology SKILL.md (scan → dedup → detect → merged gate → place)</name>
  <files>.claude/skills/session-archaeology/SKILL.md</files>
  <read_first>
    - .claude/skills/route-inbox-to-projects/SKILL.md (frontmatter shape, two-pass yes/edit/abort gate, the MATCH→INFER→LEAVE ladder, Vault Signal Read, Tool call reference, Common mistakes — the primary structural analog)
    - .claude/skills/capture-live-blocker/SKILL.md (create payload shape, lineage param, funnel auto-stamps agent-okay, the archaeology-vs-live-capture boundary this skill inverts)
    - 05-RESEARCH.md §"Open-Loop Detection Rubric" (verbatim four-category rubric + guaranteed-catch floor), §"Routing Reuse Mechanism" (follow ladder inline, do NOT chain the route skill), §"Dedup Read Mechanics", §"Architecture Patterns / Anti-patterns", §"Common Pitfalls"
    - 05-PATTERNS.md §"SKILL.md (skill, batch/transform)" (the per-section analog line anchors)
    - src/contracts/ast/lineage.ts (the LINEAGE_RE format the dedup parse targets)
  </read_first>
  <action>
    Create `.claude/skills/session-archaeology/SKILL.md`. Frontmatter: `name: session-archaeology`; description triggers on "scan my sessions", "session archaeology", "find open loops", "what did I leave undone" — copy the route skill's YAML description shape. Overview: a retrospective summarize-then-approve scan; OmniFocus canonical; adds no server code beyond the Plan-01 allowlist entry; on-demand only (TRIG-01; n8n deferred). Structure the procedure as two passes mirroring the route skill:

    Pass 1 (read-only): (1) **Resolve active dirs + pre-filter** — invoke `probes/archaeology-prefilter.js` inline against `~/.claude/projects/<encoded-cwd>` + `…--claude-worktrees-agent-*` siblings; it returns noise-stripped, isSidechain-excluded, 7-day-windowed records grouped by session (cite D-01/D-02/D-03; do NOT re-describe the filter rule — defer to the probe). (2) **Dedup read** — `omnifocus_read` for `filters.tags.all:["archaeology"]` with `details:true`, AND a second read including completed (`filters.completed:true` or `status:"completed"`); union the two; parse each note with `LINEAGE_RE` → `.session` into an extracted-session Set; skip any scanned session already in the Set (cite D-07). State plainly that `details:true` is mandatory (note truncates to 200 chars otherwise, dropping the end-of-note lineage block). (3) **Detect open loops** per remaining session using the verbatim four-category rubric + guaranteed-catch floor from RESEARCH §Open-Loop Detection Rubric (open question / deferred work / stated-but-unfiled intent / unfinished edit; floor = TODO / blocker / next: / unanswered question). Note the polarity: bias to RECALL (inverse of capture-live's bias-to-NOT-capture) because the approval gate is the safety mechanism. Loop extraction is **abstractive** — describe the loop in the agent's own words; never paste raw transcript text into a task name/note (threat T-05-06). (4) **Compute routing placement per loop** by following the route skill's MATCH→INFER→LEAVE ladder INLINE (read active projects + grep the vault `omnifocus-project` frontmatter per route-skill §Vault Signal Read); produce a MATCH(project)/INFER(create project)/inbox-fallback proposal per loop. Do NOT chain the route skill — that would fire its own gate (Anti-pattern, Pitfall 1). (5) **Show ONE merged table** — `Session | What it was about | Open loops? | Count` plus, per loop, the proposed placement (D-06: one decision surface). Source "What it was about" from the transcript `ai-title` when present, else a one-line agent summary (Open Q3). (6) **One gate:** "Approve this plan? (yes / edit / abort)". Wait. On **abort**, no writes. On **edit**, accept row-level corrections — drop/trim loops, override a placement — then re-show. On **yes**, proceed to Pass 2 (cite D-04/D-04a — plain text, NOT AskUserQuestion).

    Pass 2 (after approval): for each approved loop, `omnifocus_write` create/task with `data:{ name:<abstractive loop>, note:<context>, tags:["archaeology"], project:<matched-or-created-name> (omit for inbox fallback), lineage:{ sessionId:<originating session> } }`. The funnel auto-stamps `agent-okay` and the lineage stamp — pass only `archaeology` (cite D-05). For INFER placements, check the project exists before create (route-skill precedent — no duplicate projects). Capture per-loop errors without aborting the batch.

    Pass 3 (report): one summary line — N created · placements (matched/created/inbox) · sessions skipped as already-extracted · errors.

    Add a **Tool call reference** table (dedup read, active-projects read, create+file, create-project) reusing the route/capture skills' shapes. Add a **Common mistakes** table covering: chaining the route skill (double gate — Pitfall 1); keeping all user lines / re-describing the filter instead of using the probe (Pitfall 2); dedup read without `details:true` (Pitfall 3); JXA tag assignment (use the funnel); pasting raw transcript verbatim (abstractive only); filtering by file mtime (D-02 forbids — content timestamp only); AskUserQuestion instead of plain-text gate (D-04a). Add an **Out of scope** section: the Phase-6 JessOS perspective filtering on archaeology, n8n polling (TRIG-02), the hybrid per-loop dedup key (D-08), and the `archaeology`-is-never-on-live-capture boundary (inverse of capture-live).

  </action>
  <verify>
    <automated>test -f .claude/skills/session-archaeology/SKILL.md && grep -q "yes / edit / abort" .claude/skills/session-archaeology/SKILL.md && grep -q "archaeology-prefilter" .claude/skills/session-archaeology/SKILL.md && grep -q "details:true\|details: true" .claude/skills/session-archaeology/SKILL.md && grep -q "LINEAGE_RE\|of-mcp:lineage" .claude/skills/session-archaeology/SKILL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/session-archaeology/SKILL.md` exists with `name: session-archaeology` frontmatter and trigger phrases
    - contains the single merged gate primitive `yes / edit / abort` (D-04a) and exactly one approval gate (no second "Approve this plan?" — Pitfall 1)
    - invokes `probes/archaeology-prefilter.js` inline (not a reimplemented filter rule)
    - dedup step reads `filters.tags.all:["archaeology"]` with `details:true`, unions a completed read, and parses via `LINEAGE_RE`/`of-mcp:lineage`
    - create step passes `tags:["archaeology"]` + `lineage:{ sessionId }` and relies on funnel auto-stamp for agent-okay (does NOT pass agent-okay explicitly)
    - the four-category rubric + guaranteed-catch floor are present (D-03)
    - the verify grep command exits 0
  </acceptance_criteria>
  <done>The skill is a complete, self-contained agent procedure: scan → dedup → detect → one merged gate → place, citing each D-NN, reusing the probe and the routing ladder, adding no server code.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                                                               |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| transcript content → OmniFocus task | Loop text crosses from raw transcripts into persisted OmniFocus task names/notes; secrets/PII in transcripts must not be copied verbatim. |
| skill → write funnel                | Every create crosses the single mutation funnel + write-verifier; no new write path is introduced.                                        |
| skill → user (consent)              | The merged gate is the consent boundary; no task is written before `yes`.                                                                 |

## STRIDE Threat Register

| Threat ID | Category                          | Component                                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                            |
| --------- | --------------------------------- | ----------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-05-06   | Information Disclosure            | raw transcript secrets/PII copied into task name/note | mitigate    | Loop extraction is **abstractive** (describe the loop, never paste raw transcript) — stated as a rule and a Common-mistake; the Plan-02 pre-filter already strips tool_result lines (the most likely secret carriers) before the model reads, so the model rarely sees raw secrets at all. |
| T-05-07   | Repudiation / unauthorized create | bulk auto-create without consent                      | mitigate    | The single merged `yes/edit/abort` gate is mandatory before any `omnifocus_write`; ARCH-02 forbids bulk auto-create; the skill states no write occurs before `yes`. (Human-verified at run time — see VALIDATION manual checks.)                                                           |
| T-05-08   | Tampering                         | a new write path bypassing the funnel/verifier        | mitigate    | All creates go through `omnifocus_write` (the existing single funnel + write-verifier); the skill introduces no JXA/direct write. Common-mistakes table forbids JXA tag/move.                                                                                                              |
| T-05-09   | Elevation of Privilege            | self-attested lineage admits agent inbox-create       | accept      | Reuses the Phase-2/4 `lineage.sessionId` self-attested agent path (capture-live D-08b) verbatim — no new privilege; the funnel still owns the verdict. Low risk; scoped to inbox/project create the agent is already authorized for.                                                       |
| T-05-SC   | Tampering                         | npm/pip/cargo installs                                | mitigate    | Zero packages (RESEARCH §Package Legitimacy Audit — N/A). Skill is a prompt file; no install task.                                                                                                                                                                                         |

</threat_model>

<verification>
- Structural grep gate (above) passes — confirms the gate primitive, probe invocation, details:true, and lineage parse are all present.
- `npm run test:unit` remains green (skill is a doc; no code regression).
- **Human-verified (per 05-VALIDATION.md Manual-Only):** detection recall over a real 7-day scan (ARCH-01); the merged gate fires once and never auto-creates (ARCH-02); placement lands in the correct project / inbox fallback, tagged archaeology (ARCH-03). These are agent-behavioral and confirmed during `/gsd-verify-work`, not by unit test.
</verification>

<success_criteria>

- One SKILL.md that scans 7 days of active transcripts, dedups by lineage (active+completed union), detects loops by the
  D-03 rubric, presents ONE merged gate, and on `yes` creates archaeology-tagged, well-placed, lineage-stamped tasks.
- No server code added; the routing ladder is followed inline (route skill not chained); the pre-filter probe is
  invoked, not reimplemented.
- Structural grep gate green; full unit suite green. </success_criteria>

<output>
Create `.planning/phases/05-session-archaeology/05-03-SUMMARY.md` when done.
</output>
