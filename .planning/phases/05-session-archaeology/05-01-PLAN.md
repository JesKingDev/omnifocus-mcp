---
phase: 05-session-archaeology
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contracts/ast/mutation-script-builder.ts
  - tests/unit/contracts/ast/mutation-script-builder.test.ts
  - tests/unit/contracts/ast/lineage-dedup.test.ts
autonomous: true
requirements: [ARCH-03, LINE-01]
nyquist_compliant: true

must_haves:
  truths:
    - 'archaeology is a registered functional tag — an archaeology create passes the test-mode tag guard (D-05)'
    - 'A lineage stamp round-trips: composeLineageStamp writes a session ID and LINEAGE_RE + JSON.parse reads it back
      (LINE-01, D-07)'
    - 'A dedup set built from archaeology-task notes excludes any transcript whose session ID is already extracted,
      including from completed tasks (D-07)'
  artifacts:
    - path: 'src/contracts/ast/mutation-script-builder.ts'
      provides: 'archaeology entry in FUNCTIONAL_TAG_ALLOWLIST'
      contains: "'archaeology'"
    - path: 'tests/unit/contracts/ast/mutation-script-builder.test.ts'
      provides: 'allowlist membership assertion for archaeology'
      contains: "toContain('archaeology')"
    - path: 'tests/unit/contracts/ast/lineage-dedup.test.ts'
      provides: 'lineage round-trip + dedup-skip + completed-inclusion specs over fixture notes'
      min_lines: 40
  key_links:
    - from: 'tests/unit/contracts/ast/lineage-dedup.test.ts'
      to: 'src/contracts/ast/lineage.ts'
      via: 'import { LINEAGE_RE, composeLineageStamp }'
      pattern: 'LINEAGE_RE'
---

<objective>
Land the only server-side change of Phase 5 and its Wave-0 deterministic test stubs: register `archaeology` in `FUNCTIONAL_TAG_ALLOWLIST` so an archaeology-tagged create passes the funnel's test-mode tag guard (D-05, ARCH-03), and lock down the dedup backbone (D-07, LINE-01) with a unit spec that proves the lineage stamp round-trips a session ID and that a dedup set built from archaeology-task notes — active *and* completed — excludes already-extracted sessions.

Purpose: archaeology creates are rejected at runtime until the tag is allowlisted and the build is regenerated; the
dedup logic the skill relies on must be proven deterministic before the skill is written against it. Output: one-line
allowlist edit + matching allowlist assertion + a new `lineage-dedup.test.ts` spec. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-session-archaeology/05-RESEARCH.md
@.planning/phases/05-session-archaeology/05-PATTERNS.md

<interfaces>
From src/contracts/ast/lineage.ts (read-only reuse — DO NOT modify):
```typescript
export const LINEAGE_RE: RegExp = /\n\n<!-- of-mcp:lineage\n.*?\n-->/s;
export function composeLineageStamp(
  userNote: string | undefined,
  lineage: { sessionId: string; agent?: string; createdAt?: string },
): string; // payload: { v:1, agent:'claude-code', session:<sessionId>, created_at:<iso> }
```

From src/contracts/ast/mutation-script-builder.ts:

```typescript
export const FUNCTIONAL_TAG_ALLOWLIST: readonly string[] = [
  'agent-ok',
  'routing-unplaced',
  'review-output',
  'review-capture',
  'capture-live',
];
export function isTestTagAllowed(tag: string): boolean;
```

Dedup read shape the test stands in for (RESEARCH §Dedup Read Mechanics, verified vs OmniFocusReadTool.ts):
`{ query: { type:"tasks", filters:{ tags:{ all:["archaeology"] } }, details:true } }` — `details:true` mandatory (note
truncates to 200 chars otherwise, dropping the end-of-note lineage block). </interfaces> </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register archaeology in the functional tag allowlist + assert it</name>
  <files>src/contracts/ast/mutation-script-builder.ts, tests/unit/contracts/ast/mutation-script-builder.test.ts</files>
  <read_first>
    - src/contracts/ast/mutation-script-builder.ts (the FUNCTIONAL_TAG_ALLOWLIST array + its doc comment + isTestTagAllowed)
    - tests/unit/contracts/ast/mutation-script-builder.test.ts (the `describe('FUNCTIONAL_TAG_ALLOWLIST / isTestTagAllowed …')` block and its existing per-tag `it(...)` cases — the `capture-live` case is the exact analog)
    - 05-PATTERNS.md §"mutation-script-builder.ts (config — allowlist add)" and §"allowlist assertion" for the precedent line + comment style
  </read_first>
  <behavior>
    - FUNCTIONAL_TAG_ALLOWLIST includes 'archaeology'
    - isTestTagAllowed('archaeology') === true
    - existing allowlist members (agent-ok, routing-unplaced, review-output, review-capture, capture-live) remain — regression
  </behavior>
  <action>
    Append `'archaeology', // Phase 5 D-05 session-archaeology marker` as the last entry of FUNCTIONAL_TAG_ALLOWLIST, following the existing `capture-live` comment style. Extend the array's doc comment with one sentence noting the Phase-5 archaeology marker, mirroring how the comment already enumerates each prior phase's tag. Add one `it('allows archaeology (Phase 5 D-05) in test mode', …)` case inside the existing `describe('FUNCTIONAL_TAG_ALLOWLIST / isTestTagAllowed …')` block (before its closing `})`), asserting `FUNCTIONAL_TAG_ALLOWLIST` contains `'archaeology'` and `isTestTagAllowed('archaeology')` is `true` — copy the shape of the `capture-live` case verbatim. Run `npm run build` after the source edit so the compiled allowlist the funnel reads at runtime carries the new entry (RESEARCH §Runtime State Inventory — the funnel reads the compiled array).
  </action>
  <verify>
    <automated>npm run build && npm run test:unit -- mutation-script-builder</automated>
  </verify>
  <acceptance_criteria>
    - `mutation-script-builder.ts` FUNCTIONAL_TAG_ALLOWLIST contains `'archaeology'`
    - `mutation-script-builder.test.ts` asserts `isTestTagAllowed('archaeology') === true` and `FUNCTIONAL_TAG_ALLOWLIST` toContain `'archaeology'`
    - `npm run build` exits 0 (allowlist recompiled)
    - `npm run test:unit -- mutation-script-builder` exits 0
  </acceptance_criteria>
  <done>archaeology is allowlisted, the build is regenerated, and the per-tag assertion is green alongside the existing five.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lineage round-trip + dedup-skip spec (active + completed sessions)</name>
  <files>tests/unit/contracts/ast/lineage-dedup.test.ts</files>
  <read_first>
    - src/contracts/ast/lineage.ts (LINEAGE_RE, composeLineageStamp, the strip-before-reappend idempotency invariant)
    - 05-PATTERNS.md §"New unit spec: lineage round-trip + dedup-skip" (the three assertions to make + the completed-task caveat)
    - 05-RESEARCH.md §"Dedup Read Mechanics" (the details:true requirement, the `Set<sessionId>` build, the completed-task polarity Open Q1)
  </read_first>
  <behavior>
    - Round-trip: composeLineageStamp(note, { sessionId: 'S1' }) → LINEAGE_RE matches → JSON.parse of the inner payload → `.session === 'S1'`
    - Idempotency: stamping a note twice with the same input yields exactly one lineage block (LINEAGE_RE matches once)
    - Dedup-skip: given an array of fixture archaeology-task notes (each carrying a lineage block), a helper that parses each note via LINEAGE_RE builds a Set of session IDs; a transcript whose session_id is in that Set is excluded, one whose id is absent is retained
    - Completed inclusion (Open Q1 resolution): the dedup Set is built from the UNION of an active-tasks note list and a completed-tasks note list — a session present only in the completed list is still excluded (handled loops stay handled)
  </behavior>
  <action>
    Create `tests/unit/contracts/ast/lineage-dedup.test.ts` importing `LINEAGE_RE` and `composeLineageStamp` from `../../../../src/contracts/ast/lineage.js` (match the relative depth of the sibling `mutation-script-builder.test.ts` import). Define a small in-test pure helper `buildExtractedSessionSet(notes: string[]): Set<string>` that applies `LINEAGE_RE`, slices the JSON payload out of the matched comment, `JSON.parse`s it, and collects `.session` — this is the exact parse the skill will perform on the dedup read, made deterministic over fixtures. Assert the four behaviors above. For the completed-inclusion case, pass two fixture note arrays (active + completed), union their Sets, and assert a completed-only session ID is present in the union and its transcript excluded. Resolve Open Q1 in favor of union-completed (D-07 self-healing: only *deleted* tasks re-surface; *completed* loops stay handled) and document that choice in a top-of-file comment citing D-07.
  </action>
  <verify>
    <automated>npm run test:unit -- lineage-dedup</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/contracts/ast/lineage-dedup.test.ts` exists and imports `LINEAGE_RE` + `composeLineageStamp` from `src/contracts/ast/lineage`
    - spec asserts `.session` round-trips to the input sessionId
    - spec asserts double-stamp yields one LINEAGE_RE match (idempotency)
    - spec asserts a session ID present only in the completed-task fixture list is in the dedup Set and its transcript excluded
    - `npm run test:unit -- lineage-dedup` exits 0
  </acceptance_criteria>
  <done>The dedup backbone is proven deterministic over fixtures, including the union-completed polarity, before the skill is written against it.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                         |
| --------------------------------- | --------------------------------------------------------------------------------------------------- |
| compiled allowlist → write funnel | A tag string crosses into the mutation funnel's test-mode guard; an un-allowlisted tag is rejected. |

## STRIDE Threat Register

| Threat ID | Category               | Component                            | Disposition | Mitigation Plan                                                                                                                                                          |
| --------- | ---------------------- | ------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-05-01   | Tampering              | FUNCTIONAL_TAG_ALLOWLIST scope creep | mitigate    | Add exactly one entry (`archaeology`); the allowlist enumeration test fails CI if an unexpected tag appears, bounding blast radius to a known idempotent functional tag. |
| T-05-02   | Elevation of Privilege | allowlist bypasses sandbox guard     | accept      | Allowlisted tags are exempt only from the tag-prefix guard; the task itself still obeys the sandbox name/folder guards (per the array doc comment). No new privilege.    |
| T-05-SC   | Tampering              | npm/pip/cargo installs               | mitigate    | This plan installs zero packages (RESEARCH §Package Legitimacy Audit — N/A). No install task exists; no checkpoint required.                                             |

</threat_model>

<verification>
- `npm run test:unit` green (full unit suite — no regression in the allowlist enumeration or elsewhere).
- `npm run build` exits 0 (compiled allowlist carries `archaeology`).
</verification>

<success_criteria>

- `archaeology` ∈ FUNCTIONAL_TAG_ALLOWLIST and `isTestTagAllowed('archaeology')` true, asserted by a committed test.
- Lineage round-trip + dedup-skip + completed-inclusion proven over fixtures.
- Full unit suite green. </success_criteria>

<output>
Create `.planning/phases/05-session-archaeology/05-01-SUMMARY.md` when done.
</output>
