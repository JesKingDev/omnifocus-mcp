---
phase: 05-session-archaeology
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - probes/archaeology-prefilter.js
  - tests/unit/probes/archaeology-prefilter.test.ts
  - tests/fixtures/archaeology/sample-transcript.jsonl
autonomous: true
requirements: [ARCH-01]
nyquist_compliant: true

must_haves:
  truths:
    - 'A deterministic pre-filter reduces a raw transcript to user-prose + assistant-text records, dropping
      tool_use/tool_result/attachment/file-history-snapshot/system/mode/queue-operation lines AND tool_result-only user
      lines (D-03)'
    - 'The pre-filter excludes isSidechain subagent lines (D-02 v1 exclusion)'
    - 'The pre-filter windows by per-message ISO timestamp, dropping lines older than 7 days from a supplied reference
      time (D-02 content-date, not mtime)'
  artifacts:
    - path: 'probes/archaeology-prefilter.js'
      provides: 'committed throwaway noise-strip + 7-day content-date window over transcript JSONL'
      min_lines: 30
    - path: 'tests/unit/probes/archaeology-prefilter.test.ts'
      provides: 'noise-strip + isSidechain-exclude + window spec over a fixture JSONL'
      min_lines: 30
    - path: 'tests/fixtures/archaeology/sample-transcript.jsonl'
      provides:
        'one line of each observed JSONL type incl. tool_result-only user, isSidechain:true, and an out-of-window line'
  key_links:
    - from: 'tests/unit/probes/archaeology-prefilter.test.ts'
      to: 'probes/archaeology-prefilter.js'
      via: 'import the pure filter function'
      pattern: 'archaeology-prefilter'
---

<objective>
Ship the deterministic transcript pre-filter (D-03 token-budget gate, D-02 windowing) as a committed `.js` probe with a unit-testable pure core, plus the fixture JSONL and the noise-strip spec named in 05-VALIDATION.md Wave-0. Resolve RESEARCH Open Q2 in favor of the committed-helper path (the only deterministic, testable option) over inline `python3`, honoring the repo `.js`-probe precedent (`probes/disc-*.js` already exist).

Purpose: raw transcripts run to ~2.6 MB each and ~87% of `user` lines are tool_result echoes; the skill cannot feed raw
transcripts to the model. The strip rule is deterministic and must be unit-tested, not left to agent improvisation.
Output: `probes/archaeology-prefilter.js` (pure filter + thin CLI wrapper), its spec, and a one-line-per-type fixture
transcript. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-session-archaeology/05-RESEARCH.md
@.planning/phases/05-session-archaeology/05-PATTERNS.md

<interfaces>
Filter rule (RESEARCH §Pre-Filter Implementation — the verified line-type census + content-shape rule):
- exclude any line where `isSidechain === true`
- keep `user` line if `message.content` is a string OR its content array contains a `{type:'text'}` item
- keep `assistant` line if its content array contains a `{type:'text'}` item (emit the text only)
- drop everything else: tool_use / tool_result-only user / attachment / file-history-snapshot / system / mode / permission-mode / queue-operation / last-prompt / bridge-session / ai-title
- after keeping, drop any line whose per-message `timestamp` (ISO-8601) is older than `now − 7d`
- emit `{ session_id, timestamp, role, text }` records grouped by session

Observed top-level `type` values (RESEARCH, verified live): user, assistant, attachment, file-history-snapshot, system,
mode, permission-mode, queue-operation, ai-title, last-prompt, bridge-session. Key fields: `type`, `timestamp`,
`isSidechain`, `sessionId`, `message.role`, `message.content` (string | array of {type:
text|thinking|tool_use|tool_result}). </interfaces> </context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture transcript covering every line-type branch</name>
  <files>tests/fixtures/archaeology/sample-transcript.jsonl</files>
  <read_first>
    - 05-RESEARCH.md §"Raw Transcript Structure" (the per-line schema table + key fields + the tool_result-only-user refinement)
    - probes/disc-tag-01-auto-create.js (an existing committed `.js` probe — establishes the probe-file convention this fixture supports)
  </read_first>
  <action>
    Create `tests/fixtures/archaeology/sample-transcript.jsonl` with one JSON object per line covering every branch the filter must decide: (a) a `user` line with string `message.content` carrying real prose and an in-window `timestamp` (KEEP); (b) a `user` line whose `message.content` is an array containing only a `{type:'tool_result'}` item (DROP — the 87% case); (c) a `user` line whose content array contains a `{type:'text'}` item (KEEP); (d) an `assistant` line with a `{type:'text'}` item (KEEP, emit text); (e) an `assistant` line with only `{type:'tool_use'}` / `{type:'thinking'}` items (DROP); (f) one line each of `attachment`, `file-history-snapshot`, `system`, `mode`, `queue-operation`, `ai-title` (all DROP); (g) a `user` prose line with `isSidechain:true` (DROP regardless of content); (h) a `user` prose line with a `timestamp` older than 7 days from a fixed reference (DROP by window). Give each KEEP line a distinct `sessionId` or text so the spec can assert exactly which survived. Use a fixed, obviously-synthetic reference date in the fixture (no real session content, no secrets — abstractive synthetic prose only).
  </action>
  <verify>
    <automated>node -e "require('fs').readFileSync('tests/fixtures/archaeology/sample-transcript.jsonl','utf8').trim().split('\n').forEach(l=>JSON.parse(l)); console.log('fixture parses')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/archaeology/sample-transcript.jsonl` exists; every line is valid JSON
    - fixture contains at least one line for each branch: KEEP-prose, DROP-tool_result-only, KEEP-text-array, KEEP-assistant-text, DROP-assistant-no-text, DROP-noise-type, DROP-isSidechain, DROP-out-of-window
    - no real transcript content or secrets — synthetic prose only
  </acceptance_criteria>
  <done>A single fixture exercises every filter branch with synthetic, secret-free content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pre-filter probe (pure core + CLI) and its noise-strip spec</name>
  <files>probes/archaeology-prefilter.js, tests/unit/probes/archaeology-prefilter.test.ts</files>
  <read_first>
    - 05-RESEARCH.md §"Pre-Filter Implementation" (the filter pseudocode + the committed-helper option) and §"Common Pitfalls" Pitfall 2 (content-shape filtering, not type-only)
    - 05-PATTERNS.md §"Pre-filter helper" (the inline-tool analog + the committed-`.js`-under-probes recommendation)
    - probes/disc-capture-01-inbox-note-roundtrip.js (existing `.js` probe shape — module + invocation convention)
    - the fixture from Task 1
  </read_first>
  <behavior>
    - filterTranscriptLines(lines, nowMs) returns only: the KEEP-prose, KEEP-text-array, and KEEP-assistant-text records from the fixture
    - every returned record has shape { session_id, timestamp, role, text } with non-empty text
    - the isSidechain:true prose line is absent from the result
    - the out-of-window line is absent from the result
    - none of attachment / file-history-snapshot / system / mode / queue-operation / ai-title / tool_result-only-user appears
  </behavior>
  <action>
    Create `probes/archaeology-prefilter.js` as CommonJS (matching the existing `probes/*.js` convention) exporting a pure function `filterTranscriptLines(lines: object[], nowMs: number)` implementing the verified rule from the interfaces block: `isSidechain` exclusion first, then the content-shape keep rule for `user` (string OR has-`text`) and `assistant` (has-`text`, emit text only), drop all other types, then the `timestamp >= nowMs − 7*24*60*60*1000` window. Emit `{ session_id, timestamp, role, text }` records. Add a thin CLI wrapper (executed only when run as main, not on import) that resolves active dirs (`~/.claude/projects/<encoded-cwd>` + `…--claude-worktrees-agent-*` siblings), streams each `.jsonl`, and prints filtered records grouped by session — the skill invokes this wrapper inline (mirrors the route skill's inline vault grep). Keep `nowMs` a parameter (not `Date.now()` inside the pure function) so the window is deterministically testable. Create `tests/unit/probes/archaeology-prefilter.test.ts` that loads `tests/fixtures/archaeology/sample-transcript.jsonl`, parses each line, calls `filterTranscriptLines(lines, <fixed reference ms matching the fixture>)`, and asserts the five behaviors above by counting and inspecting surviving records.
  </action>
  <verify>
    <automated>npm run test:unit -- archaeology-prefilter</automated>
  </verify>
  <acceptance_criteria>
    - `probes/archaeology-prefilter.js` exports a pure `filterTranscriptLines(lines, nowMs)` taking `nowMs` as a parameter
    - the CLI wrapper runs only as main (guarded by `require.main === module`), so importing the function has no side effects
    - `archaeology-prefilter.test.ts` asserts only the three KEEP records survive and isSidechain + out-of-window + every noise type are dropped
    - each surviving record has shape `{ session_id, timestamp, role, text }`
    - `npm run test:unit -- archaeology-prefilter` exits 0
  </acceptance_criteria>
  <done>The noise-strip + isSidechain-exclude + 7-day window is a committed, deterministic, unit-tested function the skill invokes inline.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                                                               |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| transcript file → model context | Raw `.jsonl` lines (may contain secrets/tokens echoed in tool_result) cross into what the model reads; the pre-filter is the choke point. |
| filesystem scope                | The CLI wrapper resolves dirs under `~/.claude/projects/<this-repo>*` only — no traversal outside.                                        |

## STRIDE Threat Register

| Threat ID | Category                   | Component                                                    | Disposition | Mitigation Plan                                                                                                                                                                                                                                      |
| --------- | -------------------------- | ------------------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-03   | Information Disclosure     | secrets in tool_result lines reaching model context          | mitigate    | The pre-filter drops ALL `tool_result` content (the most likely secret carrier — API keys, tokens echoed from tool output) before the model reads; only `user` prose + `assistant` text survive. Asserted by the DROP-tool_result-only fixture case. |
| T-05-04   | Tampering / path traversal | CLI dir resolution reads outside the repo's transcript scope | mitigate    | The wrapper globs `~/.claude/projects/<encoded-cwd>` + `…--claude-worktrees-agent-*` siblings only; no user-supplied path, no parent traversal.                                                                                                      |
| T-05-05   | Denial of Service          | a malformed JSONL line crashes the run                       | mitigate    | The pure function is fed already-parsed objects; the CLI wrapper skips lines that fail `JSON.parse` rather than aborting (per route-skill "capture errors without aborting" precedent).                                                              |
| T-05-SC   | Tampering                  | npm/pip/cargo installs                                       | mitigate    | Zero packages installed (RESEARCH §Package Legitimacy Audit — N/A). `python3` was the alternative; the committed-`.js` path adds no dependency.                                                                                                      |

</threat_model>

<verification>
- `npm run test:unit` green (the new probe spec + no regression).
- The pure function takes `nowMs` as a parameter (window is deterministic, not wall-clock-dependent).
</verification>

<success_criteria>

- Committed `.js` pre-filter with a pure, parameterized core that strips noise + tool_result-only user lines +
  isSidechain + out-of-window lines.
- Fixture-driven noise-strip spec green.
- No secrets path: tool_result content never survives the filter. </success_criteria>

<output>
Create `.planning/phases/05-session-archaeology/05-02-SUMMARY.md` when done.
</output>
