---
phase: 03-rolegate-agent-read-paths
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - tests/unit/tools/index-rolegate.test.ts
  - tests/integration/mcp-protocol.test.ts
autonomous: false
requirements:
  - GATE-03
  - READ-01
  - READ-02
  - READ-03

must_haves:
  truths:
    - 'AGENT read dispatch (today/overdue/flagged/available/blocked/inbox/date-range/count-only) never fires the policy
      gate'
    - 'AGENT lookup by filters.id and filters.projectId passes through gate (no policy gate fires)'
    - 'AGENT type=perspectives query passes through gate (no policy gate fires)'
    - 'Integration test covers both AGENT-trimmed and OWNER-full ListTools surface'
    - 'AGENT smoke-tested end-to-end: write hot-path ops and read paths exercised via stdio'
  artifacts:
    - path: 'tests/unit/tools/index-rolegate.test.ts'
      provides: 'Activated READ-01/02/03 unit tests (read ops pass through gate without policy rejection)'
    - path: 'tests/integration/mcp-protocol.test.ts'
      provides: 'Role-aware ListTools assertions: AGENT-trimmed enum, OWNER-full enum'
  key_links:
    - from: 'tests/unit/tools/index-rolegate.test.ts'
      to: 'src/tools/index.ts dispatch gate'
      via: 'normalizeArgsToPolicy({query:...}) → [] → gate no-op for read ops'
      pattern: "normalizeArgsToPolicy returns.*\\[\\]"
    - from: 'tests/integration/mcp-protocol.test.ts'
      to: 'src/index.ts stdio startup'
      via: 'spawn without OMNIFOCUS_MCP_ROLE for AGENT; with OMNIFOCUS_MCP_ROLE=owner for OWNER'
      pattern: 'OMNIFOCUS_MCP_ROLE'
---

<objective>
Complete the agent read-surface verification (READ-01/02/03) by activating the remaining Wave 0 unit test stubs in index-rolegate.test.ts, extend the integration test with role-aware ListTools assertions, and confirm the full GATE-03 write hot-path and read surface end-to-end via a human-verify checkpoint.

Purpose: READ-01/02/03 are largely verification rather than new construction — OmniFocusReadTool already implements all
query modes and perspectives. This plan confirms the dispatch gate correctly identifies read ops as pass-through
(normalizeArgsToPolicy returns [] for query args) and that the integration test reflects the role-aware surface that now
exists.

Output: READ-01/02/03 unit tests passing, integration test extended for AGENT/OWNER role-aware surface, human verify
checkpoint confirming end-to-end AGENT write and read paths work in live OmniFocus. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rolegate-agent-read-paths/03-CONTEXT.md
@.planning/phases/03-rolegate-agent-read-paths/03-RESEARCH.md
@.planning/phases/03-rolegate-agent-read-paths/03-VALIDATION.md
@.planning/phases/03-rolegate-agent-read-paths/03-03-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/tools/unified/OmniFocusReadTool.ts (existing, no changes this plan): // type field values: 'tasks' | 'projects'
| 'tags' | 'perspectives' | 'export' // filters.id — task/project lookup by identifier (READ-02) // type='perspectives'
— list native perspectives (READ-03) // Query modes (READ-01): 'today', 'forecast', 'overdue', 'flagged', 'available',
'blocked', 'inbox', 'dateRange', and countOnly flag

From src/auth/operation-policy.ts: export function normalizeArgsToPolicy(args: Record<string, unknown>): PolicyItem[];
// Returns [] for args with no 'mutation' field (all read ops, system ops)

From tests/integration/mcp-protocol.test.ts (current): // Asserts exactly 4 tools; asserts full tool surface (all
operations visible) // Will need AGENT-trimmed assertions added without breaking OWNER assertions </interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Activate READ-01/02/03 unit stubs + extend integration test for role-aware ListTools</name>
  <files>
    tests/unit/tools/index-rolegate.test.ts
    tests/integration/mcp-protocol.test.ts
  </files>

<read_first> 1. tests/unit/tools/index-rolegate.test.ts — current state after Plan 02. Find the vi.todo() stubs for
READ-01, READ-02, READ-03. These become real tests. 2. tests/integration/mcp-protocol.test.ts — read the full file. Find
the tools/list assertions and the server spawn pattern. Note how the server is started (stdio spawn with env). The AGENT
test spawns without OMNIFOCUS_MCP_ROLE (defaults to agent); the OWNER test spawns with OMNIFOCUS_MCP_ROLE=owner. 3.
.planning/phases/03-rolegate-agent-read-paths/03-RESEARCH.md §Open Questions item 3 — recommendation for AGENT vs OWNER
integration test structure. 4. .planning/phases/03-rolegate-agent-read-paths/03-PATTERNS.md §Tests — read the AGENT
ListTools assertion pattern (checking operation enum for absence of 'delete'). 5. src/tools/unified/OmniFocusReadTool.ts
— read the inputSchema getter and the type/filters/query shape to construct valid test args for read dispatch tests.
</read_first>

  <action>
    Part A — Activate READ-01/02/03 stubs in index-rolegate.test.ts:

    Replace the vi.todo() stubs for READ-01, READ-02, READ-03 with real implementations. The core property being tested: normalizeArgsToPolicy({ query: { ... } }) returns []; therefore the dispatch gate loop is a no-op and the CallTool handler proceeds to tool.execute(). Since the tests are unit tests (not integration), mock the tool execution or call the dispatch gate logic directly. The simplest approach: construct a Server with registerTools('agent' role), then call the tools/call handler with read-type args; assert the response is not a policy denial (response text does not contain 'POLICY_DENY' in error code).

    READ-01 test: call with { name: 'omnifocus_read', arguments: { query: { type: 'tasks', filters: { status: 'active' }, countOnly: true } } }. Assert the response does not contain 'POLICY_DENY_DELETE' or 'POLICY_GATE_REQUIRES_OWNER'.

    READ-02 test: call with { name: 'omnifocus_read', arguments: { query: { type: 'tasks', filters: { id: 'test-task-id' } } } }. Assert no policy denial.

    READ-03 test: call with { name: 'omnifocus_read', arguments: { query: { type: 'perspectives' } } }. Assert no policy denial.

    Note: tool.execute() will likely fail for lack of real OmniFocus; that is acceptable. The test is about the dispatch gate being a no-op, not about the query returning data. Assert the error (if any) is not a policy error — it may be a JXA/OmniJS execution error, which is fine.

    Part B — Extend tests/integration/mcp-protocol.test.ts:

    Read the existing test structure. Add a parallel describe block (or parameterize the existing one) for AGENT-trimmed view:
    - AGENT test: spawn server WITHOUT 'OMNIFOCUS_MCP_ROLE=owner' in the env (agent is the fail-safe default). Send tools/list. Assert omnifocus_write operation enum does NOT contain 'delete'. Assert 'create' IS in the enum. Assert 4 tools still present.
    - Update or annotate existing OWNER test: spawn with OMNIFOCUS_MCP_ROLE=owner. Assert 'delete' IS in the operation enum. This confirms OWNER sees the full surface.

    The existing test asserts all operations are visible — update that assertion to be scoped to the OWNER spawn, not applied universally. Do not break the existing OWNER assertion; restructure it so the OWNER spawn confirms the full surface, and add the AGENT spawn confirming the trimmed surface.

  </action>

  <verify>
    <automated>npm run test:unit -- tests/unit/tools/index-rolegate.test.ts && npm run build</automated>
  </verify>

<acceptance_criteria> - tests/unit/tools/index-rolegate.test.ts has no vi.todo() stubs remaining for READ-01/02/03 —
grep -c "vi.todo" tests/unit/tools/index-rolegate.test.ts returns 0 or only non-READ stubs - READ-01 test: response does
not contain POLICY_DENY_DELETE when AGENT calls omnifocus_read with countOnly query - READ-02 test: response does not
contain POLICY_DENY_DELETE when AGENT calls omnifocus_read with filters.id - READ-03 test: response does not contain
POLICY_DENY_DELETE when AGENT calls omnifocus_read type=perspectives - tests/integration/mcp-protocol.test.ts contains
"OMNIFOCUS_MCP_ROLE" — grep returns match (env-based role control added) - npm run build exits 0 - npm run test:unit --
tests/unit/tools/index-rolegate.test.ts exits 0 </acceptance_criteria>

  <done>
    READ-01/02/03 unit tests confirm dispatch gate is a no-op for read ops. Integration test extended with AGENT-trimmed and OWNER-full ListTools surface assertions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — AGENT write hot-path and read surface end-to-end over stdio</name>

  <what-built>
    Phases 01-03 (Plans 01-04) have shipped:
    - Role-aware ListTools: AGENT sees trimmed operation enum; OWNER sees full surface
    - Pre-dispatch CallTool gate: denied ops return POLICY_DENY_DELETE/POLICY_GATE_REQUIRES_OWNER structured payloads before tool.execute()
    - system whoami op: AGENT gets { role, roleSource }; OWNER gets { role, identity: { transport, roleSource, principal: null } }
    - AGENT read dispatch confirmed pass-through (no policy gate fires for read ops)
    - Both registerTools call sites (index.ts + session-manager.ts) pass role and context
  </what-built>

  <how-to-verify>
    Prerequisites: OmniFocus is running. npm run build has been run.

    Step 1 — Start the server as AGENT:
    node dist/index.js
    (No OMNIFOCUS_MCP_ROLE=owner set — fail-safe defaults to agent)

    Step 2 — In a separate terminal, confirm whoami returns AGENT payload with no identity key:
    echo '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"system","arguments":{"operation":"whoami"}}}' | node dist/index.js
    Expected: response text JSON contains { role: "agent", roleSource: "fail-safe-default" } and does NOT contain "identity"

    Step 3 — Confirm ListTools AGENT omits 'delete':
    echo '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}' | node dist/index.js
    Expected: omnifocus_write tool inputSchema.properties.mutation.properties.operation.enum does not include "delete" or "bulk_delete"; includes "create", "complete", "drop"

    Step 4 — Confirm AGENT delete is rejected at dispatch (not by OmniFocus):
    echo '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"omnifocus_write","arguments":{"mutation":{"operation":"delete","target":"task","id":"fake-id"}}}}' | node dist/index.js
    Expected: response content text JSON-parses to { error: { code: "POLICY_DENY_DELETE" } } — NOT InternalError

    Step 5 — Confirm AGENT write hot-path works end-to-end (create a test task):
    echo '{"jsonrpc":"2.0","id":3,"method":"tools/call","params":{"name":"omnifocus_write","arguments":{"mutation":{"operation":"create","target":"task","data":{"name":"[GSD Phase 3 smoke test] Delete me","flagged":true}}}}}' | node dist/index.js
    Expected: success response; confirm task appears in OmniFocus inbox

    Step 6 — Confirm AGENT read works end-to-end (inbox query):
    echo '{"jsonrpc":"2.0","id":4,"method":"tools/call","params":{"name":"omnifocus_read","arguments":{"query":{"type":"tasks","filters":{"status":"available"},"countOnly":true}}}}' | node dist/index.js
    Expected: success response with metadata.total_count value (numeric)

    Step 7 — Confirm AGENT perspectives read works:
    echo '{"jsonrpc":"2.0","id":5,"method":"tools/call","params":{"name":"omnifocus_read","arguments":{"query":{"type":"perspectives"}}}}' | node dist/index.js
    Expected: success response with a perspectives array (may be empty if none configured)

    Step 8 — Clean up: delete the smoke-test task from OmniFocus manually (or mark it complete).

  </how-to-verify>

<resume-signal>Type "approved" if all 7 steps produced expected results. Or describe any step that failed with the
actual response so implementation can be diagnosed.</resume-signal> </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                  |
| --------------------------------- | -------------------------------------------------------------------------------------------- |
| Integration test → spawned server | Test controls OMNIFOCUS_MCP_ROLE env; server resolves role from that env at startup          |
| Human verify steps                | Live OmniFocus is the execution environment; smoke-test task must be cleaned up after verify |

## STRIDE Threat Register

| Threat ID            | Category               | Component                                                    | Disposition | Mitigation Plan                                                                                                                                        |
| -------------------- | ---------------------- | ------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-3-read-passthrough | Elevation of Privilege | Read ops could be mistakenly blocked by gate                 | mitigate    | READ-01/02/03 unit tests assert dispatch gate returns no policy denial for query args; normalizeArgsToPolicy({query:...}) returns [] (no policy items) |
| T-3-integ-regression | Elevation of Privilege | Integration test updated in ways that hide OWNER enforcement | mitigate    | OWNER integration test spawn still asserts 'delete' present in enum; AGENT spawn asserts 'delete' absent; both assertions required to pass             |

</threat_model>

<verification>
After both tasks complete:
- npm run build exits 0
- npm run test:unit exits 0 (full suite including READ-01/02/03 activations)
- npm run test:integration exits 0 (role-aware ListTools assertions pass for both AGENT and OWNER)
- Human checkpoint approved: AGENT smoke test confirmed end-to-end
</verification>

<success_criteria>

- READ-01/02/03: unit tests confirm dispatch gate is a no-op for read ops
- Integration test: both AGENT-trimmed and OWNER-full ListTools assertions pass
- Human verify: AGENT create, read, whoami confirmed end-to-end in live OmniFocus
- All six phase requirements (GATE-01, GATE-02, GATE-03, READ-01, READ-02, READ-03) have at least one passing automated
  test
- npm run test:unit and npm run test:integration both pass </success_criteria>

<output>
Create `.planning/phases/03-rolegate-agent-read-paths/03-04-SUMMARY.md` when done.
</output>
