---
phase: 03-rolegate-agent-read-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/auth/operation-policy.ts
  - tests/unit/auth/operation-policy.test.ts
  - tests/unit/tools/index-rolegate.test.ts
  - tests/unit/tools/system/SystemTool-whoami.test.ts
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
  - READ-01
  - READ-02
  - READ-03

must_haves:
  truths:
    - "allowedOperations('agent') returns all non-deny ops including 'gate' ops (D-05)"
    - "allowedOperations('owner') returns every key in AGENT_POLICY"
    - 'normalizeArgsToPolicy returns [] for read/system args (no mutation field)'
    - 'normalizeArgsToPolicy returns correct (operation, target) pairs for batch, bulk_delete, tag_manage, and simple
      mutations'
    - 'Parity test asserts every AGENT-advertised op is non-deny and every non-deny op is advertised'
    - "All Wave 0 test stubs exist and fail with clear 'not implemented' failures (not import errors)"
  artifacts:
    - path: 'src/auth/operation-policy.ts'
      provides: 'allowedOperations(role) export + PolicyItem type + normalizeArgsToPolicy export'
      contains: 'export function allowedOperations'
    - path: 'tests/unit/auth/operation-policy.test.ts'
      provides: 'D-06 parity test block added to existing test'
      contains: 'advertise'
    - path: 'tests/unit/tools/index-rolegate.test.ts'
      provides: 'Wave 0 stubs for GATE-01/02 and READ-01/02/03 tests'
    - path: 'tests/unit/tools/system/SystemTool-whoami.test.ts'
      provides: 'Wave 0 stubs for whoami AGENT/OWNER redaction and dual-schema tests'
  key_links:
    - from: 'src/tools/index.ts (Wave 2)'
      to: 'src/auth/operation-policy.ts'
      via: 'import { allowedOperations, normalizeArgsToPolicy }'
      pattern: 'allowedOperations|normalizeArgsToPolicy'
    - from: 'tests/unit/auth/operation-policy.test.ts'
      to: 'src/auth/operation-policy.ts'
      via: 'import { allowedOperations } for parity test'
      pattern: 'advertise.*enforce parity|D-06'
---

<objective>
Policy foundation for the RoleGate layer: add the `allowedOperations(role)` capability enumerator and the `normalizeArgsToPolicy` shared normalization helper to `src/auth/operation-policy.ts`, then create Wave 0 test stubs for all new test files and extend the existing parity test.

Purpose: Everything in Waves 2 and 3 depends on these exports. The enumerator drives ListTools advertisement (GATE-01).
The normalization helper makes the CallTool dispatch gate feed identical (operation, target) pairs into `decide()` as
the Phase 2 funnel (D-11 — OMN-119 mitigation at the dispatch layer). The parity test (D-06) is the structural defense
against advertise⟺enforce drift.

Output:

- `allowedOperations(role)` and `normalizeArgsToPolicy` exported from `operation-policy.ts`
- `PolicyItem` type exported
- D-06 parity test block added to existing operation-policy test
- Two new Wave 0 test stub files (failing tests, no import errors) </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-rolegate-agent-read-paths/03-CONTEXT.md
@.planning/phases/03-rolegate-agent-read-paths/03-RESEARCH.md
@.planning/phases/03-rolegate-agent-read-paths/03-PATTERNS.md

# Prior phase context (policy table is the source of truth)

@.planning/phases/02-operation-policy-deny-deletes-gating/02-02-SUMMARY.md
@.planning/phases/02-operation-policy-deny-deletes-gating/02-03-SUMMARY.md </context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/contracts/roles.ts: export type Role = 'owner' | 'agent'; export type PolicyOutcome = 'allow' | 'deny' |
'gate'; export type RoleSource = 'explicit-env' | 'fail-safe-default' | 'http-token'; export interface ResolvedIdentity
{ transport: 'stdio' | 'http'; roleSource: RoleSource; principal: string | null; } export interface ResolvedContext {
identity: ResolvedIdentity; role: Role; }

From src/auth/operation-policy.ts (existing): import type { PolicyOutcome, Role } from '../contracts/roles.js'; export
function decide(role: Role, operation: string, target?: string): PolicyOutcome; // AGENT_POLICY table keys: delete,
bulk_delete, complete, drop, create, update, batch, create_folder // tag_manage subtable: { delete: 'gate', merge:
'gate', perspective_delete: 'gate', create: 'allow', rename: 'allow', nest: 'allow', unnest: 'allow', reparent: 'allow'
} </interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add allowedOperations enumerator and normalizeArgsToPolicy helper to operation-policy.ts</name>
  <files>src/auth/operation-policy.ts</files>

<read_first> 1. src/auth/operation-policy.ts — read the full file; note exact AGENT_POLICY shape (keys and subtable),
existing imports, and decide() signature. The new functions go AFTER decide(). 2.
.planning/phases/03-rolegate-agent-read-paths/03-PATTERNS.md §"allowedOperations(role) enumerator" and §"Shared
normalization helper" — the exact code patterns to follow. 3.
.planning/phases/03-rolegate-agent-read-paths/03-CONTEXT.md §D-03, D-04, D-05, D-11 — locked decisions governing these
functions. </read_first>

  <behavior>
    - allowedOperations('agent'): includes all ops where AGENT_POLICY entry !== 'deny', including 'gate' ops (D-05 — gated ops are advertised-but-guarded). tag_manage itself is included; tagManageActions includes 'gate' AND 'allow' entries.
    - allowedOperations('owner'): returns ALL keys in AGENT_POLICY as operations, ALL keys in the tag_manage subtable as tagManageActions.
    - allowedOperations('agent').operations does NOT contain 'delete' or 'bulk_delete'.
    - allowedOperations('agent').tagManageActions contains 'delete', 'merge', 'perspective_delete' (gate), 'create', 'rename', 'nest', 'unnest', 'reparent' (allow) — all non-deny; since all tag_manage subtable entries are gate or allow, all are included.
    - normalizeArgsToPolicy({}) returns [].
    - normalizeArgsToPolicy({ query: {} }) returns [] (no mutation field).
    - normalizeArgsToPolicy({ mutation: { operation: 'create', target: 'task' } }) returns [{ operation: 'create', target: 'task' }].
    - normalizeArgsToPolicy({ mutation: { operation: 'delete', target: 'task' } }) returns [{ operation: 'delete', target: 'task' }].
    - normalizeArgsToPolicy({ mutation: { operation: 'bulk_delete', target: 'task' } }) returns [{ operation: 'bulk_delete', target: 'task' }].
    - normalizeArgsToPolicy({ mutation: { operation: 'tag_manage', action: 'delete' } }) returns [{ operation: 'tag_manage', target: 'delete' }].
    - normalizeArgsToPolicy({ mutation: { operation: 'tag_manage', action: 'merge' } }) returns [{ operation: 'tag_manage', target: 'merge' }].
    - normalizeArgsToPolicy({ mutation: { operation: 'batch', operations: [{ operation: 'create', target: 'task' }, { operation: 'delete', target: 'task' }] } }) returns [{ operation: 'create', target: 'task' }, { operation: 'delete', target: 'task' }].
    - normalizeArgsToPolicy with missing target defaults to 'task'.
    - PolicyItem is exported as a named type.
  </behavior>

  <action>
    Read src/auth/operation-policy.ts in full before writing. Add the following exports AFTER the existing decide() function:

    1. Export type PolicyItem = { operation: string; target: string }. Place this type before the new functions.

    2. Export function allowedOperations(role: Role): { operations: string[]; tagManageActions: string[] }. Implementation follows D-03/D-04: forward read over AGENT_POLICY using Object.entries(). For 'owner': return Object.keys(AGENT_POLICY) and Object.keys(tag_manage subtable). For 'agent': iterate entries; for string entries push if !== 'deny'; for object entries (tag_manage subtable) push 'tag_manage' unconditionally (D-05) and push each sub-key if !== 'deny'. No role === 'owner' special-case needed for tag_manage subtable iteration — the OWNER branch short-circuits.

    3. Export function normalizeArgsToPolicy(args: Record<string, unknown>): PolicyItem[]. Operates on raw MCP args (pre-Zod-compile) so the dispatch gate can call it without importing MutationCompiler. Extract args['mutation'] as Record<string,unknown>|undefined; return [] if absent. Extract op = mutation['operation']. Handle four cases: 'batch' maps mutation['operations'] array; 'bulk_delete' uses mutation['target'] ?? 'task'; 'tag_manage' uses mutation['action'] ?? '' as target; default uses mutation['target'] ?? 'task'.

    The AGENT_POLICY const need not be exported; the parity test relies on allowedOperations() and decide() only. Do not export AGENT_POLICY unless needed for testing — keep the table internal per existing pattern.

    No new imports needed (Role and PolicyOutcome already imported at line 16).

  </action>

  <verify>
    <automated>npm run test:unit -- tests/unit/auth/operation-policy.test.ts</automated>
  </verify>

<acceptance_criteria> - src/auth/operation-policy.ts contains the string "export function allowedOperations" -
src/auth/operation-policy.ts contains the string "export function normalizeArgsToPolicy" - src/auth/operation-policy.ts
contains the string "export type PolicyItem" - allowedOperations('agent').operations does not include 'delete' —
verified by grep: grep -c "'delete'" is only in the subtable handling, not in the output for agent -
allowedOperations('agent').operations includes 'tag_manage' (D-05 gated-but-advertised) -
allowedOperations('owner').operations includes 'delete' and 'bulk_delete' - normalizeArgsToPolicy({}) returns [] — unit
test asserts this - npm run test:unit -- tests/unit/auth/operation-policy.test.ts exits 0 - npm run build exits 0
(TypeScript clean) </acceptance_criteria>

  <done>
    allowedOperations(role) and normalizeArgsToPolicy are exported from src/auth/operation-policy.ts. PolicyItem type is exported. npm run build passes. Existing operation-policy tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend operation-policy.test.ts with D-06 parity test + create Wave 0 test stub files</name>
  <files>
    tests/unit/auth/operation-policy.test.ts
    tests/unit/tools/index-rolegate.test.ts
    tests/unit/tools/system/SystemTool-whoami.test.ts
  </files>

<read_first> 1. tests/unit/auth/operation-policy.test.ts — read the full file. Note import block, existing describe
structure, and test helpers. The new D-06 parity block appends to this file. 2.
tests/unit/tools/write-tool-policy-guard.test.ts — the analog for new test stubs. Read the mock cache pattern
(createMockCache), role env-var control (beforeEach/afterEach OMNIFOCUS_MCP_ROLE), and error-code assertion pattern.
Copy the mock shape. 3. .planning/phases/03-rolegate-agent-read-paths/03-PATTERNS.md §Tests — exact parity test block,
AGENT/OWNER assertion patterns, and the ListTools trim and CallTool gate assertion shapes. 4.
.planning/phases/03-rolegate-agent-read-paths/03-VALIDATION.md — the Wave 0 requirements list; every item in it must
become a describe/it stub here. </read_first>

  <action>
    Part A — Extend tests/unit/auth/operation-policy.test.ts:
    Add import for allowedOperations from '../../../src/auth/operation-policy.js'. Add a new describe block 'advertise-enforce parity (D-06)' after the existing decide() matrix tests. Include three it() blocks per the PATTERNS.md parity test shape:
    (1) 'every AGENT-advertised op resolves to decide() !== deny' — iterate allowedOperations('agent').operations, skip 'tag_manage', call decide('agent', op, 'task'); iterate allowedOperations('agent').tagManageActions, call decide('agent', 'tag_manage', action). Each must not be 'deny'.
    (2) 'every non-denied AGENT op is advertised' — verify allowedOperations('agent').operations contains all ops from the policy that are non-deny. Do NOT access AGENT_POLICY directly (not exported); use decide() to cross-check known ops by name. The known non-deny ops are: complete, drop, create, update, batch, create_folder, tag_manage.
    (3) 'OWNER allowedOperations returns all ops including delete and bulk_delete' — allowedOperations('owner').operations.includes('delete') and .includes('bulk_delete').

    Part B — Create tests/unit/tools/index-rolegate.test.ts:
    Stub file that imports from '../../src/tools/index.js' and '../../src/auth/operation-policy.js'. Add TODO-placeholder describes (vi.todo or describe.skip with clear labels) for:
    - GATE-01: 'ListTools AGENT — operation enum trimmed: delete and bulk_delete absent, create present'
    - GATE-01: 'ListTools OWNER — operation enum contains full surface including delete and bulk_delete'
    - GATE-02: 'CallTool AGENT delete → POLICY_DENY_DELETE at dispatch (not InternalError)'
    - GATE-02: 'CallTool AGENT bulk_delete → POLICY_DENY_DELETE at dispatch'
    - GATE-02: 'CallTool AGENT tag_manage/merge → POLICY_GATE_REQUIRES_OWNER at dispatch'
    - GATE-02: 'CallTool OWNER all ops pass dispatch (no pre-dispatch rejection)'
    - READ-01: 'CallTool AGENT omnifocus_read (today/overdue/flagged) never fires policy gate'
    - READ-02: 'CallTool AGENT omnifocus_read with filters.id succeeds'
    - READ-03: 'CallTool AGENT omnifocus_read type=perspectives succeeds'
    Use vi.todo() for each test body; describe blocks should exist with the exact strings above.

    Part C — Create tests/unit/tools/system/SystemTool-whoami.test.ts:
    Stub file that imports from '../../../../src/tools/system/SystemTool.js'. Add vi.todo() stubs for:
    - 'whoami AGENT: returns role and roleSource, omits identity field entirely (D-15)'
    - 'whoami OWNER: returns role and identity{transport, roleSource, principal}, principal is null'
    - 'dual-schema: SystemToolSchema Zod enum and inputSchema getter both enumerate whoami (D-15)'
    The mock cache pattern from write-tool-policy-guard.test.ts should be copied into the file (createMockCache function with all CacheManager methods as vi.fn()) so Wave 3 implementors can activate the stubs without restructuring.

    Important: vi.todo() marks are acceptable stub form; they register as pending tests, not failures. Do not use failing assertions in stubs — Wave 0 stubs must not cause test:unit to fail red.

  </action>

  <verify>
    <automated>npm run test:unit -- tests/unit/auth/operation-policy.test.ts && npm run test:unit -- tests/unit/tools/index-rolegate.test.ts && npm run test:unit -- tests/unit/tools/system/SystemTool-whoami.test.ts</automated>
  </verify>

<acceptance_criteria> - tests/unit/auth/operation-policy.test.ts contains the string "advertise" — grep -c "advertise"
tests/unit/auth/operation-policy.test.ts returns >= 1 - tests/unit/tools/index-rolegate.test.ts exists and imports from
'../../src/tools/index.js' - tests/unit/tools/system/SystemTool-whoami.test.ts exists and imports from
'../../../../src/tools/system/SystemTool.js' - npm run test:unit -- tests/unit/auth/operation-policy.test.ts exits 0
(all three new parity tests pass) - npm run test:unit -- tests/unit/tools/index-rolegate.test.ts exits 0 (stubs pending,
not failures) - npm run test:unit -- tests/unit/tools/system/SystemTool-whoami.test.ts exits 0 (stubs pending, not
failures) - npm run build exits 0 </acceptance_criteria>

  <done>
    D-06 parity tests pass. Wave 0 test stubs exist for all GATE-01/02/READ-01/02/03 and whoami behaviors. npm run test:unit passes for all three test files.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                                      |
| -------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| AGENT_POLICY → allowedOperations | The enumerator must read the table forward (D-04); inverse logic could enumerate deny ops as allowed             |
| normalizeArgsToPolicy → decide() | Normalization must produce the same (op, target) pairs the funnel produces; mismatch is the OMN-119 class of bug |

## STRIDE Threat Register

| Threat ID         | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                                                                                   |
| ----------------- | ---------------------- | ------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-3-drift-enum    | Elevation of Privilege | allowedOperations()                         | mitigate    | D-04: forward read over AGENT_POLICY only; never inverse of decide(). D-06 parity test asserts no advertised op is deny.                                                          |
| T-3-norm-mismatch | Elevation of Privilege | normalizeArgsToPolicy()                     | mitigate    | D-11: shared helper at raw-args level; dispatch gate and funnel call decide() with identical (op, target). Parity test validates same inputs produce same decide() outcomes.      |
| T-3-gate-deny-op  | Elevation of Privilege | allowedOperations('agent').tagManageActions | mitigate    | D-05: include 'gate' outcomes in advertised set (gated ops visible so agent can attempt and receive dry-run). Test asserts 'delete' (gate) present in tagManageActions for agent. |

</threat_model>

<verification>
After both tasks complete:
- npm run test:unit passes (all three targeted test files green or pending)
- npm run build exits 0
- grep for "export function allowedOperations" in src/auth/operation-policy.ts returns a result
- grep for "export function normalizeArgsToPolicy" in src/auth/operation-policy.ts returns a result
</verification>

<success_criteria>

- allowedOperations(role) exported from src/auth/operation-policy.ts with correct allow+gate inclusion per D-05
- normalizeArgsToPolicy exported, returns [] for non-mutation args, correct PolicyItem[] for all mutation shapes
- D-06 parity test block passing in operation-policy.test.ts
- Wave 0 stub files exist for index-rolegate.test.ts and SystemTool-whoami.test.ts with all required describe/it stubs
- npm run test:unit and npm run build both pass </success_criteria>

<output>
Create `.planning/phases/03-rolegate-agent-read-paths/03-01-SUMMARY.md` when done.
</output>
