---
phase: 03-rolegate-agent-read-paths
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/auth/operation-policy.ts
  - src/tools/index.ts
  - src/index.ts
  - src/session-manager.ts
  - src/tools/unified/OmniFocusWriteTool.ts
  - tests/unit/tools/index-rolegate.test.ts
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03

must_haves:
  truths:
    - 'registerTools(server, cache, pendingOperations, role, context) compiles with no TypeScript errors'
    - "AGENT ListTools response omits 'delete' and 'bulk_delete' from the omnifocus_write operation enum"
    - "OWNER ListTools response includes 'delete' and 'bulk_delete' in the enum"
    - 'AGENT CallTool delete → POLICY_DENY_DELETE structured payload (not McpError InternalError)'
    - 'AGENT CallTool bulk_delete → POLICY_DENY_DELETE structured payload'
    - 'AGENT CallTool tag_manage/merge → POLICY_GATE_REQUIRES_OWNER structured payload'
    - 'OWNER CallTool delete passes through dispatch gate (hits Phase 2 funnel, no dispatch rejection)'
    - 'src/index.ts passes role and context into registerTools'
    - 'src/session-manager.ts passes role into registerTools (Phase 4 seam wired)'
    - 'OmniFocusWriteTool.executeValidated uses normalizeArgsToPolicy (shared helper) instead of inline block'
  artifacts:
    - path: 'src/tools/index.ts'
      provides: 'registerTools with role+context params, ListTools trim, CallTool pre-dispatch gate'
      contains: 'allowedOperations'
    - path: 'src/index.ts'
      provides: 'passes role and context into registerTools'
      contains: 'registerTools(stdioServer, cacheManager, pendingOperations, role'
    - path: 'src/session-manager.ts'
      provides: 'Phase 4 seam — passes role into registerTools'
      contains: 'registerTools(server, this.cacheManager, this.pendingOperations, this.role'
    - path: 'src/tools/unified/OmniFocusWriteTool.ts'
      provides: 'Inline normalization block replaced with normalizeArgsToPolicy call'
      contains: 'normalizeArgsToPolicy'
    - path: 'tests/unit/tools/index-rolegate.test.ts'
      provides: 'Activated GATE-01/02 tests replacing Wave 0 stubs'
  key_links:
    - from: 'src/tools/index.ts'
      to: 'src/auth/operation-policy.ts'
      via: 'import { allowedOperations, normalizeArgsToPolicy, decide }'
      pattern: 'allowedOperations|normalizeArgsToPolicy'
    - from: 'src/tools/index.ts'
      to: 'src/utils/response-format.ts'
      via: 'import { createErrorResponseV2 }'
      pattern: 'createErrorResponseV2'
    - from: 'CallTool handler'
      to: 'decide()'
      via: 'normalizeArgsToPolicy(args) → decide(role, item.operation, item.target)'
      pattern: "decide\\(role"
---

<objective>
Wire role-aware dispatch into registerTools: extend the signature with role and context parameters, trim ListTools advertisement by role (GATE-01), add the pre-dispatch CallTool gate (GATE-02), update both call sites in src/index.ts and src/session-manager.ts (D-10), and replace the inline normalization block in OmniFocusWriteTool with the shared normalizeArgsToPolicy helper (D-11). Activate the Wave 0 GATE-01/02 test stubs.

Purpose: This plan delivers the core RoleGate mechanism — a complete least-privilege stdio agent that (1) only
advertises what it may do and (2) rejects denied ops at the dispatch point even if they were somehow sent despite never
being advertised.

Output: registerTools with role+context threading, role-aware ListTools, pre-dispatch gate in CallTool,
OmniFocusWriteTool using shared normalization, both call sites updated, GATE-01/02 tests passing. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rolegate-agent-read-paths/03-CONTEXT.md
@.planning/phases/03-rolegate-agent-read-paths/03-RESEARCH.md
@.planning/phases/03-rolegate-agent-read-paths/03-PATTERNS.md
@.planning/phases/03-rolegate-agent-read-paths/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/contracts/roles.ts: export type Role = 'owner' | 'agent'; export interface ResolvedContext { identity:
ResolvedIdentity; role: Role; } export interface ResolvedIdentity { transport: 'stdio' | 'http'; roleSource: RoleSource;
principal: string | null; }

From src/auth/operation-policy.ts (after Plan 01): export type PolicyItem = { operation: string; target: string };
export function decide(role: Role, operation: string, target?: string): PolicyOutcome; export function
allowedOperations(role: Role): { operations: string[]; tagManageActions: string[] }; export function
normalizeArgsToPolicy(args: Record<string, unknown>): PolicyItem[];

From src/utils/response-format.ts: export function createErrorResponseV2<T = unknown>( operation: string, errorCode:
string, message: string, suggestion?: string, details?: unknown, metadata?: Partial<StandardMetadataV2> ):
StandardResponseV2<T>;

From src/tools/index.ts (current): export function registerTools(server: Server, cache: CacheManager,
pendingOperations?: Set<Promise<unknown>>): void; // ListTools handler at lines 50–69: tools.map(t => ({ name,
description, inputSchema })) // CallTool handler: tool = tools.find(t => t.name === name); then executionPromise //
McpError is thrown from the tool-not-found guard at line 89

From src/index.ts (current, lines 143-146 and 182): const identity = cliConfig.httpMode ? resolveHttpIdentity() :
resolveStdioIdentity(); const role = parseRole();
logger.info(`resolved role=${role.toUpperCase()} source=${identity.roleSource}`); await registerTools(stdioServer,
cacheManager, pendingOperations); // line 182 — add role, context

From src/session-manager.ts (current, line 115): await registerTools(server, this.cacheManager, this.pendingOperations);
// add this.role, this.context

From src/tools/unified/OmniFocusWriteTool.ts (Phase 2 funnel): // Inline normalization block at lines 344-367 — replace
with normalizeArgsToPolicy(args) // createErrorResponseV2 call shape at lines 381-392 — reuse for dispatch gate //
inputSchema getter has operation enum at line 243 </interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend registerTools signature + ListTools role-aware advertisement + OmniFocusWriteTool inputSchema trim</name>
  <files>
    src/tools/index.ts
    src/tools/unified/OmniFocusWriteTool.ts
  </files>

<read_first> 1. src/tools/index.ts — read the full file. Note: lines 50-69 (ListTools handler), line 89 (tool not found
guard), line 103 (executionPromise start). The signature is at line 36. 2. src/tools/unified/OmniFocusWriteTool.ts —
read lines 200-310 (inputSchema getter, especially the operation enum at line 243 and any tag_manage action enum). Also
read lines 340-370 (the inline normalization block to replace per D-11). 3.
.planning/phases/03-rolegate-agent-read-paths/03-PATTERNS.md §registerTools signature extension, §ListTools handler
pattern, §OmniFocusWriteTool role-aware inputSchema — read the exact target patterns. 4.
.planning/phases/03-rolegate-agent-read-paths/03-CONTEXT.md — D-01, D-02, D-05, D-10, D-11 for the exact constraints on
what goes in these files. </read_first>

  <action>
    Changes to src/tools/index.ts:

    1. Add imports at the top of the existing import block:
       - import type { Role, ResolvedContext } from '../contracts/roles.js';
       - import { decide, allowedOperations, normalizeArgsToPolicy } from '../auth/operation-policy.js';
       - import { createErrorResponseV2 } from '../utils/response-format.js';

    2. Extend the registerTools signature (per D-10):
       registerTools(server, cache, pendingOperations?, role: Role = 'agent', context?: ResolvedContext)
       The default 'agent' is the fail-safe; both call sites pass explicit values after this plan.

    3. In the ListTools handler: call allowedOperations(role) once at the top of the handler body. Then in the tools.map(), for each tool check if t has a getRoleAwareSchema method; if so call t.getRoleAwareSchema(role, operations, tagManageActions), otherwise use t.inputSchema. Similarly for description. Build new schema objects per request — never mutate t.inputSchema in place (Pitfall 5 from RESEARCH.md).

    4. Pass context into the SystemTool constructor call when building the tools array (line 46 currently constructs new SystemTool(cache); change to new SystemTool(cache, context)).

    Changes to src/tools/unified/OmniFocusWriteTool.ts:

    5. Add getRoleAwareSchema(role: Role, allowedOps: string[], allowedTagActions: string[]): Record<string, unknown> method. It deep-copies (using spread) the base inputSchema and trims the mutation.properties.operation.enum to allowedOps, and the mutation.properties.action.enum (if it exists) to allowedTagActions. Returns a new object — do not mutate this.inputSchema.

    6. Import { Role } from '../../contracts/roles.js' if not already imported.

    7. Import { normalizeArgsToPolicy } from '../../auth/operation-policy.js'.

    8. Replace the inline normalization block at lines 344-367 with: const policyItems = normalizeArgsToPolicy(args). The rest of the funnel (the decide() loop) stays unchanged. CRITICAL (OMN-119 drift guard): before replacing, confirm the exact shape of `args` reaching executeValidated — normalizeArgsToPolicy reads args.mutation, so it ONLY works if executeValidated still receives the { mutation: {...} } wrapper. If executeValidated instead receives a flattened compiled mutation (no `mutation` key), normalizeArgsToPolicy returns [] and the funnel policy check is silently dropped. If the shape is flattened, do NOT replace with normalizeArgsToPolicy(args); instead keep the funnel on its CompiledMutation path (RESEARCH.md Open Question 1 recommendation) and rely on the D-06 parity test to pin gate/funnel equivalence. The write-tool-policy-guard.test.ts run in <verify> is the safety net: an AGENT delete MUST still resolve to POLICY_DENY_DELETE at the funnel after this change.

    Dual-schema invariant: the Zod schema on OmniFocusWriteTool does NOT change (server-side validation stays role-agnostic per D-02). Only the advertised inputSchema emitted by getRoleAwareSchema is trimmed. The base inputSchema getter is unchanged.

  </action>

  <verify>
    <automated>npm run build && npm run test:unit -- tests/unit/tools/write-tool-policy-guard.test.ts</automated>
  </verify>

<acceptance_criteria> - grep -c "allowedOperations" src/tools/index.ts returns >= 1 - grep -c "getRoleAwareSchema"
src/tools/unified/OmniFocusWriteTool.ts returns >= 1 - grep -c "normalizeArgsToPolicy"
src/tools/unified/OmniFocusWriteTool.ts returns >= 1 - npm run build exits 0 (TypeScript clean — no unused param
warnings) - npm run test:unit -- tests/unit/tools/write-tool-policy-guard.test.ts exits 0 (Phase 2 funnel tests still
pass after normalization extraction) - AGENT delete still denied at the funnel: npm run test:unit --
tests/unit/tools/write-tool-policy-guard.test.ts asserts a POLICY_DENY_DELETE outcome (the normalization extraction did
NOT silently drop the funnel policy check) </acceptance_criteria>

  <done>
    registerTools signature extended. ListTools handler closes over role and emits role-aware schemas via getRoleAwareSchema. SystemTool receives context. OmniFocusWriteTool uses normalizeArgsToPolicy. Build clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CallTool pre-dispatch gate + role plumbing call sites + activate GATE-01/02 tests</name>
  <files>
    src/tools/index.ts
    src/index.ts
    src/session-manager.ts
    tests/unit/tools/index-rolegate.test.ts
  </files>

<read_first> 1. src/tools/index.ts — the current file after Task 1. Find the CallTool handler; locate the tool-not-found
guard (the McpError throw) and the executionPromise creation. The gate goes between these two. 2. src/index.ts — read
lines 140-185. The identity and role are resolved at lines 143-146; registerTools is called at line 182. Add context
construction and pass role+context. 3. src/session-manager.ts — read lines 100-120. The registerTools call is at
line 115. The SessionManager likely has a parseRole() call during construction; capture that as this.role. Add
this.context population. 4. .planning/phases/03-rolegate-agent-read-paths/03-PATTERNS.md §CallTool gate pattern — exact
gate code shape including MCP content envelope wrapping (Pitfall 2). 5.
.planning/phases/03-rolegate-agent-read-paths/03-PATTERNS.md §Tests — index-rolegate.test.ts activation patterns for
ListTools trim and CallTool gate assertions. 6. tests/unit/tools/write-tool-policy-guard.test.ts — mock cache and role
env-var patterns to copy for the new dispatch-level tests. </read_first>

  <behavior>
    AGENT callTool delete:
    - normalizeArgsToPolicy({ mutation: { operation: 'delete', target: 'task' } }) → [{ operation: 'delete', target: 'task' }]
    - decide('agent', 'delete', 'task') → 'deny'
    - gate returns content: [{ type: 'text', text: JSON.stringify(createErrorResponseV2(..., 'POLICY_DENY_DELETE', ...)) }]
    - tool.execute() is never called (the gate short-circuits before executionPromise)

    AGENT callTool tag_manage/merge:
    - normalizeArgsToPolicy({ mutation: { operation: 'tag_manage', action: 'merge' } }) → [{ operation: 'tag_manage', target: 'merge' }]
    - decide('agent', 'tag_manage', 'merge') → 'gate'
    - gate returns POLICY_GATE_REQUIRES_OWNER structured payload in content envelope

    OWNER callTool delete:
    - normalizeArgsToPolicy returns [{ operation: 'delete', target: 'task' }]
    - decide('owner', 'delete', 'task') → 'allow'
    - gate is a no-op; tool.execute() proceeds (Phase 2 funnel handles owner path)

    AGENT omnifocus_read callTool:
    - normalizeArgsToPolicy({ query: { type: 'tasks' } }) → [] (no mutation field)
    - gate loop over [] is a no-op; read proceeds

    Response shape for denied calls:
    - outer object has 'content' key with array of objects with 'type' == 'text' and 'text' key
    - text JSON-parses to StandardResponseV2 with 'error.code' == 'POLICY_DENY_DELETE' or 'POLICY_GATE_REQUIRES_OWNER'
    - NOT McpError; NOT { error: { code: InternalError } }

  </behavior>

  <action>
    Changes to src/tools/index.ts — add pre-dispatch gate:

    After the tool-not-found McpError throw, BEFORE the executionPromise creation, insert the gate block:
    1. Call normalizeArgsToPolicy(args ?? {}) to get items.
    2. Loop over items. For each item, call decide(role, item.operation, item.target).
    3. If outcome is 'deny': build errorPayload via createErrorResponseV2(name, isDelete ? 'POLICY_DENY_DELETE' : 'POLICY_DENY', message, suggestion, { role, operation: item.operation, target: item.target }). Determine isDelete as item.operation === 'delete' || item.operation === 'bulk_delete'.
    4. If outcome is 'gate': build errorPayload via createErrorResponseV2(name, 'POLICY_GATE_REQUIRES_OWNER', 'This structural operation requires owner approval before execution.', 'Re-run from an owner connection using the ownerCommand below.', { dryRun: true, ownerCommand: { mutation: args ?? {} } }).
    5. Return { content: [{ type: 'text' as const, text: JSON.stringify(errorPayload, null, 2) }] } — note the MCP content envelope wrapping (critical — without this the SDK rejects the response per Pitfall 2 in RESEARCH.md). This is a direct return from the CallTool handler, NOT thrown.
    6. No special-case on tool name (D-08 — universal gate, no if (name === 'omnifocus_write')).

    Changes to src/index.ts:
    After the existing identity+role resolution block (lines 143-146), construct: const context: ResolvedContext = { identity, role };
    Import ResolvedContext from './contracts/roles.js' if not already imported.
    Update the registerTools call at line 182 to: await registerTools(stdioServer, cacheManager, pendingOperations, role, context).

    Changes to src/session-manager.ts:
    Read the full session-manager.ts before editing. The SessionManager class likely calls parseRole() during construction or in a method. Add this.role: Role (default 'agent', fail-safe per D-10) and this.context: ResolvedContext properties. Populate them from parseRole()/resolveStdioIdentity() at the appropriate initialization point (the same pattern as src/index.ts). Update the registerTools call at line 115 to pass this.role and this.context. Import Role, ResolvedContext from '../contracts/roles.js' and parseRole, resolveStdioIdentity from '../auth/role-resolver.js' if not already present. This is the Phase 4 seam; Phase 4 replaces per-session values here. Verify it compiles.

    Activate tests/unit/tools/index-rolegate.test.ts:
    Replace the vi.todo() stubs for GATE-01 and GATE-02 with real test implementations. Use the mock cache pattern from write-tool-policy-guard.test.ts (createMockCache function). The test must exercise registerTools by constructing a real Server instance (or by calling the registered handler directly). Use process.env['OMNIFOCUS_MCP_ROLE'] env-var control for role setup per the existing test analog pattern. Assert:
    - AGENT ListTools: find omnifocus_write tool, get operation.enum from inputSchema.properties.mutation.properties.operation.enum, assert 'delete' absent and 'create' present.
    - AGENT CallTool delete: call handler with { name: 'omnifocus_write', arguments: { mutation: { operation: 'delete', target: 'task' } } }; parse response content[0].text as JSON; assert error.code === 'POLICY_DENY_DELETE'.
    - AGENT CallTool bulk_delete: same pattern, assert POLICY_DENY_DELETE.
    - AGENT CallTool tag_manage/merge: assert POLICY_GATE_REQUIRES_OWNER.
    - OWNER CallTool delete: assert the response is NOT a policy denial (the tool may error for other reasons in test but must not have POLICY_DENY_DELETE).
    - READ-01/02/03 stubs can remain vi.todo() for activation in Plan 04.

  </action>

  <verify>
    <automated>npm run build && npm run test:unit -- tests/unit/tools/index-rolegate.test.ts</automated>
  </verify>

<acceptance_criteria> - grep -c "decide(role" src/tools/index.ts returns >= 1 (gate uses closure-captured role) - grep
-c "parseRole()" src/tools/index.ts returns 0 (gate never re-calls parseRole — D-10) - grep -c
"POLICY_DENY_DELETE\|POLICY_GATE_REQUIRES_OWNER\|POLICY_DENY" src/tools/index.ts returns >= 1 - grep -c
"normalizeArgsToPolicy" src/tools/index.ts returns >= 1 - src/index.ts contains "registerTools(stdioServer,
cacheManager, pendingOperations, role" — grep returns match - src/session-manager.ts contains "this.role" — grep returns
match - npm run build exits 0 - npm run test:unit -- tests/unit/tools/index-rolegate.test.ts exits 0 with GATE-01/02
tests passing - npm run test:unit -- tests/unit/tools/write-tool-policy-guard.test.ts exits 0 (Phase 2 funnel tests
unaffected) - AGENT delete CallTool response: content[0].text JSON-parses to object with error.code ==
'POLICY_DENY_DELETE' (not 'InternalError') </acceptance_criteria>

  <done>
    Pre-dispatch gate active in CallTool. Both registerTools call sites pass role. GATE-01 ListTools trim verified by unit test. GATE-02 dispatch rejection verified by unit test. Build clean.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                        |
| ---------------------------------------- | -------------------------------------------------------------------------------------------------- |
| MCP client → CallTool handler            | Untrusted operation names/args arrive here; gate must run before tool.execute()                    |
| CallTool handler → createErrorResponseV2 | Denial must be returned as structured payload, never thrown (D-09)                                 |
| registerTools closure → role             | Role must be closure-captured from parameter, never re-read from process.env inside handler (D-10) |

## STRIDE Threat Register

| Threat ID      | Category               | Component                                               | Disposition | Mitigation Plan                                                                                                                                    |
| -------------- | ---------------------- | ------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-3-drift      | Elevation of Privilege | ListTools advertisement vs CallTool gate                | mitigate    | D-03: both consume allowedOperations(role) from same AGENT_POLICY table; D-06 parity test enforces no gap                                          |
| T-3-session    | Elevation of Privilege | Role re-read from process.env in handler                | mitigate    | D-10: closure-captured role param; grep gate asserts parseRole() call count is 0 inside index.ts handler body                                      |
| T-3-mangle     | Tampering              | McpError thrown from gate mangles POLICY_DENY payload   | mitigate    | D-09: gate returns structured payload in MCP content envelope; never throws. Test asserts error.code === 'POLICY_DENY_DELETE', not 'InternalError' |
| T-3-noenvelope | Tampering              | Gate return missing MCP content envelope                | mitigate    | Pitfall 2 from RESEARCH.md: wrap in { content: [{ type: 'text', text: JSON.stringify(payload) }] } before return                                   |
| T-3-seam       | Elevation of Privilege | session-manager not updated → HTTP path hardcoded AGENT | mitigate    | Both call sites updated this plan; TypeScript compile verifies signature match                                                                     |

</threat_model>

<verification>
After both tasks complete:
- npm run build exits 0
- npm run test:unit exits 0 (all test files including Phase 2 regression)
- grep confirms no parseRole() call inside registerTools handler body in src/tools/index.ts
- AGENT delete CallTool returns POLICY_DENY_DELETE in content envelope (asserted by index-rolegate.test.ts)
- AGENT ListTools omits 'delete' from operation enum (asserted by index-rolegate.test.ts)
</verification>

<success_criteria>

- registerTools(server, cache, pendingOperations, role, context) — signature extended with defaults
- ListTools handler closes over role, emits role-correct inputSchema for omnifocus_write
- CallTool pre-dispatch gate: normalizeArgsToPolicy → decide(role) → structured return, no McpError throw
- src/index.ts passes role and context into registerTools
- src/session-manager.ts passes this.role and this.context into registerTools (Phase 4 seam)
- OmniFocusWriteTool uses normalizeArgsToPolicy (D-11 shared helper)
- GATE-01 and GATE-02 unit tests pass
- All Phase 2 regression tests still pass </success_criteria>

<output>
Create `.planning/phases/03-rolegate-agent-read-paths/03-02-SUMMARY.md` when done.
</output>
