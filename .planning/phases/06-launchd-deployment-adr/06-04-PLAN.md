---
phase: 06-launchd-deployment-adr
plan: 04
type: execute
wave: 2
depends_on: ['06-01', '06-02', '06-03']
files_modified:
  - deploy/launchd/RUNBOOK.md
autonomous: false
requirements: [DEPLOY-01, DEPLOY-04]
must_haves:
  truths:
    - 'A runbook documents pinning the Developer-ID node, seeding the first-run grant, install, and the brew-upgrade
      re-copy'
    - 'The TCC host spike (S0–S5) is run under launchctl and its results recorded'
    - 'The grant survives an in-place Developer-ID node overwrite with no interactive prompt (spike S4)'
    - 'A permission-denial exit does not restart-loop under KeepAlive=Crashed-only (spike S5)'
    - 'A real end-to-end write create + read-back succeeds under launchctl with no prompt (spike S6)'
    - 'The ADR 001 back-reference stub is added in the JessOS vault (manual follow-up)'
  artifacts:
    - path: 'deploy/launchd/RUNBOOK.md'
      provides: 'Node-pinning + first-grant + install + upgrade runbook and recorded spike results'
      contains: 'of-mcp-node'
      min_lines: 30
  key_links:
    - from: 'deploy/launchd/RUNBOOK.md'
      to: 'Developer-ID node source'
      via: 'documented in-place copy to ~/.local/libexec/of-mcp-node (NOT brew)'
      pattern: 'Developer-ID'
---

<objective>
Document the node-pinning + first-grant + install + upgrade runbook, run the on-host TCC verification spike (S0–S5) under `launchctl`, record its results, and complete the two manual follow-ups (ADR 001 vault back-reference). Closes the verification side of DEPLOY-01 and the supersede mechanics of DEPLOY-04.

Purpose: The real DEPLOY-01 proof — grant survives `brew upgrade`, no restart loop on denial — can only be observed on
the host under `launchctl` (a terminal-run probe inherits the terminal's grant and gives a false pass). This plan is
where the user seeds the interactive grant and observes the prompts. Output: `deploy/launchd/RUNBOOK.md` with recorded
spike results; the manual vault edit done. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-launchd-deployment-adr/06-RESEARCH.md
@.planning/phases/06-launchd-deployment-adr/06-CONTEXT.md
@.planning/phases/06-launchd-deployment-adr/06-VALIDATION.md
@deploy/launchd/com.kip-d.omnifocus-mcp.plist
@Makefile
@docs/adr/ADR-005-deployment-posture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the deployment runbook</name>
  <read_first>
    - .planning/phases/06-launchd-deployment-adr/06-RESEARCH.md (Verification Spike S0–S5; Node-source note table; Pitfall 1 ad-hoc-overwrite, Pitfall 3 no-consent-UI-under-launchd / first-grant seeding)
    - .planning/phases/06-launchd-deployment-adr/06-CONTEXT.md (D-01/D-02 amended: pin a Developer-ID node, not brew; D-06 vault back-reference follow-up)
    - deploy/launchd/com.kip-d.omnifocus-mcp.plist (the template install fills)
    - Makefile (install/uninstall targets the runbook invokes)
  </read_first>
  <action>Create `deploy/launchd/RUNBOOK.md` documenting, in order: (1) Pin the Developer-ID-signed node — copy the official nodejs.org `.pkg` / nvm node (NOT `$(brew --prefix)/bin/node`, which is ad-hoc-signed) to `~/.local/libexec/of-mcp-node`, and verify its designated requirement is identity-based with `codesign -d -r- ~/.local/libexec/of-mcp-node` (expect `identifier "node" and anchor apple generic ...`, NOT `cdhash H"..."`). (2) Seed the first-run grant interactively by running the pinned binary as the responsible process so macOS prompts once (launchd has no consent UI — Pitfall 3). (3) Set the real `MCP_AGENT_TOKEN` / `MCP_OWNER_TOKEN` and run `make install`. (4) The brew-upgrade survival runbook: on a deliberate node upgrade, re-copy the Developer-ID node in place to the same path and `launchctl kickstart -k gui/$(id -u)/com.kip-d.omnifocus-mcp` — because the DR is identity-based, no re-grant is needed. (5) A leave-empty "Spike Results" section (Task 2 fills it). Cross-link ADR-005. Apply the `neurodivergent-visual-org` skill with a Mermaid TL;DR of the install/upgrade flow.</action>
  <verify>
    <automated>grep -qi "Developer-ID" deploy/launchd/RUNBOOK.md && grep -q "of-mcp-node" deploy/launchd/RUNBOOK.md && ! grep -q "brew --prefix" deploy/launchd/RUNBOOK.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - deploy/launchd/RUNBOOK.md exists and covers: pin Developer-ID node, codesign DR check, first-grant seeding, make install, upgrade re-copy + kickstart.
    - Runbook explicitly says to use a Developer-ID node, NOT brew's ad-hoc node (grep "Developer-ID"; absence of "brew --prefix" as the copy source).
    - Runbook contains a "Spike Results" section placeholder for Task 2.
    - Runbook contains a ```mermaid TL;DR block.
  </acceptance_criteria>
  <done>Runbook authored with the Developer-ID pinning + seeding + upgrade procedure and an empty spike-results section.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the TCC host spike (S0–S5) under launchctl and record results</name>
  <read_first>
    - .planning/phases/06-launchd-deployment-adr/06-RESEARCH.md (Verification Spike S0–S5 commands and pass criteria)
    - .planning/phases/06-launchd-deployment-adr/06-VALIDATION.md (Manual-Only Verifications table)
    - deploy/launchd/RUNBOOK.md (the procedure to follow)
  </read_first>
  <what-built>The probe (Plan 01), the least-privilege plist + Makefile (Plan 03), and the runbook (Task 1) are in place. This step verifies the deployment end-to-end on the actual Mac — which cannot be automated, because a terminal-run probe inherits the terminal's Automation grant and gives a false pass.</what-built>
  <how-to-verify>
    Follow `deploy/launchd/RUNBOOK.md` / 06-RESEARCH.md spike S0–S5 on this Mac:
    1. S0: pin a Developer-ID node to `~/.local/libexec/of-mcp-node`; confirm `codesign -d -r-` shows an identity-based DR (not a cdhash).
    2. S1: run the pinned binary interactively once; approve the one-time Automation prompt naming node.
    3. S3: `make install`, then `tail -f ~/Library/Logs/omnifocus-mcp/server.err` — expect a clean start, no `-1743`.
    4. S4 (core survival test): overwrite `~/.local/libexec/of-mcp-node` in place with a DIFFERENT Developer-ID node version, `launchctl kickstart -k gui/$(id -u)/com.kip-d.omnifocus-mcp` — expect PASS, no re-prompt. (Optional contrast: repeat with brew's ad-hoc node and OBSERVE the `-1743` failure.)
    5. S5 (no restart loop): revoke the grant in System Settings → Automation, kickstart, watch logs — expect ONE `exit(1)` with remediation and the agent stays DOWN (no rapid respawn).
    6. S6 (end-to-end WRITE round-trip — success criterion 1's literal proof): with the grant re-granted and the agent running under `launchctl`, perform a real `omnifocus_write` create against the live server, then read it back (e.g. `omnifocus_read` by id or name) — expect the task to exist with the written fields, with NO interactive prompt. This proves "a verified end-to-end write succeeds under `launchctl`", not merely that the read probe's grant survived.
    Record the observed result of each step in the "Spike Results" section of `deploy/launchd/RUNBOOK.md` (pass/fail + any `-1743` observations + the TCC.db row from S2 if captured + the S6 created-task id/read-back confirmation).
  </how-to-verify>
  <acceptance_criteria>
    - RUNBOOK.md "Spike Results" section records S0–S6 outcomes with the S4 in-place-overwrite survival result, the S5 no-restart-loop observation, and the S6 write round-trip.
    - S4 (Developer-ID) recorded as PASS (grant survives in-place overwrite, no prompt).
    - S5 recorded: a denial exit stays down, no restart loop under KeepAlive=Crashed-only.
    - S6 recorded as PASS: a real write create + read-back succeeds under `launchctl` with no interactive prompt (success criterion 1's "verified end-to-end write").
  </acceptance_criteria>
  <resume-signal>Type "approved" once the spike is run and results are recorded, or describe what failed.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Add ADR 001 back-reference stub in the JessOS vault</name>
  <read_first>
    - .planning/phases/06-launchd-deployment-adr/06-CONTEXT.md (D-06: cross-store supersede; the vault edit is a manual follow-up, not a code task)
    - docs/adr/ADR-005-deployment-posture.md (the superseding ADR to reference)
  </read_first>
  <what-built>ADR-005 (Plan 02) carries `Status: Accepted — Supersedes ADR 001`. The reciprocal back-reference must be added to ADR 001, which lives in the JessOS Obsidian vault — outside this repo. This is the only manual cross-store step.</what-built>
  <how-to-verify>
    In the JessOS Obsidian vault, open ADR 001 (obsidian-tasks-plugin) and add a one-line status/back-reference:
    `Superseded by ADR 005 — omnifocus-mcp repo, docs/adr/ADR-005-deployment-posture.md`.
    There is no in-repo file to change for this step.
  </how-to-verify>
  <acceptance_criteria>
    - ADR 001 in the JessOS vault carries the `Superseded by ADR 005 …` back-reference line.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the vault back-reference is added, or "skip" to defer it.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                                                                        |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| Host TCC ↔ pinned node             | The first-run grant is seeded against the pinned node as responsible process; the spike verifies the attribution and its survival across overwrite |
| In-repo ADR ↔ JessOS vault ADR 001 | The supersede relationship spans two stores; the back-reference closes the loop manually                                                           |

## STRIDE Threat Register

| Threat ID | Category          | Component                                                                     | Disposition | Mitigation Plan                                                                                                                                                                                  |
| --------- | ----------------- | ----------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-06-06   | Tampering         | Binary substitution at the pinned path (different-signer node)                | mitigate    | The runbook pins a Developer-ID node whose csreq TCC re-validates; the spike S4 confirms an identity-based DR survives in-place overwrite while a different signer would be rejected (Tasks 1-2) |
| T-06-01   | Denial of Service | Silent grant loss after upgrade (the exact failure DEPLOY-01 defends against) | mitigate    | Spike S4 proves the Developer-ID grant survives `brew upgrade`-style overwrite; runbook documents the re-copy from the Developer-ID source, never brew (Tasks 1-2)                               |
| T-06-SC   | Tampering         | npm/pip/cargo installs                                                        | accept      | No packages installed; node pinning is a manual copy of an already-trusted Developer-ID binary, recorded in the runbook                                                                          |

</threat_model>

<verification>
- `deploy/launchd/RUNBOOK.md` documents the Developer-ID pinning + seeding + upgrade procedure.
- Host spike S0–S5 run under `launchctl`; S4 survival and S5 no-restart-loop recorded in the runbook.
- ADR 001 vault back-reference added (manual).
</verification>

<success_criteria>

- DEPLOY-01 met (verified): the server runs as a LaunchAgent with a pinned, stable Developer-ID Node path; a verified
  end-to-end write succeeds under `launchctl` (spike S6), and the grant survives an in-place node overwrite with no
  interactive prompt (spike S4).
- DEPLOY-04 supersede mechanics complete: the reciprocal ADR 001 back-reference is in place. </success_criteria>

<output>
Create `.planning/phases/06-launchd-deployment-adr/06-04-SUMMARY.md` when done.
</output>
