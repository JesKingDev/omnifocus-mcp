---
phase: 06-launchd-deployment-adr
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/launchd/com.kip-d.omnifocus-mcp.plist
  - Makefile
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02]
must_haves:
  truths:
    - 'A versioned launchd plist template pins the Node binary to ~/.local/libexec/of-mcp-node'
    - 'The plist requests Automation only — no Full Disk Access, no Sockets, no entitlement keys, SessionCreate unset'
    - 'The plist uses KeepAlive={Crashed=true} so a clean permission-denial exit does not restart-loop'
    - 'make install bootstraps the LaunchAgent; make uninstall boots it out'
  artifacts:
    - path: 'deploy/launchd/com.kip-d.omnifocus-mcp.plist'
      provides: 'Least-privilege LaunchAgent template'
      contains: 'com.kip-d.omnifocus-mcp'
      min_lines: 30
    - path: 'Makefile'
      provides: 'install / uninstall targets using launchctl bootstrap/bootout'
      contains: 'launchctl bootstrap'
      min_lines: 15
  key_links:
    - from: 'Makefile install target'
      to: '~/Library/LaunchAgents/com.kip-d.omnifocus-mcp.plist'
      via: 'launchctl bootstrap gui/$(id -u)'
      pattern: 'launchctl bootstrap'
    - from: 'deploy/launchd/com.kip-d.omnifocus-mcp.plist'
      to: '~/.local/libexec/of-mcp-node'
      via: 'ProgramArguments[0]'
      pattern: 'of-mcp-node'
---

<objective>
Author the versioned, least-privilege LaunchAgent plist template and the Makefile install/uninstall targets. Closes the artifact side of DEPLOY-01 (pinned stable Node path) and DEPLOY-02 (Automation-only, no FDA / no open network).

Purpose: One git-tracked, reviewable plist + one-command install. The plist hygiene (least-privilege keys,
KeepAlive=Crashed-only, SessionCreate unset) is the structural mitigation for over-broad permissions and restart-loop
DoS. Output: `deploy/launchd/com.kip-d.omnifocus-mcp.plist` and a repo-root `Makefile`. These are net-new with no
in-repo analog — content comes from 06-RESEARCH.md. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-launchd-deployment-adr/06-RESEARCH.md
@.planning/phases/06-launchd-deployment-adr/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the least-privilege LaunchAgent plist template</name>
  <read_first>
    - .planning/phases/06-launchd-deployment-adr/06-RESEARCH.md (launchd Lifecycle — verified key semantics table; the system architecture diagram; the token-placeholder note in Runtime State Inventory)
    - .planning/phases/06-launchd-deployment-adr/06-CONTEXT.md (D-08 lifecycle keys, D-09 plist hygiene, the specifics block with exact paths)
  </read_first>
  <action>Create `deploy/launchd/com.kip-d.omnifocus-mcp.plist` as a versioned XML plist template with exactly these keys and no others that grant privilege: `Label` = `com.kip-d.omnifocus-mcp`; `ProgramArguments` whose first element is the pinned path `~/.local/libexec/of-mcp-node` (use a substitution placeholder the Makefile fills, since plists do not expand `~`) followed by the server entrypoint; `RunAtLoad` = true; `KeepAlive` = a dict with only `Crashed` = true (NOT SuccessfulExit, NOT a bare boolean); `ThrottleInterval` = 10; `ProcessType` = `Background`; `StandardOutPath` = `~/Library/Logs/omnifocus-mcp/server.log` and `StandardErrorPath` = `~/Library/Logs/omnifocus-mcp/server.err` (absolute paths the Makefile substitutes); `EnvironmentVariables` carrying `MCP_AGENT_TOKEN` and `MCP_OWNER_TOKEN` as PLACEHOLDERS only (e.g. `__MCP_AGENT_TOKEN__`) — never a real token, and the server runs in HTTP mode (the long-running Tailscale-Serve daemon per Phase 4 D-08). Do NOT include: `SessionCreate`, `UserName`, `GroupName`, `Sockets`, or any entitlement / FullDiskAccess key. Add an XML comment at the top noting it is a template and the placeholders are filled by `make install`.</action>
  <verify>
    <automated>plutil -lint deploy/launchd/com.kip-d.omnifocus-mcp.plist && grep -q "com.kip-d.omnifocus-mcp" deploy/launchd/com.kip-d.omnifocus-mcp.plist && ! grep -Eq "SessionCreate|FullDiskAccess|<key>Sockets</key>|<key>UserName</key>" deploy/launchd/com.kip-d.omnifocus-mcp.plist && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `plutil -lint` reports the plist OK (well-formed).
    - plist Label is `com.kip-d.omnifocus-mcp` and ProgramArguments[0] references `of-mcp-node`.
    - KeepAlive is a dict containing only `Crashed` = true (no SuccessfulExit key).
    - plist contains NONE of: `SessionCreate`, `UserName`, `GroupName`, `Sockets`, any FullDiskAccess / entitlement key (grep confirms absence).
    - Token env vars are placeholders, not real values (grep shows `__MCP_AGENT_TOKEN__` style, no hex secret).
  </acceptance_criteria>
  <done>Least-privilege plist template authored, lints clean, hygiene asserted by grep.</done>
</task>

<task type="auto">
  <name>Task 2: Author Makefile install / uninstall targets</name>
  <read_first>
    - .planning/phases/06-launchd-deployment-adr/06-RESEARCH.md (Pattern 2 — install/uninstall via bootstrap/bootout, host-verified syntax)
    - .planning/phases/06-launchd-deployment-adr/06-CONTEXT.md (D-07 Makefile decision; D-01 pinned path; Claude's Discretion on the optional verify target)
    - deploy/launchd/com.kip-d.omnifocus-mcp.plist (the template the install target substitutes)
  </read_first>
  <action>Create a repo-root `Makefile` with `install` and `uninstall` targets (plus `.PHONY`). `install`: `mkdir -p ~/Library/Logs/omnifocus-mcp` and `~/.local/libexec`; substitute the absolute pinned path, the absolute log paths, and the token env placeholders into the plist template and write the result to `~/Library/LaunchAgents/com.kip-d.omnifocus-mcp.plist`; then `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.kip-d.omnifocus-mcp.plist`. `uninstall`: `launchctl bootout gui/$(id -u)/com.kip-d.omnifocus-mcp` (tolerate "not loaded") and remove the installed plist. The install target must NOT copy the pinned node binary from `$(brew --prefix)/bin/node` — copying the Developer-ID node is a runbook step (documented in Plan 04), not an automated brew copy. Optionally add a `verify` target (Claude's discretion per D-Discretion) that runs the probe under the installed agent / tails the err log; if added, document it as advisory. Add a one-line comment block at the top of the Makefile pointing at ADR-005 and the runbook for the node-pinning step.</action>
  <verify>
    <automated>make -n install >/dev/null 2>&1 && grep -q "launchctl bootstrap" Makefile && grep -q "launchctl bootout" Makefile && ! grep -q "brew --prefix" Makefile && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `make -n install` parses (dry-run, no error).
    - Makefile `install` target contains `launchctl bootstrap gui/$(id -u)` and writes to `~/Library/LaunchAgents/com.kip-d.omnifocus-mcp.plist`.
    - Makefile `uninstall` target contains `launchctl bootout`.
    - Makefile does NOT reference `brew --prefix` (the node copy is a runbook step, not an automated brew copy).
    - install creates `~/Library/Logs/omnifocus-mcp` before bootstrapping.
  </acceptance_criteria>
  <done>Makefile install/uninstall authored, parses, uses bootstrap/bootout, no brew copy.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                                       |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| plist ↔ macOS launchd / TCC  | The plist declares which privileges and lifecycle the OS grants; an over-broad key is a least-privilege violation |
| plist template ↔ git history | Real tokens must never be committed; placeholders cross this boundary, secrets do not                             |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                | Disposition | Mitigation Plan                                                                                                                                               |
| --------- | ---------------------- | -------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-03   | Elevation of Privilege | Over-broad TCC grant (FDA / entitlements / open network) | mitigate    | plist carries no FDA/entitlement/Sockets keys and SessionCreate is unset — Automation is the only grant requested at runtime (Task 1); asserted by grep       |
| T-06-04   | Denial of Service      | Restart-loop on a permission-denial exit                 | mitigate    | `KeepAlive={Crashed=true}` is signal-only, so the probe's `process.exit(1/2)` clean exit keeps the agent DOWN, no respawn loop (Task 1)                       |
| T-06-05   | Information Disclosure | Token leakage via committed plist                        | mitigate    | plist template uses `__MCP_AGENT_TOKEN__` / `__MCP_OWNER_TOKEN__` placeholders; Makefile substitutes real tokens only at install time on the host (Tasks 1-2) |
| T-06-SC   | Tampering              | npm/pip/cargo installs                                   | accept      | Config-only plan; no packages installed                                                                                                                       |

</threat_model>

<verification>
- `plutil -lint` on the plist passes; hygiene grep asserts no FDA/Sockets/SessionCreate/UserName.
- `make -n install` parses; bootstrap/bootout present; no brew copy.
</verification>

<success_criteria>

- DEPLOY-02 met: the plist requests Automation only — no FDA, no open network keys.
- DEPLOY-01 (artifact side) met: the plist pins the stable Node path and the Makefile installs it in one command.
  </success_criteria>

<output>
Create `.planning/phases/06-launchd-deployment-adr/06-03-SUMMARY.md` when done.
</output>
