---
phase: 06-launchd-deployment-adr
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/ADR-005-deployment-posture.md
autonomous: true
requirements: [DEPLOY-04]
must_haves:
  truths:
    - 'A new in-repo ADR documents the deployment posture and security model'
    - 'The ADR explicitly supersedes ADR 001'
    - 'The ADR records the localhost-default / Tailscale-Serve-optional / cloud-ruled-out-by-Mac-pin posture carried
      from Phase 4'
    - 'The ADR records the TCC responsible-process + csreq finding and the Developer-ID-vs-Homebrew-ad-hoc node decision'
  artifacts:
    - path: 'docs/adr/ADR-005-deployment-posture.md'
      provides: 'Nygard ADR (Status / Context / Decision / Consequences) for the launchd deployment posture'
      contains: 'Supersedes ADR 001'
      min_lines: 60
  key_links:
    - from: 'docs/adr/ADR-005-deployment-posture.md'
      to: 'ADR 001 (JessOS vault)'
      via: 'Status line supersede + named back-reference'
      pattern: 'Supersedes ADR 001'
---

<objective>
Write the first in-repo ADR documenting the Phase 6 deployment posture and security model, explicitly superseding ADR 001. Closes DEPLOY-04.

Purpose: Record WHY the deployment looks the way it does — the locked decisions, the TCC responsible-process finding,
and the Phase 4 network posture this phase carries forward without re-deciding — so the rationale survives in version
control alongside the plist and probe it governs. Output: `docs/adr/ADR-005-deployment-posture.md` (Nygard format), the
first ADR in `docs/adr/`. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-launchd-deployment-adr/06-RESEARCH.md
@.planning/phases/06-launchd-deployment-adr/06-CONTEXT.md
@.planning/phases/04-http-edge-hardening/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-005-deployment-posture.md</name>
  <read_first>
    - .planning/phases/06-launchd-deployment-adr/06-RESEARCH.md (Nygard ADR format section; the TCC responsible-process + csreq finding; the Developer-ID-vs-Homebrew-ad-hoc node table; launchd lifecycle semantics; macOS 26 version caveat)
    - .planning/phases/06-launchd-deployment-adr/06-CONTEXT.md (D-01..D-09 locked decisions)
    - .planning/phases/04-http-edge-hardening/04-CONTEXT.md (D-13 loopback bind, D-16 Tailscale Serve + Cloudflare declined, D-17 funnel guard declined — the posture the ADR DOCUMENTS, does not re-decide)
  </read_first>
  <action>Create `docs/adr/ADR-005-deployment-posture.md` in Nygard format with these sections in order: a Title (`ADR-005: Deployment Posture and Security Model`); `Status: Accepted — Supersedes ADR 001`; a Context section stating the forces (least-privilege macOS deployment, no consent UI under launchd, a destructive-capable owner token now on the tailnet surface from Phase 4); a Decision section recording the locked choices — pinned Developer-ID-signed node at `~/.local/libexec/of-mcp-node` (NOT Homebrew's ad-hoc node, because TCC re-validates a stored csreq/designated-requirement on every access and an ad-hoc binary's DR is its cdhash which changes on upgrade, whereas a Developer-ID DR is identity-based and survives in-place overwrite), Automation-only TCC grant with no FDA/entitlement/open-network keys, the fail-fast probe (exit 1 on -1743, exit 2 on timeout) with `KeepAlive=Crashed-only` so a denial does not restart-loop, launchd install via `launchctl bootstrap`, and the network posture carried from Phase 4 (loopback-default `127.0.0.1` bind, Tailscale Serve never Funnel, Cloudflare evaluated and declined, cloud hosting ruled out by the Mac/`osascript` pin); a Consequences section listing positives AND negatives honestly (e.g. the runbook must re-copy the node from the Developer-ID source on upgrade; first-run grant must be seeded interactively because launchd has no consent UI; findings are specific to macOS 26.x and re-open on a major OS upgrade). Note the manual JessOS-vault back-reference to ADR 001 as a follow-up (handled in Plan 04), not part of this file. Do not pin a version string in prose (per CLAUDE.md). Apply the `neurodivergent-visual-org` skill and embed a Mermaid TL;DR of the deployment flow near the top.</action>
  <verify>
    <automated>grep -q "Supersedes ADR 001" docs/adr/ADR-005-deployment-posture.md && grep -qi "Developer-ID" docs/adr/ADR-005-deployment-posture.md && grep -qi "Tailscale Serve" docs/adr/ADR-005-deployment-posture.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/adr/ADR-005-deployment-posture.md exists with Nygard sections Status / Context / Decision / Consequences.
    - File contains the literal string "Supersedes ADR 001".
    - File documents the Developer-ID-vs-ad-hoc node DR rationale (grep "Developer-ID" and "cdhash" or "csreq").
    - File documents the carried-forward Phase 4 posture: loopback bind, Tailscale Serve never Funnel, Cloudflare declined, cloud ruled out by Mac pin.
    - File contains a fenced ```mermaid block (visual TL;DR).
    - No hardcoded product version string in prose.
  </acceptance_criteria>
  <done>ADR-005 authored in Nygard format, supersede line present, Phase 4 posture and TCC finding documented, Mermaid TL;DR embedded.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                                                    |
| ------------------------------------------ | -------------------------------------------------------------------------------------------------------------- |
| Documented posture ↔ implemented artifacts | The ADR records security decisions; drift between ADR and plist/probe is the risk this register guards against |

## STRIDE Threat Register

| Threat ID   | Category    | Component                                                                           | Disposition | Mitigation Plan                                                                                                                                           |
| ----------- | ----------- | ----------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-02-DOC | Repudiation | Undocumented security posture (why loopback / why Serve-only / why Automation-only) | mitigate    | ADR-005 records the network posture, least-privilege grant, and the Cloudflare/Funnel declines so they are not re-litigated or silently weakened (Task 1) |
| T-06-SC     | Tampering   | npm/pip/cargo installs                                                              | accept      | Doc-only plan; no packages installed                                                                                                                      |

</threat_model>

<verification>
- `docs/adr/ADR-005-deployment-posture.md` exists and contains "Supersedes ADR 001".
- Nygard sections present; Phase 4 posture and TCC finding documented; Mermaid TL;DR embedded.
</verification>

<success_criteria>

- DEPLOY-04 met: a new ADR documents the deployment posture and security model and explicitly supersedes ADR 001.
  </success_criteria>

<output>
Create `.planning/phases/06-launchd-deployment-adr/06-02-SUMMARY.md` when done.
</output>
